---
phase: quick-260702-rg2
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - /data/workspace/kais-movie-agent/lib/iteration-engine.js
  - /data/workspace/kais-aigc-platform/src/routes/v1/iteration/index.ts
  - /data/workspace/kais-aigc-platform/src/router.ts
  - /data/workspace/kais-movie-agent/test/iteration-engine.test.mjs
autonomous: true
requirements:
  - ITER-ENGINE-01
must_haves:
  truths:
    - "IterationEngine class can be imported from lib/iteration-engine.js"
    - "plan() collects feedback via HTTP and returns an IterationPlan with diagnosis (reroll/pipeline_adjust/upstream_fix)"
    - "execute(planId) forks a branch and regenerates nodes in topological order, marking failures per-node without aborting the iteration"
    - "confirm(branchId) marks branch active and applies approved pipeline adjustments to prompt-overrides.json"
    - "discard(branchId, reason) marks branch rejected"
    - "POST /api/v1/iteration/plan|execute|confirm|discard|approve-adjustment endpoints are registered and reachable"
    - "GET /api/v1/iteration/plans and /status/:planId return stored plan data"
    - "node --test passes all iteration-engine unit tests"
  artifacts:
    - path: /data/workspace/kais-movie-agent/lib/iteration-engine.js
      provides: "IterationEngine class with collectFeedback/diagnose/plan/execute/confirm/discard + private helpers"
      contains: "export class IterationEngine"
    - path: /data/workspace/kais-aigc-platform/src/routes/v1/iteration/index.ts
      provides: "7 iteration API routes bridged to node ESM via subprocess (mirrors reflection pattern)"
      contains: "routeIteration"
    - path: /data/workspace/kais-movie-agent/test/iteration-engine.test.mjs
      provides: "unit tests covering all public methods + topologicalSort + storage + error paths"
      contains: "describe('IterationEngine'"
  key_links:
    - from: /data/workspace/kais-aigc-platform/src/router.ts
      to: /data/workspace/kais-aigc-platform/src/routes/v1/iteration/index.ts
      via: "import routeIteration + app.use('/api/v1/iteration', routeIteration)"
      pattern: "routeIteration"
    - from: /data/workspace/kais-movie-agent/lib/iteration-engine.js
      to: /data/workspace/kais-movie-agent/lib/hermes-adapter.js
      via: "callLLM import"
      pattern: "from './hermes-adapter.js'"
    - from: /data/workspace/kais-aigc-platform/src/routes/v1/iteration/index.ts
      to: /data/workspace/kais-movie-agent/lib/iteration-engine.js
      via: "spawnSync node subprocess (mirrors reflection bridge)"
      pattern: "spawnSync"
---

<objective>
Implement IterationEngine — feedback-driven versioned iteration engine. Single集内版本化迭代: collect feedback → LLM diagnose (3 types) → fork branch → regenerate nodes in topological order → user confirm/discard.

Purpose: Close the gap between feedback collection and automated asset regeneration. PipelineReflector handles cross-episode strategy; IterationEngine handles per-episode version iteration.
Output: lib/iteration-engine.js, src/routes/v1/iteration/index.ts, router.ts registration, test/iteration-engine.test.mjs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/PROJECT.md
@.planning/quick/260702-rg2-implement-iteration-engine-feedback-driv/260702-rg2-SPEC.md

# Existing patterns to mirror EXACTLY
@/data/workspace/kais-movie-agent/lib/pipeline-reflector.js
@/data/workspace/kais-movie-agent/test/pipeline-reflector.test.mjs
@/data/workspace/kais-aigc-platform/src/routes/v1/reflection/index.ts

<interfaces>
<!-- From lib/hermes-adapter.js (LLM caller to reuse) -->
```javascript
export async function callLLM(arg1, arg2);
// callLLM({ prompt, system, responseFormat: 'json' }) → Promise<string>
// Injectable as opts.llmCaller in constructor (mirror PipelineReflector pattern)
```

<!-- From lib/pipeline-reflector.js (storage helpers to mirror) -->
```javascript
// JSONL helpers (read optional, write, append) — copy this exact pattern
async _readJsonlOptional(absPath)  // returns [], never throws
async _writeJsonl(absPath, rows)
async _appendJsonl(absPath, row)
async _ensureDir()
_readJsonOptional / _stripFences / _callJson
_genId()  // mirror format: `iter-${Date.now().toString(36)}-${randomBytes(4).toString('hex')}`
```

<!-- Reflection route bridge pattern (subprocess isolation for TS→ESM) -->
```typescript
// src/routes/v1/reflection/index.ts pattern:
// 1. REFLECTOR_PATH = absolute constant
// 2. ALLOW_ROOT = "/data/workspace"
// 3. workdirSchema (zod, rejects "..", /etc, /usr, must be under ALLOW_ROOT)
// 4. _runReflector(workdir, method, args, extra) → spawnSync node --input-type=module -e script
//    - env-passed args (NEVER string-interpolate user input) — T-q6l-02 injection mitigation
//    - script imports class, instantiates from process.env, calls method, prints {ok,data|error}
// 5. router.post("/run"), router.get("/pending"), etc.
```

<!-- Feedback API that iteration calls (HTTP, already exists) -->
```
GET  /api/v1/feedback/project/:projectId           → feedback rows array
GET  /api/v1/feedback/propagation/:assetId?projectId=&direction=downstream  → { downstream, upstream, affectedWithFeedback }
POST /api/v1/feedback/propagate                    → equivalent POST version
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create lib/iteration-engine.js core module</name>
  <files>/data/workspace/kais-movie-agent/lib/iteration-engine.js</files>
  <behavior>
    - Module load: import succeeds, IterationEngine is a class
    - constructor(workdir, opts) accepts workdir + {apiBase, projectId, episodesId, llmCaller}
    - collectFeedback(): calls GET /api/v1/feedback/project/:projectId and per-asset propagation, groups feedback by nodeId, returns {byNode, topology}
    - diagnose(feedback): builds LLM prompt from spec template (3-type framework), calls llmCaller, returns IterationPlan; throws on missing diagnosis.type or invalid type
    - diagnose() accepts only type ∈ {reroll, pipeline_adjust, upstream_fix}; rejects others
    - plan(): collectFeedback → diagnose → _storePlan → returns plan with id/createdAt
    - _topologicalSort(actions): returns actions ordered so that ones with dependsOn come after their dependencies; handles cycles by throwing
    - execute(planId): reads plan; if plan.requiresApproval && !plan.adjustmentApproved → throws; forks branch via HTTP POST, iterates actions topologically, calls _callEngine per node, marks failures without aborting
    - execute() marks each result status ∈ {success, failed, pending}
    - confirm(branchId): sets branch active via HTTP PATCH; if plan has pipelineAdjustment writes to prompt-overrides.json
    - discard(branchId, reason): sets branch rejected via HTTP PATCH
    - approveAdjustment(planId): sets plan.adjustmentApproved = true, persists
    - _storePlan(plan): appends JSONL row to .pipeline-assets/iteration-plans.jsonl with status:'pending'
    - _readPlan(planId): reads JSONL, returns row or throws
    - listPlans(): returns all rows from iteration-plans.jsonl
    - JSONL/storage helpers mirror PipelineReflector exactly (_readJsonlOptional/_writeJsonl/_appendJsonl/_ensureDir)
    - _genId format: `iter-{timestamp36}-{hex8}` (mirrors refl- pattern)
  </behavior>
  <action>
Create ESM module at /data/workspace/kais-movie-agent/lib/iteration-engine.js.

OPEN the spec at .planning/quick/260702-rg2-implement-iteration-engine-feedback-driv/260702-rg2-SPEC.md and the existing /data/workspace/kais-movie-agent/lib/pipeline-reflector.js side-by-side. Mirror PipelineReflector's structure for: imports (fs/promises, path, crypto, callLLM), constants block, JSDoc typedefs, class shape, and storage helpers.

LAYOUT (top to bottom):
1. Header JSDoc — describe IterationEngine purpose, contrast with PipelineReflector (per-episode version iteration vs cross-episode strategy), note the 3 diagnosis types (reroll/pipeline_adjust/upstream_fix).
2. Imports: `readFile, writeFile, appendFile, mkdir` from 'node:fs/promises'; `join` from 'node:path'; `existsSync` from 'node:fs'; `randomBytes` from 'node:crypto'; `callLLM` from './hermes-adapter.js'.
3. Constants: ASSETS_DIR='.pipeline-assets'; PLANS_FILE='iteration-plans.jsonl'; CURRENT_FILE='iteration-current.json'; PROMPT_OVERRIDES_FILE='prompt-overrides.json'; SYSTEM_MSG = the diagnosis framework prompt from spec (the one that defines A/B/C types — copy the spec's "诊断 LLM Prompt 模板" verbatim as the system message body, ending with the "## 输出格式（只输出 JSON）" section). REQUIRED_PLAN_KEYS, VALID_DIAGNOSIS_TYPES Set, VALID_ACTION_TYPES Set (regenerate|regenerate_after_parent|skip), VALID_ADJUSTMENT_TYPES Set (prompt_modification|threshold_adjustment|parameter_change).
4. JSDoc typedefs for IterationPlan, IterationAction, IterationResult (mirror spec's interfaces).
5. export class IterationEngine:
   - constructor(workdir, opts={}): store workdir, apiBase (default 'http://localhost:10588'), projectId, episodesId, llmCaller (default (...args)=>callLLM(...args)), resolve assetsDir/plansPath/currentPath/overridesPath.
   - Storage helpers: _ensureDir, _readJsonOptional, _readJsonlOptional, _writeJsonl, _appendJsonl — COPY these verbatim from PipelineReflector (same exact logic).
   - _genId(): return `iter-${Date.now().toString(36)}-${randomBytes(4).toString('hex')}`.
   - _stripFences(text) and _callJson(prompt, system) — copy from PipelineReflector.
   - async collectFeedback(): if !projectId throw. fetch GET `${apiBase}/api/v1/feedback/project/${projectId}` (headers: accept json). Parse response — the platform returns {success, data} envelope; use .data array. Group by assetId → byNode map. For nodes with reject/contest verdict, fetch propagation via GET `${apiBase}/api/v1/feedback/propagation/${assetId}?projectId=${projectId}&direction=downstream`. Collect downstream[] arrays into topology map. Return {byNode, topology, summary}. Wrap fetch in try/catch — network failure throws Error with clear message.
   - async diagnose(feedback): build prompt via _buildDiagnosisPrompt(feedback) (use the spec's "诊断 LLM Prompt 模板" template — populate {feedback_by_node} {topology_summary} {evaluation_summary} sections from feedback data). Call _callJson(prompt, SYSTEM_MSG). Parse JSON (handle code fences). Validate: parsed.diagnosis must exist, diagnosis.type ∈ VALID_DIAGNOSIS_TYPES, parsed.actions is array. For each action validate action ∈ VALID_ACTION_TYPES. If any action has pipelineAdjustment, set requiresApproval=true. branchLabel defaults to `v${Date.now().toString(36)}` if missing. Return assembled plan object {id:_genId(), episodeId, branchLabel, diagnosis, actions, requiresApproval, createdAt:new Date().toISOString(), status:'pending', adjustmentApproved:false}.
   - async plan(): const fb = await collectFeedback(); const p = await diagnose(fb); await _storePlan(p); return p.
   - _buildDiagnosisPrompt(feedback): assemble the spec template — feedback_by_node section listing each nodeId + verdicts + contents (truncated 200 chars); topology_summary listing each rejected node + downstream count; evaluation_summary placeholder (use '(无)' if no eval data).
   - _topologicalSort(actions): Kahn's algorithm. Build node set, in-degree map, dependency edges from action.dependsOn (filter to existing action nodeIds). Seed queue with in-degree-0 actions (preserve original order). Pop, push to result, decrement neighbors. If result.length !== actions.length → throw Error('cycle detected in iteration actions').
   - async execute(planId): read plan via _readPlan(planId). If plan.requiresApproval && !plan.adjustmentApproved → throw Error('Pipeline adjustment requires approval before execution'). If plan.adjustmentApproved && plan.diagnosis.pipelineAdjustment → await _applyPipelineAdjustment(plan). const branchId = await _forkBranch(plan). const sorted = _topologicalSort(plan.actions). const results = []. For each action: skip if action.action==='skip' (push {status:'pending'}). Else build prompt via _buildPrompt(action), try await _callEngine(action.nodeId, prompt, branchId), on success push {nodeId, newNodeId:`${action.nodeId}-v${plan.id.slice(-6)}`, status:'success', outputUrl}, on failure push {status:'failed', error:err.message} (DO NOT rethrow — continue iteration per spec constraint #8). Persist IterationResult to current.json + append to plans JSONL with result. Return {planId, branchId, regeneratedNodes:results}.
   - async _forkBranch(plan): POST `${apiBase}/api/canvas/v2/branches` with {projectId, label:plan.branchLabel, parentLabel:null}. Parse response, return branchId. Wrap in try/catch with clear error.
   - async _callEngine(nodeId, prompt, branchId): POST `${apiBase}/api/canvas/execute` with {nodeId, prompt, branchId, projectId}. Return response data (outputUrl etc.). Wrap fetch — network/HTTP error → throw.
   - async _buildPrompt(action): look up original prompt from feedback context if available; if action.promptDelta → return `${base}\n\n[迭代增补] ${action.promptDelta}`. If no delta → return base || ''.
   - async _applyPipelineAdjustment(plan): same logic as PipelineReflector._applyOverride — read prompt-overrides.json, write by type (prompt_modification appends to target array; threshold_adjustment writes thresholds[target]; parameter_change records only). Mirror that method's structure.
   - async confirm(branchId): PATCH `${apiBase}/api/canvas/v2/branches/${branchId}` with {status:'active'}. Find the plan that created this branch via listPlans(). If that plan has pipelineAdjustment → _applyPipelineAdjustment(plan). Archive prior active branch — best-effort: PATCH previous branch to status:'archived' (no throw on failure).
   - async discard(branchId, reason=''): PATCH `${apiBase}/api/canvas/v2/branches/${branchId}` with {status:'rejected', reason}. Update plan row status to 'discarded'.
   - async approveAdjustment(planId): read plan, set adjustmentApproved=true, status='approved', persist updated JSONL.
   - async _storePlan(plan): await _appendJsonl(plansPath, plan).
   - async _readPlan(planId): read all from plansPath, find row.id===planId, throw if not found.
   - async listPlans(): return _readJsonlOptional(plansPath).
   - async getStatus(planId): read plan, return {planId, status:plan.status, result:plan.result||null}.

CONSTRAINTS to honor verbatim from spec section "## 实现约束":
- #1 不自动执行: plan() returns plan, execute() is separate call
- #2 不自动确认管线改动: requiresApproval gate, approveAdjustment separate
- #3 分支隔离: new branch via _forkBranch, no mutation of source branch
- #4 拓扑顺序: _topologicalSort before regeneration loop
- #5 LLM 调用复用: callLLM from hermes-adapter.js
- #6 ESM: project is "type":"module"
- #7 零外部依赖: only node: builtins + callLLM
- #8 失败降级: per-node try/catch, mark failed, continue
- #9 引擎调用通过 HTTP: apiBase default 'http://localhost:10588', all engine/branch ops via fetch
- #10 WebSocket: emit progress via apiBase (best-effort POST to /api/canvas/v2/events — wrap in try/catch, ignore failure)

NEVER inline implementation code in <action> for downstream tasks — this is the only production code task; be thorough.
  </action>
  <verify>
    <automated>cd /data/workspace/kais-movie-agent && node -e "import('./lib/iteration-engine.js').then(m => { if (typeof m.IterationEngine !== 'function') process.exit(1); console.log('module OK'); })"</automated>
  </verify>
  <done>lib/iteration-engine.js exports IterationEngine class; module imports without error; all methods listed in spec section "IterationEngine 类 API" are present as class methods.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Write test/iteration-engine.test.mjs</name>
  <files>/data/workspace/kais-movie-agent/test/iteration-engine.test.mjs</files>
  <behavior>
    - Module load: IterationEngine is a class, constructs with (tmpDir, {})
    - diagnose(): with mocked llmCaller returning fixed reroll JSON, returns valid IterationPlan with diagnosis.type='reroll'
    - diagnose(): with mocked llmCaller returning fenced ```json``` payload, still parses (strips fences)
    - diagnose(): with mocked llmCaller returning invalid diagnosis.type, throws
    - diagnose(): with mocked llmCaller returning non-JSON, throws
    - _topologicalSort(): linear chain A→B→C returns [A,B,C]
    - _topologicalSort(): diamond D depends on B,C both depending on A — returns valid topo order
    - _topologicalSort(): cycle throws
    - plan(): with mocked llmCaller + mocked HTTP (inject fetch via global override), returns plan + persists JSONL
    - _storePlan + _readPlan round-trip: plan written, read back by id
    - _readPlan unknown id throws
    - listPlans returns all rows
    - execute(): with mocked fetch + mocked plan with requiresApproval=false, returns IterationResult with branchId + regeneratedNodes
    - execute(): with mocked fetch returning engine failure for one node, that node marked 'failed' but iteration completes
    - execute(): with plan.requiresApproval=true && adjustmentApproved=false → throws
    - approveAdjustment(): sets adjustmentApproved=true on plan row
    - confirm(): with mocked fetch PATCH success, calls _applyPipelineAdjustment if plan has pipelineAdjustment
    - discard(): updates plan row status='discarded'
    - _applyPipelineAdjustment(): writes prompt_modification to prompt-overrides.json keyed by target
  </behavior>
  <action>
Create /data/workspace/kais-movie-agent/test/iteration-engine.test.mjs using node:test (describe/it/beforeEach/afterEach) and node:assert/strict — mirror pipeline-reflector.test.mjs layout exactly.

Structure:
- Header JSDoc: "Quick Task 260702-rg2: IterationEngine unit tests" + coverage list (mirror the q6l header).
- Imports: describe/it/beforeEach/afterEach from 'node:test'; assert from 'node:assert/strict'; mkdtemp/rm/writeFile/mkdir from 'node:fs/promises'; existsSync from 'node:fs'; join from 'node:path'; tmpdir from 'node:os'; IterationEngine from '../lib/iteration-engine.js'.
- makeTmpDir() helper (same as q6l).
- A `mockFetch(routes)` helper that returns a function (url, opts) => Promise resolving to a fake Response {ok, status, json:async()=>body, text:async()=>''}. Routes: map of {method+urlSubstring → responseBody | function(opts)}. This is the key test infrastructure — design it so tests inject HTTP behavior without spinning up a real server. Install via `global.fetch = mockFetch(...)` in beforeEach, restore original in afterEach (capture original const ORIGINAL_FETCH = global.fetch at module top).
- Each test constructs IterationEngine with tmpDir + llmCaller (async ()=>JSON.stringify(payload)) + minimal fetch mock covering only what that test exercises.

Write tests in this order (matching behavior list above):
1. 'module load' — class constructs
2. 'diagnose() parses LLM reroll JSON' — payload with diagnosis.type='reroll', 1 action regenerate, requiresApproval false
3. 'diagnose() strips ```json fences'
4. 'diagnose() throws on invalid type' — type='unknown'
5. 'diagnose() throws on non-JSON LLM output'
6. '_topologicalSort() linear A→B→C' — call engine._topologicalSort([{nodeId:'A'},{nodeId:'B',dependsOn:['A']},{nodeId:'C',dependsOn:['B']}]) → ['A','B','C'] (compare nodeId sequence)
7. '_topologicalSort() diamond' — A→{B,C}→D
8. '_topologicalSort() cycle throws' — A→B→A
9. 'plan() with mocked HTTP persists JSONL and returns plan' — mock fetch to return feedback list + empty propagation; mock llmCaller; call engine.plan(); assert plansPath JSONL has 1 row, returned plan has id
10. '_storePlan + _readPlan round-trip' — manual store + read
11. '_readPlan unknown id throws'
12. 'listPlans returns all rows' — write 2 manually, list returns 2
13. 'execute() happy path' — mock plan in JSONL (requiresApproval=false), mock _forkBranch fetch returning branchId='br1', mock _callEngine fetch returning {outputUrl:'http://x'}, call execute(planId), assert regeneratedNodes all success
14. 'execute() per-node failure does not abort iteration' — mock _callEngine throwing for one nodeId; assert that node status='failed', others 'success'
15. 'execute() rejects when requiresApproval && !adjustmentApproved'
16. 'approveAdjustment() flips flag' — store plan, approveAdjustment(planId), _readPlan returns adjustmentApproved=true
17. 'confirm() applies pipelineAdjustment' — mock fetch PATCH success, mock plan with pipelineAdjustment.prompt_modification; call confirm(branchId); assert prompt-overrides.json has target entry
18. 'discard() updates status' — mock fetch, call discard, _readPlan shows status='discarded'
19. '_applyPipelineAdjustment() writes prompt_modification override' — direct call, assert overrides file

DO NOT use real network. Every fetch must be mocked. Tests must run offline.

Run: `cd /data/workspace/kais-movie-agent && node --test test/iteration-engine.test.mjs`
  </action>
  <verify>
    <automated>cd /data/workspace/kais-movie-agent && node --test test/iteration-engine.test.mjs 2>&1 | tail -30</automated>
  </verify>
  <done>All test cases pass; `node --test test/iteration-engine.test.mjs` exits 0; test count matches behavior list (≥19 tests).</done>
</task>

<task type="auto">
  <name>Task 3: Create src/routes/v1/iteration/index.ts + register in router.ts</name>
  <files>/data/workspace/kais-aigc-platform/src/routes/v1/iteration/index.ts, /data/workspace/kais-aigc-platform/src/router.ts</files>
  <action>
Create /data/workspace/kais-aigc-platform/src/routes/v1/iteration/index.ts.

MIRROR /data/workspace/kais-aigc-platform/src/routes/v1/reflection/index.ts structure EXACTLY:
1. Imports: express, z, { spawnSync } from 'child_process', { success, error } from '@/lib/responseFormat'.
2. Constants:
   - ENGINE_PATH = '/data/workspace/kais-movie-agent/lib/iteration-engine.js'
   - ALLOW_ROOT = '/data/workspace/kais-movie-agent' (NOTE: different from reflection — iteration's workdir is the movie-agent workdir, default to /data/workspace/kais-movie-agent so .pipeline-assets lands correctly; but to keep parity with reflection allow /data/workspace too if operator wants per-project subdirs. Use ALLOW_ROOT = '/data/workspace' to match reflection exactly.)
3. workdirSchema — copy from reflection/index.ts verbatim (rejects .., /etc, /usr, must be under ALLOW_ROOT).
4. _runEngine(workdir, method, args=[], extra={}) — copy reflection's _runReflector pattern verbatim, rename to _runEngine, change:
   - import from ENGINE_PATH
   - class name: IterationEngine
   - env var prefix: RG2_ (instead of Q6L_) — RG2_WORKDIR, RG2_METHOD, RG2_ARGS, RG2_PROJECT_ID, RG2_EPISODES_ID, RG2_API_BASE
   - ctorOpts: apiBase (from extra.apiBase || process.env.KAIS_AIGC_API_BASE || 'http://localhost:10588'), projectId, episodesId
   - timeout: 120_000 (execute() is longer-running than reflection — bump from 60_000)
   - same subprocess script template with JSON stdout {ok,data|error}
5. Routes (per spec section "### API 设计"):

   POST /plan — body: {workdir, projectId, episodesId, apiBase?}. Validate via planSchema (z.object with workdir + projectId number + episodesId string optional + apiBase string optional). Call _runEngine(workdir,'plan',[],{projectId,episodesId,apiBase}). Return success({status:'ok', plan}).

   POST /execute — body: {workdir, planId, projectId, episodesId, apiBase?}. Validate. Call _runEngine(workdir,'execute',[planId],{projectId,episodesId,apiBase}). Return success({status:'ok', result}).

   POST /confirm — body: {workdir, branchId, projectId, episodesId, apiBase?}. Call _runEngine(workdir,'confirm',[branchId],{...}). Return success({status:'ok'}).

   POST /discard — body: {workdir, branchId, reason?, projectId, episodesId, apiBase?}. discardSchema includes reason z.string().max(500).optional().default(''). Call _runEngine(workdir,'discard',[branchId, reason],{...}).

   GET /plans — query: workdir (required), projectId, episodesId. Validate workdir via workdirSchema. Call _runEngine(workdir,'listPlans',[],{projectId,episodesId}). Cap result at 1000 rows (T-q6l-04 DoS mitigation parity). Return success(capped).

   GET /status/:planId — query: workdir. Call _runEngine(workdir,'getStatus',[planId]). Return success({status, planId, result}).

   POST /approve-adjustment — body: {workdir, planId}. Call _runEngine(workdir,'approveAdjustment',[planId]). Return success({status:'ok', planId}).

6. Each route handler wrapped in try/catch with console.error(`[v1/iteration/{name}] failed:`, err) + 500 response (mirror reflection exactly).
7. export default router.

THEN modify /data/workspace/kais-aigc-platform/src/router.ts:
- Add import (alphabetical position after route104 reflection):
  `import routeIteration from "./routes/v1/iteration/index";`
- Add registration (after the reflection line `app.use("/api/v1/reflection", route104);`):
  `app.use("/api/v1/iteration", routeIteration);`
- IMPORTANT: Do NOT renumber existing routes. Do NOT touch the @routes-hash comment line at top.
- Verify with: `grep -c 'routeIteration' src/router.ts` should return 2.

Parity constraints from reflection/index.ts to preserve:
- Subprocess isolation (T-q6l-02 injection mitigation): user-controlled values via process.env ONLY, never string interpolation in the script template.
- workdir under ALLOW_ROOT only.
- spawnSync with --input-type=module.
- Per-route zod validation with safeParse.
- success()/error() response envelope.
  </action>
  <verify>
    <automated>cd /data/workspace/kais-aigc-platform && npx tsc --noEmit 2>&1 | tail -20 && grep -c 'routeIteration' src/router.ts | grep -q '^2$' && echo ROUTER_OK</automated>
  </verify>
  <done>iteration/index.ts compiles under tsc --noEmit; router.ts has exactly 2 references to routeIteration (1 import, 1 app.use); 7 routes registered (plan, execute, confirm, discard, plans, status/:planId, approve-adjustment).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→API | Untrusted HTTP body crosses into Express route handler (zod-validated) |
| API→subprocess | User-supplied workdir/method/args cross into node child process (env-passed, never interpolated) |
| subprocess→LLM | Diagnosis prompt sent to external LLM (callLLM via hermes-adapter) |
| subprocess→platform HTTP | IterationEngine fetches platform canvas/feedback APIs (localhost) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-rg2-01 | Spoofing | POST /iteration/execute caller | accept | Internal API on localhost; auth handled by platform-wide middleware (out of scope) |
| T-rg2-02 | Tampering | workdir param → subprocess | mitigate | env-passed only (mirror T-q6l-02); zod workdirSchema rejects .., /etc, /usr; ALLOW_ROOT confinement |
| T-rg2-03 | Repudiation | iteration actions (plan/execute/confirm) | accept | Logging via console.error on failures; full audit trail out of scope for quick task |
| T-rg2-04 | Info Disclosure | LLM prompt includes feedback content | accept | Feedback is operator-generated project data, not PII; LLM is configured operator-side |
| T-rg2-05 | DoS | GET /iteration/plans unbounded | mitigate | Cap at 1000 rows (parity with reflection T-q6l-04) |
| T-rg2-06 | Elevation | pipelineAdjustment auto-applied without approval | mitigate | requiresApproval gate in execute(); approveAdjustment is separate endpoint; spec constraint #1 & #2 |
| T-rg2-07 | Tampering | LLM returns malicious pipelineAdjustment | mitigate | zod/programmatic validation of adjustment.type ∈ {prompt_modification,threshold_adjustment,parameter_change}; _applyPipelineAdjustment writes only to prompt-overrides.json (no source mutation) |
| T-rg2-08 | Tampering | _topologicalSort cycle | mitigate | Cycle detection throws (no infinite loop) |
| T-rg2-SC | Tampering | npm/pip installs | accept | Zero new external deps per spec constraint #7; only node: builtins + existing callLLM |
</threat_model>

<verification>
- `cd /data/workspace/kais-movie-agent && node -e "import('./lib/iteration-engine.js').then(m=>console.log('OK',typeof m.IterationEngine))"` → prints "OK function"
- `cd /data/workspace/kais-movie-agent && node --test test/iteration-engine.test.mjs` → all tests pass, exit 0
- `cd /data/workspace/kais-aigc-platform && npx tsc --noEmit` → no errors
- `grep -c 'routeIteration' /data/workspace/kais-aigc-platform/src/router.ts` → 2
- `grep -c 'app.use("/api/v1/iteration"' /data/workspace/kais-aigc-platform/src/router.ts` → 1
</verification>

<success_criteria>
- lib/iteration-engine.js exports IterationEngine with all spec-listed methods (collectFeedback, diagnose, plan, execute, confirm, discard, _forkBranch, _callEngine, _createNodeOnBranch, _topologicalSort, _buildPrompt, _storePlan, _readPlan)
- 7 API routes registered: POST /plan, POST /execute, POST /confirm, POST /discard, GET /plans, GET /status/:planId, POST /approve-adjustment
- All spec constraints honored (no auto-execute, no auto-approval, branch isolation, topological order, ESM, zero deps, per-node failure tolerance)
- All unit tests pass offline with mocked HTTP + mocked LLM
- router.ts untouched except for the routeIteration import + app.use line
</success_criteria>

<output>
Create .planning/quick/260702-rg2-implement-iteration-engine-feedback-driv/260702-rg2-SUMMARY.md when done.
</output>
