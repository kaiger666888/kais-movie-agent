# Roadmap Archive: v4.0 Production Pipeline Remediation

**Shipped:** 2026-06-24
**Phases:** 26-30 (5 phases, 12 plans)
**Requirements:** 9 (PIPE-DATA-01/02, PIPE-RENDER-01/02, PIPE-INTEGRITY-01/02, PIPE-COMPOSE-01/02, PIPE-GUARD-01)
**Audit:** `passed` — see [v4.0-MILESTONE-AUDIT.md](../v4.0-MILESTONE-AUDIT.md)

## Overview

修复 2026-06-23 端到端数据流审计发现的 9 项沉默失败点，让 V6 管线从"单元测试全过但成片无法产出"升级到"能产成片"。Phase 编号继续 v3.0（19-25），v4.0 从 26 起。阶段划分遵循数据流依赖链：上游 data spine → 真实渲染 → 跨系统完整性/安全 → composition 尾部 + 质量门控。前 3 个 phase 为基础修复，使 Phase 29 的 composition 端到端测试有真实输入可用。

## Phases

- [x] **Phase 26: Data Spine Repair** — 修复 V6 数据流上游断裂（requirement.json 失写 + scene↔sts 时序倒置），为下游 composition 提供真实输入
- [x] **Phase 27: Real Render Path Restoration** — 修复真实渲染沉默失败（motion-preview Blender 字段大小写 + jimeng-client 死引用清理）
- [x] **Phase 28: Cross-System Integrity & Safety Hardening** — canvas 双写竞态修复 + SQL 注入面修复（独立 hardening track）
- [x] **Phase 29: Composition Tail + Quality Gate Activation** — composition phase 实现 + 文件名对齐 + consistency-guard 阻塞化 + 死代码清理（管线成片 + 质量门控统一在 composition 判定）
- [x] **Phase 30: End-to-End Shipping Verification** — 全链路 degraded E2E 跑通产出 master.mp4，验证 9 项审计点全部闭环

**Critical path:** 26 → 27 → 29 → 30 (Phase 28 独立 hardening 可并行)

## Phase Details

### Phase 26: Data Spine Repair

**Goal**: V6 数据流上游真实化 — character-generation 拿到真实角色数据、scene-generation 拿到真实 sts 产物（不再退回 fallback 默认值），让后续渲染/composition 测试有真实输入可用
**Depends on**: Nothing (first v4.0 phase — foundation for all downstream 修复)
**Requirements**: PIPE-DATA-01, PIPE-DATA-02
**Plans**: 2 (26-01 character data source migration, 26-02 scene↔sts stageOrder reorder)

**Shipped:**
- `_loadCharactersForGeneration` 改为读 `pain-report.json` (Tier 1) → `requirement.json` (Tier 2, legacy) → `pipeline.config.characters` (Tier 3, degraded)，Tier 2/3 都加 console.warn (SC#4)
- PHASES 数组 reorder: spatio-temporal-script(8) → scene-generation(9) → scene-selection(10)，stageOrder + 数组索引同步
- 13 new tests (7 character + 6 sts order)，baseline 461 → 474

### Phase 27: Real Render Path Restoration

**Goal**: 真实渲染路径不再沉默失败 — motion-preview 的 Blender 调用能成功提交任务并接收 taskId，jimeng-client 标注为 fallback-only
**Depends on**: Phase 26（character/scene 真实数据流就绪，渲染才有真实输入可测）
**Requirements**: PIPE-RENDER-01, PIPE-RENDER-02
**Plans**: 2 (27-01 motion-preview camelCase fix, 27-02 jimeng deprecate warn)

**Shipped:**
- motion-preview submitTask 改用 `taskType`/`taskId` (camelCase) — line 1074/1078 修复（line 1115 是 EvaluationCollector schema，不动）
- `_warnJimengDeprecate()` 模块级 helper + 3 处调用点（character-generation, soul-visual, scene-generation）包装，module-level flag 防日志洪水
- 9 new tests (4 motion-preview + 5 jimeng deprecate)，baseline 474 → 483

### Phase 28: Cross-System Integrity & Safety Hardening

**Goal**: 修复跨系统数据完整性（canvas 双写竞态）+ 安全（SQL 注入面）
**Depends on**: Nothing（独立 hardening track）
**Requirements**: PIPE-INTEGRITY-01, PIPE-INTEGRITY-02
**Plans**: 2 (28-01 canvas HTTP API migration, 28-02 SQL injection guard)

**Shipped:**
- `lib/canvas-content-sync.js` saveGraph 改用 `CanvasClient.saveCanvas()` HTTP API (`/api/canvas/v2/save-v2`)，删除 `execSync('sqlite3 ... UPDATE')`；loadGraph 保留 sqlite 直读（read-only）；HTTP 不可达时 degrade warn
- `bin/repair-canvas-truncated-scenes.js` 加 `assertPositiveInt(raw, label)` helper (`/^\d+$/` + `Number.isInteger && > 0`)，`--projectId` / `--episodesId` 都校验，stderr + exit 1
- 10 new tests (4 HTTP migration + 6 injection vectors including `1; DROP TABLE`)，baseline 483 → 493

### Phase 29: Composition Tail + Quality Gate Activation (v4.0 决战点)

**Goal**: composition phase 真实产出成片（master.mp4 + web-preview.mp4），delivery 能找到对应文件（文件名对齐），consistency-guard 在 composition 阶段阻塞化判定（fail 不再沉默吞掉）
**Depends on**: Phase 26（真实数据输入）+ Phase 27（真实渲染输出）+ Phase 28（canvas 数据完整不丢）
**Requirements**: PIPE-COMPOSE-01, PIPE-COMPOSE-02, PIPE-GUARD-01
**Plans**: 3 (29-01 composition output rename + web-preview, 29-02 delivery filename alignment, 29-03 guard blocking + dead code deletion)

**Shipped:**
- composition handler 输出改为 `master.mp4` + `web-preview.mp4`（FFmpeg transcode，degraded 时 0-byte 占位）
- delivery handler 检查 `master.mp4`（不是 `final.mp4`）+ degrade-tolerant `web-preview.mp4` + `_composition.delivered_mastermp4` marker in quality-report.json
- consistency-guard fail 时 throw `CONSISTENCY_BLOCKED` + 写 `consistency-blocked.json` with `_consistencyBlocked: true`，Pipeline.run catch 标记 episode failed
- **DELETED**: `lib/gate-constraints.js` (418 lines) + `lib/invariant-bus.js` (329 lines) — 0 生产 import 的死代码
- 12 new tests (4+4+4)，baseline 493 → 508 (final: 505 main + 3 e2e)

### Phase 30: End-to-End Shipping Verification (acceptance gate)

**Goal**: 验证 v4.0 全 9 项审计点闭环 — degraded E2E 跑通全 20 阶段实际产出 master.mp4
**Depends on**: Phase 29（composition 实现就绪）+ 所有前序 phase
**Requirements**: NONE (acceptance gate verifying Phases 26-29 closures)
**Plans**: 3 (30-01 degraded E2E test, 30-02 9-finding audit regression suite, 30-03 runbook update)

**Shipped:**
- `test/e2e/degraded-shipping.test.mjs` (3 tests) — Pipeline.run() across 20 stages produces real master.mp4 in degraded mode + `_composition.delivered_mastermp4 === true`
- `test/audit-v4-acceptance.test.mjs` (9 tests F1-F9) — automated regression for each audit finding via brace-depth-tracked sliceHandlerBody
- `docs/E2E-RUNBOOK.md` (324 → 478 lines) — added §0 "Shipping master.mp4 — Two Paths" (Path A degraded CI-verifiable, Path B real GPU operator-deferred) + Ship-Readiness Gate + 9-finding audit matrix
- baseline 508 → 517 (main test glob), 7 → 10 (e2e suite separate)

## Cross-cutting Constraints

- Existing 461/461 test baseline preserved (no regression) — ended at 517/517 (+56 tests)
- 零 npm 依赖 (PROJECT.md principles honored)
- DEGRADE-01/02/03 contracts maintained — every external call has degrade path
- 9/9 audit findings from 2026-06-23 closed with discrete regression tests
