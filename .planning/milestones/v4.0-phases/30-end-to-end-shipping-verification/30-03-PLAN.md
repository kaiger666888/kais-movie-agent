---
phase: 30-end-to-end-shipping-verification
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/E2E-RUNBOOK.md
autonomous: false
requirements: []
acceptance_scs: [SC4]
user_setup:
  - service: operator-gpu
    why: "Real-GPU master.mp4 production path is operator-deferred per v4.0 roadmap"
    dashboard_config:
      - task: "Real GPU E2E validation (out of Phase 30 scope, deferred to v4.1+)"
        location: "Operator runbook section — marked deferred, not exercised in CI"

must_haves:
  truths:
    - "E2E-RUNBOOK.md documents the degraded-mode path to produce master.mp4 (all 20 stages)"
    - "E2E-RUNBOOK.md documents the real-GPU path to produce master.mp4 (marked operator-deferred per v4.0 roadmap)"
    - "Both paths reference bin/pipeline.js run --episode X --to delivery as the entrypoint"
    - "The runbook references the 9-finding audit regression test as the gate for ship readiness"
  artifacts:
    - path: "docs/E2E-RUNBOOK.md"
      provides: "Operator runbook for producing master.mp4 in degraded + real GPU modes"
      contains: "master.mp4"
  key_links:
    - from: "docs/E2E-RUNBOOK.md"
      to: "bin/pipeline.js"
      via: "documented command invocation"
      pattern: "bin/pipeline\\.js run"
    - from: "docs/E2E-RUNBOOK.md"
      to: "test/audit-v4-acceptance.test.mjs"
      via: "ship-readiness gate reference"
      pattern: "audit-v4-acceptance"
---

<objective>
Update docs/E2E-RUNBOOK.md (currently 324 lines, exists) to document both degraded-mode and real-GPU paths for producing master.mp4, satisfying ROADMAP SC#4. Real GPU path is operator-deferred (out of v4.0 CI scope) but must be documented so operators know the procedure.

Purpose: Operators and future maintainers need a single document explaining how to ship a deliverable. SC#4 requires both paths documented; only the degraded path is exercised in Phase 30 automated tests.
Output: Updated docs/E2E-RUNBOOK.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-end-to-end-shipping-verification/30-CONTEXT.md

<interfaces>
<!-- Existing runbook to be extended, not recreated. Read first to preserve structure. -->
docs/E2E-RUNBOOK.md (324 lines, exists)

CLI contract (consistent with Plan 01):
  DEGRADED=1 node bin/pipeline.js run --episode <EP_ID> --to delivery
    → writes output/<EP_ID>/master.mp4 (placeholder in degraded, real mp4 with GPU)

Real-GPU path (operator-deferred, document only):
  - Requires external services reachable (GT client for Blender, dreamina CLI for jimeng-equivalent)
  - Produces real H.264 mp4 via composition handler (Phase 29 PIPE-COMPOSE-01)
  - Operator must set real API keys / service URLs and unset DEGRADED

Ship gate:
  - test/audit-v4-acceptance.test.mjs must pass (9 findings closed)
  - test/e2e/degraded-shipping.test.mjs must pass (E2E degraded path)
  - npm test baseline ≥ 461 (currently 508)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Read existing runbook and update with v4.0 shipping paths</name>
  <files>docs/E2E-RUNBOOK.md</files>
  <action>
    First read docs/E2E-RUNBOOK.md fully to understand existing structure (sections, headers, conventions). Preserve all existing content that is still accurate; only ADD or UPDATE sections for v4.0.

    Add or update the following sections (use existing header style):

    1. **Shipping master.mp4 — Two Paths** (new top-level section near the top, after the overview):
       - Brief intro: degraded path is CI-verifiable, real-GPU path requires operator setup
       - Both paths use the same entrypoint: `node bin/pipeline.js run --episode &lt;EP_ID&gt; --to delivery`

    2. **Path A: Degraded Mode (CI-verifiable)**:
       - Command: `DEGRADED=1 node bin/pipeline.js run --episode EP01 --to delivery`
       - Expected output: `output/&lt;EP_ID&gt;/master.mp4` (0-byte placeholder when composition falls back)
       - What "degraded" means: all external-service-dependent handlers (Blender/GT, jimeng/dreamina, canvas platform HTTP) fall back to placeholder/passthrough
       - When to use: smoke testing, CI, regression verification, no-GPU environments
       - Automated test: `node --test test/e2e/degraded-shipping.test.mjs`

    3. **Path B: Real GPU Mode (Operator)**:
       - Mark clearly as DEFERRED for full v4.0 CI validation — operator must validate per-episode
       - Command: `node bin/pipeline.js run --episode &lt;EP_ID&gt; --to delivery` (no DEGRADED env)
       - Prerequisites:
         - GT client reachable (Blender submitTask succeeds — see Phase 27 P01 fix)
         - dreamina CLI installed OR jimeng-client reachable (Phase 27 P02 — jimeng is fallback-only, deprecated warning emits)
         - External API keys configured (env vars — enumerate by grepping lib/ for process.env)
         - ffmpeg installed for web-preview.mp4 transcode (Phase 29 P01)
       - Expected output: real H.264 mp4 at `output/&lt;EP_ID&gt;/master.mp4` + sibling `web-preview.mp4`
       - When composition fails but pipeline completes: degraded placeholders still written (PIPE-COMPOSE-01 degrade path)
       - Operator checklist before declaring an episode shipped:
         - [ ] quality-report.json shows `_composition.delivered_mastermp4: true`
         - [ ] master.mp4 file size &gt; 0 (real video, not placeholder)
         - [ ] consistency-guard did not throw (check absence of consistency-blocked.json)
         - [ ] No `_consistencyBlocked: true` marker in quality-report.json

    4. **Ship-Readiness Gate** (cross-reference to test files):
       - Before tagging a release: `npm test` must pass with baseline ≥ 461 (currently 508)
       - `node --test test/audit-v4-acceptance.test.mjs` — 9 audit findings closed
       - `node --test test/e2e/degraded-shipping.test.mjs` — degraded path smoke
       - If any of the 9 audit tests fail: the v4.0 ship is BLOCKED. Do not tag.

    5. **Reference: 2026-06-23 Audit Matrix**:
       - Brief table listing the 9 findings + closing phase + verification command (mirror 30-CONTEXT.md matrix). This gives operators a single-source view of what was fixed and why.

    Tone: operator-facing, concrete commands only. No internal-only jargon. Cross-link to MEMORY entry project_pipeline-audit_2026-06-23.md for the original audit rationale.

    Do NOT delete existing runbook content unless it contradicts v4.0 (e.g., references to deleted gate-constraints.js / invariant-bus.js — those should be removed/updated since Phase 29 P03 deleted the files).
  </action>
  <verify>
    <automated>grep -c "master.mp4" docs/E2E-RUNBOOK.md | grep -qv '^0$' && grep -c "DEGRADED=1" docs/E2E-RUNBOOK.md | grep -qv '^0$' && grep -c "audit-v4-acceptance" docs/E2E-RUNBOOK.md | grep -qv '^0$' && test ! -f lib/gate-constraints.js && echo "runbook updated"</automated>
  </verify>
  <done>
    - docs/E2E-RUNBOOK.md contains explicit Path A (degraded) and Path B (real GPU) sections
    - Both paths reference `bin/pipeline.js run --to delivery` and `master.mp4`
    - Real GPU path is marked operator-deferred per v4.0 roadmap
    - Ship-readiness gate section references test/audit-v4-acceptance.test.mjs and test/e2e/degraded-shipping.test.mjs
    - No references to deleted gate-constraints.js / invariant-bus.js remain
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Updated docs/E2E-RUNBOOK.md with two-path shipping procedure (degraded + real GPU) and ship-readiness gate cross-references.
  </what-built>
  <how-to-verify>
    1. Open docs/E2E-RUNBOOK.md in an editor
    2. Confirm "Path A: Degraded Mode" section exists with the DEGRADED=1 command and master.mp4 output
    3. Confirm "Path B: Real GPU Mode" section exists, marked operator-deferred, with prerequisites (GT client, dreamina/jimeng, ffmpeg)
    4. Confirm "Ship-Readiness Gate" section references test/audit-v4-acceptance.test.mjs and test/e2e/degraded-shipping.test.mjs
    5. Confirm no stale references to gate-constraints.js / invariant-bus.js (deleted in Phase 29 P03)
    6. Confirm the 9-finding audit matrix is present (table form acceptable)
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| runbook → operator actions | Documentation guides operator commands; stale/wrong commands cause mis-shipping |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-05 | Information disclosure | stale references to deleted code | mitigate | Task explicitly removes references to gate-constraints.js / invariant-bus.js (deleted Phase 29 P03) |
| T-30-06 | Repudiation | operator claims "shipped per runbook" without running gate | mitigate | Ship-Readiness Gate section cross-references the executable test that fails if 9 findings regress |
</threat_model>

<verification>
- docs/E2E-RUNBOOK.md grep-confirmed to contain master.mp4, DEGRADED=1, audit-v4-acceptance references
- Human verification checkpoint confirms both paths documented and stale references removed
</verification>

<success_criteria>
- SC#4 (E2E-RUNBOOK.md documents both degraded + real GPU paths) satisfied
- Runbook is the single source of truth for shipping master.mp4
- Real GPU path clearly marked operator-deferred (consistent with v4.0 STATE.md Deferred Items)
</success_criteria>

<output>
Create `.planning/phases/30-end-to-end-shipping-verification/30-03-SUMMARY.md` when done
</output>
