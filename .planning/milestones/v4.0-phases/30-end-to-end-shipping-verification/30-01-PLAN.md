---
phase: 30-end-to-end-shipping-verification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - test/e2e/degraded-shipping.test.mjs
autonomous: true
requirements: []
acceptance_scs: [SC1, SC3]

must_haves:
  truths:
    - "bin/pipeline.js run --episode EP01 --to delivery in degraded mode completes all 20 stages without throwing"
    - "A master.mp4 placeholder file is produced under the episode output dir"
    - "The test uses an isolated temp workdir — no writes to output/EP01 in repo"
    - "Test suite baseline of 508 is preserved (no regression)"
  artifacts:
    - path: "test/e2e/degraded-shipping.test.mjs"
      provides: "E2E degraded-mode shipping test"
      contains: "bin/pipeline.js"
  key_links:
    - from: "test/e2e/degraded-shipping.test.mjs"
      to: "bin/pipeline.js"
      via: "child_process spawnSync node bin/pipeline.js run"
      pattern: "spawnSync.*bin/pipeline\\.js"
---

<objective>
Add an end-to-end test that exercises `bin/pipeline.js run --episode EP01 --to delivery` in degraded mode and asserts master.mp4 placeholder is produced, covering ROADMAP SC#1 and contributing to SC#3 (test baseline).

Purpose: Prove the v4.0 pipeline no longer silently breaks end-to-end — degraded mode (Phase 26-29 fixes in place) flows through all 20 stages and produces the deliverable file.
Output: One new test file under test/e2e/.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-end-to-end-shipping-verification/30-CONTEXT.md

<interfaces>
<!-- Pipeline CLI invocation contract (from bin/pipeline.js). -->
<!-- Test MUST invoke as subprocess, NOT import — to exercise real CLI path. -->

bin/pipeline.js run --episode <EP_ID> --to <phase-name>
  - Exit code 0 on success, non-zero on failure
  - Writes per-stage output under output/<EP_ID>/ by default
  - Accepts env DEGRADED=1 (or relies on degraded fallbacks when external services unreachable)
  - --to delivery runs all 20 stages through composition (PIPE-COMPOSE-01) then delivery (PIPE-COMPOSE-02)

Degraded-mode expectations (from Phase 29 SUMMARYs):
  - composition handler writes 0-byte master.mp4 placeholder when compose() returns null
  - delivery handler verifies master.mp4 exists, marks _composition.delivered_mastermp4 in quality-report.json
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write degraded E2E shipping test</name>
  <files>test/e2e/degraded-shipping.test.mjs</files>
  <behavior>
    - Test 1 (happy path): spawn `node bin/pipeline.js run --episode EP01-SMOKE --to delivery` with DEGRADED=1 in a temp workdir (mkdtempSync); assert exit code 0; assert output/&lt;EP&gt;/master.mp4 exists; assert output/&lt;EP&gt;/web-preview.mp4 exists (degrade-tolerant per PIPE-COMPOSE-02 — warn-not-fail, so assert master only strictly; web-preview soft-assert).
    - Test 2 (no repo pollution): after the test run, assert the temp workdir was cleaned up OR at minimum that output/EP01-SMOKE does NOT exist in the repo working tree (use a unique episode id like EP30-SMOKE to avoid colliding with operator data).
    - Test 3 (degraded marker): parse quality-report.json (or equivalent) and assert _composition.delivered_mastermp4 is truthy.
  </behavior>
  <action>
    Create the test file. Use node:test + node:assert/strict. Pattern after existing test/phases/*.test.mjs files for import style. Use child_process.spawnSync to invoke the CLI as a real subprocess (NOT import) so the actual CLI path is exercised.

    Workdir isolation (per CONTEXT.md Claude's Discretion): use fs.mkdtempSync(os.tmpdir(), 'kmai-e2e-30-') and pass --output-root (or equivalent bin/pipeline.js flag — verify by grepping bin/pipeline.js for --output or workdir). If no flag exists, set cwd via spawnSync options {cwd: tempDir} and run the pipeline with a relative output path so all writes land in the temp dir. Clean up with fs.rmSync(tempDir, {recursive:true, force:true}) in test teardown.

    Episode ID: use a smoke-only ID like 'EP30-E2E-SMOKE' to avoid colliding with real operator episodes (output/EP01 etc). Set DEGRADED=1 in env so all external-service-dependent handlers fall back to degraded mode (this is the established v2.0 Phase 17 pattern — composition placeholder path is gated on degraded per Phase 29 P01 SUMMARY).

    Assertions (strict, in order):
      1. spawnSync result.status === 0 (pipeline completed all 20 stages)
      2. fs.existsSync(path.join(tempDir, 'output', episodeId, 'master.mp4')) === true
      3. fs.existsSync for web-preview.mp4 — soft assert (if absent, log warn not fail, per PIPE-COMPOSE-02 degrade-tolerant spec)
      4. Read quality-report.json if present, assert JSON._composition.delivered_mastermp4 === true

    Timeout: spawnSync with timeout: 120000 (2 min) — degraded mode should finish in well under that.

    Do NOT add a Test 4+ — keep this plan scoped to SC#1 + SC#3. The audit-closure assertions belong in Plan 02.

    If `node bin/pipeline.js run` requires additional env (e.g., API keys for non-degraded paths), confirm via grep that DEGRADED=1 short-circuits them. If it does not, document in the test header and skip-on-missing-env using a guard.
  </action>
  <verify>
    <automated>node --test test/e2e/degraded-shipping.test.mjs</automated>
  </verify>
  <done>
    - Test file exists at test/e2e/degraded-shipping.test.mjs
    - All assertions pass when DEGRADED=1 is set
    - No new files appear under output/ in the repo working tree after the run
    - Full suite still passes: npm test (baseline 508 → 509+)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test process → filesystem | Test creates temp dir and invokes CLI that writes files there |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-01 | Tampering | temp workdir cleanup | mitigate | Use fs.rmSync in test teardown; use unique episode ID to avoid colliding with real data |
| T-30-02 | DoS | pipeline subprocess timeout | mitigate | spawnSync timeout: 120000 — test fails fast if pipeline hangs |
</threat_model>

<verification>
- npm test passes with the new test added (≥509 tests)
- No regression in existing 508 tests
- Manual smoke: DEGRADED=1 node bin/pipeline.js run --episode EP30-SMOKE-VERIFY --to delivery exits 0 and produces master.mp4
</verification>

<success_criteria>
- SC#1 (degraded E2E produces master.mp4) is verified by an automated test
- SC#3 baseline grows (508 → 509+) with no regressions
- Test is hermetic (temp workdir, no repo pollution)
</success_criteria>

<output>
Create `.planning/phases/30-end-to-end-shipping-verification/30-01-SUMMARY.md` when done
</output>
