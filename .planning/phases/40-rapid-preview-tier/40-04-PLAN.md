---
phase: 40-rapid-preview-tier
plan: 04
type: tdd
wave: 4
depends_on:
  - 40-01
  - 40-02
  - 40-03
files_modified:
  - /data/workspace/hermes-agent/skills/kais-movie-pipeline/tests/test_p10b_dual_engine_e2e.py
  - /data/workspace/hermes-agent/skills/kais-movie-pipeline/tests/test_p10b_jsonl_format.py
  - /data/workspace/hermes-agent/skills/kais-movie-pipeline/tests/test_p10b_degrade_warning.py
  - /data/workspace/hermes-agent/skills/kais-movie-pipeline/tests/test_p10b_full_dag_integration.py
  - /data/workspace/hermes-agent/skills/kais-movie-pipeline/tests/test_v50_regression.py
autonomous: true
requirements:
  - RAPID-PREVIEW-02
  - RAPID-PREVIEW-05
  - RAPID-PREVIEW-07

must_haves:
  truths:
    - "Both engine paths (slideshow via mocked FFmpeg subprocess, ltx via httpx.MockTransport) produce preview variants that are written to rapid-preview-clips JSONL"
    - "The degrade path emits a WARN log (not INFO, not ERROR) with the literal token 'preview_skipped' — verified via captured log level + message content"
    - "The rapid-preview-clips JSONL file parses line-by-line as valid JSON; each line has all 6 required fields with correct types"
    - "The degrade path writes the preview_skipped flag to the 'episode-meta' AssetBus slot (NOT 'pipeline-state') with shape {episode_id, preview_skipped: True, skip_reason}"
    - "V5.0's 502-test baseline is preserved — explicit regression test asserts the canonical V5.0 test files still pass"
    - "Cross-engine integration works: p10b with engine='slideshow' writes slideshow-tagged records; p10b with KAIS_PREVIEW_ENGINE=ltx writes ltx-tagged records"
    - "Full DAG integration: runner iterates PHASE_REGISTRY to p10b and `result['phases']['p10b_rapid_preview']` exists with the expected output shape after a full DAG run with mocked engines (WARNING #9 fix)"
  artifacts:
    - path: "/data/workspace/hermes-agent/skills/kais-movie-pipeline/tests/test_p10b_dual_engine_e2e.py"
      provides: "End-to-end dual-engine verification with mocked subprocess + mocked HTTP"
      contains: "class TestP10bDualEngineE2E"
    - path: "/data/workspace/hermes-agent/skills/kais-movie-pipeline/tests/test_p10b_jsonl_format.py"
      provides: "rapid-preview-clips JSONL format validation (6 required fields, correct types, valid JSON per line)"
      contains: "class TestPreviewClipsJsonlFormat"
    - path: "/data/workspace/hermes-agent/skills/kais-movie-pipeline/tests/test_p10b_degrade_warning.py"
      provides: "WARN-level degrade log verification (level=WARNING, message contains 'preview_skipped') + episode-meta slot assertion"
      contains: "class TestP10bDegradeWarning"
    - path: "/data/workspace/hermes-agent/skills/kais-movie-pipeline/tests/test_p10b_full_dag_integration.py"
      provides: "Full DAG integration test asserting PHASE_REGISTRY iterates to p10b with expected output shape (WARNING #9)"
      contains: "class TestP10bFullDagIntegration"
    - path: "/data/workspace/hermes-agent/skills/kais-movie-pipeline/tests/test_v50_regression.py"
      provides: "Explicit V5.0 regression assertion — runs a curated subset of V5.0 tests + asserts the 502-test baseline"
      contains: "class TestV50Regression"
  key_links:
    - from: "test_p10b_dual_engine_e2e.py"
      to: "p10b_rapid_preview.run() with engine injection"
      via: "monkeypatch select_engine to return FakeSlideshowEngine or FakeLTXEngine"
      pattern: "select_engine"
    - from: "test_p10b_jsonl_format.py"
      to: "AssetBus.read_lines('rapid-preview-clips')"
      via: "Parse each line as JSON, assert 6 required fields present"
      pattern: "read_lines.*rapid-preview-clips"
    - from: "test_p10b_degrade_warning.py"
      to: "caplog.records (or captured logging.Handler) + asset_bus_write capture"
      via: "Assert at least one record has levelno==logging.WARNING and message contains 'preview_skipped'; assert one asset_bus_write call targets 'episode-meta' slot"
      pattern: "preview_skipped"
    - from: "test_p10b_full_dag_integration.py"
      to: "run_episode() with mocked engines"
      via: "Assert result['phases']['p10b_rapid_preview'] exists with output shape {phase, outputs, gate}"
      pattern: "p10b_rapid_preview"
    - from: "test_v50_regression.py"
      to: "V5.0 test files (curated list)"
      via: "pytest.importorskip + subprocess.run(['pytest', ...])"
      pattern: "test_phase_registry_full"
---

<objective>
Deliver RAPID-PREVIEW-07 (the test coverage requirement) as a dedicated verification plan. Plans 02 and 03 already include unit tests for the engines and the phase module — this plan adds CROSS-CUTTING integration tests that verify the assembly works end-to-end with BOTH engines, that degrade warnings are visible at the right log level, that the JSONL format is machine-parseable, that the preview_skipped flag lands on the correct AssetBus slot (episode-meta — BLOCKER #1), that the runner correctly iterates PHASE_REGISTRY to p10b in a full DAG run (WARNING #9), and that V5.0's 502-test baseline is explicitly preserved.

Purpose: This plan is `type: tdd` because every behavior is an assertion against an observable output (log record, JSONL file content, slot name, test count). It closes the verification gap that plans 02/03 unit tests alone cannot cover (cross-engine paths, log level, format invariants, slot-name disambiguation, full-DAG integration, baseline regression).

BLOCKER #5 fix: All slot-name references in this plan use `rapid-preview-clips` (NOT `preview-clips`). The latter is documented in v3.0-era SKILL.md for a future p06.5 phase; renaming avoids namespace collision.

BLOCKER #1 fix: Degrade tests assert the preview_skipped flag is written to the `episode-meta` AssetBus slot (NOT `pipeline-state`, which is a separate PipelineStateStore file).

WARNING #9 fix: Add a dedicated E2E integration test (`test_p10b_full_dag_integration.py`) that asserts `result["phases"]["p10b_rapid_preview"]` exists with the expected output shape after running `run_episode()` with mocked engines across the full DAG.

Output:
- `tests/test_p10b_dual_engine_e2e.py` — mocked dual-engine integration
- `tests/test_p10b_jsonl_format.py` — JSONL format contract verification (rapid-preview-clips slot)
- `tests/test_p10b_degrade_warning.py` — WARN-level degrade log assertion + episode-meta slot assertion
- `tests/test_p10b_full_dag_integration.py` — Full DAG integration test (WARNING #9)
- `tests/test_v50_regression.py` — explicit V5.0 regression guard
</objective>

<execution_context>
@/home/kai/.claude/get-shit-done/workflows/execute-plan.md
@/home/kai/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/data/workspace/kais-movie-agent/.planning/PROJECT.md
@/data/workspace/kais-movie-agent/.planning/ROADMAP.md
@/data/workspace/kais-movie-agent/.planning/phases/40-rapid-preview-tier/40-CONTEXT.md
@/data/workspace/hermes-agent/skills/kais-movie-pipeline/tests/test_p11_unit.py
@/data/workspace/hermes-agent/skills/kais-movie-pipeline/tests/test_p10b_unit.py
@/data/workspace/hermes-agent/skills/kais-movie-pipeline/tests/test_runner_full_dag.py
@/data/workspace/hermes-agent/plugins/kais_aigc/preview_engine.py
@/data/workspace/hermes-agent/plugins/pipeline_state/asset_bus.py
@/data/workspace/hermes-agent/plugins/kais_aigc/gold_team.py

<interfaces>
<!-- Test contracts the executor MUST honor. -->

From test_p11_unit.py — test double + assertion patterns to mirror:
- `_SKILL_DIR = Path(__file__).resolve().parent.parent; sys.path.insert(0, str(_SKILL_DIR))` for skill-local imports
- Fake delegate_task / fake asset_bus_read / fake asset_bus_write via simple closures or classes
- pytest fixtures for tmp_path (asset bus workdir)

From test_runner_full_dag.py (V5.0) — patterns the new full-DAG integration test (WARNING #9) MUST mirror:
- `_make_full_dag_delegate_spy()` — full-DAG delegate spy returning canned outputs for every phase
- `_StubStore` + `_StubBus` — minimal in-memory store + bus for runner injection
- `RunnerConfig(workdir=..., enable_gates=False)` + `run_episode(episode_id, cfg, inject={"store":..., "bus":..., "delegate_task":...})`
- The test runs the full DAG (14 phases including p10b after plan 01) with mocked engines

From pytest's caplog fixture (built-in):
```python
def test_warn_emitted(self, caplog):
    with caplog.at_level(logging.WARNING):
        result = p10b.run(...)
    warn_records = [r for r in caplog.records if r.levelno == logging.WARNING]
    assert any("preview_skipped" in r.getMessage() for r in warn_records)
```

From AssetBus round-trip test pattern (BLOCKER #5 — slot name is rapid-preview-clips):
```python
bus = AssetBus(tmp_path)
bus.append_line("rapid-preview-clips", {"shot_id": "s1", ...})
records = bus.read_lines("rapid-preview-clips")
assert len(records) == 1
assert records[0]["shot_id"] == "s1"
```

JSONL format contract (RAPID-PREVIEW-04 + RAPID-PREVIEW-07c):
```python
REQUIRED_FIELDS = {"shot_id": str, "variant_id": str, "structure_delta": dict,
                   "clip_path": str, "generation_time_ms": int, "engine": str}
VALID_ENGINE_VALUES = {"slideshow", "ltx"}
VALID_DELTA_KEYS = {"hook_position_sec", "emotion_sequence", "turning_points_sec", "ending_state"}
```

Episode-meta slot contract (BLOCKER #1):
```python
# On full-degrade, p10b writes:
asset_bus_write("episode-meta", {
    "episode_id": str,        # the episode_id arg passed to run()
    "preview_skipped": True,  # bool, always True on full-degrade
    "skip_reason": str,       # human-readable, e.g., "all_variants_degraded: 12/12"
})
# The slot name is "episode-meta" — NOT "pipeline-state" (which is a separate PipelineStateStore file)
```

V5.0 regression baseline (canonical test files — DO NOT MODIFY beyond what plan 01 already updated):
- skills/kais-movie-pipeline/tests/test_phase_registry_full.py (plan 01 updated it to 14 phases)
- skills/kais-movie-pipeline/tests/test_p01_unit.py through test_p13_unit.py (12 phase unit tests — must still pass)
- skills/kais-movie-pipeline/tests/test_runner_full_dag.py (plan 01 updated phase counts 13→14, 6→7 for resumed slice; must include p10b in DAG)
- skills/kais-movie-pipeline/tests/test_runner.py (runner mechanics — unaffected by p10b insertion)
- skills/kais-movie-pipeline/tests/test_e2e_degraded.py (V5.0 degraded E2E — plan 01 updated phase count 13→14; must still pass)
- plugins/pipeline_state/tests/test_asset_bus.py (plan 01 added rapid-preview-clips + episode-meta slot tests; must still pass)
- plugins/kais_aigc/tests/test_gold_team.py (unaffected — must still pass)
- plugins/review_gates/tests/* (unaffected — must still pass)
</interfaces>
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: Dual-engine end-to-end test (mocked FFmpeg + mocked HTTP)</name>
  <files>
    /data/workspace/hermes-agent/skills/kais-movie-pipeline/tests/test_p10b_dual_engine_e2e.py
  </files>
  <read_first>
    - /data/workspace/hermes-agent/skills/kais-movie-pipeline/tests/test_p11_unit.py (FULL FILE — mirror the test double patterns for asset_bus_read / asset_bus_write / delegate_task + parallel_shots)
    - /data/workspace/hermes-agent/skills/kais-movie-pipeline/tests/test_p10b_unit.py (FULL FILE — the unit tests from plan 03; this task EXTENDS them with cross-engine integration scenarios)
    - /data/workspace/hermes-agent/skills/kais-movie-pipeline/pipeline/phases/p10b_rapid_preview.py (FULL FILE — the module under test)
    - /data/workspace/hermes-agent/plugins/kais_aigc/preview_engine.py (FULL FILE — select_engine + PreviewEngine contract)
  </read_first>
  <behavior>
    - Test 1 (slideshow E2E): With `KAIS_PREVIEW_ENGINE` unset (default → slideshow) AND monkeypatched `preview_engine.select_engine` to return a `FakeSlideshowEngine` whose `generate()` returns `{"clip_path": "/out.mp4", "generation_time_ms": 9500, "engine": "slideshow"}`, `p10b.run()` with 2 shots × 3 variants produces 6 records written via `asset_bus_write("rapid-preview-clips", ...)`. Each record's `engine` field equals `"slideshow"`.
    - Test 2 (ltx E2E): Same as Test 1 but with `KAIS_PREVIEW_ENGINE=ltx` AND monkeypatched select_engine returning `FakeLTXEngine` whose `generate()` returns `{"clip_path": "/out.mp4", "generation_time_ms": 1200, "engine": "ltx"}`. Each record's `engine` field equals `"ltx"`.
    - Test 3 (engine injection via monkeypatch): Verify that p10b.run() calls `preview_engine.select_engine()` exactly ONCE per phase invocation (not per variant) — confirmed by counting calls on a wrapped select_engine.
    - Test 4 (parallel_shots=4 fan-out): 4 shots × 3 variants = 12 generate() calls with parallel_shots=4. Use a thread-safe counter (threading.Lock + int) — final count == 12.
    - Test 5 (output envelope shape): Both engines return `{"phase": "p10b_rapid_preview", "outputs": {"variants_generated": <int>, "variants_degraded": <int>}, "gate": None}` — variants_generated + variants_degraded == total expected variants.
  </behavior>
  <action>
    RED: Write `test_p10b_dual_engine_e2e.py` with a `TestP10bDualEngineE2E` class containing the 5 tests above.

    Test doubles to define in the file (mirroring test_p11_unit.py patterns):
    - `FakeSlideshowEngine(PreviewEngine)`: `generate()` returns a fixed slideshow-tagged success envelope; constructor accepts a thread-safe call counter.
    - `FakeLTXEngine(PreviewEngine)`: `generate()` returns a fixed ltx-tagged success envelope; constructor accepts a thread-safe call counter.
    - `_make_fake_asset_bus()`: returns a (read_callable, write_callable, records_dict) tuple where write_callable captures all writes by slot into records_dict.
    - `_fake_delegate_task(goal, context, toolsets)`: returns `{"summary": ""}` (p10b does not call delegate_task, so the mock just needs to exist).

    Monkeypatch `preview_engine.select_engine` via:
    ```python
    import pipeline.phases.p10b_rapid_preview as p10b_mod
    monkeypatch.setattr(p10b_mod, "select_engine", lambda *a, **kw: fake_engine)
    ```
    NOTE: p10b module imports `select_engine` at module load time — verify whether the import is `from plugins.kais_aigc.preview_engine import select_engine` (binds the symbol in p10b's namespace, monkeypatch target is `p10b_mod.select_engine`) vs `from plugins.kais_aigc import preview_engine` then `preview_engine.select_engine()` (monkeypatch target is `preview_engine.select_engine`). Read the plan 03 implementation to determine which form was used, and match the monkeypatch target accordingly. Document this in the test file docstring.

    GREEN: No production code changes needed — this task is pure verification. If any test FAILS, the failure indicates a bug in plan 02 or plan 03; surface it back to the relevant plan (do NOT silently patch production code here — that's plan-revision territory).
  </action>
  <verify>
    <automated>cd /data/workspace/hermes-agent && python -m pytest skills/kais-movie-pipeline/tests/test_p10b_dual_engine_e2e.py -x -q</automated>
  </verify>
  <done>
    - 5 dual-engine E2E tests pass
    - Both slideshow and ltx paths produce rapid-preview-clips records
    - select_engine called exactly once per phase run
    - parallel_shots=4 correctly fans out 12 generate() calls for 4-shot fixture
  </done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: rapid-preview-clips JSONL format validation</name>
  <files>
    /data/workspace/hermes-agent/skills/kais-movie-pipeline/tests/test_p10b_jsonl_format.py
  </files>
  <read_first>
    - /data/workspace/hermes-agent/plugins/pipeline_state/asset_bus.py (lines 503-540 — append_line + read_lines API; lines 40-282 — ASSET_SCHEMA including rapid-preview-clips + episode-meta from plan 01)
    - /data/workspace/hermes-agent/skills/kais-movie-pipeline/tests/test_p10b_unit.py (the unit tests from plan 03 — this task EXTENDS them with format invariants)
    - /data/workspace/hermes-agent/plugins/pipeline_state/tests/test_asset_bus.py (FULL FILE — mirror existing JSONL test patterns for finetune-dataset)
  </read_first>
  <behavior>
    - Test 1 (file extension): After writing at least 1 record, the rapid-preview-clips file path ends in `.jsonl` (assert via AssetBus internals or by listing tmp_path/.pipeline-assets/).
    - Test 2 (one JSON object per line): Read the raw file content; split by newline; each non-empty line parses as `json.loads(line)` without raising.
    - Test 3 (6 required fields): For each parsed record, assert all 6 keys present: `shot_id, variant_id, structure_delta, clip_path, generation_time_ms, engine`. No missing keys.
    - Test 4 (field types): `isinstance(record["shot_id"], str)`; `isinstance(record["variant_id"], str)`; `isinstance(record["structure_delta"], dict)`; `isinstance(record["clip_path"], str)`; `isinstance(record["generation_time_ms"], int)` (NOT float — strict); `isinstance(record["engine"], str)`.
    - Test 5 (engine value domain): `record["engine"] in {"slideshow", "ltx"}` — no other values allowed.
    - Test 6 (single-delta in record): `len(record["structure_delta"]) == 1` for every record; the single key is in `{"hook_position_sec", "emotion_sequence", "turning_points_sec", "ending_state"}`.
    - Test 7 (variant_id naming): `record["variant_id"]` matches pattern `"{shot_id}__v{N}_{param_name}"` where N is 1-3 and param_name matches the structure_delta key. Validate via regex or string split.
    - Test 8 (append semantics): Write 3 records; read_lines returns them in insertion order (no reorder, no dedup).
    - Test 9 (empty slot reads as []): `AssetBus(tmp_path).read_lines("rapid-preview-clips")` returns `[]` when no file exists yet.
    - Test 10 (integration with p10b output): Run p10b with a mocked engine producing 2 records; then read the rapid-preview-clips slot via a real AssetBus instance pointed at the same tmp_path; verify the 6-field + single-delta contract on the actual written records.
    - Test 11 (BLOCKER #4 cycling matrix coverage): Run p10b across a 4-shot fixture with a mocked engine; collect all records; assert `turning_points_sec` appears as a `structure_delta` key in ≥1 variant across the episode (proves the cycling matrix from plan 03 covers all 4 params).
  </behavior>
  <action>
    RED: Write `test_p10b_jsonl_format.py` with a `TestPreviewClipsJsonlFormat` class containing the 11 tests above (Test 11 is the BLOCKER #4 cross-plan assertion — same invariant as plan 03 Task 1 Test 3, but here verified at the JSONL file level rather than the unit-test level). Use `tmp_path` fixture for an isolated AssetBus workdir.

    GREEN: No production code changes — pure verification. If a test fails, the failure indicates a contract violation in plan 03's record-construction code; surface back to plan 03 (do NOT patch here).

    NOTE on Test 10 + Test 11: these require importing both AssetBus (from plugins.pipeline_state) AND p10b_rapid_preview (from pipeline.phases). Use the sys.path manipulation pattern from test_p11_unit.py to make both importable. The test creates a real AssetBus(tmp_path), wraps its read/write methods as the callables passed to p10b.run(), runs p10b, then re-opens AssetBus(tmp_path) to read what was written (simulating a fresh process reading the same files).

    For Test 11 (BLOCKER #4), use a 4-shot fixture (matching plan 03's cycling matrix test) — iterate the 4-shot run, collect every record's structure_delta keys, assert `"turning_points_sec" in collected_keys`.
  </action>
  <verify>
    <automated>cd /data/workspace/hermes-agent && python -m pytest skills/kais-movie-pipeline/tests/test_p10b_jsonl_format.py -x -q</automated>
  </verify>
  <done>
    - 11 JSONL format tests pass (including Test 11 BLOCKER #4 cycling matrix coverage)
    - rapid-preview-clips file is valid JSONL (one JSON object per non-blank line)
    - Every record carries all 6 required fields with correct types
    - structure_delta always single-key with a valid param name
    - variant_id follows the documented naming convention
    - Cycling matrix covers all 4 params across a multi-shot episode (turning_points_sec appears in ≥1 variant)
  </done>
</task>

<task type="tdd" tdd="true">
  <name>Task 3: WARN-level degrade log verification + episode-meta slot assertion (RAPID-PREVIEW-05, BLOCKER #1)</name>
  <files>
    /data/workspace/hermes-agent/skills/kais-movie-pipeline/tests/test_p10b_degrade_warning.py
  </files>
  <read_first>
    - /data/workspace/hermes-agent/skills/kais-movie-pipeline/pipeline/phases/p10b_rapid_preview.py (FULL FILE — focus on the degrade WARN path implemented in plan 03 Task 2)
    - /data/workspace/hermes-agent/skills/kais-movie-pipeline/tests/test_p10b_unit.py (the TestP10bDegradePath class from plan 03 Task 2 — this task makes the WARN-level assertion STRICTER and more comprehensive)
    - /data/workspace/kais-movie-agent/.planning/phases/40-rapid-preview-tier/40-CONTEXT.md (DECISIONS section — "Degradation signaling" decision: logger.warning() + preview_skipped=True written to the **episode-meta** AssetBus slot — BLOCKER #1)
  </read_first>
  <behavior>
    - Test 1 (level is WARNING, not INFO/ERROR): On full-degrade scenario, at least one log record has `r.levelno == logging.WARNING`. Assert NOT `logging.INFO`, NOT `logging.ERROR` (INFO would be silent-swallow; ERROR would crash monitoring).
    - Test 2 (message contains "preview_skipped"): The WARNING record's `getMessage()` contains the literal substring `"preview_skipped"` — this is the canonical token monitoring/alerting will grep for.
    - Test 3 (message contains episode_id): The WARNING record's message contains the actual `episode_id` argument value, so operators can correlate which episode degraded.
    - Test 4 (BLOCKER #1 — preview_skipped flag persisted to episode-meta slot): After full-degrade, `asset_bus_write` was called with `("episode-meta", {"episode_id": ..., "preview_skipped": True, "skip_reason": ...})` — verify by capturing writes and asserting one matches this shape. CRITICAL: the slot name MUST be `"episode-meta"` (NOT `"pipeline-state"`). The dict MUST contain exactly the 3 keys `episode_id`, `preview_skipped: True`, `skip_reason: <str>`.
    - Test 5 (no WARN on partial degrade): If only 1 of 3 variants degrades (2 succeed), NO WARNING record is emitted at the episode level — the per-variant degrade is counted in `outputs.variants_degraded` but does not trigger the episode-level WARN. Assert `len([r for r in caplog.records if r.levelno >= logging.WARNING]) == 0`.
    - Test 6 (no WARN on full success): If all variants succeed, NO WARNING record is emitted. `outputs.variants_degraded == 0` and no preview_skipped flag is written.
    - Test 7 (WARN fires exactly once for multi-shot full degrade): 4 shots × 3 variants = 12 all-degrade → exactly 1 WARNING record emitted (not 12) — the WARN is episode-level, not per-shot. AND exactly 1 asset_bus_write call to "episode-meta" (not 12).
    - Test 8 (return value on full degrade): `run()` returns `{"phase": "p10b_rapid_preview", "outputs": {"variants_generated": 0, "variants_degraded": 12}, "gate": None}` — and does NOT raise.
    - Test 9 (BLOCKER #1 negative assertion — NOT pipeline-state): Capture all `asset_bus_write` calls during a full-degrade run and assert NO call has `args[0] == "pipeline-state"` (proves the slot rename landed — only "episode-meta" + "rapid-preview-clips" should appear, and "rapid-preview-clips" should have ZERO writes on full-degrade since no variants succeeded).
  </behavior>
  <action>
    RED: Write `test_p10b_degrade_warning.py` with a `TestP10bDegradeWarning` class containing the 9 tests above. Use pytest's `caplog` fixture (built-in) to capture log records.

    GREEN: No production code changes — pure verification against the plan 03 implementation. If any test fails, plan 03's degrade path has a bug; surface back to plan 03 Task 2.

    Test double: define `AlwaysDegradeEngine(PreviewEngine)` whose `generate()` returns `{"degraded": True, "engine": "slideshow", "reason": "test forced degrade"}`. Also `PartialDegradeEngine(PreviewEngine)` that degrades only when `structure_delta` matches a specific key (e.g., always degrades on `"emotion_sequence"` variant, succeeds on others) — for Test 5.

    For Test 4 + Test 9 (BLOCKER #1), capture `asset_bus_write` calls into a list and assert:
    - Test 4: ONE call has `args[0] == "episode-meta"` AND `args[1]` is a dict with keys `{episode_id, preview_skipped, skip_reason}` AND `args[1]["preview_skipped"] is True`.
    - Test 9: ZERO calls have `args[0] == "pipeline-state"` (negative assertion — proves BLOCKER #1 fix landed).
  </action>
  <verify>
    <automated>cd /data/workspace/hermes-agent && python -m pytest skills/kais-movie-pipeline/tests/test_p10b_degrade_warning.py -x -q</automated>
  </verify>
  <done>
    - 9 degrade-warning tests pass (including Test 4 + Test 9 for BLOCKER #1)
    - WARN level (not INFO, not ERROR) verified
    - "preview_skipped" canonical token present in message
    - Episode-level WARN fires exactly once on full degrade; silent on partial degrade
    - preview_skipped flag persisted to **episode-meta** AssetBus slot on full degrade (BLOCKER #1 — NOT pipeline-state, proven by Test 9 negative assertion)
  </done>
</task>

<task type="tdd" tdd="true">
  <name>Task 4: Full DAG integration test (WARNING #9) — runner iterates PHASE_REGISTRY to p10b</name>
  <files>
    /data/workspace/hermes-agent/skills/kais-movie-pipeline/tests/test_p10b_full_dag_integration.py
  </files>
  <read_first>
    - /data/workspace/hermes-agent/skills/kais-movie-pipeline/tests/test_runner_full_dag.py (FULL FILE — mirror the `_make_full_dag_delegate_spy`, `_StubStore`, `_StubBus`, `RunnerConfig`, `run_episode` patterns)
    - /data/workspace/hermes-agent/skills/kais-movie-pipeline/pipeline/phases/p10b_rapid_preview.py (FULL FILE — the module under test)
    - /data/workspace/hermes-agent/plugins/kais_aigc/preview_engine.py (FULL FILE — to monkeypatch select_engine for the DAG run)
    - /data/workspace/hermes-agent/skills/kais-movie-pipeline/pipeline/runner.py (FULL FILE — to understand run_episode + inject contract)
  </read_first>
  <behavior>
    - Test 1 (p10b appears in result["phases"]): After a full DAG run with mocked engines (monkeypatch `preview_engine.select_engine` to return a FakeSlideshowEngine that succeeds), `result["phases"]["p10b_rapid_preview"]` exists as a dict.
    - Test 2 (p10b output shape): `result["phases"]["p10b_rapid_preview"]` is a dict with `phase == "p10b_rapid_preview"`, `outputs` is a dict with keys `variants_generated` (int) + `variants_degraded` (int), and `gate is None`.
    - Test 3 (p10b runs between p10 and p11): Inspect the captured delegate/asset_bus call order OR the checkpoint save order — verify p10_voice runs before p10b_rapid_preview which runs before p11_video_render. (Pattern: capture save calls from _StubStore and assert the saved_phases list contains ["p10_voice", "p10b_rapid_preview", "p11_video_render"] in that relative order.)
    - Test 4 (runner handles p10b degrade gracefully in-DAG): With monkeypatched select_engine returning AlwaysDegradeEngine, the full DAG run still completes (p10b returns its degrade envelope, runner proceeds to p11). `result["phases"]["p10b_rapid_preview"]` exists with `outputs.variants_degraded == total_variants` AND `outputs.variants_generated == 0`. The runner does NOT raise.
    - Test 5 (rapid-preview-clips slot is populated after successful DAG run): After Test 1's run, the AssetBus's rapid-preview-clips slot has ≥1 record (use `_StubBus` to capture writes, OR re-read the bus's `.pipeline-assets/rapid-preview-clips.jsonl` file). Each record has all 6 fields per the format contract.
    - Test 6 (PHASE_REGISTRY index sanity): `[e["id"] for e in PHASE_REGISTRY].index("p10b_rapid_preview") == 11` (after p10_voice at 10, before p11_video_render at 12) — verifies the plan 01 insertion landed and survived through plan 03.
  </behavior>
  <action>
    RED: Write `test_p10b_full_dag_integration.py` with a `TestP10bFullDagIntegration` class containing the 6 tests above. Mirror the test_runner_full_dag.py file header (sys.path setup, imports, `_StubStore`, `_StubBus`, `_make_full_dag_delegate_spy`).

    The key setup: monkeypatch `preview_engine.select_engine` BEFORE calling `run_episode(...)` so that p10b's call to `select_engine()` returns the fake engine. Since p10b imports `select_engine` into its own module namespace (per plan 03), the monkeypatch target is `pipeline.phases.p10b_rapid_preview.select_engine` (verify by reading plan 03's actual import statement).

    For Test 5 (rapid-preview-clips populated), use `_StubBus` to capture all `append_line`/`write` calls — assert that ≥1 call has `slot == "rapid-preview-clips"` AND the line_obj has all 6 required fields.

    GREEN: No production code changes — pure integration verification. If any test fails, the failure indicates either (a) plan 01's PHASE_REGISTRY insertion didn't survive, (b) plan 03's run() raises unexpectedly, or (c) the runner doesn't correctly dispatch to p10b. Surface back to the relevant plan.
  </action>
  <verify>
    <automated>cd /data/workspace/hermes-agent && python -m pytest skills/kais-movie-pipeline/tests/test_p10b_full_dag_integration.py -x -q</automated>
  </verify>
  <done>
    - 6 full-DAG integration tests pass (WARNING #9 fix)
    - Runner correctly iterates PHASE_REGISTRY to p10b
    - p10b output shape verified in result["phases"]["p10b_rapid_preview"]
    - p10b runs between p10 and p11 (checkpoint save order verified)
    - Degrade path doesn't break the DAG run (runner proceeds to p11)
    - rapid-preview-clips slot populated on success
  </done>
</task>

<task type="tdd" tdd="true">
  <name>Task 5: V5.0 explicit regression guard (502-test baseline preserved)</name>
  <files>
    /data/workspace/hermes-agent/skills/kais-movie-pipeline/tests/test_v50_regression.py
  </files>
  <read_first>
    - /data/workspace/hermes-agent/skills/kais-movie-pipeline/tests/test_phase_registry_full.py (FULL FILE — the canonical V5.0+1 test that plan 01 updated; this regression test asserts it still passes)
    - /data/workspace/hermes-agent/skills/kais-movie-pipeline/tests/test_runner_full_dag.py (FULL FILE — V5.0 DAG traversal test; plan 01 updated phase counts; must include p10b)
    - /data/workspace/hermes-agent/skills/kais-movie-pipeline/tests/test_e2e_degraded.py (FULL FILE — V5.0 degraded E2E test; plan 01 updated phase count; must still pass)
  </read_first>
  <behavior>
    - Test 1 (registry count + order): `python -m pytest skills/kais-movie-pipeline/tests/test_phase_registry_full.py -q` exits 0. Assert via `subprocess.run([sys.executable, "-m", "pytest", "..."], check=True)`.
    - Test 2 (full DAG traversal): `python -m pytest skills/kais-movie-pipeline/tests/test_runner_full_dag.py -q` exits 0. The runner must traverse p10 → p10b → p11 cleanly (p10b.run no longer raises NotImplementedError after plan 03).
    - Test 3 (V5.0 degraded E2E): `python -m pytest skills/kais-movie-pipeline/tests/test_e2e_degraded.py -q` exits 0.
    - Test 4 (asset_bus V5.0 + Phase 40 slots): `python -m pytest plugins/pipeline_state/tests/test_asset_bus.py plugins/pipeline_state/tests/test_asset_bus_phase35_slots.py -q` exits 0.
    - Test 5 (gold_team unaffected): `python -m pytest plugins/kais_aigc/tests/test_gold_team.py -q` exits 0 (if the file exists; skip with pytest.importorskip if not).
    - Test 6 (review gates unaffected): `python -m pytest plugins/review_gates/tests/ -q` exits 0.
    - Test 7 (total test count ≥ V5.0 baseline): Run the full test suite and assert the total passing count is `>= 502` (V5.0 SHIPPED count). Use `--tb=no -q` and parse the `N passed` line from stdout. The expected count after Phase 40 is approximately 502 + 17 (plan 01) + 28 (plan 02) + 17 (plan 03) + 36 (plan 04) ≈ 600; the assertion is `>= 502` to allow for variance in test granularity.
    - Test 8 (no V5.0 test file deleted): A hardcoded list of canonical V5.0 test files (from the `<interfaces>` block above) all still exist on disk. Use `pathlib.Path(...).exists()` for each.
  </behavior>
  <action>
    RED: Write `test_v50_regression.py` with a `TestV50Regression` class. Use `subprocess.run` to invoke pytest on each canonical V5.0 test file (clean subprocess isolation — avoids import-state contamination from the v6.0 tests). Use `pytest.importorskip` for optional files (test_gold_team.py).

    Pattern:
    ```python
    import subprocess, sys
    from pathlib import Path

    HERMES_ROOT = Path(__file__).resolve().parents[3]  # adjust to reach hermes-agent root

    class TestV50Regression:
        def test_phase_registry_still_passes(self):
            result = subprocess.run(
                [sys.executable, "-m", "pytest", "-q", "--tb=short",
                 str(HERMES_ROOT / "skills/kais-movie-pipeline/tests/test_phase_registry_full.py")],
                capture_output=True, text=True, cwd=str(HERMES_ROOT),
            )
            assert result.returncode == 0, f"V5.0 registry test failed:\n{result.stdout}\n{result.stderr}"

        def test_total_test_count_meets_v50_baseline(self):
            result = subprocess.run(
                [sys.executable, "-m", "pytest", "-q", "--tb=no",
                 "skills/kais-movie-pipeline/tests/", "plugins/pipeline_state/tests/",
                 "plugins/kais_aigc/tests/", "plugins/review_gates/tests/"],
                capture_output=True, text=True, cwd=str(HERMES_ROOT),
            )
            # Parse "===== N passed in X.XXs =====" from stdout
            import re
            match = re.search(r"(\d+) passed", result.stdout)
            assert match, f"could not parse pass count from:\n{result.stdout}"
            count = int(match.group(1))
            assert count >= 502, f"V5.0 baseline 502 tests regressed to {count}"
    ```

    GREEN: No production code changes — pure regression guard. If any test fails, Phase 40 broke V5.0; STOP and surface to the orchestrator for plan revision (this is exactly the brownfield risk flagged in STATE.md Key Risks item 1).

    IMPORTANT: This task MUST run LAST in plan 04 (after Tasks 1-4 land). It is the final gate before Phase 40 can be marked complete.
  </action>
  <verify>
    <automated>cd /data/workspace/hermes-agent && python -m pytest skills/kais-movie-pipeline/tests/test_v50_regression.py -x -q</automated>
  </verify>
  <done>
    - 8 V5.0 regression tests pass
    - All canonical V5.0 test files still exist and pass in clean subprocesses
    - Total test count ≥ 502 (the V5.0 SHIPPED baseline)
    - Phase 40 verification complete — no V5.0 regression introduced
  </done>
</task>

<task type="auto">
  <name>Task 6: Commit Phase 40 verification tests + write Phase SUMMARY</name>
  <files>(no production changes — verification tests + summary only)</files>
  <read_first>
    - All 5 test files written in Tasks 1-5 (confirm they exist + tests pass)
  </read_first>
  <action>
    After all 5 verification test files pass, commit them:
    ```
    gsd-sdk query commit "test(40-04): verify p10b dual-engine + degrade WARN + JSONL format + full-DAG integration + V5.0 regression (RAPID-PREVIEW-07)" --files \
      /data/workspace/hermes-agent/skills/kais-movie-pipeline/tests/test_p10b_dual_engine_e2e.py \
      /data/workspace/hermes-agent/skills/kais-movie-pipeline/tests/test_p10b_jsonl_format.py \
      /data/workspace/hermes-agent/skills/kais-movie-pipeline/tests/test_p10b_degrade_warning.py \
      /data/workspace/hermes-agent/skills/kais-movie-pipeline/tests/test_p10b_full_dag_integration.py \
      /data/workspace/hermes-agent/skills/kais-movie-pipeline/tests/test_v50_regression.py
    ```

    Then write the Phase 40 summary at `.planning/phases/40-rapid-preview-tier/40-04-SUMMARY.md` (this serves as the phase-level summary since plan 04 is the final plan). Include:
    - Final test count (V5.0 baseline + Phase 40 additions)
    - Confirmation all 7 RAPID-PREVIEW-XX requirements are satisfied (cross-reference each requirement to the plan/test that covers it)
    - The variant generation rule chosen in plan 03 (CYCLING matrix across 4 params — BLOCKER #4)
    - The slot naming decisions (rapid-preview-clips + episode-meta — BLOCKERS #1 + #5)
    - The degrade semantics decision (per-variant silent count vs episode-level WARN threshold; flag on episode-meta slot — BLOCKER #1)
    - Any deviations from the plan (with rationale)
    - Confirmation that V5.0's 502-test baseline is preserved
  </action>
  <verify>
    <automated>cd /data/workspace/hermes-agent && python -m pytest skills/kais-movie-pipeline/tests/ plugins/pipeline_state/tests/ plugins/kais_aigc/tests/ plugins/review_gates/tests/ -q 2>&1 | tail -3 && ls -la /data/workspace/kais-movie-agent/.planning/phases/40-rapid-preview-tier/40-04-SUMMARY.md</automated>
  </verify>
  <done>
    - All 5 verification test files committed
    - Phase 40 SUMMARY.md written
    - Phase 40 ready for orchestrator verification
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test subprocess → V5.0 test files | subprocess.run isolation prevents v6.0 import state from contaminating V5.0 regression runs |
| caplog → p10b logger | Log capture fixture observes the actual logger.warning() calls emitted by p10b |
| Full DAG run → runner + p10b | run_episode integration test exercises the actual runner dispatch into p10b |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-40-16 | Repudiation | Degrade WARN silently downgraded to INFO by misconfigured logger | mitigate | Test 1 (Task 3) asserts levelno == logging.WARNING strictly (INFO would fail the test) |
| T-40-17 | Tampering | V5.0 test deleted/moved to make regression test pass | mitigate | Test 8 (Task 5) asserts canonical V5.0 test files exist on disk (path-existence check) |
| T-40-18 | Denial of Service | Regression test count assertion is too strict (flakey) | mitigate | Test 7 (Task 5) uses `>= 502` (not exact equality) to allow for test-granularity variance |
| T-40-19 | Information Disclosure | caplog captures episode_id in WARN message | accept | episode_id is an internal pipeline identifier (not PII); mirrors V5.0 log disclosure profile |
| T-40-20 | Tampering | Slot name silently reverts from rapid-preview-clips → preview-clips or episode-meta → pipeline-state | mitigate | Test 4 + Test 9 (Task 3) assert the literal slot names; Test 1 (Task 2) asserts rapid-preview-clips file extension; Test 5 (Task 4) asserts rapid-preview-clips slot is populated |
</threat_model>

<verification>
- `python -m pytest skills/kais-movie-pipeline/tests/test_p10b_dual_engine_e2e.py skills/kais-movie-pipeline/tests/test_p10b_jsonl_format.py skills/kais-movie-pipeline/tests/test_p10b_degrade_warning.py skills/kais-movie-pipeline/tests/test_p10b_full_dag_integration.py skills/kais-movie-pipeline/tests/test_v50_regression.py -q` — all 42+ Phase 40 verification tests pass
- Full suite: `python -m pytest skills/kais-movie-pipeline/tests/ plugins/pipeline_state/tests/ plugins/kais_aigc/tests/ plugins/review_gates/tests/ -q` — V5.0 + Phase 40 total ≥ 502
</verification>

<success_criteria>
- RAPID-PREVIEW-02 (dual-engine integration verified end-to-end with mocked FFmpeg + mocked HTTP) ✓
- RAPID-PREVIEW-05 (WARN-level degrade log verified: level==WARNING, message contains "preview_skipped", fires exactly once on episode-level full degrade; flag persisted to episode-meta slot — BLOCKER #1) ✓
- RAPID-PREVIEW-07 (test coverage complete: (a) dual-engine paths produce previews, (b) degrade path emits warning not silent skip + flag on correct slot, (c) rapid-preview-clips JSONL format valid, (d) full-DAG runner integration works — WARNING #9) ✓
- V5.0 502-test regression explicitly guarded by automated test ✓
- Phase 40 SUMMARY documents requirement coverage + final test count ✓
</success_criteria>

<output>
Create `.planning/phases/40-rapid-preview-tier/40-04-SUMMARY.md` (also serves as the Phase 40 summary). Include:
- Final test count for the full V5.0 + v6.0-Phase-40 suite
- Cross-reference table: each RAPID-PREVIEW-XX → plans that cover it → tests that verify it
- Variant generation rule (CYCLING matrix across 4 params — BLOCKER #4)
- Slot naming decisions (rapid-preview-clips + episode-meta — BLOCKERS #1 + #5; rationale: avoid v3.0-era SKILL.md collision + disambiguate from PipelineStateStore file)
- Degrade semantics (per-variant silent vs episode-level WARN; flag target = episode-meta slot)
- Confirmation V5.0 baseline preserved (≥ 502 tests)
- Phase 40 status: READY FOR VERIFICATION
</output>
