---
phase: 40-rapid-preview-tier
plan: 03
type: tdd
wave: 3
depends_on:
  - 40-01
  - 40-02
files_modified:
  - /data/workspace/hermes-agent/skills/kais-movie-pipeline/pipeline/phases/p10b_rapid_preview.py
  - /data/workspace/hermes-agent/skills/kais-movie-pipeline/tests/test_p10b_unit.py
autonomous: true
requirements:
  - RAPID-PREVIEW-01
  - RAPID-PREVIEW-02
  - RAPID-PREVIEW-03
  - RAPID-PREVIEW-04
  - RAPID-PREVIEW-05
  - RAPID-PREVIEW-06

must_haves:
  truths:
    - "Runner can invoke p10b.run() between p10/p11 without TypeError (matches p11 D-36-08 extended signature: (episode_id, asset_bus_read, asset_bus_write, delegate_task, trigger_gate=None, *, parallel_shots=4) -> dict)"  # WARNING #8 rephrase
    - "p10b reads INPUT_SLOTS = ['voice-clips', 'voice-timeline', 'e-konte-sheets'] from the asset bus"
    - "p10b fans out per-shot via ThreadPoolExecutor(max_workers=parallel_shots) generating exactly 3 variants per shot. Across consecutive shots, the 3 variant params are selected by a CYCLING matrix: shot N (0-indexed) uses params [STRUCTURE_PARAMS[N mod 4], STRUCTURE_PARAMS[(N+1) mod 4], STRUCTURE_PARAMS[(N+2) mod 4]] from the 4-param list [hook_position_sec, emotion_sequence, turning_points_sec, ending_state]. This ensures all 4 params are deterministically covered across a multi-shot episode (BLOCKER #4 fix)."
    - "Each variant's structure_delta is a single-key dict {param_name: new_value} — multi-key deltas are rejected at write time (Notion 红线 #6)"
    - "Each variant record written to rapid-preview-clips JSONL has fields: shot_id, variant_id, structure_delta, clip_path, generation_time_ms, engine — all 6 fields present, no extras required"
    - "When engine.generate() returns a degrade envelope for ALL variants across ALL shots in the episode, p10b emits logger.warning('preview_skipped: episode=%s ...') AND writes {'episode_id': ..., 'preview_skipped': True, 'skip_reason': ...} to the AssetBus 'episode-meta' slot via asset_bus_write('episode-meta', {...}) (BLOCKER #1 fix — NOT 'pipeline-state' slot, which is a separate PipelineStateStore file)"
    - "When engine is degraded at the phase level (e.g., constructor or all-shots degrade), p10b returns cleanly with preview_skipped flag set, does NOT raise — pipeline proceeds to p11 (RAPID-PREVIEW-05 degrade to Seedance)"
    - "p10b triggers NO review gate (GATE_ID = None) — the 4 inherited red-line gates (RAPID-PREVIEW-06) work via the existing asset-envelope atomic write + consistency-guard mechanisms, NOT a new gate"
    - "p10b failure (e.g., delegate_task raises, or all variants degrade AND non-delegate exception) reaches max_retries via the runner's existing retry loop — runner handles episode-level fail (RAPID-PREVIEW-06, no new retry logic in p10b)"
  artifacts:
    - path: "/data/workspace/hermes-agent/skills/kais-movie-pipeline/pipeline/phases/p10b_rapid_preview.py"
      provides: "Full p10b phase module — replaces plan 01 stub with real implementation"
      contains: "def run("
    - path: "/data/workspace/hermes-agent/skills/kais-movie-pipeline/tests/test_p10b_unit.py"
      provides: "Mocked unit tests for p10b (mirrors test_p11_unit.py patterns) — includes cycling variant matrix coverage test"
      contains: "class TestP10bRapidPreview"
  key_links:
    - from: "p10b_rapid_preview.py run()"
      to: "plugins.kais_aigc.preview_engine.select_engine"
      via: "Engine strategy selection based on KAIS_PREVIEW_ENGINE env var"
      pattern: "select_engine"
    - from: "p10b_rapid_preview.py per-shot fan-out"
      to: "engine.generate(shot_id=..., prompt=..., structure_delta={...}, keyframe_image_path=..., voice_clip_path=..., output_path=...)"
      via: "3 calls per shot, one per variant, in ThreadPoolExecutor"
      pattern: "engine\\.generate"
    - from: "p10b_rapid_preview.py write path"
      to: "AssetBus.append_line('rapid-preview-clips', record)"
      via: "JSONL append for each successful variant (degraded variants are NOT appended — they are logged)"
      pattern: "append_line.*rapid-preview-clips"
    - from: "p10b_rapid_preview.py degrade path"
      to: "logger.warning + asset_bus_write('episode-meta', {...})"
      via: "WARN log emitted on episode-level full-degrade; preview_skipped=True written to 'episode-meta' AssetBus slot (NOT 'pipeline-state')"
      pattern: "preview_skipped"
---

<objective>
Replace the plan 01 stub with the full p10b_rapid_preview phase module. This is pure orchestration (mirrors p10/p11 D-35-04 contract — NO LLM calls, NO prompt templates). It composes the PreviewEngine strategy from plan 02, fans out per-shot via ThreadPoolExecutor (3 variants per shot, single-delta each — Notion 红线 #6), writes successful variants to rapid-preview-clips JSONL, and emits the phase-level WARN degrade path when engines fail.

Purpose: Delivers the core of RAPID-PREVIEW-01 (real phase module), RAPID-PREVIEW-03 (3 variants per shot with control variable + cycling matrix ensuring all 4 params are covered across multi-shot episodes), RAPID-PREVIEW-04 (rapid-preview-clips JSONL write format with all required fields), RAPID-PREVIEW-05 (phase-level preview_skipped WARN — completing what plan 02's engine-layer degrade started), and RAPID-PREVIEW-06 (inherits 4 red-line gates via existing mechanisms — no new gate).

BLOCKER #1 fix: The `preview_skipped` flag is written to the **`episode-meta` AssetBus slot** (registered in plan 01), NOT `pipeline-state` (which is a separate `.pipeline-state.json` file managed by PipelineStateStore — NOT an AssetBus slot).

BLOCKER #4 fix: Variant matrix CYCLES all 4 params across shots. Shot N uses `[STRUCTURE_PARAMS[N mod 4], STRUCTURE_PARAMS[(N+1) mod 4], STRUCTURE_PARAMS[(N+2) mod 4]]`. This ensures `turning_points_sec` is deterministically included in ≥1 variant across a multi-shot episode (verified by an explicit test).

Output:
- `phases/p10b_rapid_preview.py` — full implementation replacing the plan 01 stub
- `tests/test_p10b_unit.py` — new unit test file mirroring test_p11_unit.py patterns (mocked delegate_task / asset_bus / ThreadPoolExecutor) — includes cycling variant matrix coverage test
</objective>

<execution_context>
@/home/kai/.claude/get-shit-done/workflows/execute-plan.md
@/home/kai/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/data/workspace/kais-movie-agent/.planning/PROJECT.md
@/data/workspace/kais-movie-agent/.planning/ROADMAP.md
@/data/workspace/kais-movie-agent/.planning/phases/40-rapid-preview-tier/40-CONTEXT.md
@/data/workspace/hermes-agent/skills/kais-movie-pipeline/pipeline/phases/p10_voice.py
@/data/workspace/hermes-agent/skills/kais-movie-pipeline/pipeline/phases/p11_video_render.py
@/data/workspace/hermes-agent/skills/kais-movie-pipeline/tests/test_p11_unit.py
@/data/workspace/hermes-agent/plugins/kais_aigc/preview_engine.py
@/data/workspace/hermes-agent/plugins/pipeline_state/asset_bus.py

<interfaces>
<!-- Contracts plan 03 consumes (built by plans 01 + 02). -->

From plugins/kais_aigc/preview_engine.py (plan 02 output):
```python
class PreviewEngine(abc.ABC):
    def generate(self, *, shot_id: str, prompt: str, structure_delta: dict,
                 keyframe_image_path: str | None = None,
                 voice_clip_path: str | None = None,
                 output_path: str | None = None) -> dict: ...  # abstract

def select_engine(env: str | None = None) -> PreviewEngine: ...
# Returns dict shaped:
#   SUCCESS: {"clip_path": str, "generation_time_ms": int, "engine": "slideshow"|"ltx"}
#   DEGRADE: {"degraded": True, "engine": "slideshow"|"ltx", "reason": str}
```

From plugins/pipeline_state/asset_bus.py (plan 01 + V5.0):
```python
class AssetBus:
    def read(self, slot: str) -> Any | None: ...   # for JSON slots (e.g., episode-meta)
    def append_line(self, slot: str, line_obj: dict) -> str: ...  # for JSONL slots (e.g., rapid-preview-clips)
    def read_lines(self, slot: str) -> list[dict]: ...
    def write(self, slot: str, obj: Any) -> str: ...  # for JSON slots (e.g., episode-meta)
```

From pipeline/phases/p11_video_render.py — the run() signature p10b MUST mirror (D-36-08 extension):
```python
def run(
    episode_id: str,
    asset_bus_read: Callable[[str], Any],
    asset_bus_write: Callable[[str, dict], None],
    delegate_task: Callable[[str, str, list[str]], dict],
    trigger_gate: Callable[[str, str], dict] | None = None,
    *,
    parallel_shots: int = 4,  # D-36-08 — p10b also accepts this (ThreadPoolExecutor fan-out)
) -> dict: ...
```
CRITICAL DIFFERENCE from p11: p10b does NOT call delegate_task per shot (no expert delegation — the PreviewEngine replaces expert delegation per CONTEXT D-35-04). The `delegate_task` parameter MUST still be in the signature (runner.py passes it positionally to all phases) but p10b ignores it. Document this in the docstring.

From test_p11_unit.py — test double patterns p10b tests MUST mirror:
- `_make_per_shot_delegate(captured: dict)` — mock delegate_task that records calls
- Test fixture with shot_list of N shots
- parallel_shots=1 for deterministic ordering assertions
- parallel_shots=4 + 4-shot fixture for fan-out count assertions

Rapid-preview-clips JSONL record shape (RAPID-PREVIEW-04, all 6 fields required):
```python
{
    "shot_id": str,           # e.g., "shot_003"
    "variant_id": str,        # e.g., "shot_003__v1_hook_position_sec"
    "structure_delta": dict,  # single-key dict, e.g., {"hook_position_sec": 5}
    "clip_path": str,         # path to generated .mp4
    "generation_time_ms": int,# wall time of engine.generate() call
    "engine": str,            # "slideshow" or "ltx"
}
```

Episode-meta JSON record shape (BLOCKER #1 fix — written on episode-level full-degrade):
```python
{
    "episode_id": str,        # the episode_id passed to run()
    "preview_skipped": bool,  # True on episode-level full-degrade
    "skip_reason": str,       # human-readable reason, e.g., "all_variants_degraded: 12/12"
}
```

Single-delta validation (Notion 红线 #6 enforcement, per CONTEXT.md specifics):
- `structure_delta` MUST be a dict with EXACTLY ONE key
- Valid keys: `"hook_position_sec" | "emotion_sequence" | "turning_points_sec" | "ending_state"`
- Multi-key deltas → raise ValueError at variant construction time (before engine call)
- Invalid key names → raise ValueError

Variant generation matrix (BLOCKER #4 fix — CYCLING all 4 params across shots, per CONTEXT.md "Variant Matrix" decision):
```python
STRUCTURE_PARAMS = ("hook_position_sec", "emotion_sequence", "turning_points_sec", "ending_state")
# Shot N (0-indexed) uses params:
#   params_for_shot = [STRUCTURE_PARAMS[N % 4], STRUCTURE_PARAMS[(N+1) % 4], STRUCTURE_PARAMS[(N+2) % 4]]
# This produces:
#   shot 0: [hook_position_sec, emotion_sequence, turning_points_sec]
#   shot 1: [emotion_sequence, turning_points_sec, ending_state]
#   shot 2: [turning_points_sec, ending_state, hook_position_sec]
#   shot 3: [ending_state, hook_position_sec, emotion_sequence]
#   shot 4: same as shot 0 (cycles)
# Across shots 0-3, ALL 4 params are covered (each appears in exactly 3 of the 4 shots).
```
The exact "new_value" derivation per param is operator-tunable; the plan provides a deterministic default that tests can assert against. The key constraints are (a) single-delta per variant, (b) one variant per selected param, (c) the cycling matrix ensures all 4 params are exercised across a multi-shot episode.
</interfaces>
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: Write p10b run() skeleton + single-delta variant builder with cycling matrix + JSONL write</name>
  <files>
    /data/workspace/hermes-agent/skills/kais-movie-pipeline/pipeline/phases/p10b_rapid_preview.py,
    /data/workspace/hermes-agent/skills/kais-movie-pipeline/tests/test_p10b_unit.py
  </files>
  <read_first>
    - /data/workspace/hermes-agent/skills/kais-movie-pipeline/pipeline/phases/p10b_rapid_preview.py (the plan 01 stub being replaced)
    - /data/workspace/hermes-agent/skills/kais-movie-pipeline/pipeline/phases/p11_video_render.py (FULL FILE — the run() signature, ThreadPoolExecutor fan-out pattern, _goal_for_shot helper structure, aggregated write pattern)
    - /data/workspace/hermes-agent/skills/kais-movie-pipeline/pipeline/phases/p10_voice.py (FULL FILE — INPUT_SLOTS / OUTPUT_SLOTS / GATE_ID constants pattern)
    - /data/workspace/hermes-agent/skills/kais-movie-pipeline/tests/test_p11_unit.py (FULL FILE — test double patterns, _make_per_shot_delegate, parallel_shots=1 deterministic test, parallel_shots=4 fan-out test)
    - /data/workspace/hermes-agent/plugins/kais_aigc/preview_engine.py (FULL FILE — select_engine() signature, PreviewEngine.generate() signature, degrade envelope shape)
    - /data/workspace/hermes-agent/plugins/pipeline_state/asset_bus.py (lines 503-540 — append_line + read_lines API for rapid-preview-clips; lines 409-470 — write + read API for episode-meta)
    - /data/workspace/kais-movie-agent/.planning/phases/40-rapid-preview-tier/40-CONTEXT.md (DECISIONS + SPECIFICS sections — single-delta rule, variant count=3, CYCLING matrix across 4 params, parallel_shots=4, JSONL format, degrade WARN path; "Variant Matrix" subsection with the cycling formula)
  </read_first>
  <behavior>
    - Test 1 (constants): `p10b_rapid_preview.PHASE_ID == "p10b_rapid_preview"`; `p10b_rapid_preview.INPUT_SLOTS == ["voice-clips", "voice-timeline", "e-konte-sheets"]`; `p10b_rapid_preview.OUTPUT_SLOTS == ["rapid-preview-clips", "episode-meta"]` (BOTH slots per BLOCKER #1); `p10b_rapid_preview.GATE_ID is None`; `p10b_rapid_preview.EXPERT is None` (pure orchestration).
    - Test 2 (variant builder — single shot): `_build_variants(shot_id="shot_001", shot_index=0, baseline_structure={"hook_position_sec": 3, "emotion_sequence": ["suppress","thrill"], "turning_points_sec": [3,15], "ending_state": "new_suspense"})` returns a list of exactly 3 dicts, each with `variant_id` and `structure_delta` keys; each `structure_delta` has EXACTLY ONE key drawn from the 4 valid param names. For shot_index=0, the 3 params selected are `[hook_position_sec, emotion_sequence, turning_points_sec]` (the first 3 of the cycling matrix).
    - Test 3 (BLOCKER #4 — cycling matrix across multi-shot episode): Running `_build_variants` across 4 consecutive shots (shot_index 0, 1, 2, 3) with the same baseline_structure, collecting all variant `structure_delta` keys, the union MUST be all 4 params: `{hook_position_sec, emotion_sequence, turning_points_sec, ending_state}`. AND specifically, `turning_points_sec` MUST appear as a structure_delta key in ≥1 variant across the 4-shot fixture (this is the BLOCKER #4 explicit assertion — proves the cycling matrix covers all params).
    - Test 4 (single-delta enforcement): Constructing a variant with `structure_delta={"hook_position_sec": 5, "emotion_sequence": [...]}` (2 keys) raises `ValueError` containing "single-delta" or "Notion 红线 #6" in the message.
    - Test 5 (invalid param name): `_build_variants` with an unrecognized param name (e.g., `"unknown_param"`) raises `ValueError`.
    - Test 6 (happy path, single shot, mocked engine): With a fake engine returning `{"clip_path": "/x.mp4", "generation_time_ms": 100, "engine": "slideshow"}` for every call, and asset_bus_read returning 1 shot + voice-clips + e-konte-sheets, `run(parallel_shots=1)` calls `engine.generate()` 3 times (once per variant), and calls `asset_bus_write("rapid-preview-clips", record)` 3 times — one per variant. (NOTE: asset_bus_write is the runner-injected callable that dispatches to AssetBus.append_line for jsonl slots.)
    - Test 7 (record fields): Each record written to rapid-preview-clips contains all 6 required fields: `shot_id, variant_id, structure_delta, clip_path, generation_time_ms, engine`. No field is None or missing.
    - Test 8 (parallel fan-out): With 4 shots and `parallel_shots=4`, `run()` calls `engine.generate()` exactly 12 times (4 shots × 3 variants). Use a thread-safe counter to verify.
    - Test 9 (return shape): `run()` returns `{"phase": "p10b_rapid_preview", "outputs": {...}, "gate": None}` — gate is always None (GATE_ID is None, CF-36-04 conditional skip).
  </behavior>
  <action>
    RED: Write `test_p10b_unit.py` with the 9 tests above in a `TestP10bRapidPreview` class. Mirror the test_p11_unit.py file header (sys.path setup, imports, `_shot_summary` helper if needed). Use a fake engine class `FakeEngine(PreviewEngine)` whose `generate()` returns a fixed-success envelope and increments a counter. For Test 3 (cycling matrix), iterate shot_index 0..3, collect all structure_delta keys, assert the union == all 4 params AND that `turning_points_sec` is in the union.

    GREEN: Replace the plan 01 stub in `p10b_rapid_preview.py` with the full implementation.

    (a) Module docstring — explain p10b is pure orchestration, fans out 3 single-delta variants per shot via ThreadPoolExecutor using a CYCLING matrix across the 4 structure params (so all 4 are covered across a multi-shot episode — BLOCKER #4 fix), writes successful variants to rapid-preview-clips JSONL, degrades to WARN + preview_skipped flag when engines fail. Reference CONTEXT decisions.

    (b) Module constants (the plan 01 stub already has these — verify they match):
        - `PHASE_ID = "p10b_rapid_preview"`
        - `EXPERT = None`
        - `INPUT_SLOTS = ["voice-clips", "voice-timeline", "e-konte-sheets"]`
        - `OUTPUT_SLOTS = ["rapid-preview-clips", "episode-meta"]`  # BOTH slots per BLOCKER #1
        - `GATE_ID = None`
        - `STRUCTURE_PARAMS = ("hook_position_sec", "emotion_sequence", "turning_points_sec", "ending_state")` — the 4 valid structure params (per CONTEXT decision)
        - `VARIANTS_PER_SHOT = 3` (per CONTEXT decision "Variant count: exactly 3")

    (c) Helper `_build_variants(shot_id: str, shot_index: int, baseline_structure: dict) -> list[dict]` — BLOCKER #4 fix: CYCLING MATRIX:
        - Compute the 3 param indices for this shot: `indices = [(shot_index + offset) % len(STRUCTURE_PARAMS) for offset in range(VARIANTS_PER_SHOT)]` (yields `[shot_index % 4, (shot_index+1) % 4, (shot_index+2) % 4]`).
        - For each computed index: `param_name = STRUCTURE_PARAMS[idx]`; build `structure_delta = {param_name: _derive_new_value(param_name, baseline_structure[param_name])}`.
        - Validate: `len(structure_delta) == 1` (single-delta — Notion 红线 #6); param name in `STRUCTURE_PARAMS`.
        - Build `variant_id = f"{shot_id}__v{i+1}_{param_name}"` where i is the position within the 3-variant list (0, 1, 2).
        - Return list of `{"variant_id": variant_id, "structure_delta": structure_delta}`.

    (d) Helper `_derive_new_value(param_name: str, baseline_value: Any) -> Any`:
        - `"hook_position_sec"`: return `baseline_value + 2` (deterministic shift; tests can assert)
        - `"emotion_sequence"`: rotate list by 1 (last element moves to front)
        - `"turning_points_sec"`: shift each timestamp by +2 (cap at episode duration — but p10b doesn't know duration, so just shift)
        - `"ending_state"`: cycle to next from `("new_suspense", "cliffhanger", "twist")` — find baseline in tuple, return next
        - Document: "Operator-tunable. Defaults are deterministic for testability."

    (e) Helper `_validate_structure_delta(delta: dict) -> None`:
        - If `len(delta) != 1`: raise `ValueError(f"single-delta violation (Notion 红线 #6): structure_delta must have exactly one key, got {len(delta)}: {list(delta.keys())}")`
        - If the single key not in `STRUCTURE_PARAMS`: raise `ValueError(f"invalid structure_delta param: {key!r}; valid: {STRUCTURE_PARAMS}")`

    (f) `run()` function with the full signature from the interfaces block above. Body:
        - Gather inputs: `voice_clips = asset_bus_read("voice-clips") or []`; `voice_timeline = asset_bus_read("voice-timeline") or {}`; `e_konte = asset_bus_read("e-konte-sheets") or {}`. Derive `shot_list` from `voice_clips` (each voice clip has a shot_id; p10b iterates these). Track `shot_index` (0-based) for the cycling matrix.
        - Engine selection: `engine = select_engine()` (reads KAIS_PREVIEW_ENGINE env var; defaults to slideshow per plan 02).
        - For each shot: build the 3 variants via `_build_variants(shot_id, shot_index, baseline_structure)`; for each variant: call `engine.generate(shot_id=..., prompt=<derive from shot intent>, structure_delta=variant["structure_delta"], keyframe_image_path=<derive from e_konte>, voice_clip_path=<derive from voice_clips>, output_path=<compute>)`.
        - Fan out via `ThreadPoolExecutor(max_workers=parallel_shots)`.
        - For each engine result: if `result.get("degraded")`: increment degrade counter, do NOT write to rapid-preview-clips; else: build the 6-field record and call `asset_bus_write("rapid-preview-clips", record)` (the runner-injected callable dispatches to AssetBus.append_line).
        - After fan-out completes: if ALL variants across ALL shots degraded → emit `logger.warning("preview_skipped: episode=%s all_variants_degraded=%d/%d — falling back to p11 direct Seedance", episode_id, degrade_count, total_variants)` AND call `asset_bus_write("episode-meta", {"episode_id": episode_id, "preview_skipped": True, "skip_reason": f"all_variants_degraded: {degrade_count}/{total_variants}"})` (BLOCKER #1 fix — write to `episode-meta` AssetBus slot, NOT `pipeline-state`).
        - Return `{"phase": PHASE_ID, "outputs": {"variants_generated": <count>, "variants_degraded": <count>}, "gate": None}`.

    NOTE on delegate_task: the parameter is in the signature (runner.py passes it positionally to all phases), but p10b's body does NOT call it. Add a docstring note: "delegate_task is accepted for signature compatibility with the runner's uniform phase dispatch (D-35-02) but is NOT called — p10b is pure orchestration over PreviewEngine, no expert delegation (CONTEXT D-35-04)."

    NOTE on trigger_gate: GATE_ID is None, so the `if GATE_ID and trigger_gate is not None:` conditional short-circuits (CF-36-04 conditional skip — mirror p10_voice.py line 122).

    Module imports: `from __future__ import annotations`, `import json, logging`, `from concurrent.futures import ThreadPoolExecutor`, `from typing import Any, Callable`. Plus the cross-plugin import: `from plugins.kais_aigc.preview_engine import PreviewEngine, select_engine`. VERIFY the import path works at test time (test_p11_unit.py uses sys.path manipulation to make `pipeline` importable; the same conftest.py may need to expose `plugins` — check `skills/kais-movie-pipeline/tests/conftest.py` and extend if needed).

    DENSITY NOTE (WARNING #6 — accepted): This task is dense (skeleton + variant builder + validators + ThreadPoolExecutor branch + asset bus writes). Splitting into 1a/1b was considered but rejected because the skeleton, builders, and validators are mutually dependent — splitting would require duplicating the FakeEngine test fixture across tasks and would fragment the RED→GREEN cycle. The density is documented and accepted; if the executor finds context budget exceeding ~40%, they may split into Task 1a (constants + _build_variants + _derive_new_value + _validate_structure_delta — pure functions, no ThreadPoolExecutor) and Task 1b (run() body wiring + ThreadPoolExecutor + asset bus writes) at their discretion.
  </action>
  <verify>
    <automated>cd /data/workspace/hermes-agent && python -m pytest skills/kais-movie-pipeline/tests/test_p10b_unit.py::TestP10bRapidPreview -x -q</automated>
  </verify>
  <done>
    - p10b_rapid_preview.run() fully implemented (stub from plan 01 replaced)
    - 9 behavior tests pass (RED→GREEN) — including Test 3 (BLOCKER #4 cycling matrix coverage assertion)
    - Constants match plan 01 stub exactly EXCEPT OUTPUT_SLOTS now includes both rapid-preview-clips AND episode-meta (BLOCKER #1) — verify test_phase_registry_full.py still passes (it asserts OUTPUT_SLOTS exists, not its exact contents — but if it does assert exact contents, that test was updated in plan 01 to expect the new value)
    - Single-delta rule enforced via _validate_structure_delta
    - Cycling matrix ensures all 4 params are covered across a multi-shot episode
    - 6-field JSONL records written on engine success
  </done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: Implement phase-level degrade WARN path + preview_skipped flag on episode-meta slot (RAPID-PREVIEW-05, BLOCKER #1)</name>
  <files>
    /data/workspace/hermes-agent/skills/kais-movie-pipeline/pipeline/phases/p10b_rapid_preview.py,
    /data/workspace/hermes-agent/skills/kais-movie-pipeline/tests/test_p10b_unit.py
  </files>
  <read_first>
    - /data/workspace/hermes-agent/skills/kais-movie-pipeline/pipeline/phases/p10b_rapid_preview.py (the file being expanded — Task 1 implementation)
    - /data/workspace/hermes-agent/skills/kais-movie-pipeline/tests/test_p10b_unit.py (Task 1 tests)
    - /data/workspace/kais-movie-agent/.planning/phases/40-rapid-preview-tier/40-CONTEXT.md (DECISIONS section — "Degradation signaling" decision: logger.warning + episode_meta["preview_skipped"] = True written to the 'episode-meta' AssetBus slot — NOT 'pipeline-state')
  </read_first>
  <behavior>
    - Test 1 (all-degrade single shot): With a fake engine that ALWAYS returns `{"degraded": True, "engine": "slideshow", "reason": "test"}`, `run()` with 1 shot × 3 variants → 3 degrade envelopes; p10b emits `logger.warning("preview_skipped: ...")` (verify via caplog or a captured logging handler); writes `{"episode_id": ..., "preview_skipped": True, "skip_reason": ...}` to the **`episode-meta`** AssetBus slot via `asset_bus_write("episode-meta", {...})` (BLOCKER #1 fix — NOT `pipeline-state`); writes NOTHING to `rapid-preview-clips`.
    - Test 2 (partial degrade): With a fake engine that degrades on variant 2 but succeeds on variants 1 + 3 of a single shot, `run()` writes 2 records to rapid-preview-clips (variants 1 + 3), does NOT write preview_skipped flag (since not ALL variants degraded — only emit WARN at episode level if ALL variants across ALL shots degrade). Document the rule: WARN + preview_skipped triggers when ALL variants of ALL shots degrade (episode-level degrade), not partial.
    - Test 3 (return shape on full degrade): `run()` returns `{"phase": "p10b_rapid_preview", "outputs": {"variants_generated": 0, "variants_degraded": 3}, "gate": None}` — does NOT raise.
    - Test 4 (WARN message content): The warning log message contains the literal string `"preview_skipped"` AND the `episode_id` (assert via caplog.records).
    - Test 5 (episode-meta slot content — BLOCKER #1 explicit): The `asset_bus_write` call on full-degrade has `args[0] == "episode-meta"` (NOT `"pipeline-state"`) AND `args[1]` is a dict containing keys `episode_id`, `preview_skipped == True`, and `skip_reason` (a string). Capture all writes and assert ONE call matches this shape.
    - Test 6 (no silent path): There is NO code path where engine failure is swallowed without either a WARN log OR a raise. Verify by code inspection: every `result.get("degraded")` branch either logs OR increments a counter that triggers an episode-level WARN.
    - Test 7 (multi-shot full degrade): 4 shots × 3 variants = 12 degrade envelopes → exactly 1 episode-level WARN log (not 12); preview_skipped flag written once to episode-meta.
    - Test 8 (engine constructor failure): If `select_engine()` itself raises (e.g., import error — defensive), p10b catches at the top of run(), emits WARN, writes preview_skipped flag to episode-meta, returns the standard envelope. (NOTE: this is defensive — plan 02's select_engine should not raise in practice, but p10b must be robust.)
  </behavior>
  <action>
    RED: Add the 8 degrade-path tests to a `TestP10bDegradePath` class in test_p10b_unit.py. Use `caplog` fixture (pytest) or a custom logging.Handler to capture WARN messages. Test 5 explicitly asserts the slot name is `"episode-meta"` (BLOCKER #1).

    GREEN: Expand the Task 1 run() body to add the degrade WARN path:

    (a) Wrap the entire run body in a try/except for unexpected exceptions (defensive — Test 8). On caught exception: log WARN `preview_skipped: episode=%s error=%s`, write preview_skipped flag to episode-meta slot, return degrade envelope `{"phase": PHASE_ID, "outputs": {"variants_generated": 0, "variants_degraded": 0, "error": str(exc)}, "gate": None}`.

    (b) Track `total_variants = len(shot_list) * VARIANTS_PER_SHOT` and `degraded_count` (incremented per degrade envelope).

    (c) BLOCKER #1 fix — After the ThreadPoolExecutor fan-out completes, check: if `degraded_count == total_variants AND total_variants > 0` (all variants degraded), emit the episode-level WARN and write to the **`episode-meta`** AssetBus slot (NOT `pipeline-state`):
        ```python
        skip_reason = f"all_variants_degraded: {degraded_count}/{total_variants}"
        logger.warning(
            "preview_skipped: episode=%s %s — falling back to p11 direct Seedance",
            episode_id, skip_reason,
        )
        asset_bus_write("episode-meta", {
            "episode_id": episode_id,
            "preview_skipped": True,
            "skip_reason": skip_reason,
        })
        ```

    (d) Ensure NO `logger.warning` is emitted for partial degrades (only the episode-level all-degrade triggers WARN). Partial degrades are silently counted in outputs.variants_degraded (the count itself is the signal — downstream consumers check it).

    (e) Document the rule in the docstring: "Degrade semantics (RAPID-PREVIEW-05): per-variant degrade is silent (counted in outputs.variants_degraded). Episode-level full-degrade (all variants degraded) triggers WARN log + preview_skipped=True flag written to the **episode-meta AssetBus slot** (BLOCKER #1 fix — NOT pipeline-state, which is a separate PipelineStateStore file). This is the inherited v4.0 'no silent swallow' semantics — episode-level fail is visible; per-variant fail is recoverable."
  </action>
  <verify>
    <automated>cd /data/workspace/hermes-agent && python -m pytest skills/kais-movie-pipeline/tests/test_p10b_unit.py::TestP10bDegradePath -x -q</automated>
  </verify>
  <done>
    - 8 degrade-path tests pass — including Test 5 (BLOCKER #1: asserts slot name is "episode-meta" with the 3-key dict shape)
    - Episode-level full-degrade emits WARN log containing "preview_skipped" + episode_id
    - Episode-level full-degrade writes {episode_id, preview_skipped=True, skip_reason} to episode-meta AssetBus slot (NOT pipeline-state)
    - Partial degrade is counted but does NOT emit episode-level WARN (per-variant degrade is recoverable)
    - Engine constructor failure is caught defensively (Test 8)
  </done>
</task>

<task type="auto">
  <name>Task 3: Full V5.0 regression + commit p10b module</name>
  <files>(no file changes — verification + commit only)</files>
  <read_first>
    - /data/workspace/hermes-agent/skills/kais-movie-pipeline/pipeline/phases/p10b_rapid_preview.py (the final state — confirm both tasks landed)
  </read_first>
  <action>
    Run the COMPLETE V5.0 + v6.0-so-far test suite to confirm:
    - Plan 01 scaffolding tests still pass (registry 14 phases, rapid-preview-clips + episode-meta slots)
    - Plan 02 preview_engine tests still pass (28+ tests)
    - Plan 03 p10b unit tests pass (17+ tests across Tasks 1+2, including BLOCKER #4 cycling matrix test + BLOCKER #1 episode-meta test)
    - All V5.0 tests still pass (no regression from p10b stub → full impl swap)

    Command:
    ```
    cd /data/workspace/hermes-agent
    python -m pytest skills/kais-movie-pipeline/tests/ plugins/pipeline_state/tests/ plugins/kais_aigc/tests/ plugins/review_gates/tests/ -q
    ```
    Expected count: V5.0 baseline (502) + plan 01 (~17 new) + plan 02 (~28 new) + plan 03 (~17 new) ≈ 564+ tests.

    Specifically verify:
    - `test_phase_registry_full.py` still passes (p10b module still has all 5 required constants after the stub → full impl swap)
    - `test_runner_full_dag.py` still passes (runner can traverse p10 → p10b → p11 in DAG order — p10b.run() now executes instead of raising NotImplementedError, but tests inject mocks so no real engine calls happen)

    After tests pass, commit:
    ```
    gsd-sdk query commit "feat(40-03): implement p10b_rapid_preview phase module (3 variants/shot, cycling matrix, ThreadPoolExecutor fan-out, episode-meta degrade WARN)" --files \
      /data/workspace/hermes-agent/skills/kais-movie-pipeline/pipeline/phases/p10b_rapid_preview.py \
      /data/workspace/hermes-agent/skills/kais-movie-pipeline/tests/test_p10b_unit.py
    ```
  </action>
  <verify>
    <automated>cd /data/workspace/hermes-agent && python -m pytest skills/kais-movie-pipeline/tests/ plugins/pipeline_state/tests/ plugins/kais_aigc/tests/ plugins/review_gates/tests/ -q 2>&1 | tail -5</automated>
  </verify>
  <done>
    - Full V5.0 + v6.0-so-far test suite passes
    - p10b_rapid_preview.run() executes without NotImplementedError
    - Runner DAG test confirms p10 → p10b → p11 traversal
    - Single commit `feat(40-03): ...` lands in hermes-agent working tree
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Runner → p10b.run() | Phase entry point; standard 6-arg contract (D-35-02 + D-36-08) |
| p10b → PreviewEngine | In-process strategy call; structure_delta crosses to engine layer |
| p10b → AssetBus.append_line("rapid-preview-clips", ...) | JSONL write of variant records (success path only) |
| p10b → AssetBus.write("episode-meta", ...) | Episode-meta flag write (degrade path only) — BLOCKER #1 fix |
| ThreadPoolExecutor | Per-shot fan-out (4 workers); shared engine instance must be thread-safe |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-40-10 | Tampering | Multi-key structure_delta smuggled past validation | mitigate | _validate_structure_delta raises ValueError before engine call; tests cover 2-key rejection (Test 4) |
| T-40-11 | Denial of Service | ThreadPoolExecutor thread leak on engine hang | accept | Engine degrade-first contract (plan 02) bounds hang via httpx timeout; SlideshowEngine relies on FFmpeg subprocess timeout (default behavior). v6.0 scope: no explicit thread kill. |
| T-40-12 | Repudiation | preview_skipped flag missing on full degrade | mitigate | Test 1 + Test 7 verify flag is written; Test 5 (BLOCKER #1) verifies it lands on episode-meta slot with correct 3-key shape; Test 6 verifies no silent path (code inspection + caplog) |
| T-40-13 | Information Disclosure | Episode-level WARN log leaks sensitive data | accept | WARN message contains only episode_id + counts + skip_reason (no PII, no creative content); mirrors V5.0 log disclosure profile |
| T-40-14 | Elevation of Privilege | Concurrent ThreadPoolExecutor writes to rapid-preview-clips JSONL corrupt the file | mitigate | AssetBus.append_line uses open(..., "a") O_APPEND which is atomic per-write on POSIX (mirrors finetune-dataset slot thread-safety — V5.0 plan 33-04 convention) |
| T-40-15 | Tampering | Runner retry loop interaction with p10b partial degrade | accept | Runner retries on RAISED exceptions only; p10b never raises on engine degrade (returns envelope). max_retries triggers episode-level fail only on delegate_task raises or unexpected exceptions — RAPID-PREVIEW-06 honored via existing runner contract, no new retry logic. |
</threat_model>

<verification>
- `python -m pytest skills/kais-movie-pipeline/tests/test_p10b_unit.py -q` — all p10b tests pass (including BLOCKER #1 episode-meta test + BLOCKER #4 cycling matrix test)
- `python -m pytest skills/kais-movie-pipeline/tests/test_phase_registry_full.py skills/kais-movie-pipeline/tests/test_runner_full_dag.py -q` — V5.0 registry + runner tests still pass
- Full V5.0 + v6.0 regression: `python -m pytest skills/kais-movie-pipeline/tests/ plugins/pipeline_state/tests/ plugins/kais_aigc/tests/ plugins/review_gates/tests/ -q` passes
- `python -c "from pipeline.phases import p10b_rapid_preview; r = p10b_rapid_preview.run('ep', lambda s: [], lambda s,d: None, lambda g,c,t: {}, None, parallel_shots=1); assert r['phase']=='p10b_rapid_preview' and r['gate'] is None"` (smoke test — empty shot_list returns cleanly)
</verification>

<success_criteria>
- RAPID-PREVIEW-01 (real phase module with full contract — run() signature, INPUT/OUTPUT_SLOTS including both rapid-preview-clips + episode-meta, GATE_ID) ✓
- RAPID-PREVIEW-02 (p10b calls select_engine() — env-var-driven dual-engine selection) ✓
- RAPID-PREVIEW-03 (3 variants per shot via CYCLING matrix across 4 params, single-delta each, Notion 红线 #6 enforced via ValueError; all 4 params deterministically covered across multi-shot episode — BLOCKER #4) ✓
- RAPID-PREVIEW-04 (rapid-preview-clips JSONL records with all 6 required fields) ✓
- RAPID-PREVIEW-05 (phase-level preview_skipped WARN + flag on episode-meta AssetBus slot on episode-level full degrade; never silent; BLOCKER #1 fix — NOT pipeline-state) ✓
- RAPID-PREVIEW-06 (4 red-line gates inherited via existing mechanisms; no new gate; runner retry contract honored) ✓
- V5.0 502 tests stay green (no regression from stub → full impl swap) ✓
</success_criteria>

<output>
Create `.planning/phases/40-rapid-preview-tier/40-03-SUMMARY.md` when done. Include:
- Final test count
- Variant generation rule decision: CYCLING matrix `[STRUCTURE_PARAMS[N mod 4], STRUCTURE_PARAMS[(N+1) mod 4], STRUCTURE_PARAMS[(N+2) mod 4]]` per shot — BLOCKER #4 fix rationale (ensures all 4 params covered across multi-shot episodes)
- Degrade slot decision: `episode-meta` AssetBus slot (NOT `pipeline-state`) — BLOCKER #1 fix rationale
- Degrade semantics decision (per-variant silent count vs episode-level WARN threshold)
- Confirmation that runner DAG traversal p10 → p10b → p11 works end-to-end with mocked engines
- WARNING #6 note: Task 1 density documented as accepted (skeleton + builders + validators + ThreadPoolExecutor + asset bus writes mutually dependent — splitting would fragment RED→GREEN cycle)
</output>
