---
phase: 40-rapid-preview-tier
plan: 02
type: tdd
wave: 2
depends_on:
  - 40-01
files_modified:
  - /data/workspace/hermes-agent/plugins/kais_aigc/preview_engine.py
  - /data/workspace/hermes-agent/plugins/kais_aigc/tests/test_preview_engine.py
autonomous: true
requirements:
  - RAPID-PREVIEW-02
  - RAPID-PREVIEW-05

must_haves:
  truths:
    - "PreviewEngine is an ABC with a generate(shot_id, prompt, structure_delta) -> dict abstract method"
    - "SlideshowEngine implements PreviewEngine; uses FFmpeg subprocess (-i img -i audio -c:v libx264 -c:a aac out.mp4); returns {clip_path, generation_time_ms, engine: 'slideshow'}"
    - "LTXVideoEngine implements PreviewEngine; uses httpx POST to :9001/api/v1/ltx; returns {clip_path, generation_time_ms, engine: 'ltx'}"
    - "LTXVideoEngine follows D-09 degrade-first contract: connection errors / timeouts / 5xx / 429 return {degraded: True, ...} envelope; 4xx raises PreviewEngineError"
    - "Default engine selection when KAIS_PREVIEW_ENGINE unset is 'slideshow' (safer fallback per CONTEXT decision)"
    - "KAIS_PREVIEW_ENGINE=ltx selects LTXVideoEngine; KAIS_PREVIEW_ENGINE=slideshow selects SlideshowEngine; unknown value falls back to slideshow + WARN log"
    - "SlideshowEngine degrades gracefully when FFmpeg binary missing or returns non-zero exit: returns degraded envelope (NOT raise)"
    - "Both engines accept a transport/subprocess-injection kwarg for hermetic testing (httpx.MockTransport for LTX, fake runner for Slideshow)"
  artifacts:
    - path: "/data/workspace/hermes-agent/plugins/kais_aigc/preview_engine.py"
      provides: "PreviewEngine ABC + SlideshowEngine + LTXVideoEngine + PreviewEngineError + select_engine factory"
      contains: "class PreviewEngine"
    - path: "/data/workspace/hermes-agent/plugins/kais_aigc/tests/test_preview_engine.py"
      provides: "Mocked tests for both engines + degrade paths + select_engine factory"
      contains: "class TestPreviewEngine"
  key_links:
    - from: "preview_engine.py LTXVideoEngine"
      to: "httpx.Client.post :9001/api/v1/ltx"
      via: "POST with {shot_id, prompt, structure_delta} body"
      pattern: "/api/v1/ltx"
    - from: "preview_engine.py SlideshowEngine"
      to: "subprocess.run(['ffmpeg', '-i', img, '-i', audio, '-c:v', 'libx264', ...])"
      via: "FFmpeg subprocess with keyframe image + TTS voice-clip inputs"
      pattern: "ffmpeg"
    - from: "preview_engine.py select_engine"
      to: "os.environ.get('KAIS_PREVIEW_ENGINE', 'slideshow')"
      via: "env-var dispatch with slideshow as default"
      pattern: "KAIS_PREVIEW_ENGINE"
---

<objective>
Build the PreviewEngine strategy pattern: an abstract base class + two concrete engines (SlideshowEngine via FFmpeg subprocess, LTXVideoEngine via mocked HTTP). This plan is pure library code — NO integration with p10b phase module yet (that is plan 03). The plan is `type: tdd` because every behavior has a clean input→output contract suitable for RED→GREEN→REFACTOR.

Purpose: This plan delivers RAPID-PREVIEW-02 (dual-engine + env selection) and the foundational layer of RAPID-PREVIEW-05 (degrade-first contract at the engine layer). Plan 03 will compose these engines into the p10b phase fan-out and add the phase-level `preview_skipped` WARN.

Output:
- `plugins/kais_aigc/preview_engine.py` — new module with PreviewEngine ABC, SlideshowEngine, LTXVideoEngine, PreviewEngineError, select_engine()
- `plugins/kais_aigc/tests/test_preview_engine.py` — comprehensive mocked tests (no real FFmpeg, no real HTTP)
</objective>

<execution_context>
@/home/kai/.claude/get-shit-done/workflows/execute-plan.md
@/home/kai/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/data/workspace/kais-movie-agent/.planning/PROJECT.md
@/data/workspace/kais-movie-agent/.planning/ROADMAP.md
@/data/workspace/kais-movie-agent/.planning/phases/40-rapid-preview-tier/40-CONTEXT.md
@/data/workspace/hermes-agent/plugins/kais_aigc/gold_team.py
@/data/workspace/hermes-agent/skills/kais-movie-pipeline/pipeline/phases/p11_video_render.py

<interfaces>
<!-- D-09 degrade-first contract template (extracted from gold_team.py). -->
<!-- LTXVideoEngine MUST mirror this contract structure. -->

From plugins/kais_aigc/gold_team.py — degrade envelope shape:
```python
def _degrade(self, operation: str, reason: str) -> dict[str, Any]:
    logger.warning("gold_team %s degraded: %s", operation, reason)
    return {
        "degraded": True,
        "client": "gold_team",
        "operation": operation,
        "reason": reason,
    }
```

From plugins/kais_aigc/gold_team.py — request wrapper contract (lines 170-221):
- ConnectError / TimeoutException / HTTPError → degrade (return envelope)
- status >= 500 or status == 429 → degrade
- status >= 400 → raise (caller bug)
- 2xx → parse JSON, return

From plugins/kais_aigc/gold_team.py — constructor injection pattern (line 92-122):
- `transport: httpx.BaseTransport | None = None` lets tests inject `httpx.MockTransport(handler)`
- `base_url` / `api_key` / `timeout` constructor kwargs read env vars as fallback (D-06 env-var configuration at construction time, NOT at module import)

LTX-Video API contract (v6.0 MOCKED — per CONTEXT.md "LTX-Video API contract" decision):
```
POST http://localhost:9001/api/v1/ltx
Body: {"shot_id": str, "prompt": str, "structure_delta": dict}
Response 2xx: {"clip_path": str, "generation_time_ms": int}
Response 5xx/429/timeout: degrade envelope
Response 4xx: raise PreviewEngineError (caller bug)
```

SlideshowEngine FFmpeg contract (per CONTEXT.md specifics):
```
Inputs:
  - keyframe_image_path (from p09 e-konte-sheets — passed at generate() time)
  - voice_clip_path (from p10 voice-clips — passed at generate() time)
  - output_path (where to write the .mp4)
Command: ffmpeg -loop 1 -i {img} -i {audio} -c:v libx264 -tune stillimage -c:a aac -b:a 192k -shortest -pix_fmt yuv420p {out}
Target generation time: < 10s per variant
```
</interfaces>
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: Write PreviewEngine ABC + PreviewEngineError + select_engine factory (RED→GREEN)</name>
  <files>
    /data/workspace/hermes-agent/plugins/kais_aigc/preview_engine.py,
    /data/workspace/hermes-agent/plugins/kais_aigc/tests/test_preview_engine.py
  </files>
  <read_first>
    - /data/workspace/hermes-agent/plugins/kais_aigc/gold_team.py (FULL FILE — D-09 degrade-first contract template, _degrade() shape, _request() wrapper, constructor injection pattern)
    - /data/workspace/hermes-agent/plugins/kais_aigc/__init__.py (if exists — to confirm plugin module structure)
    - /data/workspace/hermes-agent/plugins/kais_aigc/tests/test_gold_team.py (if exists — to mirror test patterns for degrade-path coverage)
    - /data/workspace/kais-movie-agent/.planning/phases/40-rapid-preview-tier/40-CONTEXT.md (DECISIONS section — engine class structure, env var default, LTX-Video API contract)
  </read_first>
  <behavior>
    - Test 1: `PreviewEngine` is an ABC — `from abc import ABC; assert issubclass(PreviewEngine, ABC)` and `hasattr(PreviewEngine, 'generate')` and `getattr(PreviewEngine.generate, '__isabstractmethod__', False) is True`
    - Test 2: Instantiating `PreviewEngine()` directly raises `TypeError` (cannot instantiate ABC)
    - Test 3: `PreviewEngineError` is a subclass of `Exception`
    - Test 4: `select_engine()` with `KAIS_PREVIEW_ENGINE` unset returns a `SlideshowEngine` instance (default fallback per CONTEXT decision)
    - Test 5: `select_engine(env="ltx")` returns an `LTXVideoEngine` instance
    - Test 6: `select_engine(env="slideshow")` returns a `SlideshowEngine` instance
    - Test 7: `select_engine(env="unknown_value")` returns a `SlideshowEngine` instance AND emits a `logger.warning` containing the unknown value (graceful fallback)
    - Test 8: `select_engine()` reads `os.environ.get("KAIS_PREVIEW_ENGINE")` at call time (not import time) — verified by monkeypatching `os.environ` between calls
  </behavior>
  <action>
    RED: Write the test file with the 8 tests above. Run pytest — it should fail with ImportError (module not yet created).

    GREEN: Create `plugins/kais_aigc/preview_engine.py` with:

    (a) `class PreviewEngineError(Exception)` — empty body, just a docstring.

    (b) `class PreviewEngine(abc.ABC)` with:
        - Module docstring explaining the strategy pattern + dual-engine purpose
        - Abstract method `generate(self, *, shot_id: str, prompt: str, structure_delta: dict, keyframe_image_path: str | None = None, voice_clip_path: str | None = None, output_path: str | None = None) -> dict` — returns `{"clip_path": str, "generation_time_ms": int, "engine": str}` or `{"degraded": True, ...}` envelope.
        - Concrete helper `_record_time(self, start: float) -> int` — returns `int((time.monotonic() - start) * 1000)` for generation_time_ms measurement.

    (c) `def select_engine(env: str | None = None) -> PreviewEngine:` factory:
        - Reads `env = env if env is not None else os.environ.get("KAIS_PREVIEW_ENGINE", "slideshow")`
        - Dispatches: `"ltx"` → `LTXVideoEngine()`; `"slideshow"` → `SlideshowEngine()`; default → `SlideshowEngine()` + `logger.warning("unknown KAIS_PREVIEW_ENGINE=%r, falling back to slideshow", env)`
        - NOTE: Forward-reference `LTXVideoEngine` and `SlideshowEngine` — they are defined AFTER `select_engine` in the module OR imported from below. Since they don't exist yet (Task 2 + 3 implement them), Task 1's tests MUST NOT exercise the ltx/slideshow paths — only assert the factory dispatches to the right class. Use `pytest.importorskip` or define the classes as stubs in this task and expand them in Tasks 2/3. RECOMMENDED: define minimal SlideshowEngine and LTXVideoEngine stubs at the bottom of the module (subclass PreviewEngine, implement generate() to raise NotImplementedError) so select_engine() can import them; Tasks 2 and 3 EXPAND those stubs.

    Module imports needed: `from __future__ import annotations`, `import abc, logging, os, time`, `from typing import Any, Callable`.

    Module logger: `logger = logging.getLogger(__name__)`.
  </action>
  <verify>
    <automated>cd /data/workspace/hermes-agent && python -m pytest plugins/kais_aigc/tests/test_preview_engine.py::TestPreviewEngineABC -x -q</automated>
  </verify>
  <done>
    - PreviewEngine ABC + PreviewEngineError + select_engine() factory implemented
    - All 8 behavior tests pass (RED→GREEN cycle complete)
    - SlideshowEngine and LTXVideoEngine stubs exist so select_engine() returns real instances
  </done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: Implement SlideshowEngine (FFmpeg subprocess) + degrade path</name>
  <files>
    /data/workspace/hermes-agent/plugins/kais_aigc/preview_engine.py,
    /data/workspace/hermes-agent/plugins/kais_aigc/tests/test_preview_engine.py
  </files>
  <read_first>
    - /data/workspace/hermes-agent/plugins/kais_aigc/preview_engine.py (the file being expanded — has the ABC + stubs from Task 1)
    - /data/workspace/hermes-agent/plugins/kais_aigc/tests/test_preview_engine.py (the file being expanded — has Task 1 tests)
    - /data/workspace/kais-movie-agent/.planning/phases/40-rapid-preview-tier/40-CONTEXT.md (specifics section — Slideshow FFmpeg contract: `-i img -i audio -c:v libx264 -c:a aac out.mp4`, < 10s target)
  </read_first>
  <behavior>
    - Test 1: `SlideshowEngine().generate(shot_id="s1", prompt="...", structure_delta={"hook_position_sec": 5}, keyframe_image_path="/tmp/kf.png", voice_clip_path="/tmp/vo.wav", output_path="/tmp/out.mp4")` with a mocked subprocess runner returns `{"clip_path": "/tmp/out.mp4", "generation_time_ms": <int>, "engine": "slideshow"}` — no `degraded` key
    - Test 2: The mocked subprocess runner receives the FFmpeg command with `-loop 1 -i /tmp/kf.png -i /tmp/vo.wav -c:v libx264 -tune stillimage -c:a aac -b:a 192k -shortest -pix_fmt yuv420p /tmp/out.mp4` (exact arg ordering verified)
    - Test 3: When the subprocess runner returns a non-zero exit code (simulated FFmpeg failure), `generate()` returns a degrade envelope `{"degraded": True, "engine": "slideshow", "reason": <str containing stderr/returncode>}` and does NOT raise
    - Test 4: When `keyframe_image_path` is None or empty, `generate()` returns a degrade envelope `{"degraded": True, "engine": "slideshow", "reason": "missing keyframe_image_path"}` (input validation)
    - Test 5: When `voice_clip_path` is None or empty, degrade envelope with reason `"missing voice_clip_path"`
    - Test 6: When `shutil.which("ffmpeg")` returns None (FFmpeg binary not installed), degrade envelope with reason `"ffmpeg binary not found in PATH"`
    - Test 7: The constructor accepts an optional `subprocess_runner: Callable[[list[str]], int] | None = None` kwarg for test injection; default is `None` which means use real `subprocess.run`
    - Test 8: `generation_time_ms` is a non-negative int (measured around the subprocess call)
  </behavior>
  <action>
    RED: Add the 8 SlideshowEngine tests to `test_preview_engine.py` in a new `TestSlideshowEngine` class.

    GREEN: Expand the `SlideshowEngine` stub (created in Task 1) into a full implementation:

    (a) Constructor: `def __init__(self, *, subprocess_runner: Callable[[list[str]], "subprocess.CompletedProcess"] | None = None) -> None:` — stores the runner; if None, defaults to a wrapper around `subprocess.run`.

    (b) `generate()` method:
        - Validate inputs first: if `not keyframe_image_path` → return degrade envelope; same for `voice_clip_path` and `output_path`.
        - If `subprocess_runner is None`: check `shutil.which("ffmpeg")`; if None → return degrade envelope.
        - Build the FFmpeg command list: `["ffmpeg", "-y", "-loop", "1", "-i", keyframe_image_path, "-i", voice_clip_path, "-c:v", "libx264", "-tune", "stillimage", "-c:a", "aac", "-b:a", "192k", "-shortest", "-pix_fmt", "yuv420p", output_path]`
        - Record `start = time.monotonic()`.
        - Call the runner (or real subprocess). Capture returncode.
        - If returncode != 0: return degrade envelope `{"degraded": True, "engine": "slideshow", "reason": f"ffmpeg exited {returncode}: {stderr[:200]}"}`.
        - On success: return `{"clip_path": output_path, "generation_time_ms": int((time.monotonic() - start) * 1000), "engine": "slideshow"}`.
        - Wrap the whole thing in try/except for unexpected exceptions (OSError etc.) → degrade envelope.

    (c) Module imports: add `import shutil, subprocess` to the existing import block.
  </action>
  <verify>
    <automated>cd /data/workspace/hermes-agent && python -m pytest plugins/kais_aigc/tests/test_preview_engine.py::TestSlideshowEngine -x -q</automated>
  </verify>
  <done>
    - SlideshowEngine fully implemented with FFmpeg subprocess contract
    - All 8 SlideshowEngine tests pass
    - Degrade paths covered: missing inputs, missing ffmpeg binary, non-zero exit
  </done>
</task>

<task type="tdd" tdd="true">
  <name>Task 3: Implement LTXVideoEngine (mocked httpx POST) + degrade-first contract</name>
  <files>
    /data/workspace/hermes-agent/plugins/kais_aigc/preview_engine.py,
    /data/workspace/hermes-agent/plugins/kais_aigc/tests/test_preview_engine.py
  </files>
  <read_first>
    - /data/workspace/hermes-agent/plugins/kais_aigc/preview_engine.py (the file being expanded)
    - /data/workspace/hermes-agent/plugins/kais_aigc/tests/test_preview_engine.py (the file being expanded)
    - /data/workspace/hermes-agent/plugins/kais_aigc/gold_team.py (RE-READ the _request() method lines 170-221 and _degrade() lines 147-155 — the LTXVideoEngine MUST mirror this contract EXACTLY: ConnectError/Timeout/5xx/429 → degrade, 4xx → raise)
  </read_first>
  <behavior>
    - Test 1: `LTXVideoEngine(transport=httpx.MockTransport(handler))` where handler returns HTTP 200 `{"clip_path": "/x.mp4", "generation_time_ms": 1200}` — `generate()` returns `{"clip_path": "/x.mp4", "generation_time_ms": 1200, "engine": "ltx"}` (NOTE: the returned `generation_time_ms` should be the engine-reported value from the response body OR the locally-measured wall time — pick one and document it; recommend: use locally-measured wall time, ignore the response body's `generation_time_ms` field which is service-side-reported and may be unreliable)
    - Test 2: The handler receives a POST to `/api/v1/ltx` with JSON body `{"shot_id": "s1", "prompt": "...", "structure_delta": {"hook_position_sec": 5}}` (verify request method, path, body keys)
    - Test 3: Handler raises `httpx.ConnectError` → `generate()` returns degrade envelope `{"degraded": True, "engine": "ltx", "reason": <str mentioning ConnectError>}` and does NOT raise
    - Test 4: Handler raises `httpx.TimeoutException` → degrade envelope
    - Test 5: Handler returns HTTP 500 → degrade envelope with `reason` containing "HTTP 500"
    - Test 6: Handler returns HTTP 429 → degrade envelope with `reason` containing "HTTP 429"
    - Test 7: Handler returns HTTP 400 → raises `PreviewEngineError` (4xx is caller bug, per D-09)
    - Test 8: Handler returns HTTP 200 with invalid JSON body → raises `PreviewEngineError` (service-side bug — invalid contract)
    - Test 9: Handler returns HTTP 200 but missing `clip_path` in body → degrade envelope with reason `"missing clip_path in ltx response"`
    - Test 10: Constructor reads `base_url` from kwarg or `KAIS_LTX_URL` env var (default `http://localhost:9001`); accepts `transport: httpx.BaseTransport | None = None` for test injection (mirrors GoldTeamClient D-04)
    - Test 11: `LTXVideoEngine()` with no transport uses a real `httpx.Client` — verify via introspection (`engine._client` is an `httpx.Client` with the right base_url) — do NOT make a real network call in the test
  </behavior>
  <action>
    RED: Add the 11 LTXVideoEngine tests to a new `TestLTXVideoEngine` class.

    GREEN: Expand the `LTXVideoEngine` stub into a full implementation that MIRRORS `GoldTeamClient` structure:

    (a) Class constants:
        - `DEFAULT_BASE_URL = "http://localhost:9001"` (per CONTEXT decision)
        - `DEFAULT_TIMEOUT = 30.0` (LTX-Video is second-scale; allow generous timeout)

    (b) Constructor (mirror GoldTeamClient.__init__):
        ```python
        def __init__(self, *, base_url: str | None = None, timeout: float | None = None, transport: httpx.BaseTransport | None = None) -> None:
            self._base_url = (base_url or os.environ.get("KAIS_LTX_URL") or self.DEFAULT_BASE_URL).rstrip("/")
            self._timeout = timeout if timeout is not None else self.DEFAULT_TIMEOUT
            self._client = httpx.Client(timeout=self._timeout, transport=transport)
        ```

    (c) `_degrade(self, reason: str) -> dict` helper — MIRROR GoldTeamClient._degrade exactly:
        ```python
        logger.warning("ltx_engine degraded: %s", reason)
        return {"degraded": True, "engine": "ltx", "reason": reason}
        ```

    (d) `_request(self, body: dict) -> Any` helper — MIRROR GoldTeamClient._request (lines 170-221):
        - try `self._client.post(f"{self._base_url}/api/v1/ltx", headers={"Content-Type": "application/json"}, json=body)`
        - except `(httpx.ConnectError, httpx.TimeoutException)` → return `self._degrade(f"{type(exc).__name__}: {exc}")`
        - except `httpx.HTTPError` → return `self._degrade(...)`
        - if status >= 500 or status == 429 → return `self._degrade(f"HTTP {status}")`
        - if status >= 400 → raise `PreviewEngineError(f"HTTP {status}: {text[:200]}")`
        - try `response.json()` — except → raise `PreviewEngineError(f"Invalid JSON: {text[:200]}")`
        - return parsed json

    (e) `generate()` method:
        - Build body: `{"shot_id": shot_id, "prompt": prompt, "structure_delta": structure_delta}`
        - Record `start = time.monotonic()`
        - `result = self._request(body)`
        - If `result.get("degraded")` → return result (pass-through degrade envelope)
        - Validate response has `clip_path`: if not → return `self._degrade("missing clip_path in ltx response")`
        - Return `{"clip_path": result["clip_path"], "generation_time_ms": int((time.monotonic() - start) * 1000), "engine": "ltx"}` (locally-measured wall time — see Test 1 note)

    (f) Add `close()` + context manager methods (`__enter__`/`__exit__`) mirroring GoldTeamClient.

    Module imports: add `import httpx` to the import block.
  </action>
  <verify>
    <automated>cd /data/workspace/hermes-agent && python -m pytest plugins/kais_aigc/tests/test_preview_engine.py -x -q</automated>
  </verify>
  <done>
    - LTXVideoEngine fully implemented mirroring GoldTeamClient D-09 contract
    - All 11 LTXVideoEngine tests pass
    - Degrade paths covered: ConnectError, Timeout, 5xx, 429, missing fields
    - Raise paths covered: 4xx, invalid JSON
    - SlideshowEngine + PreviewEngine ABC tests still pass (no regression)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| p10b phase → LTXVideoEngine | In-process call; structure_delta crosses trust boundary to external HTTP service |
| LTXVideoEngine → :9001/api/v1/ltx | Outbound HTTP to LTX-Video GPU service (mocked in v6.0) |
| SlideshowEngine → FFmpeg subprocess | Local subprocess invocation with file path arguments |
| select_engine → os.environ | Env var read at call time (not import time) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-40-05 | Tampering | structure_delta passed to LTX-Video HTTP body | accept | structure_delta is {param_name: new_value} single-key dict (validated in plan 03 at write time); no shell metachar exposure (JSON body, not shell args) |
| T-40-06 | Tampering | FFmpeg subprocess argument injection | mitigate | SlideshowEngine builds argv as a Python list (no shell=True); keyframe_image_path / voice_clip_path / output_path are filesystem paths supplied by p10b phase (in-process trust); subprocess.run receives list, not string |
| T-40-07 | Denial of Service | LTX-Video service unavailable blocks p10b | mitigate | D-09 degrade-first: ConnectError/Timeout/5xx/429 → degrade envelope returned to p10b, which then takes phase-level WARN degrade path (RAPID-PREVIEW-05, plan 03) |
| T-40-08 | Information Disclosure | LTX-Video service logging prompt content | accept | v6.0 LTX-Video is mocked/local; real service is operator-side per Out of Scope; no PII in prompts (creative content only) |
| T-40-09 | Elevation of Privilege | Malicious env var KAIS_PREVIEW_ENGINE value | mitigate | select_engine() unknown values fall back to slideshow + WARN log (Test 7); no eval(), no dynamic class lookup |
</threat_model>

<verification>
- `python -m pytest plugins/kais_aigc/tests/test_preview_engine.py -q` — all 27+ tests pass (8 ABC + 8 Slideshow + 11 LTX)
- `python -c "from plugins.kais_aigc.preview_engine import PreviewEngine, SlideshowEngine, LTXVideoEngine, PreviewEngineError, select_engine; e = select_engine(); assert isinstance(e, SlideshowEngine)"` (default fallback)
- `KAIS_PREVIEW_ENGINE=ltx python -c "from plugins.kais_aigc.preview_engine import select_engine; e = select_engine(); assert type(e).__name__ == 'LTXVideoEngine'"`
- Full V5.0 regression: `python -m pytest skills/kais-movie-pipeline/tests/ plugins/pipeline_state/tests/ plugins/kais_aigc/tests/ plugins/review_gates/tests/ -q` passes (no regression from plan 01)
</verification>

<success_criteria>
- RAPID-PREVIEW-02 (dual-engine + env selection + default slideshow) ✓
- RAPID-PREVIEW-05 (engine-layer degrade-first contract — connection errors / 5xx / 429 degrade, 4xx raises, never silent) ✓
- Both engines testable without real FFmpeg binary or real HTTP service (transport/subprocess injection) ✓
- Gold team D-09 contract pattern faithfully mirrored (no silent swallow, no innovation on the degrade envelope shape) ✓
</success_criteria>

<output>
Create `.planning/phases/40-rapid-preview-tier/40-02-SUMMARY.md` when done. Include:
- Final test count for preview_engine tests
- Decision: locally-measured generation_time_ms (vs service-reported) — document why
- Confirmation that GoldTeamClient D-09 contract is faithfully mirrored
- Note: plan 03 will compose these engines into the p10b phase fan-out
</output>
