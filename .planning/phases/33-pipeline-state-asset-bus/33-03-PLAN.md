---
phase: 33-pipeline-state-asset-bus
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - /data/workspace/hermes-agent/plugins/pipeline_state/creative_history.py
  - /data/workspace/hermes-agent/plugins/pipeline_state/tests/test_creative_history.py
autonomous: true
requirements: []   # foundation: implicit support for B4-03/04/05 (v3.0 flagship)

must_haves:
  truths:
    - "CreativeHistoryTracker.stamp appends a record to creative-history slot"
    - "Multiple stamps accumulate atomically (read-modify-write)"
    - "Stamp on broken AssetBus returns False (degraded mode, no throw)"
    - "Stamp rejects entries missing asset_slot or asset_id (raises ValueError)"
    - "Reverse BFS over chain A→B→C returns both B and C when A changes"
    - "Leaf node (no derived) returns empty affected list"
    - "Diamond DAG deduplicates shared descendants"
    - "Blast radius cap sets truncated=True when exceeded"
    - "Depth cap stops BFS at maxDepth layers"
    - "diff() batches multiple hashes, returns union + per_hash breakdown"
    - "BFS over 1000-stamp chain completes in <500ms (B4-04 perf budget)"
  artifacts:
    - path: "/data/workspace/hermes-agent/plugins/pipeline_state/creative_history.py"
      provides: "CreativeHistoryTracker class + write_blast_radius_report helper"
      min_lines: 180
    - path: "/data/workspace/hermes-agent/plugins/pipeline_state/tests/test_creative_history.py"
      provides: "pytest unit + perf tests for CreativeHistoryTracker"
      min_lines: 220
  key_links:
    - from: "CreativeHistoryTracker.__init__"
      to: "AssetBus instance (constructor kwarg)"
      via: "asset_bus= kwarg (required)"
      pattern: "def __init__\\(self, \\*, asset_bus"
    - from: "CreativeHistoryTracker._build_index"
      to: "Map<source_hash, list[record]>"
      via: "lazy build + cache invalidation on stamp"
      pattern: "_index_cache|_build_index"
    - from: "find_affected BFS queue"
      to: "collections.deque.popleft (O(1))"
      via: "deque for BFS FIFO"
      pattern: "deque|popleft"
---

<objective>
Port Node.js `lib/creative-history-tracker.js` (272 lines) — the v3.0 flagship
"Git-for-AIGC-movies MVP" — to Python `plugins/pipeline_state/creative_history.py`.

Purpose: SC#3 — DAG + reverse BFS + blast radius cap. Implements B4-03/04/05.
"改剧本一行 → 自动定位受影响镜头列表" — change script hash, BFS finds all
downstream shots that need re-render.

Output: `creative_history.py` (~200-300 LOC) + `test_creative_history.py` (12-15 tests incl. perf).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/33-pipeline-state-asset-bus/CONTEXT.md
@.planning/phases/33-pipeline-state-asset-bus/PATTERNS.md

# Node.js reference — direct port source (read fully once)
@lib/creative-history-tracker.js

# Node.js test refs (behavior + perf baseline — read once for test mapping)
@/data/workspace/kais-movie-agent/test/phases/creative-history-tracker.test.mjs
@/data/workspace/kais-movie-agent/test/phases/creative-history-perf.test.mjs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement CreativeHistoryTracker + write_blast_radius_report</name>
  <files>/data/workspace/hermes-agent/plugins/pipeline_state/creative_history.py</files>
  <behavior>
    - `DEFAULT_MAX_BLAST_RADIUS == 20` and `DEFAULT_MAX_DEPTH == 5` (class constants — match Node.js creative-history-tracker.js:30-31, B4-04 spec)
    - `CreativeHistoryTracker.hash({"k":"v"})` (static) returns 64-char SHA-256 hex (same algorithm as AssetBus._compute_content_hash for cross-compat — sort_keys=True)
    - `CreativeHistoryTracker(asset_bus=None)` raises ValueError ("asset_bus required")
    - `tracker.stamp({"asset_slot": "x", "asset_id": "y", "source_hashes": ["h1"], "content_hash": "v1"})` returns True, appends record to creative-history slot
    - After stamp, the creative-history slot's "shots" array contains the new record (asset_slot, asset_id, source_hashes, content_hash, timestamp ISO format)
    - `tracker.stamp(...)` 5 times → 5 records in slot, in order
    - `tracker.stamp({"asset_id": "x"})` (missing asset_slot) raises ValueError
    - `tracker.stamp({})` (missing fields) raises ValueError
    - `tracker.stamp(entry_without_content_hash)` computes hash from entry itself (mirror Node.js creative-history-tracker.js:79)
    - Stamp on broken AssetBus (read raises) returns False, logs warning, does NOT raise
    - `tracker.find_affected("hash-A")` where A→B→C returns list [B_record, C_record] with depth=1 and depth=2
    - `find_affected` on leaf hash returns `{"affected": [], "truncated": False, "blast_radius": 0, ...}`
    - Diamond DAG (A→B, A→C, B→D, C→D): find_affected("A") returns [B, C, D] with D appearing once (dedup by asset_key)
    - With `max_blast_radius=2`, BFS over 3+ derived assets returns `truncated=True` and `blast_radius=2`
    - With `max_depth=1`, BFS over depth-3 chain stops at depth 1 (`max_depth=1` in response)
    - Response dict shape: `{"affected": [...], "truncated": bool, "blast_radius": int, "max_depth": int, "cap": {"maxBlastRadius": int, "maxDepth": int}}` — keys match Node.js for report compat
    - `tracker.diff(["h1", "h2"])` returns union of affected + per_hash dict
    - `tracker.diff([])` returns `{"affected": [], "truncated": False, "per_hash": {}}`
    - `_build_index()` returns dict with by_source mapping; called lazily, cached
    - After stamp(), `_index_cache` is None (invalidated)
    - `write_blast_radius_report(result_dict, output_path, changed_hash=None)` writes JSON report file with keys: generated_at, changed_hash, affected_count, truncated, blast_radius, max_depth, cap, affected, note
  </behavior>
  <action>
    Create `plugins/pipeline_state/creative_history.py` with:
    1. Module docstring referencing `kais-movie-agent/lib/creative-history-tracker.js` as port source. Note this is the v3.0 flagship capability (B4-03/04/05).
    2. Imports: `from __future__ import annotations`, stdlib only (`collections.deque`, `datetime`, `hashlib`, `json`, `logging`, `pathlib`, `typing`). NO async, NO third-party.
    3. Module constants (mirror creative-history-tracker.js:30-31):
       - `DEFAULT_MAX_BLAST_RADIUS = 20`
       - `DEFAULT_MAX_DEPTH = 5`
    4. Module-level `logger = logging.getLogger(__name__)`.
    5. Module-level helper `_compute_hash(value) -> str` — same as AssetBus._compute_content_hash (sort_keys=True). NOTE: ideally import from asset_bus module, but to avoid coupling at module-load time (in case asset_bus has not loaded yet in test), replicate the function locally. Add a comment noting the duplication and that hashes will match across modules.
    6. Class `CreativeHistoryTracker`:
       - Class constants: `DEFAULT_MAX_BLAST_RADIUS = 20`, `DEFAULT_MAX_DEPTH = 5`.
       - Static method `hash(value: Any) -> str` — delegates to module `_compute_hash`.
       - `__init__(self, *, asset_bus: "AssetBus", max_blast_radius: int = 20, max_depth: int = 5)`:
         - If `asset_bus is None`: raise ValueError("CreativeHistoryTracker: asset_bus required").
         - `self._bus = asset_bus`, `self._max_blast_radius = max_blast_radius`, `self._max_blast_depth = max_depth`.
         - `self._index_cache: dict | None = None`.
       - `stamp(self, entry: dict) -> bool` — mirror creative-history-tracker.js:74-102:
         - Validate: if not entry or not entry.get("asset_id") or not entry.get("asset_slot"): raise ValueError.
         - `source_hashes = entry.get("source_hashes") or []`; ensure list.
         - `content_hash = entry.get("content_hash") or self.hash(entry)`.
         - `record = {"asset_slot": entry["asset_slot"], "asset_id": entry["asset_id"], "source_hashes": source_hashes, "content_hash": content_hash, "timestamp": entry.get("timestamp") or datetime.now(timezone.utc).isoformat()}`.
         - Try: `current = self._bus.read("creative-history") or {"shots": [], "version": 1}`; ensure `current["shots"]` is list; append record; `self._bus.write("creative-history", current, envelope=True)`; set `self._index_cache = None`; return True.
         - Except Exception as e: `logger.warning("CreativeHistoryTracker stamp degraded: %s", e)`; return False.
       - `find_affected(self, changed_hash: str) -> dict` — mirror creative-history-tracker.js:118-176:
         - `index = self._build_index()`.
         - Init: `affected = []`, `seen_hashes = {changed_hash}`, `seen_asset_ids = set()`, `truncated = False`, `max_depth_reached = 0`, `queue = deque([(changed_hash, 0)])`.
         - While queue: `h, depth = queue.popleft()`. If `depth >= self._max_depth`: continue. `derived_records = index.get(h, [])`. For each record: compute `asset_key = f"{record['asset_slot']}:{record['asset_id']}"`; skip if in seen_asset_ids; add to seen; if `len(affected) >= self._max_blast_radius`: set truncated=True, break; append to affected (with depth=depth+1); update max_depth_reached; if record["content_hash"] not in seen_hashes: add and queue.append((content_hash, depth+1)). Break outer loop if truncated.
         - Return dict: `{"affected": affected, "truncated": truncated, "blast_radius": len(affected), "max_depth": max_depth_reached, "cap": {"maxBlastRadius": self._max_blast_radius, "maxDepth": self._max_depth}}`.
       - `diff(self, changed_hashes: list[str]) -> dict` — mirror creative-history-tracker.js:183-201:
         - If empty list: return `{"affected": [], "truncated": False, "per_hash": {}}`.
         - For each hash: `r = self.find_affected(h)`; track truncated; merge into `all_map` keyed by asset_key (dedup); record per_hash[h] = r.
         - Return `{"affected": list(all_map.values()), "truncated": truncated, "per_hash": per_hash}`.
       - `_build_index(self) -> dict` — mirror creative-history-tracker.js:209-233:
         - If `self._index_cache`: return it.
         - Try `data = self._bus.read("creative-history") or {"shots": []}`; except: `data = {"shots": []}`.
         - `records = data.get("shots") or []`.
         - `by_source: dict[str, list[dict]] = {}`; for each record: if no content_hash: skip; for each src in record.get("source_hashes") or []: append record to by_source[src] list.
         - `self._index_cache = by_source`; return it. (Cache only the by_source map — Node.js caches a tuple, we keep just the dict for simplicity.)
    7. Module-level function `write_blast_radius_report(find_affected_result: dict, output_path: str | Path, changed_hash: str | None = None) -> str` — mirror creative-history-tracker.js:253-272:
       - Build report dict: `generated_at`, `changed_hash`, `affected_count`, `truncated`, `blast_radius`, `max_depth`, `cap`, `affected`, `note`.
       - Note text: if truncated: `f"Scope exceeded maxBlastRadius={cap['maxBlastRadius']}. Review manually or re-run tracker.find_affected(hash, max_blast_radius=N)."` else: `"All affected assets captured."`.
       - `path = Path(output_path)`, `path.parent.mkdir(parents=True, exist_ok=True)`, `path.write_text(json.dumps(report, indent=2, ensure_ascii=False), encoding="utf-8")`. Return str(path).
  </action>
  <verify>
    <automated>cd /data/workspace/hermes-agent && python -m pytest plugins/pipeline_state/tests/test_creative_history.py -v</automated>
  </verify>
  <done>
    - `creative_history.py` exists with CreativeHistoryTracker class + write_blast_radius_report function
    - DEFAULT_MAX_BLAST_RADIUS=20, DEFAULT_MAX_DEPTH=5 constants present
    - BFS uses `collections.deque` (grep returns ≥1 match for `deque` or `popleft`)
    - Stamp on broken bus returns False (degraded mode)
    - All 12-15 tests in test_creative_history.py pass (Task 2)
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Write pytest unit + perf tests for CreativeHistoryTracker (≥12 tests)</name>
  <files>/data/workspace/hermes-agent/plugins/pipeline_state/tests/test_creative_history.py</files>
  <behavior>
    Mirror Node.js creative-history-tracker.test.mjs (13 tests) + creative-history-perf.test.mjs (4 tests).
    See Task 1 <behavior> bullets for full coverage list.
  </behavior>
  <action>
    Create `plugins/pipeline_state/tests/test_creative_history.py`. Use `tmp_path: Path` fixture. Import both AssetBus and CreativeHistoryTracker — each test instantiates `bus = AssetBus(tmp_path)` then `tracker = CreativeHistoryTracker(asset_bus=bus)`.

    Class structure (mirrors Node.js describe blocks):

    - `class TestStamp`:
      - `test_single_stamp_appends_to_creative_history_slot`: stamp one, read slot, assert 1 record with expected fields.
      - `test_multiple_stamps_accumulate`: stamp 5 times, assert 5 ordered records.
      - `test_stamp_degraded_returns_false_on_broken_bus`: build a fake bus whose read/write raise; stamp returns False, no exception. Mirror creative-history-tracker.test.mjs:77.
      - `test_stamp_rejects_entry_missing_required_fields`: stamp({}) and stamp({"asset_id":"x"}) both raise ValueError.
      - `test_stamp_computes_content_hash_when_omitted`: stamp without content_hash, verify record.content_hash is 64 hex chars.

    - `class TestFindAffected`:
      - `test_chain_a_b_c_returns_both_b_and_c`: build chain A→B→C via stamps; find_affected("A") returns [B, C] with depths 1 and 2. Mirror creative-history-tracker.test.mjs:109.
      - `test_leaf_hash_returns_empty`: find_affected on hash with no derived returns affected=[].
      - `test_blast_radius_cap_truncated_true`: tracker with max_blast_radius=2; build 3 derived; find_affected returns truncated=True, blast_radius=2.
      - `test_depth_cap_stops_at_max_depth`: tracker with max_depth=1; build chain depth 3; response max_depth=1.
      - `test_diamond_dag_deduplicates`: A→B, A→C, B→D, C→D; find_affected("A") returns 3 unique records (D appears once).
      - `test_cap_dict_in_response_has_camel_case_keys`: response["cap"] has keys "maxBlastRadius" and "maxDepth" (NOT max_blast_radius — Node.js compat for reports).

    - `class TestDiff`:
      - `test_diff_multiple_hashes_returns_union_with_per_hash`: build two independent chains; diff both; affected has union; per_hash has both keys.
      - `test_diff_empty_returns_empty`: diff([]) returns {"affected":[], "truncated":False, "per_hash":{}}.

    - `class TestWriteBlastRadiusReport`:
      - `test_write_report_truncated`: build a truncated result, write report to file, read back, assert keys (generated_at, changed_hash, affected_count, truncated=True, note mentions "exceeded").
      - `test_write_report_non_truncated`: same but truncated=False, note="All affected assets captured."

    - `class TestPerformance` (perf — mirror creative-history-perf.test.mjs):
      - `test_bfs_1000_chain_under_500ms`: build 1000-link chain (each stamp sources from previous content_hash); time `tracker.find_affected(root_hash)`; assert elapsed < 500ms (B4-04 budget per v3.0 spec); assert blast_radius == 1000, truncated == False.
      - `test_deep_chain_depth_10_under_500ms` (optional, if time permits): similar but chain depth 10 with max_depth=10.

    Use `time.perf_counter()` for timing (more precise than `time.time()`).

    CRITICAL for the 1000-stamp perf test: the chain build will call `stamp()` 1000 times, each doing a read-modify-write cycle. This will be SLOW (potentially seconds). Two options:
    (a) Keep the test as-is and accept the setup time — only the `find_affected` call needs to be <500ms.
    (b) Build the chain via direct bus.write once (one big shots[] array of 1000 records) instead of 1000 stamp() calls.

    Prefer (b) for test speed — manually construct the shots list and write once. Document in test docstring that this bypasses stamp() to keep test runtime manageable while still exercising BFS over 1000 records.

    Run from `/data/workspace/hermes-agent`.
  </action>
  <verify>
    <automated>cd /data/workspace/hermes-agent && python -m pytest plugins/pipeline_state/tests/test_creative_history.py -v --tb=short</automated>
  </verify>
  <done>
    - test_creative_history.py exists with ≥12 tests, all passing
    - Perf test (test_bfs_1000_chain_under_500ms) passes — BFS over 1000 records in <500ms
    - Degraded mode test (broken bus returns False) passes
    - Diamond DAG dedup test passes
    - All response dict keys are camelCase where Node.js uses camelCase (cap.maxBlastRadius, cap.maxDepth, blast_radius, max_depth) — verified by assertion
  </done>
</task>

</tasks>

<verification>
Plan 33-03 complete when:
- `plugins/pipeline_state/creative_history.py` exists, imports cleanly, exports CreativeHistoryTracker + write_blast_radius_report
- `plugins/pipeline_state/tests/test_creative_history.py` passes (≥12 tests)
- BFS uses `collections.deque` (not list.pop(0))
- DEFAULT_MAX_BLAST_RADIUS=20 and DEFAULT_MAX_DEPTH=5 constants match Node.js
- Perf test confirms BFS over 1000-stamp chain < 500ms (B4-04 budget)
- No async, no third-party imports
</verification>

<success_criteria>
- SC#3 (ROADMAP): `pipeline_state/creative_history.py` implements CreativeHistoryTracker DAG + reverse BFS + blast radius cap (1000-asset BFS < 500ms — matches B4-04 perf budget; Node.js measured 0.47ms is informational) — MET
- B4-03 (DAG + BFS): MET
- B4-04 (blast radius cap + ≤500ms): MET (caps + perf test)
- B4-05 (hash-stamping downstream lineage): MET (stamp() method)
- Node.js baseline: creative-history-tracker.test.mjs (13) + creative-history-perf.test.mjs (4) = 17 Node.js tests; Python targets ≥12 cases (combined behavior + perf)
</success_criteria>

<output>
Create `.planning/phases/33-pipeline-state-asset-bus/33-03-SUMMARY.md` when done.
</output>
