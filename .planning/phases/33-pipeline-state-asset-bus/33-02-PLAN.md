---
phase: 33-pipeline-state-asset-bus
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - /data/workspace/hermes-agent/plugins/pipeline_state/asset_bus.py
  - /data/workspace/hermes-agent/plugins/pipeline_state/tests/test_asset_bus.py
autonomous: true
requirements: []   # foundation: implicit support for SCHEMA-01/02/03 + B4-01/02

must_haves:
  truths:
    - "AssetBus.write + read round-trips a slot payload with envelope wrap/unwrap"
    - "Atomic write leaves no .tmp residue (write-tmp-then-rename strategy)"
    - "mtime cache invalidates on write (next read sees new data)"
    - "append_line writes one JSONL line; read_lines parses them back"
    - "write rejects JSONL slots; append_line rejects JSON slots (format mismatch raises)"
    - "Unknown slot raises AssetBusError"
    - "v2.0 raw JSON (no envelope) is read back as-is (backward compat)"
    - "derived_from non-empty forces envelope (content_hash linkage)"
  artifacts:
    - path: "/data/workspace/hermes-agent/plugins/pipeline_state/asset_bus.py"
      provides: "AssetBus class + wrap_envelope/unwrap_envelope + _compute_content_hash + ASSET_SCHEMA + SCHEMA_VERSION"
      min_lines: 220
    - path: "/data/workspace/hermes-agent/plugins/pipeline_state/tests/test_asset_bus.py"
      provides: "pytest unit tests for AssetBus (≥15 tests)"
      min_lines: 250
  key_links:
    - from: "AssetBus.write"
      to: "wrap_envelope → _atomic_write_text"
      via: "tmp + os.replace"
      pattern: "os\\.replace|wrap_envelope"
    - from: "AssetBus.read"
      to: "_cache_key (mtime_ns) → unwrap_envelope"
      via: "stat().st_mtime_ns lookup"
      pattern: "st_mtime_ns|unwrap_envelope"
    - from: "AssetBus.append_line"
      to: ".pipeline-assets/finetune-dataset.jsonl"
      via: "open(path, 'a') + json.dumps + '\\n'"
      pattern: "open\\(.*['\"]a['\"]"
---

<objective>
Port Node.js `lib/asset-bus.js` (332 lines) to Python `plugins/pipeline_state/asset_bus.py`.

Purpose: SC#2 — AssetBus V3 with 3 typed slots (creative-history / failed-shots
/ finetune-dataset) + envelope + atomic write (JSONL append for finetune-dataset).
Implements SCHEMA-01/02/03 + B4-01/02.

Scope: 3 v3.0 typed slots only (per CONTEXT CF-05). V2/V4.1 slots (art-bible,
character-assets, etc.) are out of scope. `review-outcomes` slot routed as
generic JSON per D-33-03 (Phase 34 defines schema).

Output: `asset_bus.py` (~250-350 LOC) + `test_asset_bus.py` (15-18 tests).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/33-pipeline-state-asset-bus/CONTEXT.md
@.planning/phases/33-pipeline-state-asset-bus/PATTERNS.md

# Node.js reference — direct port source (read fully once)
@lib/asset-bus.js

# Node.js test refs (behavior baseline — read once for test case mapping)
@/data/workspace/kais-movie-agent/test/phases/asset-bus.test.mjs
@/data/workspace/kais-movie-agent/test/phases/asset-bus-derived-from.test.mjs

# Skeleton (do NOT modify in this plan — Plan 33-04 owns tools.py)
@/data/workspace/hermes-agent/plugins/pipeline_state/__init__.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement AssetBus + envelope helpers + atomic write + JSONL append</name>
  <files>/data/workspace/hermes-agent/plugins/pipeline_state/asset_bus.py</files>
  <behavior>
    - `SCHEMA_VERSION == "3.0"`
    - `ASSET_SCHEMA` dict has 4 keys: creative-history, failed-shots, finetune-dataset, review-outcomes
    - `finetune-dataset` slot has `format: "jsonl"`; others have `format: "json"` (or absent)
    - `_compute_content_hash({"a":1})` returns 64-char lowercase hex (SHA-256)
    - `_compute_content_hash({"a":1}) == _compute_content_hash({"a":1})` (deterministic — uses sort_keys=True)
    - `wrap_envelope({"x":1}, derived_from=["h1"])` produces dict with keys: value, derived_from, content_hash (64 hex), schema_version="3.0"
    - `wrap_envelope(v)` defaults derived_from to []
    - `unwrap_envelope(envelope_dict)` returns envelope_dict["value"]
    - `unwrap_envelope({"legacy": "data"})` returns input unchanged (v2.0 backward compat)
    - `unwrap_envelope([1,2,3])` returns input unchanged (arrays are not envelopes)
    - `AssetBus(workdir).write("failed-shots", {"failures":[], "version":1})` writes `.pipeline-assets/failed-shots.json`
    - After write, no `.tmp.*` file in `.pipeline-assets/`
    - `bus.write(...)` then `bus.read("failed-shots")` round-trips the payload (unwrapped from envelope)
    - `bus.write("failed-shots", data, envelope=False)` writes raw data (no envelope wrap)
    - `bus.write("failed-shots", data, derived_from=["h1"])` forces envelope (even if envelope=False passed)
    - `bus.write("finetune-dataset", data)` raises AssetBusError ("use append_line instead")
    - `bus.append_line("finetune-dataset", {"sample": 1})` appends one JSONL line
    - `bus.append_line("failed-shots", {})` raises AssetBusError ("not JSONL")
    - `bus.read_lines("finetune-dataset")` returns list of parsed dicts
    - `bus.read_lines("finetune-dataset")` on missing file returns []
    - `bus.read_lines("finetune-dataset")` skips blank lines
    - `bus.write("unknown-slot", data)` raises AssetBusError
    - `bus.read("missing-slot-file")` returns None (no throw)
    - `bus.read("creative-history")` on file with envelope returns unwrapped value
    - `bus.read("creative-history")` on v2.0 raw file returns raw (backward compat)
    - mtime cache: write then read then write then read returns the NEW data (cache invalidated)
    - mtime cache: two reads without intervening write return same object (cache hit)
    - 10 concurrent appends to finetune-dataset produce 10 readable lines (no corruption)
  </behavior>
  <action>
    Create `plugins/pipeline_state/asset_bus.py` with:
    1. Module docstring referencing `kais-movie-agent/lib/asset-bus.js` as port source.
    2. Imports: `from __future__ import annotations`, stdlib only (`hashlib`, `json`, `logging`, `os`, `tempfile`, `time`, `typing`, `pathlib.Path`). NO httpx, NO pydantic, NO aiofiles (D-33-01).
    3. Module constants:
       - `SCHEMA_VERSION = "3.0"` (matches Node.js asset-bus.js:20)
       - `ASSETS_DIR = ".pipeline-assets"`
       - `ASSET_SCHEMA: dict[str, dict]` — 4 slots per CONTEXT CF-05:
         - `"creative-history"`: `{"file": "creative-history.json", "format": "json", "schema": {"shots": "Array<record>", "version": "number"}}`
         - `"failed-shots"`: `{"file": "failed-shots.json", "format": "json", "schema": {"failures": "Array<record>", "version": "number"}}`
         - `"finetune-dataset"`: `{"file": "finetune-dataset.jsonl", "format": "jsonl"}`
         - `"review-outcomes"`: `{"file": "review-outcomes.json", "format": "json"}` (D-33-03: generic JSON; Phase 34 tightens)
    4. Module-level `logger = logging.getLogger(__name__)`.
    5. `class AssetBusError(Exception): pass` — programmer-error exception (unknown slot, format mismatch).
    6. Module-level functions (mirror Node.js exports at asset-bus.js:330):
       - `_compute_content_hash(value: Any) -> str` — `hashlib.sha256(json.dumps(value, sort_keys=True, ensure_ascii=False).encode("utf-8")).hexdigest()`. CRITICAL: `sort_keys=True` per PATTERNS (deterministic; differs from Node.js insertion-order but removes a class of bugs).
       - `wrap_envelope(value: Any, derived_from: list[str] | None = None) -> dict` — mirror asset-bus.js:122-129. Returns `{"value": value, "derived_from": list(derived_from or []), "content_hash": _compute_content_hash(value), "schema_version": SCHEMA_VERSION}`.
       - `unwrap_envelope(raw: Any) -> Any` — mirror asset-bus.js:136-143. Returns `raw["value"]` IFF `isinstance(raw, dict) and not isinstance(raw, list) and raw.get("schema_version") == SCHEMA_VERSION and "value" in raw`, else returns `raw` unchanged.
    7. Module-level `_atomic_write_text(path: Path, data: str) -> None` — `path.parent.mkdir(parents=True, exist_ok=True)` then `tempfile.mkstemp(prefix=f"{path.name}.tmp.", dir=str(path.parent))` then write+close then `os.replace(tmp, str(path))`. Tmp name includes pid + time + urandom hex (mirror Node.js asset-bus.js:162). On exception, attempt to clean up tmp file (best-effort).
    8. Class `AssetBus`:
       - Class attrs: `JSONL_SLOTS = frozenset({"finetune-dataset"})` — used by tools.py dispatch (Plan 33-04) to pick read_lines vs read.
       - `__init__(self, workdir: str | Path)` — `self._dir = Path(workdir) / ASSETS_DIR`, `self._cache: dict[str, Any] = {}`.
       - `_cache_key(self, slot: str) -> str | None` — `path = self._dir / ASSET_SCHEMA[slot]["file"]`, try `st = path.stat()`, return `f"{slot}:{st.st_mtime_ns}"`. On `FileNotFoundError` or `KeyError`, return None.
       - `_invalidate_cache(self, slot: str) -> None` — delete all cache keys with prefix `f"{slot}:"` or exact match `slot`.
       - `write(self, slot: str, data: Any, *, envelope: bool = True, derived_from: list[str] | None = None) -> str`:
         - Raise `AssetBusError(f"Unknown asset: {slot}")` if slot not in ASSET_SCHEMA.
         - Raise `AssetBusError(f"Slot {slot} is JSONL — use append_line() instead")` if `ASSET_SCHEMA[slot].get("format") == "jsonl"`.
         - Compute effective envelope: per Node.js asset-bus.js:210, `derived_list = list(derived_from or [])`, `use_envelope = (len(derived_list) > 0) or envelope`.
         - `payload = wrap_envelope(data, derived_list) if use_envelope else data`.
         - `path = self._dir / ASSET_SCHEMA[slot]["file"]`, `_atomic_write_text(path, json.dumps(payload, indent=2, ensure_ascii=False))`.
         - Invalidate cache for slot, then prime cache with new mtime key.
         - Return `str(path)`.
       - `read(self, slot: str) -> Any | None`:
         - Raise AssetBusError if unknown slot.
         - Raise AssetBusError if slot is JSONL format ("use read_lines instead").
         - `key = self._cache_key(slot)`, if key and key in cache → return `unwrap_envelope(self._cache[key])`.
         - Try read file, `parsed = json.loads(raw)`, prime cache with key+parsed, return `unwrap_envelope(parsed)`.
         - On `FileNotFoundError`/`JSONDecodeError`, return None.
       - `read_envelope(self, slot: str) -> dict | None` — like read but returns raw parsed (no unwrap). Useful for inspecting derived_from/content_hash metadata.
       - `require(self, slot: str) -> Any` — like read but raise `AssetBusError(f"Required asset '{slot}' not found")` on None.
       - `append_line(self, slot: str, line_obj: dict) -> str`:
         - Raise AssetBusError if unknown slot.
         - Raise AssetBusError(f"Slot {slot} is not JSONL") if format != jsonl.
         - `path = self._dir / ASSET_SCHEMA[slot]["file"]`, `path.parent.mkdir(parents=True, exist_ok=True)`.
         - `with open(path, "a", encoding="utf-8") as f: f.write(json.dumps(line_obj, ensure_ascii=False) + "\n")` (mirror Node.js appendFile, NO fsync).
         - Invalidate cache, return str(path).
       - `read_lines(self, slot: str) -> list[dict]`:
         - Raise AssetBusError if unknown slot or not JSONL.
         - Try read file. Split on "\n", filter empty (`.strip()`), `json.loads` each line. Return list.
         - On `FileNotFoundError`, return [].
       - `list_asset_names(self) -> list[str]` — `list(ASSET_SCHEMA.keys())`.
  </action>
  <verify>
    <automated>cd /data/workspace/hermes-agent && python -m pytest plugins/pipeline_state/tests/test_asset_bus.py -v</automated>
  </verify>
  <done>
    - `asset_bus.py` exists with AssetBus class + wrap_envelope/unwrap_envelope/_compute_content_hash + ASSET_SCHEMA (4 slots) + SCHEMA_VERSION
    - `_atomic_write_text` uses os.replace (grep `os\.replace` returns ≥1 match)
    - JSONL append uses `open(..., "a")` (grep returns ≥1 match)
    - No async, no third-party imports
    - All 15-18 tests in test_asset_bus.py pass (Task 2)
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Write pytest unit tests for AssetBus (≥15 tests)</name>
  <files>/data/workspace/hermes-agent/plugins/pipeline_state/tests/test_asset_bus.py</files>
  <behavior>
    See Task 1 <behavior> — every behavior bullet becomes one test.
    Test classes group by concern (mirrors Node.js describe blocks in asset-bus.test.mjs).
  </behavior>
  <action>
    Create `plugins/pipeline_state/tests/test_asset_bus.py` with pytest tests. Use `tmp_path: Path` fixture. Group into classes mirroring Node.js describe blocks (asset-bus.test.mjs):

    - `class TestSchemaConfig`: 4 slots present in ASSET_SCHEMA; finetune-dataset has format jsonl; SCHEMA_VERSION == "3.0"; `list_asset_names()` returns all 4.
    - `class TestContentHash`: 64-char hex, deterministic (same input → same hash), different inputs → different hashes.
    - `class TestEnvelopeHelpers`: wrap produces complete shape (4 keys); derived_from defaults to []; unwrap detects v3 envelope; unwrap passes v2 raw through; unwrap passes arrays through.
    - `class TestAtomicWrite`: no `.tmp.*` residue after write (mirror Node.js asset-bus.test.mjs:197); file exists at expected path.
    - `class TestWriteRead`: write+read round-trip with envelope; envelope=False writes raw; derived_from non-empty forces envelope; write rejects JSONL slot; write rejects unknown slot.
    - `class TestBackwardCompat`: v2.0 raw file (no envelope) reads back as-is — simulate by writing with envelope=False then reading.
    - `class TestMtimeCache`: write then read sees new data; double-read without write hits cache (could test by spying on file reads — keep simple, just verify data consistency); 20ms sleep then write again sees new data (mirror asset-bus.test.mjs:214-233 but use `time.sleep(0.001)` since st_mtime_ns has ns resolution).
    - `class TestJSONL`: append_line writes one line; write rejects jsonl slot; append_line rejects non-jsonl slot; read_lines parses all lines; read_lines empty returns []; read_lines skips blank lines; 100 appends produce 100 ordered lines.
    - `class TestReadMissing`: read on missing file returns None; require raises AssetBusError.

    Each test method named sentence-style: `test_write_then_read_round_trips_with_envelope`, `test_no_tmp_residue_after_write`, etc. Aim for ≥15 tests total (Node.js baseline: asset-bus.test.mjs has 33 — we hit the high-value ones, can defer V2 legacy slot compat tests since V2 slots are out of scope).

    Run from `/data/workspace/hermes-agent` (matches existing Phase 31 test convention).
  </action>
  <verify>
    <automated>cd /data/workspace/hermes-agent && python -m pytest plugins/pipeline_state/tests/test_asset_bus.py -v --tb=short</automated>
  </verify>
  <done>
    - test_asset_bus.py exists with ≥15 tests, all passing
    - Test class grouping mirrors Node.js describe blocks
    - No test uses manual mkdtemp (uses tmp_path fixture)
    - Atomic write verified by `.tmp.*` glob returning empty
    - mtime cache invalidation verified
    - JSONL append + read_lines round-trip verified
  </done>
</task>

</tasks>

<verification>
Plan 33-02 complete when:
- `plugins/pipeline_state/asset_bus.py` exists, imports cleanly, exports AssetBus + wrap_envelope/unwrap_envelope
- `plugins/pipeline_state/tests/test_asset_bus.py` passes (≥15 tests)
- `_atomic_write_text` uses `os.replace`
- `append_line` uses `open(..., "a")` mode
- ASSET_SCHEMA has exactly 4 slots (creative-history, failed-shots, finetune-dataset, review-outcomes)
- No async, no third-party imports
</verification>

<success_criteria>
- SC#2 (ROADMAP): `pipeline_state/asset_bus.py` implements AssetBus V3 — 3 typed slots + envelope + atomic write (JSONL append, no half-writes) — MET
- SCHEMA-01 (3 typed slots): MET (4 slots incl. review-outcomes routing)
- SCHEMA-02 (envelope backward compat): MET
- SCHEMA-03 (atomic write + cache invalidation): MET
- Node.js baseline: asset-bus.test.mjs (33 tests) + asset-bus-derived-from.test.mjs (8 tests) = 41 Node.js tests; Python targets ≥15 high-value cases (deferred V2 slot tests documented in PATTERNS)
</success_criteria>

<output>
Create `.planning/phases/33-pipeline-state-asset-bus/33-02-SUMMARY.md` when done.
</output>
