---
phase: 33-pipeline-state-asset-bus
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - /data/workspace/hermes-agent/plugins/pipeline_state/store.py
  - /data/workspace/hermes-agent/plugins/pipeline_state/tests/test_store.py
autonomous: true
requirements: []   # foundation: implicit support for HERMES-SKILL-02 checkpoint resume

must_haves:
  truths:
    - "PipelineStateStore can save a checkpoint for an episode + phase"
    - "PipelineStateStore can load the most recent checkpoint for an episode"
    - "PipelineStateStore can find the resume phase (first not-done phase)"
    - "Atomic write leaves no .tmp residue"
    - "Corrupt state file does not crash — returns empty state"
  artifacts:
    - path: "/data/workspace/hermes-agent/plugins/pipeline_state/store.py"
      provides: "PipelineState dataclass + PipelineStateStore (save/load/save_checkpoint/load_latest_checkpoint/find_resume_phase)"
      min_lines: 100
    - path: "/data/workspace/hermes-agent/plugins/pipeline_state/tests/test_store.py"
      provides: "pytest unit tests for PipelineStateStore"
      min_lines: 120
  key_links:
    - from: "PipelineStateStore.save_checkpoint"
      to: ".pipeline-state.json (workdir root)"
      via: "_atomic_write_text (tmp + os.replace)"
      pattern: "os\\.replace|_atomic_write_text"
    - from: "PipelineStateStore.find_resume_phase"
      to: "DONE_STATUSES set"
      via: "status lookup per phase_id"
      pattern: "DONE_STATUSES"
---

<objective>
Port `PipelineStateStore` from Node.js `lib/pipeline.js` (state-management
extract) to Python `plugins/pipeline_state/store.py`.

Purpose: SC#1 — checkpoint save/load, episode-level state persistence, resume
from last checkpoint. Foundation for HERMES-SKILL-02 (Phase 35 runner resume).

Output: `store.py` (~150-200 LOC) + `test_store.py` (10-12 tests).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/33-pipeline-state-asset-bus/CONTEXT.md
@.planning/phases/33-pipeline-state-asset-bus/PATTERNS.md

# Node.js reference — state-related code lives inline in Pipeline class
@lib/pipeline.js

# Skeleton (do NOT modify in this plan — Plan 33-04 owns tools.py)
@/data/workspace/hermes-agent/plugins/pipeline_state/__init__.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement PipelineStateStore + PipelineState dataclass</name>
  <files>/data/workspace/hermes-agent/plugins/pipeline_state/store.py</files>
  <behavior>
    - `PipelineState(episode="EP01")` constructs with empty phases dict, current_phase_id=None
    - `PipelineState.DONE_STATUSES == frozenset({"completed", "approved", "awaiting_review"})`
    - `PipelineStateStore(workdir).load()` on missing file → returns `PipelineState(episode="")` (no throw)
    - `PipelineStateStore(workdir).load()` on corrupt JSON → returns `PipelineState(episode="")` (no throw)
    - `PipelineStateStore(workdir).save(state)` writes `.pipeline-state.json` at workdir root
    - After `save()`, no `.tmp.*` file remains in workdir (atomic rename worked)
    - `save_checkpoint(episode_id="EP01", phase="p02", payload={...})` updates `state.phases["p02"]` with status=completed + completed_at + result=payload, sets current_phase_id="p02"
    - `load_latest_checkpoint("EP01")` returns the phases[current_phase_id] dict (or None if no current phase)
    - `load_latest_checkpoint("OTHER")` returns None when episode mismatch
    - `find_resume_phase(["p01", "p02", "p03"])` returns first phase NOT in DONE_STATUSES
    - `find_resume_phase(...)` returns None when all phases are completed/approved/awaiting_review
  </behavior>
  <action>
    Create `plugins/pipeline_state/store.py` with:
    1. Module docstring referencing Node.js source (`lib/pipeline.js:217-249, 611-618`).
    2. Imports: `from __future__ import annotations`, `dataclasses`, `datetime`, `json`, `logging`, `os`, `pathlib`, `tempfile`, `time`, `typing`.
    3. Module-level `logger = logging.getLogger(__name__)`.
    4. `@dataclass` `PipelineState` with fields: `episode: str`, `phases: dict[str, dict] = field(default_factory=dict)`, `current_phase_id: str | None = None`, `started_at: str | None = None`, `completed_at: str | None = None`, `last_resumed_at: str | None = None`, `trace_id: str | None = None`. Add class attribute `DONE_STATUSES = frozenset({"completed", "approved", "awaiting_review"})` — match Node.js pipeline.js:553,612,668 exactly.
    5. Module-level helper `_atomic_write_text(path: Path, data: str) -> None` — uses `tempfile.mkstemp` + `os.fdopen(fd, "w")` + `os.replace(tmp, target)`. Tmp filename format: `f"{path.name}.tmp.{os.getpid()}.{int(time.time()*1000)}.{os.urandom(3).hex()}"` — mirrors Node.js pattern (CONTEXT CF-01).
    6. Class `PipelineStateStore`:
       - `STATE_FILE = ".pipeline-state.json"` class attr (file at workdir ROOT, not under .pipeline-assets/ — CONTEXT CF-06).
       - `__init__(self, workdir: str | Path)` — store `self._workdir = Path(workdir)`, `self._path = self._workdir / self.STATE_FILE`.
       - `load(self) -> PipelineState` — try `json.loads(self._path.read_text(encoding="utf-8"))`, filter to known PipelineState field names (drop unknown keys for forward compat), return `PipelineState(**filtered)`. On `FileNotFoundError` or `json.JSONDecodeError`, return `PipelineState(episode="")`. Match Node.js `_loadState` catch behavior (pipeline.js:217-224).
       - `save(self, state: PipelineState) -> None` — `dataclasses.asdict(state)` + `json.dumps(..., indent=2, ensure_ascii=False)` + `_atomic_write_text(self._path, data)`. D-33-02: deviates from Node.js (which uses raw writeFile) for atomicity hardening.
       - `save_checkpoint(self, episode_id: str, phase: str, payload: dict) -> None` — load state, set `state.episode = episode_id` if empty, set `state.phases[phase] = {"status": "completed", "completed_at": datetime.now(timezone.utc).isoformat(), "result": payload}`, set `state.current_phase_id = phase`, save.
       - `load_latest_checkpoint(self, episode_id: str) -> dict | None` — load state, return None if `state.episode != episode_id` or `state.current_phase_id is None`, else return `state.phases.get(state.current_phase_id)`.
       - `find_resume_phase(self, phase_order: list[str]) -> str | None` — iterate phase_order, return first phase_id where `state.phases.get(phase_id)` is None OR `phase_state.get("status") not in DONE_STATUSES`. Return None if all done. Mirror pipeline.js:611-618.

    Do NOT add migration logic (Node.js V2_MIGRATION_MAP at pipeline.js:122-149 is V2/V4.1 history — out of Phase 33 scope; v5.0 starts fresh state files).
  </action>
  <verify>
    <automated>cd /data/workspace/hermes-agent && python -m pytest plugins/pipeline_state/tests/test_store.py -v</automated>
  </verify>
  <done>
    - `store.py` exists with PipelineState dataclass + PipelineStateStore class + DONE_STATUSES frozenset
    - `_atomic_write_text` helper uses os.replace (atomic)
    - `python -c "from plugins.pipeline_state.store import PipelineStateStore, PipelineState; print('OK')"` exits 0
    - All 10-12 tests in test_store.py pass (Task 2)
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Write pytest unit tests for PipelineStateStore</name>
  <files>/data/workspace/hermes-agent/plugins/pipeline_state/tests/test_store.py</files>
  <behavior>
    - Empty workdir → load() returns PipelineState(episode="")
    - Save then load round-trips all fields
    - save_checkpoint sets phases[phase] + current_phase_id correctly
    - save_checkpoint twice (same episode, different phase) preserves prior phase entry
    - load_latest_checkpoint returns the most recent (current_phase_id's) checkpoint
    - load_latest_checkpoint returns None for episode mismatch
    - load_latest_checkpoint returns None when no checkpoint saved
    - find_resume_phase returns first incomplete phase
    - find_resume_phase returns None when all phases are completed
    - find_resume_phase treats awaiting_review as done (does not re-run)
    - Corrupt JSON file → load() returns empty state (no exception)
    - No .tmp residue after save_checkpoint (atomic write verification)
  </behavior>
  <action>
    Create `plugins/pipeline_state/tests/test_store.py` with pytest tests. Use pytest's `tmp_path` fixture (auto-cleanup — no manual mkdtemp). Group tests in classes mirroring Node.js describe blocks:
    - `class TestPipelineStateStoreLoad`: empty workdir, corrupt JSON, missing file → all return empty state.
    - `class TestPipelineStateStoreSave`: round-trip, no .tmp residue (glob `tmp_path.glob("*.tmp.*")` must be empty).
    - `class TestSaveCheckpoint`: single save, double save preserves prior phase, payload recorded, completed_at is ISO format.
    - `class TestLoadLatestCheckpoint`: returns current phase's checkpoint, returns None for episode mismatch, returns None when no current_phase_id.
    - `class TestFindResumePhase`: returns first pending, returns None when all completed, awaiting_review treated as done.

    Each test uses `tmp_path: Path` fixture and instantiates `PipelineStateStore(tmp_path)`. Import via absolute path `from plugins.pipeline_state.store import PipelineStateStore, PipelineState`.

    Run from `/data/workspace/hermes-agent` so the `plugins.*` namespace resolves (existing Phase 31 tests do the same — see `tests/test_smoke.py`).
  </action>
  <verify>
    <automated>cd /data/workspace/hermes-agent && python -m pytest plugins/pipeline_state/tests/test_store.py -v --tb=short</automated>
  </verify>
  <done>
    - test_store.py exists with ≥10 tests, all passing
    - Test names are sentence-style (test_save_checkpoint_preserves_prior_phase_entry)
    - No test relies on manual tmpdir cleanup (uses tmp_path fixture)
    - Coverage: load (3 cases), save (2), save_checkpoint (3), load_latest_checkpoint (3), find_resume_phase (3) = ~14 tests minimum
  </done>
</task>

</tasks>

<verification>
Plan 33-01 complete when:
- `plugins/pipeline_state/store.py` exists, imports cleanly, exports PipelineState + PipelineStateStore
- `plugins/pipeline_state/tests/test_store.py` passes (≥10 tests)
- `_atomic_write_text` uses `os.replace` (grep `os\.replace` returns ≥1 match)
- No async code (grep `async def` returns 0 in store.py)
- No third-party imports (grep `^import |^from ` excludes stdlib only)
</verification>

<success_criteria>
- SC#1 (ROADMAP): `pipeline_state/store.py` implements PipelineStateStore — checkpoint save/load, episode persistence, resume from last checkpoint — MET
- Node.js baseline coverage: pipeline.js state extract has 4 implicit test paths in `test/phases/*` (load missing, load corrupt, save round-trip, resume detection) — Python has ≥10 tests covering same paths
</success_criteria>

<output>
Create `.planning/phases/33-pipeline-state-asset-bus/33-01-SUMMARY.md` when done.
</output>
