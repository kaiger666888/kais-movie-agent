---
phase: 35-orchestration-skill-skeleton
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - /data/workspace/hermes-agent/skills/kais-movie-pipeline/SKILL.md
  - /data/workspace/hermes-agent/skills/kais-movie-pipeline/pipeline/__init__.py
  - /data/workspace/hermes-agent/skills/kais-movie-pipeline/pipeline/phases/__init__.py
autonomous: true
requirements: [HERMES-SKILL-01]
user_setup: []

must_haves:
  truths:
    - "SKILL.md exists with valid YAML frontmatter parseable by agent.skill_utils.parse_frontmatter"
    - "metadata.hermes.related_skills lists all 15 movie-experts"
    - "SKILL.md body contains the 13-step DAG (Mermaid or ASCII)"
    - "SKILL.md body contains an operator-setup section describing skills.external_dirs config"
    - "pipeline/phases/__init__.py exposes PHASE_REGISTRY (stub for Phase 35; p01-p03 entries added in 35-03)"
  artifacts:
    - path: "/data/workspace/hermes-agent/skills/kais-movie-pipeline/SKILL.md"
      provides: "Skill manifest + 13-step DAG + 15-expert collaboration graph"
      contains: "name: kais-movie-pipeline"
      min_lines: 200
    - path: "/data/workspace/hermes-agent/skills/kais-movie-pipeline/pipeline/__init__.py"
      provides: "Pipeline package marker"
    - path: "/data/workspace/hermes-agent/skills/kais-movie-pipeline/pipeline/phases/__init__.py"
      provides: "Stub PHASE_REGISTRY (empty list — populated by 35-03)"
      contains: "PHASE_REGISTRY"
  key_links:
    - from: "SKILL.md body"
      to: "references/*.md (created by 35-04)"
      via: "## References table"
      pattern: "references/(pipeline-dag|review-gates|asset-bus-schema|expert-mapping)\\.md"
---

<objective>
Create the SKILL.md manifest + directory scaffold for the `kais-movie-pipeline` orchestration skill. This is the keystone artifact that makes the skill discoverable by hermes-agent's skill loader and gives operators a single slash command to invoke the entire 13-step V8.6 short-drama pipeline.

Purpose: Without SKILL.md the skill is invisible to `skills_list`/`skill_view` and cannot be invoked via `/kais-movie-pipeline`. This is SC#1 (HERMES-SKILL-01) plus the discovery half of SC#4.

Output: SKILL.md (~250-350 lines) + `pipeline/__init__.py` package marker + `pipeline/phases/__init__.py` with stub PHASE_REGISTRY. The stub registry is populated by sub-plan 35-03; sub-plan 35-04 fills references/.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/35-orchestration-skill-skeleton/CONTEXT.md
@.planning/phases/35-orchestration-skill-skeleton/PATTERNS.md

# Skill format reference
@/data/workspace/hermes-agent/skills/movie-experts/_shared/SKILL-LAYOUT.md
@/data/workspace/hermes-agent/skills/movie-experts/hook_retention/SKILL.md

# Canonical 13-step mapping (source for the DAG)
@/data/workspace/hermes-agent/skills/movie-experts/_shared/v86-pipeline-mapping.md

# Skill discovery mechanism (how the loader finds SKILL.md)
@/data/workspace/hermes-agent/tools/skills_tool.py

<interfaces>
From agent/skill_utils.py + tools/skills_tool.py:
```python
# Discovery: recursive scan of get_skills_dir() + get_external_skills_dirs() for SKILL.md
# - Filename MUST be uppercase "SKILL.md" (loader skips lowercase)
# - Frontmatter parsed via parse_frontmatter — YAML
# - Mandatory frontmatter fields: name, description, version, metadata.hermes.{tags, related_skills, expert_id, metrics}
# - name MUST match directory name (e.g. kais-movie-pipeline/kais-movie-pipeline... wait, name = "kais-movie-pipeline", dir = kais-movie-pipeline/)
# - description ≤1024 chars; name ≤64 chars
# - For the skill to be discoverable in dev: add /data/workspace/hermes-agent/skills to
#   skills.external_dirs in ~/.hermes/config.yaml (SKILL.md body must document this)
```

From v86-pipeline-mapping.md (canonical 13-step → expert_id):
```
Step 1: hook_retention (atomic)
Step 2: creative_source + screenplay (atomic)
Step 3: screenplay + script_auditor (atomic)
Step 4: character_designer + visual_executor (drawer)
Step 5: cinematographer + style_genome + visual_executor (drawer)
Step 6: screenplay + cinematographer + script_auditor (atomic)
Step 7: visual_executor + prompt_injector + style_genome + colorist (atomic)
Step 7B: audio_pipeline (voicer + composer + foley)
Step 8: cinematographer + editor
Step 9: continuity_auditor
Step 10: (dreamina CLI exec)
Step 11: audio_pipeline (all 6 sub-steps, atomic)
Step 12-13: delivery (Phase 36 will define)

15 active movie-experts (per movie-experts/README.md Bucket 1):
creative_source, style_genome, screenplay, script_auditor, character_designer,
cinematographer, prompt_injector, visual_executor, continuity_auditor, audio_pipeline,
editor, colorist, hook_retention, compliance_gate, theory_critic
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create SKILL.md manifest with valid YAML frontmatter + 13-step DAG + operator-setup section</name>
  <files>/data/workspace/hermes-agent/skills/kais-movie-pipeline/SKILL.md</files>
  <behavior>
    - Frontmatter parses cleanly via yaml.safe_load (no syntax errors)
    - Frontmatter has all mandatory fields: name="kais-movie-pipeline", description (≤1024 chars), version="0.1.0", metadata.hermes.{tags, related_skills (15 experts), expert_id="kais-movie-pipeline", metrics}
    - Body has H1 with bilingual title
    - Body has "## When to use this skill" section
    - Body has "## References" table listing 4 reference files (pipeline-dag.md, review-gates.md, asset-bus-schema.md, expert-mapping.md)
    - Body has "## Pipeline DAG" section with the 13-step Mermaid (or ASCII) graph
    - Body has "## Phase ↔ Expert Mapping" table (13 rows, sourced from v86-pipeline-mapping.md)
    - Body has "## Review Gates" table (8 rows from v86-pipeline-mapping.md §V8.6 8-Gate Review Structure)
    - Body has "## Runner" section describing runner.py invocation + resume semantics
    - Body has "## Operator Setup" section with the skills.external_dirs config snippet
    - Body has "## What NOT to do" anti-patterns
  </behavior>
  <action>
    Create directory `/data/workspace/hermes-agent/skills/kais-movie-pipeline/` then write SKILL.md.

    Use the PATTERNS.md "Adopted Pattern: SKILL.md frontmatter + body" section as the template. Frontmatter MUST follow hook_retention/SKILL.md shape exactly (the loader's parser is the same).

    Key content requirements (per D-35-01, D-35-02):
    - **Frontmatter description**: one-line EN+CN summary mentioning 13-step V8.6 pipeline + 15 movie-experts + checkpoint/resume + 8 HIL gates.
    - **metadata.hermes.related_skills**: list all 15 movie-expert expert_ids (creative_source, style_genome, screenplay, script_auditor, character_designer, cinematographer, prompt_injector, visual_executor, continuity_auditor, audio_pipeline, editor, colorist, hook_retention, compliance_gate, theory_critic) — order: alphabetical or DAG-order, either is fine. This drives cross-skill recommendation in the loader.
    - **metadata.hermes.pipeline** sub-key (extension): `{version: v86, step_count: 13, gate_count: 8, parallel_shots: 4}`. This is a non-standard metadata extension but valid YAML; documents the V8.6 carry-forward.
    - **prerequisites.tools**: list the plugin tool names this skill's Python code dispatches to: delegate_task, skill_view, pipeline_checkpoint_save, pipeline_checkpoint_load, asset_bus_read, asset_bus_write, gate_submit, gate_resolve. These are advisory (loader doesn't gate on them) but document the runtime contract.

    **Pipeline DAG section:** Use a Mermaid graph showing the 13 Steps with their atomic-operation annotations. Source the topology from `_shared/v86-pipeline-mapping.md` §"The 13-Step V8.6 Pipeline → expert_id Mapping". Mark Steps 1-3 as "Phase 35 (vertical slice)" and Steps 4-13 as "Phase 36".

    **Phase ↔ Expert Mapping section:** Reproduce the 13-row table from v86-pipeline-mapping.md verbatim (it's the canonical source). Add a column noting Phase 35 vs Phase 36 scope.

    **Review Gates section:** Reproduce the 8-gate table from v86-pipeline-mapping.md §"V8.6 8-Gate Review Structure" verbatim. Note that gate trigger logic is implemented in runner.py (35-02) and the gate framework itself shipped in Phase 34.

    **Operator Setup section (CRITICAL for SC#4):** Operators must add the hermes-agent skills tree to `skills.external_dirs` to make this skill discoverable without installing to `~/.hermes/skills/`. Provide this exact YAML snippet for `~/.hermes/config.yaml`:

    ```yaml
    skills:
      external_dirs:
        - /data/workspace/hermes-agent/skills
    ```

    Also document the env vars consumed by the underlying plugins (KAIS_GOLD_TEAM_URL, KAIS_REVIEW_URL, KAIS_CANVAS_URL, KAIS_JIMENG_URL + API key/JWT secret — per GPU-DIRECT-05 already documented in Phase 32). Note these are degrade-tolerant (services unreachable → warn + fallback, not crash).

    **What NOT to do section:** Mirror the anti-patterns in PATTERNS.md (don't call skill_view in parent context; don't write business logic in phase modules; don't dynamically import by string).

    Minimum line target: 200 lines (hooks_retention is ~340 lines for comparison; we're skeleton).
  </action>
  <verify>
    <automated>cd /data/workspace/hermes-agent && python -c "
import yaml
with open('skills/kais-movie-pipeline/SKILL.md') as f:
    content = f.read()
# Extract frontmatter between first pair of ---
assert content.startswith('---'), 'Missing frontmatter opener'
parts = content.split('---', 2)
assert len(parts) >= 3, 'Missing frontmatter closer'
fm = yaml.safe_load(parts[1])
assert fm['name'] == 'kais-movie-pipeline', f\"name={fm['name']}\"
assert len(fm['name']) <= 64, 'name too long'
assert len(fm['description']) <= 1024, 'description too long'
assert 'version' in fm, 'missing version'
hm = fm['metadata']['hermes']
assert 'tags' in hm and len(hm['tags']) >= 5, 'tags missing or sparse'
assert len(hm['related_skills']) == 15, f\"related_skills count={len(hm['related_skills'])} (expected 15)\"
assert hm['expert_id'] == 'kais-movie-pipeline'
assert 'metrics' in hm
# Body checks
body = parts[2]
for section in ['## When to use this skill', '## References', '## Pipeline DAG', '## Phase', '## Review Gates', '## Runner', '## Operator Setup', '## What NOT to do']:
    assert section in body, f'missing section: {section}'
assert 'external_dirs' in body, 'missing external_dirs config in operator setup'
assert '/data/workspace/hermes-agent/skills' in body, 'missing external_dirs path'
print('SKILL.md validates OK')
print('related_skills:', hm['related_skills'])
"
    </automated>
  </verify>
  <done>
    - SKILL.md exists at `/data/workspace/hermes-agent/skills/kais-movie-pipeline/SKILL.md`
    - Frontmatter parses cleanly via yaml.safe_load with all mandatory fields
    - metadata.hermes.related_skills has exactly 15 entries
    - Body has all 8 required sections including the operator-setup with external_dirs snippet
    - Validation script exits 0
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Create pipeline package markers + stub PHASE_REGISTRY</name>
  <files>/data/workspace/hermes-agent/skills/kais-movie-pipeline/pipeline/__init__.py, /data/workspace/hermes-agent/skills/kais-movie-pipeline/pipeline/phases/__init__.py</files>
  <behavior>
    - pipeline/__init__.py is a valid Python package marker (empty or with module docstring)
    - pipeline/phases/__init__.py exposes PHASE_REGISTRY as an empty list with a TODO comment for 35-03
    - Both files import cleanly via `python -c "import pipeline.phases; print(pipeline.phases.PHASE_REGISTRY)"`
  </behavior>
  <action>
    Create directory structure:
    ```
    /data/workspace/hermes-agent/skills/kais-movie-pipeline/
    └── pipeline/
        ├── __init__.py       # package marker
        └── phases/
            └── __init__.py   # PHASE_REGISTRY stub
    ```

    `pipeline/__init__.py`: minimal docstring identifying this as the kais-movie-pipeline orchestration package.

    `pipeline/phases/__init__.py`:
    - Module docstring explaining the registry's purpose (maps phase_id → module + depends_on graph)
    - `PHASE_REGISTRY: list[dict] = []` with a comment: `# Populated by sub-plan 35-03 (p01-p03). Phase 36 appends p04-p13.`
    - `__all__ = ["PHASE_REGISTRY"]`
    - DO NOT import p01/p02/p03 yet — they don't exist (35-03 creates them). Empty registry for now.
  </action>
  <verify>
    <automated>cd /data/workspace/hermes-agent/skills/kais-movie-pipeline && python -c "
import sys
sys.path.insert(0, '.')
# Run from skill root so 'pipeline' imports as a top-level package
import pipeline
import pipeline.phases as phases
assert hasattr(phases, 'PHASE_REGISTRY'), 'PHASE_REGISTRY missing'
assert isinstance(phases.PHASE_REGISTRY, list), 'PHASE_REGISTRY not a list'
assert len(phases.PHASE_REGISTRY) == 0, f\"registry should be empty stub, has {len(phases.PHASE_REGISTRY)} entries\"
print('pipeline package + stub registry OK')
"
    </automated>
  </verify>
  <done>
    - `pipeline/__init__.py` and `pipeline/phases/__init__.py` exist
    - `PHASE_REGISTRY` is an empty list (stub for 35-03 to populate)
    - Both files import without error
  </done>
</task>

</tasks>

<success_criteria>
- SC#1 (HERMES-SKILL-01): SKILL.md exists with valid YAML frontmatter, 13-step DAG, 15-expert collaboration graph — MET
- Discovery half of SC#4: SKILL.md has the operator-setup section with external_dirs config snippet (the actual discovery test is in 35-05)
- Directory scaffold in place for 35-02 (runner.py) and 35-03 (phase modules) to fill in
</success_criteria>

<output>
Create `.planning/phases/35-orchestration-skill-skeleton/35-01-SUMMARY.md` when done.
</output>
