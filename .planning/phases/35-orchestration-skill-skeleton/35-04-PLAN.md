---
phase: 35-orchestration-skill-skeleton
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - /data/workspace/hermes-agent/skills/kais-movie-pipeline/references/pipeline-dag.md
  - /data/workspace/hermes-agent/skills/kais-movie-pipeline/references/review-gates.md
  - /data/workspace/hermes-agent/skills/kais-movie-pipeline/references/asset-bus-schema.md
  - /data/workspace/hermes-agent/skills/kais-movie-pipeline/references/expert-mapping.md
autonomous: true
requirements: [HERMES-SKILL-05]
user_setup: []

must_haves:
  truths:
    - "references/pipeline-dag.md exists with the 13-step dependency graph (skeleton form: structure + section headers + 1-2 sentences per section)"
    - "references/review-gates.md exists with the 8-gate per-phase mapping table"
    - "references/asset-bus-schema.md exists documenting slot types + lifecycle"
    - "references/expert-mapping.md exists with 13 phase ↔ movie-expert mapping table"
    - "All 4 docs are skeleton form (not full prose); refined in Phase 36 after port experience"
  artifacts:
    - path: "/data/workspace/hermes-agent/skills/kais-movie-pipeline/references/pipeline-dag.md"
      provides: "13-step V8.6 dependency graph"
      contains: "Step 1"
    - path: "/data/workspace/hermes-agent/skills/kais-movie-pipeline/references/review-gates.md"
      provides: "8-gate per-phase mapping"
      contains: "Gate 1"
    - path: "/data/workspace/hermes-agent/skills/kais-movie-pipeline/references/asset-bus-schema.md"
      provides: "Slot types + lifecycle"
      contains: "topic-kernel"
    - path: "/data/workspace/hermes-agent/skills/kais-movie-pipeline/references/expert-mapping.md"
      provides: "13 phase ↔ movie-expert mapping"
      contains: "hook_retention"
  key_links: []
---

<objective>
Create the 4 reference docs under `references/` that document the pipeline architecture. Per ROADMAP SC#5 these are SKELETON form (structure + section headers + 1-2 sentences per section, not full content); Phase 36 refines them with actual port experience.

Purpose: Without these docs the skill lacks the architectural reference that Phase 36 port engineers need to systematically port p04-p13. Each doc has a specific role:
- **pipeline-dag.md**: answers "what runs after what, and what depends on what"
- **review-gates.md**: answers "which gates fire when, and what reviewer role / mode each uses"
- **asset-bus-schema.md**: answers "what slots exist, what format, what reads/writes each"
- **expert-mapping.md**: answers "which movie-expert is invoked at each step, and what they produce"

Output: 4 markdown files, ~50-100 lines each (skeleton). Total ~250-400 LOC of prose.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/35-orchestration-skill-skeleton/CONTEXT.md
@.planning/phases/35-orchestration-skill-skeleton/PATTERNS.md

# Canonical source for all 4 docs
@/data/workspace/hermes-agent/skills/movie-experts/_shared/v86-pipeline-mapping.md

# Source data for asset-bus-schema.md
@/data/workspace/hermes-agent/plugins/pipeline_state/asset_bus.py

<interfaces>
From v86-pipeline-mapping.md (single source of truth for 13-step DAG, 8 gates, expert mapping):

```
13 Steps (Phase 35 = 1-3, Phase 36 = 4-13):
  Step 1: hook_retention (atomic §1)
  Step 2: creative_source + screenplay (atomic §2)
  Step 3: screenplay + script_auditor (atomic §3)
  Step 4: character_designer + visual_executor
  Step 5: cinematographer + style_genome + visual_executor
  Step 6: screenplay + cinematographer + script_auditor (atomic)
  Step 7: visual_executor + prompt_injector + style_genome + colorist (atomic §4)
  Step 7B: audio_pipeline (voicer + composer + foley)
  Step 8: cinematographer + editor
  Step 9: continuity_auditor
  Step 10: dreamina CLI exec (visual_executor supervises)
  Step 11: audio_pipeline (all 6 sub-steps, atomic §6)
  Step 12-13: delivery (Phase 36 defines)

8 Gates:
  Gate 1: selection-topic-hook (after Step 1)
  Gate 2: story-framework-outline (after Step 2)
  Gate 3: script-audit (after Step 3)
  Gate 4: character-asset-library (after Step 4)
  Gate 5: spatio-script-cinematography (after Step 6)
  Gate 6: visual-seed-styling-audio (after Step 7+7B)
  Gate 7: continuity-check (after Step 9)
  Gate 8: final-composition-audio (after Step 11)
```

From plugins/pipeline_state/asset_bus.py ASSET_SCHEMA (after 35-02 extension):
```
Phase 33 slots (PRESERVED):
  creative-history (json), failed-shots (json), finetune-dataset (jsonl), review-outcomes (json)

Phase 35 added slots (D-35-05):
  requirement (json)         — operator input, read by p01
  topic-kernel (json)        — p01 output, read by p02
  hook-design (json)         — p01 output
  story-framework (json)     — p02 output, read by p03
  script-draft (json)        — p03 output
  audit-report (json)        — p03 output

Phase 36 will add: character-bible, scene-design, shot-list, voice-timeline,
video-clips, audio-stems, master-mp4, etc. (~20 more slots)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Write pipeline-dag.md + review-gates.md + expert-mapping.md (3 docs sourced from v86-pipeline-mapping.md)</name>
  <files>/data/workspace/hermes-agent/skills/kais-movie-pipeline/references/pipeline-dag.md, /data/workspace/hermes-agent/skills/kais-movie-pipeline/references/review-gates.md, /data/workspace/hermes-agent/skills/kais-movie-pipeline/references/expert-mapping.md</files>
  <action>
    Create the `references/` directory under the skill root.

    **pipeline-dag.md** (skeleton, ~80-120 lines):
    - Header with `**Source:**` (cite `_shared/v86-pipeline-mapping.md`), `**Copyright:** Fair Use`, `**Last-verified:** 2026-06-25`
    - `## Summary` — 2-3 sentences explaining this is the 13-step V8.6 pipeline dependency graph
    - `## The 13 Steps` — Mermaid graph + ASCII fallback table reproducing the v86-pipeline-mapping.md 13-step table
    - `## Atomic Operations` — table of the 6 V8.6 §x atomic merges (Step 1 §1, Step 2 §2, Step 3 §3, Step 6 §5, Step 7 §4, Step 11 §6)
    - `## Phase 35 vs Phase 36 Scope` — clearly mark Steps 1-3 as shipped in Phase 35 vertical slice, Steps 4-13 as Phase 36
    - `## Refresh Cadence` — "Re-verified quarterly per `_shared/` convention; drift triggers when kais-movie-agent V-number upgrades"
    - `## See Also` — cross-link to review-gates.md, asset-bus-schema.md, expert-mapping.md

    **review-gates.md** (skeleton, ~60-80 lines):
    - Header (Source/Copyright/Last-verified)
    - `## Summary` — 2-3 sentences
    - `## The 8 Gates` — table reproducing v86-pipeline-mapping.md §"V8.6 8-Gate Review Structure" verbatim (gate #, gate_id, trigger phase, experts, decision content)
    - `## Hard vs Soft Gates` — list which gates are hard (compliance_gate / script_auditor <65% / continuity_auditor 4-dim fail) vs soft (theory_critic advisory)
    - `## Gate Implementation` — pointer to runner.py (35-02) + Phase 34 runner_hooks (pause_for_review, resolve_direct, resume_from_callback, mark_episode_failed)
    - `## Phase 35 Gates` — note that only Gates 1/2/3 are reachable in Phase 35 vertical slice (p01-p03); Gates 4-8 require Phase 36 phases
    - `## See Also`

    **expert-mapping.md** (skeleton, ~60-100 lines):
    - Header
    - `## Summary`
    - `## 13 Phase ↔ 15 Expert Mapping` — table with columns: Phase ID | V8.6 Step | Primary Expert | Collaborating Experts | Phase 35/36 Scope | Reference (link to v86-pipeline-mapping.md row)
      - 13 rows (p01..p13). Phase 35 = p01-p03 rows filled; Phase 36 = p04-p13 rows filled but marked "Phase 36 scope"
    - `## 15 Active Movie-Experts` — bullet list of the 15 expert_ids from movie-experts/README.md Bucket 1, with one-line role descriptions
    - `## Invocation Pattern` — brief note that each phase module invokes experts via `delegate_task` with `skill_view(name=...)` (per PATTERNS.md); point to delegate_tool.py for schema
    - `## See Also`

    All 3 docs are SKELETON — the structure is complete and the content is sourced verbatim from v86-pipeline-mapping.md (the canonical source), but the analytical depth is left for Phase 36 refinement after port experience. Skeleton = structure + 1-2 sentences per section.
  </action>
  <verify>
    <automated>cd /data/workspace/hermes-agent/skills/kais-movie-pipeline/references && for f in pipeline-dag.md review-gates.md expert-mapping.md; do
  echo "=== $f ==="
  [ -f "$f" ] || { echo "MISSING"; exit 1; }
  grep -q "^**Source:**" "$f" || { echo "missing Source header"; exit 1; }
  grep -q "Step 1" "$f" || { echo "missing Step 1 ref"; exit 1; }
done && echo "3 docs OK"</automated>
  </verify>
  <done>
    - 3 docs exist with Source/Copyright/Last-verified headers
    - pipeline-dag.md has 13-step Mermaid + atomic operations table
    - review-gates.md has 8-gate table
    - expert-mapping.md has 13-row phase ↔ expert mapping
    - All skeleton form (not full prose)
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Write asset-bus-schema.md (documents Phase 33 preserved slots + Phase 35 new slots + Phase 36 future slots)</name>
  <files>/data/workspace/hermes-agent/skills/kais-movie-pipeline/references/asset-bus-schema.md</files>
  <action>
    **asset-bus-schema.md** (skeleton, ~80-100 lines):
    - Header (Source: `plugins/pipeline_state/asset_bus.py`; Copyright: Fair Use; Last-verified: 2026-06-25)
    - `## Summary` — 2-3 sentences explaining this is the AssetBus V3 slot schema reference
    - `## Slot Format Types` — table: JSON slots (atomic write, envelope-wrapped, value replaced on write) vs JSONL slots (append-only, line per entry). Reference ASSET_SCHEMA in asset_bus.py.
    - `## Phase 33 Slots (PRESERVED)` — table of the 4 original slots (creative-history / failed-shots / finetune-dataset / review-outcomes) with format + purpose columns. Note these are PRESERVED from Phase 33; Phase 35 does NOT modify them.
    - `## Phase 35 Slots (added per D-35-05)` — table of the 6 new slots added by 35-02:
      | Slot | Format | Phase | Written by | Read by |
      |------|--------|-------|-----------|---------|
      | requirement | json | p01 input | operator (or upstream) | p01 |
      | topic-kernel | json | p01 output | p01 | p02 |
      | hook-design | json | p01 output | p01 | (gate review, canvas sync Phase 37) |
      | story-framework | json | p02 output | p02 | p03 |
      | script-draft | json | p03 output | p03 | p04-p06 downstream (Phase 36) |
      | audit-report | json | p03 output | p03 | (gate review, future training data) |
    - `## Phase 36 Future Slots` — bulleted list of expected slot names (character-bible, scene-design, shot-list, voice-timeline, video-clips, audio-stems, master-mp4, etc.) — flagged as "Phase 36 will define exact names + schemas". Skeleton only.
    - `## Envelope Schema` — short note: every JSON slot write wraps payload in `{value, derived_from, content_hash, schema_version}` envelope via `bus.write(slot, value, envelope=True)`. Read auto-unwraps. Reference `plugins/pipeline_state/asset_bus.py` for full schema.
    - `## Slot Naming Convention` — kebab-case (matches existing slots); semantic names matching V8.6 Node.js asset-bus where applicable for port-traceability.
    - `## See Also`

    Skeleton form — structure complete, content sourced from ASSET_SCHEMA in asset_bus.py.
  </action>
  <verify>
    <automated>cd /data/workspace/hermes-agent/skills/kais-movie-pipeline/references && [ -f asset-bus-schema.md ] && grep -q "topic-kernel" asset-bus-schema.md && grep -q "creative-history" asset-bus-schema.md && grep -q "PRESERVED" asset-bus-schema.md && echo "asset-bus-schema.md OK"</automated>
  </verify>
  <done>
    - asset-bus-schema.md exists
    - Documents both Phase 33 preserved slots and Phase 35 new slots
    - Lists Phase 36 future slots (placeholder names)
    - Skeleton form
  </done>
</task>

</tasks>

<success_criteria>
- SC#5 (HERMES-SKILL-05 skeleton): references/ has 4 docs in skeleton form. MET.
- All 4 docs source from v86-pipeline-mapping.md (single source of truth) and plugins/pipeline_state/asset_bus.py.
- Phase 36 will refine these docs with actual port experience (per ROADMAP Phase 36 SC#4).
</success_criteria>

<output>
Create `.planning/phases/35-orchestration-skill-skeleton/35-04-SUMMARY.md` when done.
</output>
