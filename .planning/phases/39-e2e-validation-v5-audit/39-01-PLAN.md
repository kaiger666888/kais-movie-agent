---
phase: 39-e2e-validation-v5-audit
plan: 39-01
subsystem: e2e-validation
tags: [e2e, degraded-mode, milestone-audit, v5.0-ship, openclaw-removal]
requires: [38]
provides: [e2e-degraded-master-mp4, v5.0-milestone-audit-doc]
affects:
  - hermes-agent/skills/kais-movie-pipeline/tests/test_e2e_degraded.py
  - kais-movie-agent/.planning/milestones/v5.0-MILESTONE-AUDIT.md
tech-stack:
  added: []
  patterns: [mocked-delegate E2E, mocked-clients E2E, milestone-audit trace format]
key-files:
  created:
    - hermes-agent/skills/kais-movie-pipeline/tests/test_e2e_degraded.py
    - kais-movie-agent/.planning/milestones/v5.0-MILESTONE-AUDIT.md
  modified: []
decisions:
  - Reuse Phase 36 _make_full_dag_delegate_spy pattern (D-39-02)
  - master.mp4 placeholder acceptable per D-39-03 (inherits v4.0 Phase 30 contract)
  - Audit format follows v3.0-MILESTONE-AUDIT.md (D-39-05)
metrics:
  duration_min: 15
  completed: "2026-06-26"
  tasks: 4
  files_touched: 2
---

# Phase 39 Plan 01: E2E Validation + v5.0 Audit

Final v5.0 phase. Two deliverables close the milestone: a degraded-mode E2E
test proving the 13-phase Python pipeline produces `master.mp4` with openclaw
OFF + services mocked (SC#1 + SC#2), and a comprehensive v5.0-MILESTONE-AUDIT.md
documenting the full 9-phase migration (SC#3).

## Tasks

### Task 1 — E2E degraded test (SC#1 + SC#2)

**File:** `hermes-agent/skills/kais-movie-pipeline/tests/test_e2e_degraded.py`

**Steps:**

1. Read `test_runner_full_dag.py` (Phase 36-05) to extract `_make_full_dag_delegate_spy` + `_default_per_phase_payloads` patterns. Reuse them — do NOT re-derive.
2. Build `_build_mock_clients()` helper returning a dict of:
   - `delegate_task`: Phase 36 spy (canned JSON per phase)
   - `canvas_client`: `unittest.mock.MagicMock()` with `save_graph.return_value = {"ok": True}`
   - `gold_team_client`, `review_platform_client`, `jimeng_client`: `MagicMock()` instances (the 13 phase modules receive these via the production callable factories — but in `inject` mode, pass them through)
3. Build `_make_canvas_subscriber(canvas_client)` returning a callable matching `RunnerConfig.on_phase_complete` signature `(episode_id, phase_id, result) -> None`. The subscriber calls `canvas_client.save_graph(...)` (mirrors Phase 37 `CanvasSyncSubscriber.__call__`).
4. Write 3-5 test functions:
   - `test_e2e_degraded_full_dag_produces_master_mp4(tmp_path)` — SC#2: run_episode with mocked inject, assert all 13 phases complete + `master.mp4` exists in workdir.
   - `test_e2e_canvas_subscriber_fires_without_openclaw(tmp_path)` — SC#1: same setup, assert `canvas_client.save_graph.call_count >= 13`.
   - `test_e2e_no_real_http_calls_made(tmp_path)` — T-39-01: assert none of the 4 mock clients have real-network call attempts (sanity check on the mock boundary).
   - `test_e2e_gates_suppressed_when_disabled(tmp_path)` — confirm `enable_gates=False` → `trigger_gate` is None → no gate-related side effects.
5. Run `python3 -m pytest skills/kais-movie-pipeline/tests/test_e2e_degraded.py -v` → all PASS.
6. Run full regression to confirm 498 + N new tests pass with 0 regressions.

**Acceptance:**
- All test functions PASS
- `master.mp4` exists in tmp workdir after `run_episode`
- `canvas_client.save_graph.call_count >= 13`
- No real HTTP calls (T-39-01 mitigation)

---

### Task 2 — v5.0-MILESTONE-AUDIT.md (SC#3)

**File:** `kais-movie-agent/.planning/milestones/v5.0-MILESTONE-AUDIT.md`

**Steps:**

1. Read `v3.0-MILESTONE-AUDIT.md` as the format reference (D-39-05).
2. Write §0 Executive Summary — headline numbers: 25/25 REQ-IDs covered, 9/9 phases pass VERIFICATION, ~501+ tests green, 0 BLOCKER gaps, ship recommendation.
3. Write §1 Requirements Coverage — 25-row table mapping each REQ-ID (HERMES-SKILL-01..05, GPU-DIRECT-01..06, GATE-NATIVE-01..05, CANVAS-IN-HERMES-01..04, OPENCLAW-REMOVE-01..05) → traceability → verification claim → code reality → audit verdict.
4. Write §2 Cross-Phase Integration Findings — note any cross-phase gaps (expected: none, since each phase verified independently).
5. Write §3 9-Phase Verification Trace — 9-row table (Phase 31-39) with SC-met? / test count / key evidence columns. Pull evidence from each phase's VERIFICATION.md.
6. Write §4 Test Baseline — pre-Phase-39 (498) vs post-Phase-39 (actual count from Task 1 step 6).
7. Write §5 Ship Recommendation — v5.0 ships internally. Real-GPU validation deferred to operator (PROJECT.md Out of Scope). v6.0+ items enumerated.
8. Run audit-time grep to populate §0 "0 openclaw grep results" claim:
   ```bash
   grep -ri "openclaw\|OpenClaw\|sessions_spawn\|Toonflow" \
     /data/workspace/hermes-agent/skills/kais-movie-pipeline/ \
     /data/workspace/hermes-agent/plugins/kais_aigc/ \
     /data/workspace/hermes-agent/plugins/pipeline_state/ \
     /data/workspace/hermes-agent/plugins/review_gates/ \
     --include="*.py" | grep -v "test_\|__pycache__\|no openclaw\|No openclaw" | wc -l
   ```
   Record the literal command + result (expected: 0) in the audit doc.

**Acceptance:**
- File exists at `.planning/milestones/v5.0-MILESTONE-AUDIT.md`
- All 5 required sections present (grep-verifiable)
- 25-REQ-ID coverage table complete
- 9-phase trace table complete
- 0 openclaw grep result recorded with literal command

---

### Task 3 — Final regression + ship decision summary

**Steps:**

1. Run full v5.0 regression:
   ```bash
   cd /data/workspace/hermes-agent && python3 -m pytest \
     skills/kais-movie-pipeline/tests/ \
     plugins/kais_aigc/tests/ \
     plugins/pipeline_state/tests/ \
     plugins/review_gates/tests/ 2>&1 | tail -3
   ```
2. Record the final test count in §4 of the audit doc (replace placeholder).
3. Confirm §0 ship recommendation is consistent with test results.

**Acceptance:**
- Full regression passes (501+ tests, 0 regressions)
- Audit doc §4 reflects actual final count

---

### Task 4 — SUMMARY

**File:** `.planning/phases/39-e2e-validation-v5-audit/39-01-SUMMARY.md`

Write SUMMARY following the template. Include:
- 4 tasks completed
- All 3 SC met
- Final test count
- Ship decision: v5.0 SHIPS

---

## Verification (sub-plan level)

Same as phase-level verification in PLAN.md `<verification>` block. All 4 checks must pass before committing SUMMARY.

## TDD Gate Compliance

Not applicable — `type: execute`, not `type: tdd`. The E2E test is a verification artifact, not a TDD RED/GREEN cycle.

## Threat Flags

None new. T-39-01 (no real HTTP) mitigated by MagicMock. T-39-02 (no overstatement) mitigated by v3.0 format precedent. T-39-03 (placeholder master.mp4) per D-39-03.

## Self-Check (pre-commit)

- File `hermes-agent/skills/kais-movie-pipeline/tests/test_e2e_degraded.py`: created
- File `kais-movie-agent/.planning/milestones/v5.0-MILESTONE-AUDIT.md`: created
- All E2E test functions PASS
- Full v5.0 regression green (501+)
- Audit doc has 5 required sections
- Phase 38 SC#1 re-affirmed at audit time (0 grep hits)
