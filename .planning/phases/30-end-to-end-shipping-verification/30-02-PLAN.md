---
phase: 30-end-to-end-shipping-verification
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - test/audit-v4-acceptance.test.mjs
autonomous: true
requirements: []
acceptance_scs: [SC2]

must_haves:
  truths:
    - "Each of the 9 audit findings from 2026-06-23 has a discrete, automated assertion that fails if the fix regresses"
    - "All 9 assertions pass at current HEAD"
    - "Test suite baseline grows (no regression)"
  artifacts:
    - path: "test/audit-v4-acceptance.test.mjs"
      provides: "9-finding audit regression suite"
      contains: "9 audit findings"
  key_links:
    - from: "test/audit-v4-acceptance.test.mjs"
      to: "lib/phases/index.js"
      via: "fs.readFile + grep assertions on closing-phase fixes"
      pattern: "fs\\.readFile.*phases/index\\.js"
---

<objective>
Automate the 9-finding audit closure checklist (ROADMAP SC#2) as a single regression test that fails if any of the 9 fixes regresses. Maps 1:1 to the 9-row verification matrix in 30-CONTEXT.md.

Purpose: Future-proof the v4.0 ship — any refactor that silently reintroduces a finding breaks the test. Replaces manual `grep` audit with executable contract.
Output: One new test file under test/.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-end-to-end-shipping-verification/30-CONTEXT.md
@/home/kai/.claude/projects/-data-workspace-kais-movie-agent/memory/project_pipeline-audit_2026-06-23.md

<interfaces>
<!-- 9 findings + closing-phase evidence from STATE.md decisions log -->

Finding → Fix location (from STATE.md Phase 26-29 decision entries):
  F1 composition handler missing → lib/phases/index.js exports composition handler writing master.mp4
  F2 final.mp4 vs master.mp4 filename → lib/phases/index.js delivery handler checks master.mp4 (grep final.mp4 = 0 in delivery section)
  F3 motion-preview task_type → lib/phases/index.js motion-preview handler uses taskType/taskId (grep task_type near motion-preview = 0)
  F4 requirement.json missing in V6 → _loadCharactersForGeneration reads pain-report.json
  F5 scene↔sts stageOrder → lib/pipeline.js PHASES: spatio-temporal-script stageOrder &lt; scene-generation stageOrder &lt; scene-selection stageOrder
  F6 consistency-guard non-blocking → consistency-guard throws Error + writes _consistencyBlocked; lib/gate-constraints.js + lib/invariant-bus.js deleted
  F7 jimeng-client deprecated calls → 3 call sites emit _warnJimengDeprecate warning
  F8 canvasGraph race → lib/canvas-content-sync.js no execSync sqlite3 UPDATE (uses saveGraph HTTP API)
  F9 repair-canvas SQL injection → bin/repair-canvas-truncated-scenes.js assertPositiveInt validation present
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write 9-finding audit regression test</name>
  <files>test/audit-v4-acceptance.test.mjs</files>
  <behavior>
    One test per finding (9 tests total). Each test reads the relevant source file(s) via fs.readFile and asserts the regression-prevention signal is present at HEAD.

    - F1: assert lib/phases/index.js contains a composition handler that writes 'master.mp4' (e.g., handler name 'composition' registered + master.mp4 string in handler body)
    - F2: assert delivery handler body contains 'master.mp4' AND does NOT contain 'final.mp4' as a path reference (count of 'final.mp4' === 0 within the delivery function region)
    - F3: assert motion-preview handler region contains 'taskType' (camelCase) AND does NOT contain a submitTask call with snake_case 'task_type' as the destructure target
    - F4: assert _loadCharactersForGeneration (or its replacement) reads 'pain-report.json' AND does NOT read 'requirement.json' as primary source
    - F5: parse lib/pipeline.js PHASES; assert stageOrder(spatio-temporal-script) &lt; stageOrder(scene-generation) &lt; stageOrder(scene-selection)
    - F6: assert consistency-guard code path contains 'throw' (or rejects) AND assert fs.existsSync('lib/gate-constraints.js') === false AND fs.existsSync('lib/invariant-bus.js') === false
    - F7: assert _warnJimengDeprecate (or equivalent) is exported/defined and called at the 3 known call sites (count occurrences of the warn function ≥ 3, or assert handler bodies contain deprecation gate)
    - F8: assert lib/canvas-content-sync.js does NOT contain 'execSync' with 'sqlite3' AND 'UPDATE' (no direct DB writes); assert it uses HTTP saveGraph API
    - F9: assert bin/repair-canvas-truncated-scenes.js contains 'assertPositiveInt' (or equivalent validator) AND the validation regex matches /^\d+$/ or uses Number.isInteger
  </behavior>
  <action>
    Use node:test + node:assert/strict + fs.readFile + regex assertions. Pattern after existing test/phases/*.test.mjs style.

    For each finding, write a separate test() block with a clear name like 'F1: composition handler writes master.mp4'. This makes regression failures pinpoint the exact finding.

    Implementation notes:
    - For region-scoped assertions (F2, F3), use a helper that slices the file content to the function body using a regex like /function\s+(delivery|motionPreview)\b[\s\S]*?^}/m before counting. Do NOT grep the whole file — that produces false positives from comments/other phases.
    - For F5, parse PHASES as text: extract `stageOrder:\s*(\d+)` near each phase name and compare integers.
    - For F6, two assertions (throw + files-deleted) in one test() block is fine.
    - For F9, assert both the validator exists AND the regex pattern is `^\d+$` or includes Number.isInteger (defense-in-depth per Phase 28 P02 SUMMARY).

    Headroom for future findings: structure the file so adding F10+ is mechanical (one test() block per finding). Add a header comment listing all 9 findings with their closing phase.

    Do NOT make this a "soft" test — every assertion must be strict (assert.strictEqual, assert.ok). The point is regression prevention, not documentation.

    Verify expected pass at HEAD before committing (Phase 26-29 SUMMARYs confirm all 9 fixes are in place).
  </action>
  <verify>
    <automated>node --test test/audit-v4-acceptance.test.mjs</automated>
  </verify>
  <done>
    - All 9 tests pass at current HEAD
    - Test file documents the 9-finding audit matrix in a header comment
    - Full suite npm test passes (508 → 517+ with the 9 new tests)
    - Deliberately reverting any one fix (e.g., temporarily re-adding 'final.mp4' to delivery) makes the corresponding F-test fail — spot-check this manually during implementation
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test → source files (read-only) | Test reads lib/ + bin/ files to verify closure |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-03 | Repudiation | audit closure claims | mitigate | Each finding maps to an executable assertion; no verbal "verified" claims — all evidence is grep/parse-based |
| T-30-04 | Tampering | false-positive regex matches | mitigate | Region-scope slicing for F2/F3 prevents comment/other-phase false positives |
</threat_model>

<verification>
- npm test passes with the 9 new audit tests added (≥517 total)
- All 9 F-tests pass at HEAD
- No regression in existing 508 tests
</verification>

<success_criteria>
- SC#2 (9 audit findings 100% closed at HEAD) is verified by automated test
- Adding a new finding F10+ is mechanical (one test block)
- Every fix has a regression-prevention guard
</success_criteria>

<output>
Create `.planning/phases/30-end-to-end-shipping-verification/30-02-SUMMARY.md` when done
</output>
