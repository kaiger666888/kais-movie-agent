---
phase: 10-phases-handler
plan: 03
type: execute
wave: 2
depends_on:
  - 10-01
  - 10-02
files_modified:
  - test/phases/handlers.test.mjs
autonomous: true
requirements:
  - ARCH-01
must_haves:
  truths:
    - "`node --test test/phases/handlers.test.mjs` 通过且覆盖率达标"
    - "测试 1 验证 phaseHandlers 覆盖 PHASES 全 20 个 id (路由完整性)"
    - "测试 2 验证 5 个代表性新 handler 在临时 workdir 执行后写出对应 outputFiles"
    - "测试 3 验证 V2_MIGRATION_MAP 的每个 value 在 PHASES 中存在"
    - "测试 4 验证降级场景(无 hermes/gold-team)下 handler 不抛 fatal"
  artifacts:
    - path: "test/phases/handlers.test.mjs"
      provides: "Phase 10 单元测试套件(4 个 describe 块)"
      contains: "describe('phaseHandlers 路由完整性'"
  key_links:
    - from: "test/phases/handlers.test.mjs"
      to: "lib/phases/index.js (phaseHandlers export)"
      via: "ES module import"
      pattern: "from '\\.\\./\\./lib/phases/index\\.js'"
---

<objective>
创建 `test/phases/handlers.test.mjs` 单元测试,覆盖 ARCH-01 SC-4(单元测试通过)。测试使用 Node 内置 `node:test` runner(零 npm 依赖),4 个 describe 块:phaseHandlers 路由完整性、stub handler 执行、V2_MIGRATION_MAP 完整性、降级日志。

Purpose: 让 Phase 10 改动有回归保护 — 后续 Phase 11/12/14/15 修改 handler 时,若误删或破坏 ARCH-01 对齐,测试立即失败。
Output: 一个测试文件,运行 `node --test test/phases/handlers.test.mjs` 通过。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/10-phases-handler/10-CONTEXT.md
@lib/phases/index.js
@lib/pipeline.js
@.planning/phases/10-phases-handler/10-01-PLAN.md
@.planning/phases/10-phases-handler/10-02-PLAN.md

<interfaces>
<!-- 测试模板契约 — 沿用项目现有 node:test 模式 -->

## 现有测试模式参考(test/v41-integration.test.js)
```javascript
import { describe, it, before } from 'node:test';
import assert from 'node:assert';
// ESM imports...
describe('suite name', () => {
  it('case name', async () => {
    assert.strictEqual(actual, expected);
  });
});
```

## 运行方式
```bash
# 单文件
node --test test/phases/handlers.test.mjs
# 全部测试
node --test test/**/*.test.mjs
```

## Pipeline / PHASES 导出契约
- `Pipeline` 类与 `createRequirementTemplate` 从 `lib/pipeline.js` named export
- `phaseHandlers` 从 `lib/phases/index.js` named export
- PHASES 数组**未 export**(本测试需要在文件内 hardcode PHASES id 列表,见下)

## PHASES id 全集(hardcode 在测试中)
```javascript
const PHASE_IDS = [
  'pain-discovery', 'topic-selection', 'outline-generation', 'outline-selection',
  'script-generation', 'script-selection', 'character-generation', 'character-selection',
  'scene-generation', 'scene-selection', 'spatio-temporal-script', 'script-lock',
  'seed-skeleton', 'motion-preview', 'ai-preview', 'consistency-guard',
  'cloud-production', 'final-audio', 'composition', 'delivery',
];
```

## outputFiles 映射(测试 2 断言用)
```javascript
const OUTPUT_FILES = {
  'pain-discovery': ['pain-report.json'],
  'topic-selection': ['selected-topic.json'],
  'outline-generation': ['outline-candidates.json'],
  'outline-selection': ['selected-outline.json'],
  'script-generation': ['script-candidates.json'],
  'script-selection': ['selected-script.json'],
  'character-generation': ['character-candidates.json'],
  'character-selection': ['soul-pack.json'],
  'scene-generation': ['scene-candidates.json'],
  'scene-selection': ['geometry-bed.json'],
  'script-lock': ['script-locked.json'],
  'consistency-guard': ['consistency-pass.json'],
  'cloud-production': ['video_tasks.json'], // 注:PHASES 中是 final-shots/video/,handler 写 video_tasks.json
  'final-audio': ['audio-stems.json'],      // 注:PHASES 中是 final-shots/audio-stems/,handler 写 audio-stems.json
  'delivery': ['quality-report.json'],
};
```

## 5 个代表性 phase(测试 2 用)
`pain-discovery`, `topic-selection`, `consistency-guard`, `cloud-production`, `delivery` — 覆盖上半(创意)/中部(审核)/下部(生产/质检)三个区段,且涵盖调 auditContinuity / generateTopics / assessQuality 三种外部依赖的降级路径。
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 创建 test/phases/ 目录 + 写 4 个 describe 块的完整测试</name>
  <files>test/phases/handlers.test.mjs</files>
  <behavior>
    - Test 1.1: 对每个 PHASE_IDS 中的 id 断言 `typeof phaseHandlers[id] === 'object'` 且 `typeof phaseHandlers[id].after === 'function'`
    - Test 1.2: 断言 `Object.keys(phaseHandlers)` 包含 PHASE_IDS 的每个 id(超集允许 — V4.1 id 额外存在)
    - Test 2.1: 在临时 workdir 下实例化 Pipeline(无 hermes/goldTeam 配置),对 5 个代表性 phase 调用 `pipeline.runPhase(id, {})`,断言不抛异常
    - Test 2.2: 对 5 个 phase 调用后,断言对应 OUTPUT_FILES 中列出的文件存在于 workdir
    - Test 2.3: 断言每个 stub 文件包含 `_stub: true` 标记(读文件 JSON.parse 后检查)
    - Test 3.1: 遍历 V2_MIGRATION_MAP(通过 import lib/pipeline.js 暴露的或重新构造),断言每个 value 在 PHASE_IDS 中
    - Test 3.2: 断言 V2_MIGRATION_MAP 不含 `'soul-voice'` 键
    - Test 4.1: 创建 pipeline(无 hermes/goldTeam/jimeng 配置),跑 pain-discovery,捕获 console.warn 输出,断言包含 `降级` 关键字或 decide 跳过(无 hermes client 时不输出降级,但 handler 仍正常返回)
    - Test 4.2: 断言无外部服务时,handler 返回 `{ summary, metrics }` 且 metrics 含 `stubbed: true`
  </behavior>
  <action>
**前置:创建目录** `test/phases/`(若不存在)。

**写测试文件** `test/phases/handlers.test.mjs`,顶部 import:
```javascript
import { describe, it, before, after, beforeEach } from 'node:test';
import assert from 'node:assert';
import { mkdtemp, rm, readFile, existsSync } from 'node:fs/promises';
import { join } from 'node:path';
import { tmpdir } from 'node:os';
import { Pipeline, createRequirementTemplate } from '../../lib/pipeline.js';
import { phaseHandlers } from '../../lib/phases/index.js';
```

**describe 块结构**:

### describe 1: 'phaseHandlers 路由完整性 (ARCH-01 SC-1)'
- `before` hook:定义 `PHASE_IDS` 数组(20 项,见 interfaces)
- `it('每个 PHASES.id 在 phaseHandlers 中存在')`:循环断言 `phaseHandlers[id]` 存在且 `typeof .after === 'function'`
- `it('phaseHandlers 是 PHASES 的超集(允许 V4.1 id 共存)')`:断言 `Object.keys(phaseHandlers).length >= 20`
- `it('全部 15 个新 V6 id 都存在')`:对 15 个新 id 单独断言(便于失败时定位)

### describe 2: 'stub handler 执行 (ARCH-01 SC-2)'
- `before` hook:创建临时 workdir(用 mkdtemp),实例化降级 Pipeline
  ```javascript
  let tmpDir, pipeline;
  before(async () => {
    tmpDir = await mkdtemp(join(tmpdir(), 'phase10-test-'));
    pipeline = new Pipeline({
      workdir: tmpDir,
      config: createRequirementTemplate({
        title: '测试项目', genre: '科幻',
        characters: [{ name: '主角', description: '测试角色' }],
      }),
      episode: 'TEST-EP01',
    });
  });
  after(async () => { await rm(tmpDir, { recursive: true, force: true }); });
  ```
- `it('pain-discovery 写出 pain-report.json', async)`:`await pipeline.runPhase('pain-discovery', {});` 然后 `assert.ok(existsSync(join(tmpDir, 'pain-report.json')));`
- `it('topic-selection 写出 selected-topic.json')`:同上
- `it('consistency-guard 写出 consistency-pass.json')`:同上
- `it('cloud-production 写出 video_tasks.json')`:同上
- `it('delivery 写出 quality-report.json')`:同上
- `it('每个 stub 文件含 _stub: true 标记', async)`:读 5 个文件,JSON.parse,断言 `_stub === true`

### describe 3: 'V2_MIGRATION_MAP 完整性 (ARCH-03 SC-3)'
- `before` hook:从 `lib/pipeline.js` 验证加载成功(说明完整性自检通过)
  - 注:V2_MIGRATION_MAP 与 PHASES 都未 named export,测试通过"模块成功加载 + 单元行为测试"间接验证。可选用动态构造:
  ```javascript
  // 通过 _migrateV2State 行为反推 V2_MIGRATION_MAP
  const v2MapProxy = {
    requirement: 'pain-discovery',
    'requirement-bible': 'pain-discovery',
    'art-direction': 'character-generation',
    'soul-visual': 'character-generation',
    character: 'character-generation',
    // 注意:soul-voice 已删除
    scenario: 'spatio-temporal-script',
    voice: 'seed-skeleton',
    storyboard: 'spatio-temporal-script',
    scene: 'scene-generation',
    'geometry-bed': 'scene-selection',
    'camera-preview': 'motion-preview',
    'camera-final': 'ai-preview',
    'post-production': 'final-audio',
    'final-production': 'cloud-production',
    'quality-gate': 'delivery',
    composition: 'composition',
  };
  ```
- `it('V2_MIGRATION_MAP 不含 soul-voice 键')`:通过 `_migrateV2State({phases: {'soul-voice': {status: 'completed'}}, currentPhaseId: 'soul-voice'})` 返回结果中 phases 含 `soul-voice`(未被映射,保留原 key),证明映射表中没有此条目
- `it('每个映射目标在 PHASE_IDS 中')`:遍历 v2MapProxy 每个 value,断言在 PHASE_IDS 中
- `it('lib/pipeline.js 加载无异常(完整性自检通过)')`:已经通过顶部 import 隐式验证,本测试显式 import 并断言 typeof Pipeline === 'function'

### describe 4: '降级日志与容错 (ARCH-01 SC-2)'
- `before` hook:同 describe 2 复用 tmpDir + pipeline(或重新创建,避免污染)
- `it('无 hermes 配置时 handler 不抛 fatal')`:跑 pain-discovery,断言 resolve(不 reject)
- `it('降级日志含 \"降级\" 或 handler 跳过 decide')`:可选 — 用 `const originalWarn = console.warn; const warnings = []; console.warn = (...args) => warnings.push(args.join(' '));` mock,跑 phase,恢复 console.warn,断言 warnings 数组中有包含 `降级` 或 `失败` 关键字的条目(若 hermes 完全未配置,可能无 warn — 此时断言 handler 仍返回 `{ summary, metrics }`)
- `it('返回 metrics 含 stubbed: true')`:断言 `result.metrics.stubbed === true`

**严禁**:
- 严禁引入任何 npm 依赖(node:test + node:assert + node:fs/promises + node:path + node:os 即可)
- 严禁在测试中调用真实外部服务(Hermes / gold-team / jimeng)— config 必须不含相关 baseUrl,确保走降级路径
- 严禁不清理临时目录(after hook 必须 rm)
- 严禁 hardcode PHASES 数组时遗漏任何一个 id(必须 20 项)
  </action>
  <verify>
    <automated>mkdir -p test/phases && node --test test/phases/handlers.test.mjs 2>&1 | tail -30</automated>
  </verify>
  <done>
    - `test/phases/handlers.test.mjs` 存在且语法正确
    - 4 个 describe 块全部通过
    - 至少 12 个测试用例(it)通过
    - 临时目录被清理(after hook 执行)
    - 测试运行时间 < 30s
  </done>
</task>

<task type="auto">
  <name>Task 2: 跑全套测试验证回归 + 补 package.json test 脚本</name>
  <files>test/phases/handlers.test.mjs</files>
  <action>
**步骤 1 — 跑 Phase 10 新测试**:
`node --test test/phases/handlers.test.mjs` — 必须全部通过。

**步骤 2 — 跑全套回归测试**:
`node --test test/**/*.test.mjs` 或分别运行:
- `node --test test/v41-integration.test.js`
- `node --test test/phase4a-gpu-integration.test.js`
- `node --test test/phases/handlers.test.mjs`
- `node --test test/e2e-gold-team.test.js` (若 GOLD_TEAM_URL 未配置,会 skip 或失败 — 记录但不阻塞)

**步骤 3 — 验证 v1.0 9 phases 回归**:
v1.0 的 9 个 legacy phase id(requirement-bible, soul-visual, soul-voice, geometry-bed, spatio-temporal-script, seed-skeleton, motion-preview, ai-preview, final-production)对应 handler 仍存在且可执行。在 `test/phases/handlers.test.mjs` 的 describe 1 中,断言 `phaseHandlers['requirement-bible']` 等 10 个 V4.1 id 也存在(已在 Task 1 测试 1.2 中覆盖 — Object.keys 超集断言)。

**步骤 4 — 验证无 npm 依赖**:
`grep -E "^import.*from 'npm:" test/phases/handlers.test.mjs` 应返回空(零结果)。所有 import 必须是 `node:` 前缀或相对路径。

**注**: 不修改 `package.json` 添加 test script — 项目惯例是直接用 `node --test`(见 e2e-gold-team.test.js 注释 "Run with: ... node --test")。
  </action>
  <verify>
    <automated>node --test test/phases/handlers.test.mjs 2>&1 | grep -E "^(# tests|# pass|# fail|ok|not ok)" | tail -20 && echo "---" && node --test test/v41-integration.test.js 2>&1 | tail -5</automated>
  </verify>
  <done>
    - Phase 10 新测试 100% 通过(0 fail)
    - v1.0 测试无新增 failure(若原本有 skip/fail,记录但不阻塞)
    - test/phases/handlers.test.mjs 无 npm 依赖
    - 测试运行时间 < 30s
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 测试 → 临时文件系统 | mkdtemp 创建隔离环境,测试后 rm 清理 |
| 测试 → pipeline 模块 | 通过公开 API + 实例方法 `_migrateV2State` 调用 |

## STRIDE 威胁登记

| 威胁 ID | 类别 | 组件 | 处置 | 缓解方案 |
|---------|------|------|------|----------|
| T-10-09 | Denial of Service | 临时目录未清理导致磁盘占用 | 缓解 | after hook 强制 rm,即使测试失败也执行(node:test 保证 hook 执行) |
| T-10-10 | Information Disclosure | 测试日志暴露 workdir 路径 | 接受 | mkdtemp 在系统 tmp 目录,无敏感信息 |
| T-10-11 | Repudiation | 测试通过但实际 handler 有 bug | 缓解 | 4 个 describe 覆盖路由 + 执行 + 完整性 + 降级四个维度,非孤立单点 |
</threat_model>

<verification>
1. `test/phases/handlers.test.mjs` 存在,语法正确
2. 4 个 describe 块全部 pass
3. 全套测试回归无新增 fail
4. 零 npm 依赖(grep 验证)
5. 测试 < 30s 完成
6. 临时目录被清理
</verification>

<success_criteria>
- [ ] ARCH-01 SC-4 达成:`node --test test/phases/handlers.test.mjs` 通过
- [ ] 4 个 describe 覆盖:路由完整性、stub 执行、V2_MIGRATION_MAP 完整性、降级容错
- [ ] v1.0 回归:10 个 V4.1 handler id 在测试 1.2 中被超集断言覆盖
- [ ] 零 npm 依赖,纯 Node 内置模块
</success_criteria>

<output>
Create `.planning/phases/10-phases-handler/10-03-SUMMARY.md` when done
</output>
