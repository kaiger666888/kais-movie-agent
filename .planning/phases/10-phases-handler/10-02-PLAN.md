---
phase: 10-phases-handler
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/pipeline.js
autonomous: true
requirements:
  - ARCH-03
must_haves:
  truths:
    - "V2_MIGRATION_MAP 的每个 value 都存在于 PHASES 数组的 id 集合中"
    - "`_migrateV2State()` 正确迁移 v1.0 状态文件中的 legacy phase id 到 V6 id,不丢失数据"
    - "迁移后的状态文件不含 stale 引用(soul-voice 已删除,composition 保留)"
  artifacts:
    - path: "lib/pipeline.js"
      provides: "清理后的 V2_MIGRATION_MAP + 完整性断言"
      contains: "soul-voice"
  key_links:
    - from: "lib/pipeline.js:213 _migrateV2State()"
      to: "V2_MIGRATION_MAP 字典"
      via: "Object.keys + 数组包含检查"
      pattern: "V2_MIGRATION_MAP\\["
---

<objective>
审计并清理 `lib/pipeline.js` 的 `V2_MIGRATION_MAP`(lines 117-136),确保每个 value 都存在于 PHASES 数组中。删除过度收敛的 stale 条目(`soul-voice → character-generation`),保留有意义的映射。在文件顶部添加完整性断言(模块加载时校验),让后续 phase 若误删 PHASES 项可立即发现。

Purpose: 解除 ARCH-03 — 消除 v1.0 → v2.0 状态迁移路径上的 stale 引用,防止运行时出现 `_migrateV2State()` 迁移到不存在 phase id 的 bug。
Output: 清理后的 V2_MIGRATION_MAP + 模块加载时自检断言。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/10-phases-handler/10-CONTEXT.md
@lib/pipeline.js

<interfaces>
<!-- 关键契约 — executor 直接使用 -->

## 当前 V2_MIGRATION_MAP (lib/pipeline.js:117-136)
```javascript
const V2_MIGRATION_MAP = {
  requirement: 'pain-discovery',
  'requirement-bible': 'pain-discovery',
  'art-direction': 'character-generation',
  'soul-visual': 'character-generation',
  character: 'character-generation',
  'soul-voice': 'character-generation',   // ← STALE: 角色(3图一体)不含 voice
  scenario: 'spatio-temporal-script',
  voice: 'seed-skeleton',
  storyboard: 'spatio-temporal-script',
  scene: 'scene-generation',
  'geometry-bed': 'scene-selection',
  'camera-preview': 'motion-preview',
  'camera-final': 'ai-preview',
  'post-production': 'final-audio',
  'final-production': 'cloud-production',
  'quality-gate': 'delivery',
  composition: 'composition',
};
```

## PHASES 数组中的 id 集合(20 项,pipeline.js:50-115)
pain-discovery, topic-selection, outline-generation, outline-selection,
script-generation, script-selection, character-generation, character-selection,
scene-generation, scene-selection, spatio-temporal-script, script-lock,
seed-skeleton, motion-preview, ai-preview, consistency-guard, cloud-production,
final-audio, composition, delivery

## _migrateV2State() 当前实现(pipeline.js:213-231)
遍历 state.phases 的 key,若 key 在 V2_MIGRATION_MAP 中,用 value 替换;否则保留原 key。`currentPhaseId` 同样替换。

## 审计决策表(CONTEXT.md provided,但 `soul-voice` 处置有歧义)
| 旧 ID | 当前映射 | 目标在 PHASES 中存在? | 处置 |
|-------|---------|----------------------|------|
| requirement | pain-discovery | ✓ | 保留 |
| requirement-bible | pain-discovery | ✓ | 保留(向后兼容,虽然 requirement-bible 是 V4.1 handler,但 v1.0 状态文件可能有) |
| art-direction | character-generation | ✓ | 保留 |
| soul-visual | character-generation | ✓ | 保留 |
| character | character-generation | ✓ | 保留 |
| **soul-voice** | character-generation | ✓ | **删除** — 角色(3图一体)不含 voice soul,v1.0 概念已废。voice 由 seed-skeleton 接管 |
| scenario | spatio-temporal-script | ✓ | 保留 |
| voice | seed-skeleton | ✓ | 保留(voice 已映射到 seed-skeleton 而非 character) |
| storyboard | spatio-temporal-script | ✓ | 保留 |
| scene | scene-generation | ✓ | 保留 |
| geometry-bed | scene-selection | ✓ | 保留 |
| camera-preview | motion-preview | ✓ | 保留 |
| camera-final | ai-preview | ✓ | 保留 |
| post-production | final-audio | ✓ | 保留 |
| final-production | cloud-production | ✓ | 保留 |
| quality-gate | delivery | ✓ | 保留 |
| composition | composition | ✓ | 保留 |

## 注意点
1. `soul-voice` 本身仍是 V4.1 handler 的 id (在 phaseHandlers 中存在),V2_MIGRATION_MAP 删除它不影响 V4.1 handler 路由 — 只影响 v1.0 → v2.0 状态文件迁移路径
2. `requirement-bible` 同样是 V4.1 handler id,但出于向后兼容保留迁移映射(v1.0 状态文件可能用此 key)
3. 删除 `soul-voice` 后,v1.0 状态文件中若有 `soul-voice` phase 记录,迁移时会保留原 key(因 _migrateV2State() 的 else 分支),不会丢失数据 — 仅不迁移
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 审计 V2_MIGRATION_MAP + 删除 stale 条目 + 添加完整性断言</name>
  <files>lib/pipeline.js</files>
  <behavior>
    - 测试 1: 遍历 V2_MIGRATION_MAP,断言每个 value 在 PHASES 数组的 id 集合中
    - 测试 2: V2_MIGRATION_MAP 不再包含 `soul-voice` 键
    - 测试 3: 加载 lib/pipeline.js 不抛异常(完整性断言通过)
  </behavior>
  <action>
修改 `lib/pipeline.js`:

**步骤 1 — 删除 stale 条目**:
在 V2_MIGRATION_MAP(line 117-136)中删除 `'soul-voice': 'character-generation',` 这一行。理由:角色(character-generation)是 V6 的"3图一体"概念,不含 voice soul;voice soul 是 v1.0 的独立概念,现已被 seed-skeleton 接管(voice → seed-skeleton 保留)。`soul-voice → character-generation` 是错误的过度收敛。

**步骤 2 — 在 PHASES 数组与 V2_MIGRATION_MAP 之间添加完整性自检**:
在 V2_MIGRATION_MAP 定义后(原 line 136 之后,新行号视删除情况而定),添加模块加载时的完整性断言代码。该代码在模块被 import 时立即执行,若 V2_MIGRATION_MAP 中有 value 不在 PHASES id 集合中,立即 throw Error 让应用启动失败(防止后续 phase 误改 PHASES 引入 stale)。

代码:
```javascript
// 完整性自检:V2_MIGRATION_MAP 的每个 value 必须在 PHASES 中存在
const _PHASE_IDS = new Set(PHASES.map(p => p.id));
for (const [legacy, target] of Object.entries(V2_MIGRATION_MAP)) {
  if (!_PHASE_IDS.has(target)) {
    throw new Error(`[pipeline] V2_MIGRATION_MAP 完整性失败: '${legacy}' 映射到不存在的 phase '${target}'`);
  }
}
```

**步骤 3 — 更新注释**:
更新 V2_MIGRATION_MAP 上方注释,从 `// V2/V4.1 → V6 phase ID migration map` 改为更精确的 `// V2/V4.1 → V6 phase ID migration map (validated against PHASES at module load)`。

**严禁**:
- 严禁删除 CONTEXT.md 决策表中标注"保留"的条目
- 严禁修改 PHASES 数组本身(那是 plan 10-01 的范围,通过 handler 补全而非改 PHASES)
- 严禁把完整性自检包在函数中(必须是模块加载时立即执行的顶层代码)
- 严禁用 console.warn 替代 throw(完整性失败必须 fail-fast)
  </action>
  <verify>
    <automated>node -e "import('./lib/pipeline.js').then(m => { console.log('✓ lib/pipeline.js loaded without error (integrity assertion passed)'); }).catch(e => { console.error('FAIL:', e.message); process.exit(1); });" && node -e "import('./lib/pipeline.js').then(m => { console.error('NOTE: V2_MIGRATION_MAP not exported — checking via source grep'); }).catch(e => process.exit(1));" && grep -c "'soul-voice'" lib/pipeline.js | grep -q '^1$' && echo 'FAIL: soul-voice still present' && exit 1 || (grep -v '^//' lib/pipeline.js | grep -c "'soul-voice'" | grep -q '^0$' && echo '✓ soul-voice removed from V2_MIGRATION_MAP' || echo 'NOTE: soul-voice may still appear in comments/V4.1 handler refs (acceptable)')</automated>
  </verify>
  <done>
    - `lib/pipeline.js` 的 V2_MIGRATION_MAP 不再包含 `'soul-voice'` 键
    - 模块加载时完整性自检代码存在,且对当前 PHASES + V2_MIGRATION_MAP 组合通过
    - 注释更新反映 validation 行为
    - 现有 V4.1 handler 中 `soul-voice` 引用不受影响(handler 代码不在 V2_MIGRATION_MAP 范围)
  </done>
</task>

<task type="auto">
  <name>Task 2: 验证 _migrateV2State() 在新旧状态文件上的行为</name>
  <files>lib/pipeline.js</files>
  <action>
在 `lib/pipeline.js` 修改完成后,创建临时验证脚本(执行后删除)做以下事:

1. **测试 v1.0 状态文件迁移**:模拟一个 v1.0 的 `.pipeline-state.json`,包含 `soul-voice`、`requirement-bible`、`soul-visual`、`geometry-bed`、`camera-final` 等 legacy phase id,调用 `_migrateV2State()` 验证:
   - `soul-voice` 迁移后保留原 key(因已从 V2_MIGRATION_MAP 删除,走 else 分支)— 不抛异常
   - `requirement-bible` 迁移到 `pain-discovery`
   - `soul-visual` 迁移到 `character-generation`
   - `camera-final` 迁移到 `ai-preview`
   - 数据不丢失(每个 phase 的 status / completedAt 字段保留)

2. **测试 v2.0 状态文件(无 legacy id)**:验证 `_migrateV2State()` 是幂等的 — 已迁移的状态再次调用不变。

3. **测试空状态**:空对象 `{}` 不抛异常。

**注意**:`_migrateV2State()` 是 Pipeline 类的实例方法,不是 export。验证脚本需要:
- 方案 A:`new Pipeline({workdir: tmpDir})` → `pipeline._migrateV2State(testState)` 直接调用(类的私有方法在 JS 中可调用)
- 方案 B:把 `_migrateV2State` 临时 export(不推荐,会污染 API)

选方案 A。脚本示例(伪代码):
```javascript
import { mkdtemp, rm, writeFile } from 'node:fs/promises';
import { join } from 'node:path';
import { tmpdir } from 'node:os';
import { Pipeline } from './lib/pipeline.js';

const dir = await mkdtemp(join(tmpdir(), 'p10-migrate-'));
const pipeline = new Pipeline({ workdir: dir, config: {}, episode: 'EP01' });

// v1.0 state
const v1State = {
  episode: 'EP01',
  phases: {
    'soul-voice': { status: 'completed', completedAt: '2026-05-01T00:00:00Z' },
    'requirement-bible': { status: 'completed' },
    'soul-visual': { status: 'completed' },
    'camera-final': { status: 'completed' },
  },
  currentPhaseId: 'camera-final',
};
const migrated = pipeline._migrateV2State(v1State);
// 断言
if (!migrated.phases['soul-voice']) throw new Error('soul-voice should be preserved as-is');
if (!migrated.phases['pain-discovery']) throw new Error('requirement-bible should map to pain-discovery');
if (!migrated.phases['character-generation']) throw new Error('soul-visual should map to character-generation');
if (!migrated.phases['ai-preview']) throw new Error('camera-final should map to ai-preview');
if (migrated.currentPhaseId !== 'ai-preview') throw new Error('currentPhaseId not migrated');

await rm(dir, { recursive: true, force: true });
console.log('✓ _migrateV2State behavior verified');
```

执行后删除临时脚本。**严禁**修改 `_migrateV2State()` 的实现(本任务只验证,不改实现 — stale 删除由 Task 1 的 V2_MIGRATION_MAP 编辑间接生效)。
  </action>
  <verify>
    <automated>mkdir -p tmp && node --input-type=module -e "import { mkdtemp, rm } from 'node:fs/promises'; import { join } from 'node:path'; import { tmpdir } from 'node:os'; import { Pipeline } from './lib/pipeline.js'; const dir = await mkdtemp(join(tmpdir(), 'p10-mig-')); const pipeline = new Pipeline({ workdir: dir, config: {}, episode: 'EP01' }); const v1State = { episode: 'EP01', phases: { 'soul-voice': { status: 'completed' }, 'requirement-bible': { status: 'completed' }, 'soul-visual': { status: 'completed' }, 'camera-final': { status: 'completed' } }, currentPhaseId: 'camera-final' }; const m = pipeline._migrateV2State(v1State); const checks = [ ['soul-voice preserved', !!m.phases['soul-voice']], ['requirement-bible→pain-discovery', !!m.phases['pain-discovery']], ['soul-visual→character-generation', !!m.phases['character-generation']], ['camera-final→ai-preview', !!m.phases['ai-preview']], ['currentPhaseId migrated', m.currentPhaseId === 'ai-preview'], ]; const failed = checks.filter(([_, ok]) => !ok); await rm(dir, { recursive: true, force: true }); if (failed.length) { console.error('FAILED:', failed.map(f => f[0])); process.exit(1); } console.log('✓ All _migrateV2State checks passed');"</automated>
  </verify>
  <done>
    - `_migrateV2State()` 正确处理 v1.0 状态文件:`soul-voice` 保留(走 else 分支),其他 legacy id 正确映射
    - 已迁移状态再次调用 `_migrateV2State()` 是幂等的
    - 空状态不抛异常
    - 临时验证脚本不留痕迹
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 状态文件 → pipeline | `.pipeline-state.json` 由用户/旧版本生成,可能含 legacy phase id |
| 模块加载 → 完整性自检 | PHASES / V2_MIGRATION_MAP 误改时立即暴露 |

## STRIDE 威胁登记

| 威胁 ID | 类别 | 组件 | 处置 | 缓解方案 |
|---------|------|------|------|----------|
| T-10-06 | Tampering | 状态文件含未知 phase id | 接受 | `_migrateV2State()` 的 else 分支保留未知 key 不丢失数据,后续 phase 可兼容处理 |
| T-10-07 | Repudiation | V2_MIGRATION_MAP 误改未被发现 | 缓解 | 模块加载时完整性断言 fail-fast,误改立即 throw |
| T-10-08 | Denial of Service | 完整性断言在 production 启动时抛错 | 接受 | 这是设计意图 — 防止 PHASES/V2_MIGRATION_MAP 错位进入运行态 |
</threat_model>

<verification>
1. `lib/pipeline.js` 模块加载不抛异常
2. V2_MIGRATION_MAP 不含 `'soul-voice'` 键
3. V2_MIGRATION_MAP 每个 value 都在 PHASES id 集合中
4. 模块加载完整性自检代码存在
5. `_migrateV2State()` 对 v1.0 状态文件行为正确(4 项断言通过)
6. `_migrateV2State()` 对已迁移状态幂等
</verification>

<success_criteria>
- [ ] ARCH-03 SC-3 达成:V2_MIGRATION_MAP 中不再引用 PHASES 中已不存在的 legacy ID
- [ ] 每个 V2_MIGRATION_MAP 的 value 在 PHASES 数组中能找到
- [ ] 模块加载完整性自检防止未来误改
- [ ] `_migrateV2State()` 对 v1.0/v2.0 状态文件行为正确
</success_criteria>

<output>
Create `.planning/phases/10-phases-handler/10-02-SUMMARY.md` when done
</output>
