---
phase: 10-phases-handler
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/phases/index.js
autonomous: true
requirements:
  - ARCH-01
must_haves:
  truths:
    - "phaseHandlers 对象的 top-level 键覆盖 PHASES 数组全部 20 个 id"
    - "调用 pipeline 跑到任意新 V6 阶段(pain-discovery/topic-selection/outline-generation/outline-selection/script-generation/script-selection/character-generation/character-selection/scene-generation/scene-selection/script-lock/consistency-guard/cloud-production/final-audio/delivery)命中 handler.after,不再抛 no-handler 或走 silent fallback"
    - "每个新 stub handler 写入对应 PHASES outputFiles 中声明的 JSON 文件(含 _stub: true 标记),下游 phase 不因文件不存在而崩溃"
    - "每个新 stub handler 调用 _hermesDecide/_hermesAudit/EvaluationCollector.record,降级日志正确(服务不可用时不抛 fatal)"
  artifacts:
    - path: "lib/phases/index.js"
      provides: "15 个新 V6 phase handler + 现有 10 个 V4.1 handler 保留"
      contains: "'pain-discovery':"
  key_links:
    - from: "lib/pipeline.js:411 (const handler = phaseHandlers[phaseId])"
      to: "lib/phases/index.js phaseHandlers 对象"
      via: "ES module export + 索引查找"
      pattern: "phaseHandlers\\['pain-discovery'\\]"
---

<objective>
补完 15 个缺失的 V6 phase handler,使 `lib/phases/index.js` 的 `phaseHandlers` 对象与 `lib/pipeline.js` 的 `PHASES` 数组(20 个 id)100% 对齐。每个新 handler 写入对应 outputFiles 中声明的 JSON 文件(stub 数据),调用 Hermes decide/audit 与 EvaluationCollector,遵循现有 V4.1 handler 的三层降级模式。

Purpose: 解除 ARCH-01 P0 架构断裂 — 让 `pipeline.runPhase()` 在任意新阶段上命中 handler 而非走 silent fallback "假完成"。
Output: 15 个新 handler 骨架,每个含 Hermes 决策、stub 数据写入、评估采集、降级日志。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-phases-handler/10-CONTEXT.md
@lib/phases/index.js
@lib/pipeline.js

<interfaces>
<!-- 关键契约 — executor 直接使用,无需重新探索代码 -->

## pipeline 对象可用字段(handler 第一参数)
```javascript
pipeline.workdir        // 工作目录绝对路径
pipeline.config         // createRequirementTemplate() 产出 + config.json override
pipeline.episode        // 'EP01' 等
pipeline.traceId        // UUID v4
pipeline.characterDNA   // Map
pipeline.sceneDNA       // Map
pipeline.mode           // resolveMode() 产出
pipeline.blueprint      // 由 generateBlueprint 写入
```

## phase 对象可用字段(handler 第二参数,来自 PHASES 数组)
```javascript
phase.id               // e.g. 'pain-discovery'
phase.name             // e.g. '痛点调查'
phase.stage            // e.g. 'pain-discovery' (stage 名称,可能与 id 不同)
phase.stageOrder       // 0-19
phase.outputFiles      // ['pain-report.json'] 等
phase.review           // review 配置对象(若存在)
```

## phaseConfig 对象(handler 第三参数,由调用方传入)
```javascript
phaseConfig.data       // 外部传入数据
phaseConfig.execute    // 自定义执行函数(若存在则优先于 handler.after)
phaseConfig.reviewCandidates  // handler 可写入,供 review 平台展示
```

## 现有 helper 函数(已在 lib/phases/index.js 顶部定义,直接调用)
```javascript
_makeHermesClient(pipeline)               // 返回 HermesClient | null
_makeCollector(pipeline)                   // 返回 EvaluationCollector
_hermesDecide(client, phase, context)     // 返回 { params, decisionId }
_hermesAudit(client, phase, decisionId, metrics, parametersUsed)  // fire-and-forget
```

## HERMES_DEFAULTS 字典
已包含全 20 阶段默认参数(lib/phases/index.js:42-167),直接用 HERMES_DEFAULTS['phase-id']。

## 关键依赖模块(handler 内可 import)
- `auditContinuity` from '../continuity-auditor.js' (consistency-guard 用)
- `generateTopics`, `assessQuality`, `deepAudienceAnalysis` from '../hooks/index.js'
- `CompositionEngine` from '../composition-engine.js' (delivery 质检用)
- `callLLM` from '../hermes-adapter.js'

## HermesClient.VALID_PHASES 限制(Phase 11 修复,本 phase 仅做降级)
VALID_PHASES 当前只含 10 个 V4.1 id,新 phase id 调用 hermes.decide() 会抛 `Invalid phase`。本 phase handler 必须 catch 此错误并降级使用 HERMES_DEFAULTS,不抛 fatal。console.warn 日志格式:`[phase-id] hermes decide 降级: Invalid phase 'pain-discovery' (将在 Phase 11 修复)`。

## PHASES 数组对应 outputFiles(必须写入的目标文件)
| Phase ID | outputFiles |
|----------|-------------|
| pain-discovery | pain-report.json |
| topic-selection | selected-topic.json |
| outline-generation | outline-candidates.json |
| outline-selection | selected-outline.json |
| script-generation | script-candidates.json |
| script-selection | selected-script.json |
| character-generation | character-candidates.json, assets/characters/ |
| character-selection | soul-pack.json |
| scene-generation | scene-candidates.json, assets/scenes/ |
| scene-selection | geometry-bed.json |
| script-lock | script-locked.json |
| consistency-guard | consistency-pass.json |
| cloud-production | final-shots/video/ |
| final-audio | final-shots/audio-stems/ |
| delivery | quality-report.json |
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 批量添加 15 个 V6 stub handler 到 phaseHandlers 对象</name>
  <files>lib/phases/index.js</files>
  <behavior>
    - 测试 1: `phaseHandlers['pain-discovery']` 存在且为 object
    - 测试 2: 对 PHASES 数组的每个 id 断言 `typeof phaseHandlers[id] === 'object'`
    - 测试 3: 调用 `phaseHandlers['pain-discovery'].after(pipeline, phase, phaseConfig)` 在临时 workdir 下创建 `pain-report.json`
    - 测试 4: 当 hermes/jimeng/gold-team 未配置(pipeline.config 无相关 baseUrl)时,handler 返回 `{ summary, metrics }` 不抛异常
  </behavior>
  <action>
在 `lib/phases/index.js` 的 `phaseHandlers` 对象内(在现有 `composition` handler 之后,1035 行左右的闭合 `}` 之前)按 PHASES 数组顺序追加 15 个新 handler 条目。每个 handler 使用 `after: async (pipeline, phase, phaseConfig) => { ... }` 签名,内部遵循以下骨架(七步结构,参考现有 `requirement-bible` handler 的 212-277 行与 CONTEXT.md 的 Stub 规范):

1. **取 hermes client 与 defaults**: `const hermes = _makeHermesClient(pipeline); const defaults = HERMES_DEFAULTS['<phase-id>'];`
2. **Hermes decide(降级容错)**:
   ```js
   let hermesDecisionId = null;
   let effectiveParams = defaults;
   if (hermes) {
     try {
       const hr = await _hermesDecide(hermes, '<phase-id>', { /* 上下文 */ });
       hermesDecisionId = hr.decisionId;
       if (hr.params) effectiveParams = { ...defaults, ...hr.params };
     } catch (e) {
       console.warn(`[<phase-id>] hermes decide 降级: ${e.message} (将在 Phase 11 修复)`);
     }
   }
   ```
3. **业务逻辑骨架 / stub 数据写入**(各 phase 不同,见下表)
4. **Hermes audit(fire-and-forget)**: `_hermesAudit(hermes, '<phase-id>', hermesDecisionId, metrics, effectiveParams);`
5. **EvaluationCollector 记录**:
   ```js
   try {
     const collector = _makeCollector(pipeline);
     await collector.record({
       phase: '<phase-id>',
       task_type: '<对应类型>',
       gpu_time_sec: 0,
       peak_vram_gb: 0,
       success: true,
       retry_count: 0,
       hermes_decision_id: hermesDecisionId,
       parameters_used: effectiveParams,
     });
   } catch (e) { console.warn(`[<phase-id>] 评估采集失败: ${e.message}`); }
   ```
6. **返回**: `return { summary: stubData, metrics: { stubbed: true, _pendingRealImplementation: 'phase-X' } };`

**各 phase 的 stub 业务逻辑(Task 3 步骤 3):**

| Phase ID | 业务逻辑骨架 |
|----------|-------------|
| pain-discovery | `import { readFile } from 'node:fs/promises'; let reqData = phaseConfig.data; try { reqData = JSON.parse(await readFile(join(pipeline.workdir, 'requirement.json'), 'utf-8')); } catch {}; const stubData = { _stub: true, _phase: 'pain-discovery', _generatedAt: new Date().toISOString(), _pendingRealImplementation: 'phase-11', requirement: reqData, pain_points: [], ...phaseConfig.data }; await writeFile(join(pipeline.workdir, 'pain-report.json'), JSON.stringify(stubData, null, 2));` |
| topic-selection | `try { const topics = await generateTopics(pipeline.config || {}, { platform: 'douyin', genre: pipeline.config?.genre, blueprint: pipeline.blueprint }); phaseConfig.reviewCandidates = (topics || []).slice(0, 3).map((t, i) => ({ id: `topic-${i+1}`, label: t.title || t.name || `候选 ${i+1}`, description: t.description || '' })); var stubData = { _stub: true, _phase: 'topic-selection', candidates: phaseConfig.reviewCandidates, selected: phaseConfig.reviewCandidates[0] || null }; } catch (e) { console.warn('[topic-selection] generateTopics 降级: ' + e.message); var stubData = { _stub: true, _phase: 'topic-selection', candidates: [], selected: null }; } await writeFile(join(pipeline.workdir, 'selected-topic.json'), JSON.stringify(stubData, null, 2));` |
| outline-generation | 多集展开:读 selected-topic.json → 生成 outline-candidates.json (max 3 candidates);stub: `const stubData = { _stub: true, _phase: 'outline-generation', candidates: Array.from({length: Math.min(3, pipeline.config?.episode_count || 1)}, (_, i) => ({ id: 'outline-' + (i+1), episodes: [] })) }; await writeFile(join(pipeline.workdir, 'outline-candidates.json'), JSON.stringify(stubData, null, 2));` |
| outline-selection | 从 candidates 中选定,写 selected-outline.json。stub: `const stubData = { _stub: true, _phase: 'outline-selection', selected: { id: 'outline-1', episodes: [] } }; await writeFile(join(pipeline.workdir, 'selected-outline.json'), JSON.stringify(stubData, null, 2));` |
| script-generation | 调用 callLLM 生成 script-candidates.json (max 3);stub: `let candidates = []; try { const llm = await callLLM({ prompt: '根据 selected-outline.json 生成剧本候选', system: '你是剧本专家' }); candidates = [{ id: 'script-1', content: llm }]; } catch (e) { console.warn('[script-generation] LLM 降级: ' + e.message); candidates = []; } const stubData = { _stub: true, _phase: 'script-generation', candidates }; await writeFile(join(pipeline.workdir, 'script-candidates.json'), JSON.stringify(stubData, null, 2));` |
| script-selection | 写 selected-script.json: `const stubData = { _stub: true, _phase: 'script-selection', selected: { id: 'script-1', content: '' } }; await writeFile(join(pipeline.workdir, 'selected-script.json'), JSON.stringify(stubData, null, 2));` |
| character-generation | 写 character-candidates.json stub + 确保 assets/characters/ 目录存在。`await mkdir(join(pipeline.workdir, 'assets/characters/'), { recursive: true }); const stubData = { _stub: true, _phase: 'character-generation', _pendingRealImplementation: 'phase-14', candidates: [], l1_anchors: [], l2_costume_cards: [] }; await writeFile(join(pipeline.workdir, 'character-candidates.json'), JSON.stringify(stubData, null, 2));` 注:真实 L1/L2 留给 Phase 14。 |
| character-selection | 写 soul-pack.json stub: `const stubData = { _stub: true, _phase: 'character-selection', _pendingRealImplementation: 'phase-14', selected: null, soul_pack: {} }; await writeFile(join(pipeline.workdir, 'soul-pack.json'), JSON.stringify(stubData, null, 2));` |
| scene-generation | 写 scene-candidates.json stub + assets/scenes/: `await mkdir(join(pipeline.workdir, 'assets/scenes/'), { recursive: true }); const stubData = { _stub: true, _phase: 'scene-generation', candidates: [] }; await writeFile(join(pipeline.workdir, 'scene-candidates.json'), JSON.stringify(stubData, null, 2));` |
| scene-selection | 写 geometry-bed.json stub: `const stubData = { _stub: true, _phase: 'scene-selection', selected: null }; await writeFile(join(pipeline.workdir, 'geometry-bed.json'), JSON.stringify(stubData, null, 2));` |
| script-lock | 读 selected-script.json + 写 script-locked.json: `let scriptData = {}; try { scriptData = JSON.parse(await readFile(join(pipeline.workdir, 'selected-script.json'), 'utf-8')); } catch {} const stubData = { _stub: true, _phase: 'script-lock', script: scriptData, lockedAt: new Date().toISOString(), review_metadata: { approved: true } }; await writeFile(join(pipeline.workdir, 'script-locked.json'), JSON.stringify(stubData, null, 2));` |
| consistency-guard | 调用 `auditContinuity` (来自 `../continuity-auditor.js`),写 consistency-pass.json。**注意:真实打分留给 Phase 12,本 phase handler 只做 stub**。`import { auditContinuity } from '../continuity-auditor.js'; let auditResult = { _stub: true, overall: 1.0, passed: true, findings: [] }; try { const r = await auditContinuity({ visuals: [], characterAssets: [], goldTeamClient: null }); auditResult = { ...auditResult, _auditContinuityCalled: true, scores: r.scores, overall: r.overall, passed: r.passed, findings: r.findings }; } catch (e) { console.warn('[consistency-guard] auditContinuity 降级(Phase 12 将实化): ' + e.message); } const stubData = { _stub: true, _phase: 'consistency-guard', _pendingRealImplementation: 'phase-12', ...auditResult }; await writeFile(join(pipeline.workdir, 'consistency-pass.json'), JSON.stringify(stubData, null, 2));` |
| cloud-production | 写 video_tasks.json stub + 确保 final-shots/video/ 存在。`await mkdir(join(pipeline.workdir, 'final-shots/video/'), { recursive: true }); const stubData = { _stub: true, _phase: 'cloud-production', _pendingRealImplementation: 'phase-15', tasks: [], parallel_shots: effectiveParams.parallel_shots || 4 }; await writeFile(join(pipeline.workdir, 'video_tasks.json'), JSON.stringify(stubData, null, 2));` 注:真实 Seedance omni_reference 留给 Phase 15。 |
| final-audio | 写 audio-stems.json stub + 确保 final-shots/audio-stems/ 存在。`await mkdir(join(pipeline.workdir, 'final-shots/audio-stems/'), { recursive: true }); const stubData = { _stub: true, _phase: 'final-audio', stems: { dialogue: null, bgm: null, sfx: null } }; await writeFile(join(pipeline.workdir, 'audio-stems.json'), JSON.stringify(stubData, null, 2));` |
| delivery | 调用 `assessQuality` + `CompositionEngine.runQualityCheck`,写 quality-report.json stub。`import { CompositionEngine } from '../composition-engine.js'; let qualityReport = { _stub: true, overall_score: 0, dimensions: {}, passed: false }; try { const composer = new CompositionEngine({ workdir: pipeline.workdir, config: pipeline.config, productionMode: pipeline.mode }); const r = await assessQuality(pipeline); qualityReport.summary = r.summary; qualityReport.metrics = r.metrics; } catch (e) { console.warn('[delivery] 质量评估降级: ' + e.message); } const stubData = { _stub: true, _phase: 'delivery', _pendingRealImplementation: 'phase-13', report: qualityReport, deliveredAt: new Date().toISOString() }; await writeFile(join(pipeline.workdir, 'quality-report.json'), JSON.stringify(stubData, null, 2));` |

**必备 imports 检查**: `readFile`, `writeFile`, `mkdir`, `join` 已在文件顶部 import;`generateTopics`, `assessQuality` 已从 `../hooks/index.js` import;`callLLM` 已从 `../hermes-adapter.js` import;需新增 `import { auditContinuity } from '../continuity-auditor.js';` 与 `import { CompositionEngine } from '../composition-engine.js';`(若尚未导入) — CompositionEngine 已在顶部 import(line 25),auditContinuity 需新增。

**严禁**:
- 严禁给 stub 数据写假的具体内容(如编造对白、编造场景描述),保持空数组 / 空对象 + `_stub: true` 标记
- 严禁实现真实业务逻辑(L1/L2、DINOv2、Seedance),真实实现由 Phase 11/12/14/15 完成
- 严禁在 handler 内直接 `import` 同步模块,所有 import 必须在文件顶部
- 严禁省略 EvaluationCollector.record 调用 — 每个新 handler 必须调用
  </action>
  <verify>
    <automated>node -e "import('./lib/phases/index.js').then(m => { const PHASES = ['pain-discovery','topic-selection','outline-generation','outline-selection','script-generation','script-selection','character-generation','character-selection','scene-generation','scene-selection','script-lock','consistency-guard','cloud-production','final-audio','delivery']; const missing = PHASES.filter(id => !m.phaseHandlers[id]); if (missing.length) { console.error('MISSING:', missing); process.exit(1); } console.log('✓ All 15 new V6 handlers present'); const v41 = ['requirement-bible','soul-visual','soul-voice','geometry-bed','spatio-temporal-script','seed-skeleton','motion-preview','ai-preview','final-production','composition']; const v41Missing = v41.filter(id => !m.phaseHandlers[id]); if (v41Missing.length) { console.error('V4.1 MISSING:', v41Missing); process.exit(1); } console.log('✓ All 10 V4.1 handlers preserved'); console.log('Total handlers:', Object.keys(m.phaseHandlers).length); });"</automated>
  </verify>
  <done>
    - `lib/phases/index.js` 的 `phaseHandlers` 对象包含全部 20 个 PHASES id (10 V4.1 + 15 新 V6,但部分 V4.1 id 与 V6 id 不重叠 — 实际是 10 + 15 = 25 条,因 V4.1 id 保留向后兼容)
    - PHASES 数组每项都在 phaseHandlers 中找到对应键
    - 节点命令验证全部 20 个 id 存在,现有 10 个 V4.1 id 保留
    - 每个新 handler 的代码块结构一致:7 步(取 client → decide → 业务 → audit → collect → return)
    - 无假数据填充(stub 字段为空数组/空对象)
  </done>
</task>

<task type="auto">
  <name>Task 2: 验证新 handler 在临时 workdir 下可执行且降级正确</name>
  <files>lib/phases/index.js</files>
  <action>
在 `lib/phases/index.js` 添加完 15 个 handler 后,创建一个临时验证脚本 `scripts/verify-phase10-handlers.mjs`(注意:脚本不写入 files_modified,执行后删除),做以下事:

1. 创建临时目录 `tmp/phase10-verify/`
2. 实例化 `Pipeline({ workdir: tmpDir, config: createRequirementTemplate({ title: 'test', genre: '科幻', characters: [{ name: '主角' }] }), episode: 'EP01' })` — config 不含 hermes/goldTeam/jimeng 的 baseUrl,模拟全部降级场景
3. 对 PHASES 数组的每个 id 调用 `pipeline.runPhase(id, {})` (从 pipeline.js export PHASES 数组或直接复制)
4. 断言每次调用返回 `{ summary, metrics }` 对象且不抛异常
5. 断言对应 outputFiles 存在(用 `existsSync`)
6. 删除临时目录
7. 删除脚本本身

同时**强化 V4.1 handler 的降级**:检查现有 V4.1 handler 中是否有依赖外部服务但未 catch 的代码路径。若发现致命 throw 路径(如 goldTeamClient 未配置但直接 `.ping()` 调用),加 try/catch 包装 + console.warn。本任务只做加固,不重写 V4.1 业务逻辑。

**注意**: 必须先 `import { PHASES } from '../lib/pipeline.js'` 或在脚本内 hardcode PHASES 数组。若 pipeline.js 未 export PHASES,本任务需在 pipeline.js 加一行 `export { PHASES };` (但这会修改 pipeline.js,不在本 plan files_modified 内 — 故脚本内 hardcode PHASES 数组更合适)。

执行后必须删除 `scripts/verify-phase10-handlers.mjs` 脚本文件本身(留作垃圾会污染仓库)。
  </action>
  <verify>
    <automated>mkdir -p tmp/phase10-verify && node --input-type=module -e "import { mkdtemp, rm, writeFile, mkdir } from 'node:fs/promises'; import { join } from 'node:path'; import { tmpdir } from 'node:os'; import { Pipeline, createRequirementTemplate } from './lib/pipeline.js'; import { phaseHandlers } from './lib/phases/index.js'; const PHASES = ['pain-discovery','topic-selection','outline-generation','outline-selection','script-generation','script-selection','character-generation','character-selection','scene-generation','scene-selection','script-lock','consistency-guard','cloud-production','final-audio','delivery']; const dir = await mkdtemp(join(tmpdir(), 'p10-verify-')); const pipeline = new Pipeline({ workdir: dir, config: createRequirementTemplate({ title: 't', genre: '科幻', characters: [{ name: '主角' }] }), episode: 'EP01' }); let failures = []; for (const id of PHASES) { try { await pipeline.runPhase(id, {}); } catch (e) { failures.push({ id, err: e.message }); } } await rm(dir, { recursive: true, force: true }); if (failures.length) { console.error('FAILURES:', JSON.stringify(failures, null, 2)); process.exit(1); } console.log('✓ All 15 new handlers executed without fatal in degraded mode');"</automated>
  </verify>
  <done>
    - 临时脚本对所有 15 个新 phase id 调用 runPhase 成功(无 fatal throw)
    - 降级场景(无 hermes/gold-team/jimeng 配置)下,handler 返回 stub 数据 + 日志输出降级信息
    - 验证脚本不留痕迹(临时目录、脚本本身都清理)
    - 现有 V4.1 handler 中的致命降级路径加固(若发现)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 外部服务 → handler | Hermes / gold-team / jimeng 不可用时进入 handler(降级路径) |
| pipeline.runPhase → handler | phaseConfig.data 由调用方传入,handler 需容错解析 |
| handler → 文件系统 | stub 数据写入 workdir,路径来自 phase.outputFiles |

## STRIDE 威胁登记

| 威胁 ID | 类别 | 组件 | 处置 | 缓解方案 |
|---------|------|------|------|----------|
| T-10-01 | Tampering | phaseConfig.data 透传到 stub | 接受 | 本 phase stub 不执行外部数据,只透传到 JSON 文件;Phase 14+ 实化时再做输入校验 |
| T-10-02 | Denial of Service | Hermes decide 抛 Invalid phase 未 catch | 缓解 | 每个 handler 的 `_hermesDecide` 调用必须 try/catch,降级使用 HERMES_DEFAULTS,console.warn 不抛 fatal |
| T-10-03 | Information Disclosure | 降级日志暴露内部错误细节 | 接受 | console.warn 仅打印 message,不含 stack trace;开发环境可接受 |
| T-10-04 | Tampering | 文件写入路径来自 phase.outputFiles | 接受 | outputFiles 是 PHASES 数组 hardcode 值,不接受外部输入 |
| T-10-05 | Elevation | EvaluationCollector 采集参数被污染 | 接受 | parameters_used 来自 HERMES_DEFAULTS 合并 Hermes 决策,无用户输入路径 |
</threat_model>

<verification>
1. `phaseHandlers` 对象的 key 集合 ⊇ PHASES 数组的 id 集合 (20/20)
2. 节点命令 `Object.keys(phaseHandlers).length >= 20` 成立(实际 25 = 10 V4.1 + 15 新 V6)
3. 临时 workdir 跑通 15 个新 phase,无 fatal
4. 降级场景:无 hermes/gold-team/jimeng 配置时,console 出现 `降级` 关键字 ≥ 15 次
5. 文件 `lib/phases/index.js` 语法正确,`node -e "import('./lib/phases/index.js')"` 不报错
6. 现有 10 个 V4.1 handler 代码未被破坏(diff 显示仅在 composition handler 后追加新代码)
</verification>

<success_criteria>
- [ ] ARCH-01 SC-1 达成:`phaseHandlers` 覆盖 PHASES 数组全部 20 个 id
- [ ] ARCH-01 SC-2 达成:任意新 V6 phase 调用 `runPhase()` 命中 handler 不抛 no-handler 错误
- [ ] 每个 stub handler 写入对应 outputFiles 中声明的 JSON 文件,标记 `_stub: true`
- [ ] 降级链(hermes/gold-team/jimeng/jimeng 任一不可用)不抛 fatal
- [ ] 现有 10 个 V4.1 handler 保留且仍可执行
</success_criteria>

<output>
Create `.planning/phases/10-phases-handler/10-01-SUMMARY.md` when done
</output>
