---
phase: 27-real-render-path-restoration
plan: 02
type: execute
wave: 2
depends_on: [27-01]
files_modified:
  - lib/phases/index.js
  - test/phases/jimeng-deprecate-warn.test.mjs
autonomous: true
requirements: [PIPE-RENDER-02]
user_setup: []

must_haves:
  truths:
    - "Each of the 3 JimengClient call sites (character-generation, soul-visual, scene-generation) emits a one-shot deprecation warn on first instantiation"
    - "When JIMENG_API_KEY is absent, the degrade path is strict — no real Jimeng API call is attempted, placeholder path is taken"
    - "Module-level deprecation flag prevents log flooding across repeated handler invocations"
    - "Existing 474/474 test baseline (plus Phase 27-01 new tests) still passes"
    - "New unit test asserts the deprecate warn fires exactly once per process even when multiple JimengClient instantiations occur"
  artifacts:
    - path: "lib/phases/index.js"
      provides: "Deprecation warn emit at 3 JimengClient call sites with module-level flood guard"
      contains: "jimeng-client fallback-only"
    - path: "test/phases/jimeng-deprecate-warn.test.mjs"
      provides: "Regression test for deprecate warn dedup + strict degrade path"
      contains: "jimeng-client fallback-only"
  key_links:
    - from: "lib/phases/index.js:638, 2171, 2591 (3 JimengClient instantiations)"
      to: "lib/jimeng-client.js:14 (deprecated class)"
      via: "new JimengClient(...) — wraps with module-level once-flag warn"
      pattern: "jimeng-client fallback-only"
    - from: "lib/phases/index.js:2630 (scene-generation degrade gate)"
      to: "placeholder path (no real API call when key/baseUrl absent)"
      via: "jimengAvailable === false → degraded = true"
      pattern: "JimengClient 未就绪"
---

<objective>
Mark the deprecated jimeng-client as fallback-only at all 3 production call sites, with a deduplicated deprecation warn and verification that the degrade path is strict.

Purpose: lib/jimeng-client.js is marked `@deprecated` (replaced by dreamina CLI per the file's JSDoc), but 3 handlers still call `new JimengClient(...)`. Per locked decision D-PIPE-RENDER-02 we do NOT migrate (no dreamina CLI in repo) — instead we make the deprecation visible at each call site so operators know the real-image path is fallback-only, and we verify that the no-API-key path strictly degrades to placeholder (never attempts a real API call). This plan closes PIPE-RENDER-02.

Output: Module-level once-flag warn helper + 3 call-site wrappings + regression test asserting (a) warn fires exactly once per process, (b) no-API-key path produces placeholders, (c) warn message contains the migration hint.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-real-render-path-restoration/27-CONTEXT.md
@.planning/phases/27-real-render-path-restoration/27-01-PLAN.md

<interfaces>
<!-- Existing call sites and the deprecation target. -->

From lib/jimeng-client.js:
```javascript
// line 4
/**
 * @deprecated 已被 dreamina CLI 取代。保留此文件仅做兼容参考。
 */
// line 14
export class JimengClient {
  constructor(baseUrl = "http://localhost:8003") { ... }
}
```

NOTE on constructor signature mismatch (do NOT fix in this plan — out of scope per D-PIPE-RENDER-02 "不重写真实生成逻辑"):
- lib/jimeng-client.js:15 declares `constructor(baseUrl = "http://localhost:8003")` (positional string).
- lib/phases/index.js:638 passes `{ apiKey: pipeline.config?.jimeng?.apiKey || process.env.JIMENG_API_KEY }` (object).
- This means at line 638, `jimeng.baseUrl` becomes the object `{ apiKey: ... }` — the soul-visual handler already has a Jimeng fallback wrapped in try/catch (line 661), so the failure is contained. Per D-PIPE-RENDER-02 we do NOT touch the real-generation path; we only add deprecation warn + verify degrade strictness. Flag this in SUMMARY for operator awareness.

The 3 production call sites in lib/phases/index.js:
```javascript
// line 638 (soul-visual handler) — passes { apiKey } object (signature mismatch, pre-existing)
const jimeng = new JimengClient({ apiKey: pipeline.config?.jimeng?.apiKey || process.env.JIMENG_API_KEY });

// line 2171 (character-generation handler) — passes baseUrl string (matches signature)
const jimeng = new JimengClient(
  pipeline.config?.jimeng?.baseUrl || process.env.JIMENG_BASE_URL || 'http://localhost:8003',
);

// line 2591 (scene-generation handler) — passes baseUrl string + has explicit ping/degrade gate
const jimeng = new JimengClient(
  pipeline.config?.jimeng?.baseUrl || process.env.JIMENG_BASE_URL || 'http://localhost:8003',
);
```

Existing degrade pattern at scene-generation (line 2597-2633):
- 1-second AbortController ping to `${jimeng.baseUrl}/health`.
- On failure: `degraded = true; degradeReason = 'JimengClient 未就绪 — 全部场景使用 placeholder 路径'`.
- Placeholder path is taken when `degraded === true` — no real API call.

Established warn pattern in the file:
- `console.warn(\`[<phase>] <reason>: ${err.message}\`)` — used at lines 652, 661, 690, 706, 974, 990, 1080, 1097, etc.
- Consistent prefix style `[<phase-name>]` for grep-ability.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add module-level deprecation warn helper + wrap 3 JimengClient call sites</name>
  <files>lib/phases/index.js</files>
  <read_first>
    - lib/phases/index.js (lines 1-50 — top of module, to find where to add the module-level flag)
    - lib/phases/index.js (lines 630-665 — soul-visual handler call site + degrade)
    - lib/phases/index.js (lines 2165-2180 — character-generation handler call site)
    - lib/phases/index.js (lines 2585-2640 — scene-generation handler call site + ping/degrade)
    - lib/jimeng-client.js (lines 1-30 — deprecated class definition)
    - .planning/phases/27-real-render-path-restoration/27-CONTEXT.md (locked decision D-PIPE-RENDER-02)
  </read_first>
  <behavior>
    - After fix: a module-level `let _jimengDeprecateWarned = false;` flag exists near the top of lib/phases/index.js.
    - After fix: a helper function `_warnJimengDeprecate()` (or inline equivalent) checks the flag, emits `console.warn('[deprecate] jimeng-client fallback-only — migrate to dreamina CLI when available')` once, then sets the flag true.
    - After fix: all 3 call sites (`new JimengClient(...)` at lines 638, 2171, 2591) call `_warnJimengDeprecate()` immediately before instantiation.
    - After fix: across N invocations of the 3 handlers, the warn message is emitted exactly once total (module-level flag prevents flood).
    - After fix: the existing scene-generation ping/degrade logic (lines 2597-2633) is unchanged — `JimengClient 未就绪` degrade path still works.
    - After fix: the existing character-generation / soul-visual try/catch+degrade structure is unchanged.
    - After fix: NO change to lib/jimeng-client.js (per D-PIPE-RENDER-02 "不在 lib/jimeng-client.js 模块层加 deprecation").
  </behavior>
  <action>
    Per locked decision D-PIPE-RENDER-02 (fallback-only marking with deprecation warn; module-level flag to avoid log flood):

    Step 1 — Add module-level flag + helper near the top of lib/phases/index.js.
    Place it adjacent to other module-level helpers (around line 40-46, after `_extractVoiceEntries` or alongside `_makeCollector`'s definition area — pick the cleanest spot near existing module-level state).

    Add:
    - A module-level flag: `let _jimengDeprecateWarned = false;`
    - A helper:
      ```javascript
      function _warnJimengDeprecate() {
        if (_jimengDeprecateWarned) return;
        _jimengDeprecateWarned = true;
        console.warn('[deprecate] jimeng-client fallback-only — migrate to dreamina CLI when available');
      }
      ```
    - Use the EXACT warn message text above so the regression test can match it.
    - Reason for module-level (not per-handler Set): per D-PIPE-RENDER-02 "用 module-level flag 避免日志洪水" — one warn per process lifetime is sufficient; operators see it once in any pipeline run.

    Step 2 — Wrap each of the 3 call sites.
    Immediately BEFORE each `const jimeng = new JimengClient(...)` line, add a single line: `_warnJimengDeprecate();`.

    - Line 638 (soul-visual): insert `_warnJimengDeprecate();` on the line before `const jimeng = new JimengClient({ apiKey: ... });`
    - Line 2171 (character-generation): insert `_warnJimengDeprecate();` on the line before `const jimeng = new JimengClient(...)`
    - Line 2591 (scene-generation): insert `_warnJimengDeprecate();` on the line before `const jimeng = new JimengClient(...)`

    Step 3 — Verify degrade path strictness (read-only check; no edit unless a hole is found).
    For each of the 3 call sites, confirm there is no code path where `new JimengClient(...)` is followed by an unconditional real-API call (e.g., `jimeng.generateImage(...)` outside a try/catch or outside a `jimengAvailable`/ping gate).

    - soul-visual (line 638): real Jimeng call is at line 658-660 inside `if (candidates.length === 0) { try { ... } catch (e) { console.warn(...) } }` — STRICT. No edit.
    - character-generation (line 2171): real Jimeng call is inside try/catch degrade pattern (search surrounding lines for `catch.*Jimeng` or `catch.*jimeng`) — confirm and document in SUMMARY. No edit unless a hole exists.
    - scene-generation (line 2591): real Jimeng call is gated by `jimengAvailable` flag (set by ping at line 2599-2625), with explicit `if (!jimengAvailable) { degraded = true; ... }` at line 2630 — STRICT. No edit.

    If any of the 3 paths is NOT strict (real API call could fire with no key/baseUrl), STOP and surface the finding in the plan SUMMARY instead of patching it — that would be a scope expansion beyond D-PIPE-RENDER-02. The locked decision is "verify degrade path strictness" (verify, not rewrite).

    DO NOT:
    - Edit lib/jimeng-client.js (out of scope per D-PIPE-RENDER-02).
    - Fix the constructor signature mismatch at line 638 (out of scope — pre-existing, contained by try/catch).
    - Migrate any call to dreamina CLI (no dreamina CLI in repo; CONTEXT.md confirmed).
    - Add warn inside JimengClient constructor (would fire on every import — explicitly rejected in D-PIPE-RENDER-02).

    Make no other edits to the file. Preserve Phase 27-01's motion-preview fix (lines 1074/1078) unchanged.
  </action>
  <verify>
    <automated>
      cd /data/workspace/kais-movie-agent &&
      echo "=== module-level flag exists ===" &&
      grep -n "_jimengDeprecateWarned" lib/phases/index.js &&
      echo "=== helper exists ===" &&
      grep -n "_warnJimengDeprecate" lib/phases/index.js | head -5 &&
      echo "=== 3 call sites wrapped ===" &&
      grep -n "_warnJimengDeprecate()" lib/phases/index.js | wc -l | tr -d ' ' &&
      echo "(expected: 3)" &&
      echo "=== exact warn message present ===" &&
      grep -c "jimeng-client fallback-only — migrate to dreamina CLI when available" lib/phases/index.js &&
      echo "=== jimeng-client.js untouched ===" &&
      git diff --name-only lib/jimeng-client.js | wc -l | tr -d ' ' &&
      echo "(expected: 0)" &&
      echo "=== Phase 27-01 motion-preview fix still intact ===" &&
      awk 'NR>=1070 && NR<=1080' lib/phases/index.js | grep -E "taskType: 'blender_render'" &&
      awk 'NR>=1070 && NR<=1080' lib/phases/index.js | grep -E "task\\.taskId" &&
      echo "=== existing baseline still green ===" &&
      node --test test/phases/handlers.test.mjs test/phases/character-generation.test.mjs 2>&1 | tail -5
    </automated>
  </verify>
  <acceptance_criteria>
    - Module-level `_jimengDeprecateWarned` flag + `_warnJimengDeprecate()` helper defined.
    - All 3 production `new JimengClient(...)` call sites invoke `_warnJimengDeprecate()` immediately before instantiation.
    - Exact warn message `[deprecate] jimeng-client fallback-only — migrate to dreamina CLI when available` present once in the helper.
    - lib/jimeng-client.js is NOT modified.
    - Phase 27-01 motion-preview fix (lines 1074/1078) intact.
    - Existing degrade gates (scene-generation ping gate; soul-visual + character-generation try/catch) unchanged.
    - Degrade path audit complete: any non-strict path documented in SUMMARY (none expected).
  </acceptance_criteria>
  <done>
    Each of the 3 JimengClient call sites warns once (process-wide) that jimeng-client is fallback-only. Operators running a pipeline see exactly one deprecation warn. Degrade path audit confirms no-API-key never triggers a real API call.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Regression test — deprecate warn dedup + strict degrade path</name>
  <files>test/phases/jimeng-deprecate-warn.test.mjs</files>
  <read_first>
    - test/phases/character-generation.test.mjs (existing pattern: Pipeline + handler invocation + console.warn spy)
    - test/phases/scene-sts-order.test.mjs (handler-level test scaffolding)
    - lib/phases/index.js (lines 40-50 — module-level flag area; lines 630-665, 2165-2180, 2585-2640 — call sites)
    - .planning/phases/27-real-render-path-restoration/27-CONTEXT.md (D-PIPE-RENDER-02 test strategy)
  </read_first>
  <behavior>
    - Test 1 (warn emits on first call): Spying on `console.warn`, invoke any one of the 3 handlers (scene-generation is the most self-contained because it has the ping gate). Assert at least one warn call has message containing `jimeng-client fallback-only — migrate to dreamina CLI when available`.
    - Test 2 (warn deduped module-wide): Reset the warn spy call list (NOT the module flag). Invoke the handler 3 times in the same process. Assert the deprecate warn message appears EXACTLY ONCE across all 3 invocations (the module-level flag suppresses repeats).
    - Test 3 (cross-handler dedup): If feasible without excessive setup, invoke 2 different handlers (e.g., scene-generation + soul-visual) and assert the deprecate warn still fires only once total. If cross-handler setup is too heavy, document in test comment that module-level flag is shared across handlers and Test 2's 3x-same-handler invocation is sufficient proof.
    - Test 4 (strict degrade — no API key): Configure a Pipeline with NO `jimeng.apiKey`, NO `jimeng.baseUrl`, NO `JIMENG_API_KEY` env, NO `JIMENG_BASE_URL` env (clear env in `before`). Invoke scene-generation handler. Assert:
      (a) handler completes without throwing,
      (b) no real network call was attempted to jimeng (use the existing ping pattern — fetch to localhost:8003/health will fail since no service is running, OR mock fetch to assert it was/wasn't called),
      (c) the resulting scene candidates are placeholders / degraded flag is true.
    - Test 5 (regression guard — relapse detection): Assert that the deprecate warn message string in source matches `/jimeng-client fallback-only — migrate to dreamina CLI when available/`. If someone removes the warn, this test fails.
  </behavior>
  <action>
    Create test/phases/jimeng-deprecate-warn.test.mjs using the node:test + ESM pattern established in test/phases/character-generation.test.mjs.

    Imports:
    - `import { describe, it, before, after, beforeEach, afterEach } from 'node:test'`
    - `import assert from 'node:assert/strict'`
    - `import { mkdtemp, rm, mkdir, writeFile } from 'node:fs/promises'`
    - `import { join } from 'node:path'`
    - `import { tmpdir } from 'node:os'`
    - `import { Pipeline } from '../../lib/pipeline.js'`
    - `import { phaseHandlers } from '../../lib/phases/index.js'`

    console.warn spy pattern (mirror Phase 26's pattern referenced in CONTEXT.md code_context):
    ```javascript
    let warnCalls = [];
    let originalWarn;
    beforeEach(() => {
      warnCalls = [];
      originalWarn = console.warn;
      console.warn = (msg, ...rest) => { warnCalls.push(String(msg)); };
    });
    afterEach(() => { console.warn = originalWarn; });
    ```

    IMPORTANT — module flag reset limitation:
    The `_jimengDeprecateWarned` module-level flag cannot be reset from the test (it's module-private). Design tests around this:
    - Test 1 (warn emits): runs FIRST in the file (order matters in node:test describe/it unless sequential). Use `describe` ordering or name tests so the emit-first test runs before the dedup test.
    - Alternative cleaner approach: export a `_resetJimengDeprecateFlagForTest()` from lib/phases/index.js (test-only helper). If taking this approach, add the export to lib/phases/index.js (one-line addition — within scope as test infrastructure). Document this in the SUMMARY.
    - Pick whichever approach matches existing test conventions in test/phases/. Check first via grep for `_forTest` or `_resetForTest` patterns — if established, follow it; otherwise use describe-ordering.

    Test setup for scene-generation handler:
    - Create temp workdir.
    - Construct Pipeline with minimal config (NO jimeng.apiKey, NO jimeng.baseUrl).
    - In `before`, delete `process.env.JIMENG_API_KEY` and `process.env.JIMENG_BASE_URL` if present; save originals; restore in `after`.
    - Pre-write AssetBus records scene-generation needs (per surrounding code in lib/phases/index.js:2550-2595 — pain-report / scene definitions; if too heavy, use the fallback `sceneDefs = [{ id: 'scene-default', ... }]` path by providing an empty scene-defs source).
    - Invoke `await phaseHandlers['scene-generation'].after(pipeline, 'scene-generation', phaseConfig)`.

    Assertions:
    - Test 1: `assert.ok(warnCalls.some(m => m.includes('jimeng-client fallback-only — migrate to dreamina CLI when available')))`.
    - Test 2: run handler 3 times in a loop (same process, same module instance). After all 3 runs: `const deprecateWarns = warnCalls.filter(m => m.includes('jimeng-client fallback-only')); assert.equal(deprecateWarns.length, 1);`.
    - Test 4: After running scene-generation with no jimeng config: assert handler did not throw, and assert degraded placeholders were produced. Read the bus `scene-generation` record and assert it has candidates (placeholders) — proving the degrade path was taken, not a real API call. To assert no real network call: stub global `fetch` to throw, confirm handler still completes via degrade (the existing ping try/catch swallows the throw).
    - Test 5: `const src = await readFile('lib/phases/index.js', 'utf-8'); assert.ok(/jimeng-client fallback-only — migrate to dreamina CLI when available/.test(src));`.

    Cleanup: `after(() => rm(workdir, { recursive: true, force: true }))` and restore env vars + fetch.

    Run: `node --test test/phases/jimeng-deprecate-warn.test.mjs`
  </action>
  <verify>
    <automated>
      cd /data/workspace/kais-movie-agent &&
      node --test test/phases/jimeng-deprecate-warn.test.mjs 2>&1 | tail -20 &&
      echo "=== full baseline still green (incl. Phase 27-01 tests) ===" &&
      npm test 2>&1 | tail -10
    </automated>
  </verify>
  <acceptance_criteria>
    - New test file test/phases/jimeng-deprecate-warn.test.mjs exists with 5 passing cases.
    - Test 1 proves the deprecate warn fires.
    - Test 2 proves warn fires EXACTLY ONCE across 3 handler invocations (module-level dedup works).
    - Test 4 proves no-API-key path strictly degrades (no real API call; placeholder produced).
    - Test 5 is a regression guard on the source string.
    - Full `npm test` baseline green (no regression to Phase 27-01 tests or the pre-existing 474).
  </acceptance_criteria>
  <done>
    Regression test prevents silent removal of the deprecation warn and verifies the strict degrade path. Future operators who delete the warn or relax the degrade gate will see this test fail.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| JimengClient call sites → external jimeng HTTP API | Deprecated outbound integration crosses here (fallback-only). |
| Pipeline config + env vars → handler behavior | Trust boundary for "is real API call permitted?" decision. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-27-03 | Denial of Service | jimeng-client fallback path (3 call sites) | mitigate | Deprecation warn makes the fallback status visible; strict degrade gate (scene-generation ping; try/catch elsewhere) ensures no-API-key never causes hangs. |
| T-27-04 | Information Disclosure | Deprecated client logging | accept | Deprecation warn contains no PII/secrets — just a migration hint. Low risk. |
| T-27-05 | Repudiation | Operator unaware of fallback-only status | mitigate | One-shot warn per process ensures visibility without log flood. |
| T-27-SC | Tampering | No package installs in this plan | accept | Pure source + test; no supply-chain surface. |
</threat_model>

<verification>
- Module-level deprecation flag + helper defined.
- All 3 `new JimengClient(...)` call sites wrapped with `_warnJimengDeprecate()`.
- Deprecation warn fires exactly once per process (module-wide dedup).
- Degrade path verified strict at all 3 sites (no real API call without key/baseUrl).
- lib/jimeng-client.js untouched.
- Phase 27-01 motion-preview fix intact.
- New regression test (5 cases) passes.
- Full `npm test` baseline green.
</verification>

<success_criteria>
- PIPE-RENDER-02 closed: deprecated jimeng-client visibly marked fallback-only at all production call sites, with verified strict degrade and deduplicated warn.
- Regression test prevents relapse.
- No real-generation logic rewritten (per D-PIPE-RENDER-02 scope).
</success_criteria>

<output>
Create `.planning/phases/27-real-render-path-restoration/27-02-SUMMARY.md` when done
</output>
