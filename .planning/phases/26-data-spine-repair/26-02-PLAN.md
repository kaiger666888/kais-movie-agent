---
phase: 26-data-spine-repair
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/pipeline.js
  - lib/hermes-client.js
  - test/phases/scene-sts-order.test.mjs
autonomous: true
requirements:
  - PIPE-DATA-02

must_haves:
  truths:
    - "spatio-temporal-script executes BEFORE scene-generation in the default PHASES iteration order"
    - "scene-generation executes BEFORE scene-selection in the default PHASES iteration order"
    - "stageOrder values are monotonically increasing and match array declaration order (no gaps, no duplicates)"
    - "scene-generation handler's bus.read('spatio-temporal-script') returns non-null in a default V6 run (sts has already written)"
    - "scene-generation handler no longer falls through to the single default scene (lines 2573-2580) in a default V6 run"
    - "lib/hermes-client.js VALID_PHASES list reflects the new ordering (kept 1:1 in sync per the file's own comment at line 13)"
    - "Existing 461/461 test baseline is preserved (no regression)"
  artifacts:
    - path: "lib/pipeline.js"
      provides: "Reordered PHASES array — spatio-temporal-script(8) → scene-generation(9) → scene-selection(10)"
      contains: "spatio-temporal-script"
    - path: "lib/hermes-client.js"
      provides: "VALID_PHASES list in sync with PHASES array declaration order"
      contains: "spatio-temporal-script"
    - path: "test/phases/scene-sts-order.test.mjs"
      provides: "Unit + integration coverage for the new scene↔sts ordering"
      contains: "spatio-temporal-script"
  key_links:
    - from: "lib/pipeline.js (PHASES array declaration order)"
      to: "lib/pipeline.js:659 (for loop `for (let i = startIdx; i < PHASES.length; i++)`)"
      via: "array index drives execution order (NOT stageOrder field)"
      pattern: "for \\(let i = startIdx; i < PHASES\\.length"
    - from: "lib/phases/index.js:2564 (scene-generation bus.read)"
      to: "lib/phases/index.js:885 (spatio-temporal-script bus.write)"
      via: "AssetBus filesystem read after upstream write"
      pattern: "bus\\.read\\('spatio-temporal-script'\\)"
---

<objective>
Reorder the PHASES array so spatio-temporal-script runs before scene-generation, restoring the real data dependency.

Purpose: scene-generation (stageOrder 8) calls `bus.read('spatio-temporal-script')` at lib/phases/index.js:2564, but spatio-temporal-script (stageOrder 10) writes the asset at lib/phases/index.js:885. In the default V6 run order, sts has NOT yet executed when scene-generation runs → the read returns null → scene-generation falls through to a single hardcoded default scene (lines 2573-2580). This silently degrades all scene data to a 1-scene placeholder and starves downstream composition/render of real scene structure.

Per user decision (CONTEXT.md PIPE-DATA-02, LOCKED): reorder via stageOrder + array position. New order: spatio-temporal-script (8) → scene-generation (9) → scene-selection (10). No stub sts path (would obscure data flow). No hard fail (would break degraded mode runnability).

Output: Patched PHASES array + synced VALID_PHASES + unit/integration tests proving the new ordering.

CRITICAL IMPLEMENTATION DETAIL: pipeline execution iterates the PHASES array by INDEX (lib/pipeline.js:659 `for (let i = startIdx; i < PHASES.length; i++)`), NOT by stageOrder field. So BOTH must change: array declaration order AND the stageOrder numeric field. The stageOrder field feeds canvas positioning (lib/canvas-sync-hook.js:142-150), reporting (lib/pipeline.js:256, 323, 698), and Hermes audit. Leaving stageOrder stale while reordering the array would create silent drift between execution order and reported order.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-data-spine-repair/26-CONTEXT.md

<interfaces>
<!-- Executor needs these contracts — extracted from codebase, no exploration required. -->

From lib/pipeline.js:50-115 (PHASES array — the structure being reordered):
```javascript
const PHASES = [
  // ... phases 0-7 unchanged (pain-discovery through character-selection) ...
  { id: 'scene-generation', name: '场景生成(6图一体)', stage: 'scene-gen', stageOrder: 8,
    outputFiles: ['scene-candidates.json', 'assets/scenes/'], review: false },          // index 8
  { id: 'scene-selection', name: '场景选择→geometry-bed', stage: 'scene-select', stageOrder: 9,
    outputFiles: ['geometry-bed.json'], review: {...} },                                  // index 9
  { id: 'spatio-temporal-script', name: '时空剧本', stage: 'spatio-temporal', stageOrder: 10,
    outputFiles: ['sts-script.json'], review: {...} },                                    // index 10
  // ... phases 11-19 unchanged (script-lock through delivery) ...
];
```
Target state after reorder:
```javascript
  { id: 'spatio-temporal-script', ..., stageOrder: 8, ... },   // was index 10, now index 8
  { id: 'scene-generation',       ..., stageOrder: 9, ... },   // was index 8,  now index 9
  { id: 'scene-selection',        ..., stageOrder: 10, ... },  // was index 9,  now index 10
```

From lib/pipeline.js:659 (execution loop — confirms array index drives order):
```javascript
for (let i = startIdx; i < PHASES.length; i++) {
  const phase = PHASES[i];
  // ... runs phase handler ...
}
```

From lib/hermes-client.js:13-24 (VALID_PHASES — must stay 1:1 in sync per the file's own comment):
```javascript
// V6 VALID_PHASES — must stay 1:1 in sync with lib/pipeline.js PHASES array.
const VALID_PHASES = [
  'pain-discovery', 'topic-selection', 'outline-generation', 'outline-selection',
  'script-generation', 'script-selection', 'character-generation', 'character-selection',
  'scene-generation', 'scene-selection', 'spatio-temporal-script',   // ← reorder these 3
  'script-lock', 'seed-skeleton', 'motion-preview', 'ai-preview',
  'consistency-guard', 'cloud-production', 'final-audio', 'composition', 'delivery',
];
```

From lib/phases/index.js:2560-2580 (scene-generation sts read — DO NOT MODIFY, only proves the fix):
```javascript
// Line 2564: const sts = await bus.read('spatio-temporal-script');
// Line 2565-2571: if (Array.isArray(sts?.shots)) { sceneDefs = sts.shots.map(...) }
// Line 2573-2580: if (!sceneDefs.length) { sceneDefs = [{ id: 'scene-default', ... }] }  // the silent fallback
```

From lib/phases/index.js:841-889 (spatio-temporal-script writer — DO NOT MODIFY):
```javascript
// Line 882: const bus = new AssetBus(pipeline.workdir);
// Line 885: await bus.write('spatio-temporal-script', { shots, audio_events, duration_coupling });
```

From lib/phases/index.js:2716-2744 (scene-selection — reads scene-candidates.json, NOT scene-generation handler directly; the reorder is safe because scene-selection depends on scene-generation's OUTPUT FILE scene-candidates.json (line 2739), not on handler call order — and scene-generation still runs before scene-selection in both old and new ordering).

Existing test pattern to follow: test/phases/handlers.test.mjs uses `node:test` + `assert`. Pipeline import pattern: `import { Pipeline } from '../../lib/pipeline.js';` (see test/phases/character-generation.test.mjs:27).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Reorder PHASES array (declaration order + stageOrder) and sync VALID_PHASES</name>
  <files>lib/pipeline.js, lib/hermes-client.js</files>
  <read_first>
    - lib/pipeline.js (lines 50-115 — full PHASES array, must see exact current order and stageOrder values 0-19)
    - lib/pipeline.js (lines 117-130 — V2_MIGRATION_MAP, confirms no scene/sts migration entry references stageOrder numerically; only maps legacy IDs to V6 IDs by string)
    - lib/pipeline.js (lines 630-665 — runResume iteration loop, confirms array index drives execution not stageOrder)
    - lib/pipeline.js (lines 690-700 — getStateSummary maps PHASES to {id, name, order: stageOrder}, confirms order field in API output comes from stageOrder — must stay consistent with array index or canvas UI shows drift)
    - lib/hermes-client.js (lines 12-25 — VALID_PHASES array + the file's own "must stay 1:1 in sync" comment)
    - lib/canvas-sync-hook.js (lines 142-150 — computeNodePosition uses stageOrder for canvas X/Y; reordering stageOrder values will reposition 3 nodes on canvas but that is intended, not a regression)
    - .planning/phases/26-data-spine-repair/26-CONTEXT.md (LOCKED decision: reorder via stageOrder; new order sts(8) → scene-gen(9) → scene-select(10); no stub sts; no hard fail; "Claude's Discretion" notes V2_MIGRATION_MAP may need check — confirm by reading lines 117-130 that no entry uses stageOrder numerically)
  </read_first>
  <behavior>
    - PHASES array at lib/pipeline.js:50-115 has the three entries reordered so the declaration sequence is: ...character-selection (index 7, stageOrder 7), spatio-temporal-script (index 8, stageOrder 8), scene-generation (index 9, stageOrder 9), scene-selection (index 10, stageOrder 10), script-lock (index 11, stageOrder 11)...
    - stageOrder values 8, 9, 10 are reassigned: spatio-temporal-script=8, scene-generation=9, scene-selection=10 (no gaps, no duplicates across the full 0-19 range)
    - All other properties of the 3 moved entries are preserved exactly (id, name, stage, outputFiles, review, notes)
    - VALID_PHASES at lib/hermes-client.js:16-24 is reordered to match: ...'character-selection', 'spatio-temporal-script', 'scene-generation', 'scene-selection', 'script-lock'...
    - V2_MIGRATION_MAP at lib/pipeline.js:118 is unchanged (entries map by string ID, not by stageOrder — confirmed by reading lines 117-130)
    - No other phase entries (indices 0-7, 11-19) are touched
    - Existing test "migrates V2/V4.1 phase IDs to V6 phases" (test/phases/handlers.test.mjs or sibling, last seen in baseline run output as "V2 State Migration (V6)") still passes
  </behavior>
  <action>
    In lib/pipeline.js, locate the three consecutive PHASES entries currently at array indices 8, 9, 10 (scene-generation stageOrder:8, scene-selection stageOrder:9, spatio-temporal-script stageOrder:10 — physical lines ~77-85). Perform a literal block move: cut the spatio-temporal-script entry (currently last of the three) and paste it as the FIRST of the three. Then update the stageOrder numeric field on each of the three entries: spatio-temporal-script gets stageOrder: 8 (was 10), scene-generation gets stageOrder: 9 (was 8), scene-selection gets stageOrder: 10 (was 9). Preserve every other field on each entry verbatim (id, name, stage, outputFiles, review object, notes). Do NOT touch any other entry in the PHASES array. Do NOT modify V2_MIGRATION_MAP (lines 117-130). In lib/hermes-client.js, locate VALID_PHASES (line 16-24) and reorder the same three string entries to match: move 'spatio-temporal-script' to BEFORE 'scene-generation' (it currently appears after 'scene-selection' on line 20). The new line 20 should read: `'spatio-temporal-script', 'scene-generation', 'scene-selection',`. Do NOT add or remove any string from VALID_PHASES — only reorder. Do NOT modify the comment at line 13. After the edit, verify the comment "must stay 1:1 in sync with lib/pipeline.js PHASES array" is now actually true by visual inspection.
  </action>
  <verify>
    <automated>node -e "import('./lib/pipeline.js').then(m => { const P=m.PHASES||m.default?.PHASES; if(!P) throw new Error('no PHASES export'); const ids=P.map(p=>p.id); const stsIdx=ids.indexOf('spatio-temporal-script'); const sgIdx=ids.indexOf('scene-generation'); const ssIdx=ids.indexOf('scene-selection'); if(!(stsIdx<sgIdx && sgIdx<ssIdx)) throw new Error('order wrong: sts='+stsIdx+' sg='+sgIdx+' ss='+ssIdx); const orders=P.map(p=>p.stageOrder); for(let i=0;i<orders.length;i++){ if(orders[i]!==i) throw new Error('stageOrder['+i+']='+orders[i]+' expected '+i); } console.log('OK: PHASES array ordered sts('+stsIdx+')→sg('+sgIdx+')→ss('+ssIdx+') and stageOrder 0-19 monotonic'); });" && node --test test/phases/handlers.test.mjs 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - The node -e one-liner above prints "OK: PHASES array ordered sts(N)→sg(N+1)→ss(N+2)" (no throw)
    - stageOrder is a 0-19 monotonic sequence with no gaps or duplicates (the for-loop check in the verify script passes)
    - `grep -n "stageOrder: 8" lib/pipeline.js` returns the line for spatio-temporal-script (NOT scene-generation)
    - `grep -n "stageOrder: 9" lib/pipeline.js` returns the line for scene-generation (NOT scene-selection)
    - `grep -n "stageOrder: 10" lib/pipeline.js` returns the line for scene-selection (NOT spatio-temporal-script)
    - VALID_PHASES in lib/hermes-client.js has 'spatio-temporal-script' positioned BEFORE 'scene-generation' (grep -A1 "spatio-temporal-script" lib/hermes-client.js shows scene-generation on a later line)
    - V2_MIGRATION_MAP unchanged: `grep -c "scenario: 'spatio-temporal-script'" lib/pipeline.js` returns 1
    - test/phases/handlers.test.mjs passes (V2 migration test still green)
  </acceptance_criteria>
  <done>
    PHASES array declaration order and stageOrder field both place spatio-temporal-script before scene-generation before scene-selection; VALID_PHASES synced; no other phases touched; existing handler tests green.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add scene↔sts ordering unit + integration tests</name>
  <files>test/phases/scene-sts-order.test.mjs</files>
  <read_first>
    - test/phases/character-generation.test.mjs (lines 1-40 — imports + internals destructuring pattern to mirror)
    - test/phases/handlers.test.mjs (full file — established pattern for testing phase handlers; mirrors the describe/it structure and Pipeline import)
    - lib/pipeline.js (lines 50-115 AFTER Task 1 lands — confirms new PHASES array shape to assert against)
    - lib/phases/index.js (lines 2536-2580 — scene-generation handler; specifically lines 2562-2572 the sts read+map logic, and lines 2573-2580 the fallback default scene that must NO LONGER trigger when sts runs first)
    - lib/phases/index.js (lines 841-889 — spatio-temporal-script handler; specifically line 885 `await bus.write('spatio-temporal-script', {shots, audio_events, duration_coupling})` which is what scene-generation reads)
    - lib/asset-bus.js (lines 145-260 — AssetBus constructor takes workdir; write(assetName, data) writes to {workdir}/.assets/{schema.file}; read(assetName) returns unwrapped payload or null)
    - .planning/phases/26-data-spine-repair/26-CONTEXT.md (LOCKED test strategy: "单元测试：scene-generation 在 sts 存在 / sts 缺失 两种情况"; "集成测试：bin/pipeline.js run --episode EP01 --to scene-generation 真实跑通，scene 数据含真实分场景结构"; "不破坏现有 461/461 测试基线")
  </read_first>
  <behavior>
    - Unit test "PHASES array 顺序: spatio-temporal-script 在 scene-generation 之前": imports PHASES from lib/pipeline.js, asserts indexOf('spatio-temporal-script') < indexOf('scene-generation') < indexOf('scene-selection')
    - Unit test "PHASES stageOrder 单调递增 0-19": asserts PHASES[i].stageOrder === i for all i (no gaps, no duplicates)
    - Unit test "VALID_PHASES 与 PHASES 同序": imports VALID_PHASES from lib/hermes-client.js AND PHASES from lib/pipeline.js, asserts VALID_PHASES is deep-equal to PHASES.map(p=>p.id) (enforces the "1:1 in sync" comment)
    - Unit test "scene-generation 在 sts 已写入后读取到真实 shots": creates tmpdir, instantiates AssetBus(tmpdir), calls bus.write('spatio-temporal-script', {shots: [{scene_id:'s1', scene_description:'d1', characters:['c1']}], audio_events:[], duration_coupling:{}}), then reads back via bus.read('spatio-temporal-script') and asserts result.shots.length === 1 && result.shots[0].scene_id === 's1' (this is a contract test proving the AssetBus read-after-write works, which is what scene-generation relies on at line 2564)
    - Unit test "scene-generation 从 sts 提取 sceneDefs 时映射字段正确": replicates the exact mapping at lib/phases/index.js:2566-2570 — given sts.shots = [{scene_id:'sX', scene_description:'desc', characters:['a','b']}], the mapped sceneDef equals {id:'sX', description:'desc', characters:['a','b']}. This is a regression guard for the mapping shape.
    - Integration test "bin/pipeline.js --list 显示新顺序": spawns `node bin/pipeline.js --list` (or whatever the list command is — read bin/pipeline.js to find the exact command) via child_process.execFileSync, parses stdout, asserts 'spatio-temporal-script' line appears before 'scene-generation' line (verifies the reorder surfaces in the operator-visible CLI)
  </behavior>
  <action>
    Create new file test/phases/scene-sts-order.test.mjs. Top of file: JSDoc comment block explaining "Phase 26 PIPE-DATA-02: scene↔sts 时序修复回归测试 — 验证 PHASES 数组顺序、stageOrder 单调性、VALID_PHASES 同步、AssetBus read-after-write 契约". Imports: `import { describe, it } from 'node:test'; import assert from 'node:assert/strict'; import { mkdtemp, rm } from 'node:fs/promises'; import { join } from 'node:path'; import { tmpdir } from 'node:os'; import { execFileSync } from 'node:child_process'; import { PHASES } from '../../lib/pipeline.js'; import { VALID_PHASES } from '../../lib/hermes-client.js'; import { AssetBus } from '../../lib/asset-bus.js';`. If PHASES is not a named export of lib/pipeline.js, fall back to `import * as P from '../../lib/pipeline.js'; const PHASES = P.PHASES;` (read lib/pipeline.js exports first — search for `export`). If VALID_PHASES is not exported, the test should `import('./lib/hermes-client.js')` and read the module's internal list via a small helper, OR skip that one assertion with a comment noting the export gap — but first attempt: most likely VALID_PHASES is module-internal; if so, replace the deep-equal assertion with a textual grep assertion: read lib/hermes-client.js source via readFile and assert the regex /'spatio-temporal-script'.*'scene-generation'/s (dotall) matches on the VALID_PHASES lines. Structure: one describe('Phase 26 PIPE-DATA-02: scene↔sts 时序修复', () => { ... }) containing 5-6 it() blocks per the behavior list. Each test self-contained (no shared before/after except mkdtemp tmpdir for the AssetBus test, cleaned up in the same it()). For the integration test reading bin/pipeline.js output, first read bin/pipeline.js to find the actual list/status command (look for `--list`, `--status`, `phase.list`, or similar — the run output at lib/pipeline.js:698 returns phases with `order` field, so any command that prints this will show the new order). Use execFileSync('node', [binPath, '--list'], {cwd: process.cwd()}) and assert on stdout. If no --list command exists, replace with a unit test that calls Pipeline.listPhases() or equivalent (read lib/pipeline.js to find the API). Run the file with `node --test test/phases/scene-sts-order.test.mjs` to confirm green before declaring done.
  </action>
  <verify>
    <automated>node --test test/phases/scene-sts-order.test.mjs 2>&1 | grep -E "tests [0-9]+|pass [0-9]+|fail [0-9]+" | head -3</automated>
  </verify>
  <acceptance_criteria>
    - File test/phases/scene-sts-order.test.mjs exists
    - node --test test/phases/scene-sts-order.test.mjs reports tests >= 5, pass >= 5, fail = 0
    - The test "PHASES stageOrder 单调递增 0-19" exists and passes (grep -c "stageOrder 单调\|monotonic\|stageOrder.*===.*i" test/phases/scene-sts-order.test.mjs returns >= 1)
    - The test "VALID_PHASES 与 PHASES 同序" (or equivalent textual-grep fallback) exists and passes
    - An AssetBus read-after-write contract test exists (grep -c "bus.write.*spatio-temporal-script\|bus.read.*spatio-temporal-script" test/phases/scene-sts-order.test.mjs returns >= 2)
    - The integration assertion (either subprocess --list or direct API call) exists and verifies sts appears before scene-generation in operator-visible output
    - No existing test file was modified (git diff shows only the new file)
  </acceptance_criteria>
  <done>
    New test file test/phases/scene-sts-order.test.mjs with 5+ green tests covering ordering, stageOrder monotonicity, VALID_PHASES sync, AssetBus contract, and operator-visible CLI output; existing 461 tests untouched.
  </done>
</task>

</tasks>

<verification>
After both tasks:
1. `node -e "import('./lib/pipeline.js').then(m => { const P=(m.PHASES||m.default?.PHASES); const ids=P.map(p=>p.id); const stsIdx=ids.indexOf('spatio-temporal-script'); const sgIdx=ids.indexOf('scene-generation'); const ssIdx=ids.indexOf('scene-selection'); if(!(stsIdx<sgIdx && sgIdx<ssIdx)) process.exit(1); const orders=P.map(p=>p.stageOrder); if(!orders.every((o,i)=>o===i)) process.exit(2); });"` — exits 0 (silent success)
2. `node --test test/phases/scene-sts-order.test.mjs 2>&1 | tail -3` — pass >= 5, fail = 0
3. `node --test 'test/**/*.test.mjs' 'test/**/*.test.js' 2>&1 | grep -E "^ℹ (tests|pass|fail)"` — tests >= 466 (461 baseline + 5 new), fail = 0
4. `grep -n "stageOrder: 8\b" lib/pipeline.js` returns the spatio-temporal-script entry (not scene-generation)
</verification>

<success_criteria>
- PHASES array declaration order AND stageOrder field both place spatio-temporal-script before scene-generation before scene-selection
- VALID_PHASES in lib/hermes-client.js reordered to match (1:1 sync comment now true)
- stageOrder values are a monotonic 0-19 sequence (no gaps, no duplicates) — canvas positioning stays consistent with execution order
- Unit tests cover: PHASES order, stageOrder monotonicity, VALID_PHASES sync, AssetBus read-after-write contract, operator-visible CLI output
- 461-test baseline preserved (no regression), +5 new tests added
- PIPE-DATA-02 root cause (scene-generation runs before sts writes → bus.read returns null → silent fallback to single default scene) is closed
- No changes to scene-generation handler, spatio-temporal-script handler, scene-selection handler, or V2_MIGRATION_MAP
</success_criteria>

<output>
Create `.planning/phases/26-data-spine-repair/26-02-SUMMARY.md` when done
</output>
