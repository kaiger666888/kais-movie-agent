---
phase: 26-data-spine-repair
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/phases/index.js
  - test/phases/character-generation.test.mjs
autonomous: true
requirements:
  - PIPE-DATA-01

must_haves:
  truths:
    - "_loadCharactersForGeneration reads characters from pain-report.json (requirement field) when present"
    - "_loadCharactersForGeneration falls back to legacy requirement.json when pain-report.json is absent (backward compat for old workdirs)"
    - "_loadCharactersForGeneration falls back to pipeline.config.characters as final degraded path"
    - "Running `bin/pipeline.js run --episode EP01 --to character-generation` in V6 (pain-discovery writes pain-report.json) yields a non-empty character list to character-generation handler"
    - "Existing 461/461 test baseline is preserved (no regression)"
  artifacts:
    - path: "lib/phases/index.js"
      provides: "Migrated _loadCharactersForGeneration reader (pain-report.json → requirement.json → pipeline.config.characters)"
      contains: "pain-report.json"
    - path: "test/phases/character-generation.test.mjs"
      provides: "Updated unit tests covering all 3 fallback paths"
      contains: "pain-report"
  key_links:
    - from: "lib/phases/index.js (_loadCharactersForGeneration)"
      to: "pain-report.json (written by pain-discovery handler at line 1522)"
      via: "readFile → JSON.parse → requirement.characters"
      pattern: "pain-report\\.json"
    - from: "lib/phases/index.js (character-generation handler, line 2169)"
      to: "_loadCharactersForGeneration return value"
      via: "const characters = await _loadCharactersForGeneration(pipeline)"
      pattern: "_loadCharactersForGeneration"
---

<objective>
Migrate `_loadCharactersForGeneration` (lib/phases/index.js:502) to read V6's actual character data source.

Purpose: V6 replaced `requirement-bible` with `pain-discovery` (lib/pipeline.js:118 V2_MIGRATION_MAP). pain-discovery writes `pain-report.json` with the requirement embedded in its `requirement` field (lib/phases/index.js:1517 `requirement: reqData`, file written at line 1522). The current reader at line 502-528 only reads `requirement.json`, which is never written in V6 → character-generation永久 falls back to `pipeline.config.characters` (empty unless operator configured it). This starves the render path of real character data and blocks Phase 27/29 from having real inputs.

Per user decision (CONTEXT.md PIPE-DATA-01, LOCKED): migrate reader to pain-report.json first, keep requirement.json as legacy fallback, keep pipeline.config.characters as final fallback. Do NOT delete the legacy requirement-bible handler (line 541) — old workdirs may still use it.

Output: Patched `_loadCharactersForGeneration` + updated unit tests covering all 3 fallback tiers.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-data-spine-repair/26-CONTEXT.md

<interfaces>
<!-- Executor needs these contracts — extracted from codebase, no exploration required. -->

From lib/phases/index.js:502-528 (CURRENT — the function being modified):
```javascript
async function _loadCharactersForGeneration(pipeline) {
  // 优先读 requirement.json (requirement-bible phase 写入)
  try {
    const raw = await readFile(join(pipeline.workdir, 'requirement.json'), 'utf-8');
    const req = JSON.parse(raw);
    if (req?.characters?.length) {
      return req.characters.map((c, i) => ({
        id: c.id || `char-${i + 1}`,
        name: c.name,
        face: c.face || c.description || '',
        body: c.body || '',
        costumes: c.costumes || ['default'],
        ...c,
      }));
    }
  } catch { /* file missing — fallback */ }
  // 降级到 pipeline.config.characters
  const chars = pipeline.config?.characters || [];
  return chars.map(/* same shape */);
}
```
Exported via ` _characterGenerationInternals` (test/phases/character-generation.test.mjs:28 imports it).

From lib/phases/index.js:1453-1522 (pain-discovery handler — DO NOT MODIFY, this is the writer):
```javascript
// Line 1477-1479: reads requirement.json into reqData
// Line 1517: stubData.requirement = reqData
// Line 1522: await writeFile(join(pipeline.workdir, 'pain-report.json'), JSON.stringify(stubData, null, 2));
```
So pain-report.json shape is:
```json
{
  "_phase": "pain-discovery",
  "requirement": { "title": "...", "genre": "...", "characters": [...], ... },
  "pain_points": [...],
  "audience_match": {...}
}
```
Characters live at `painReport.requirement.characters`.

From test/phases/character-generation.test.mjs:278-330 (existing tests — must UPDATE, not delete):
- "优先读 requirement.json" (line 288) — still valid but now tier 2
- "requirement.json 缺失时降级到 pipeline.config.characters" (line 301) — still valid but now tier 3
- "为缺 id 的角色生成 char-N" (line 312)
- "无 costumes 字段时填默认 ['default']" (line 322)

Imports already present in test file (lines 20-29): `describe, it, before, after` from node:test; `mkdtemp, rm, readFile, writeFile, mkdir` from node:fs/promises; `join` from node:path; `tmpdir` from node:os.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add pain-report.json reader tier to _loadCharactersForGeneration with deprecation warning on legacy fallback</name>
  <files>lib/phases/index.js</files>
  <read_first>
    - lib/phases/index.js (lines 499-528 — current _loadCharactersForGeneration implementation, exact shape to preserve)
    - lib/phases/index.js (lines 1453-1522 — pain-discovery handler, confirms pain-report.json shape: top-level `requirement` field holds the reqData object including `characters`)
    - lib/phases/index.js (lines 2160-2170 — character-generation handler, the single caller of _loadCharactersForGeneration, confirms contract: returns array of {id,name,face,body,costumes,...rest})
    - .planning/phases/26-data-spine-repair/26-CONTEXT.md (LOCKED decision: pain-report first → requirement.json fallback → pipeline.config fallback; do NOT delete legacy requirement-bible handler)
  </read_first>
  <behavior>
    - Tier 1 (preferred): When {workdir}/pain-report.json exists AND JSON.parse succeeds AND painReport.requirement.characters is a non-empty array → return normalized characters from painReport.requirement.characters
    - Tier 2 (legacy fallback): When pain-report.json missing/unparseable/no requirement.characters, AND {workdir}/requirement.json exists with non-empty characters array → return normalized characters from requirement.json (emit console.warn with text containing "legacy" and "requirement.json" and "pain-report" so degraded path is observable)
    - Tier 3 (degraded): When both files missing/unparseable → fall back to pipeline.config.characters (existing behavior, no warning change)
    - Normalization preserved exactly: every returned character has shape {id (default `char-${i+1}`), name, face (default from description), body, costumes (default ['default']), ...rest spread}
    - readFile errors for either file are caught silently (try/catch pattern already in place)
    - Export unchanged: function still exposed via `_characterGenerationInternals._loadCharactersForGeneration`
  </behavior>
  <action>
    Modify the body of `_loadCharactersForGeneration` (lib/phases/index.js:502-528) to add a new first try/catch block BEFORE the existing requirement.json block. The new block must:
    1. Call readFile on join(pipeline.workdir, 'pain-report.json')
    2. JSON.parse the result into a local variable named `painReport`
    3. Check `painReport?.requirement?.characters?.length` (note the nested `.requirement.` — pain-discovery embeds reqData one level deep at line 1517)
    4. If non-empty, return the same `.map(...)` normalization the existing requirement.json branch uses (id/name/face/body/costumes defaults + spread) — extract the mapping into a local helper `normalizeChar(c, i)` to avoid duplicating the normalization block across all three tiers
    5. If pain-report.json read fails OR lacks requirement.characters, fall through to the existing requirement.json try/catch block (unchanged behavior, now tier 2)
    6. Inside the requirement.json branch, add a `console.warn` with message like `[character-generation] using legacy requirement.json — migrate to pain-report.json (V6 default). Reason: ${e.message or 'pain-report.json missing requirement.characters'}` so operators see the legacy path is active
    7. Keep the final `pipeline.config?.characters || []` branch unchanged (tier 3, no new warning)
    Update the JSDoc comment at line 499-501 to read: "从 pain-report.json (V6 主路径) / requirement.json (legacy) / pipeline.config.characters (降级兜底) 读取 characters". Do NOT touch the `requirement-bible` handler at line 541 (CONTEXT.md explicitly preserves it for legacy workdirs). Do NOT touch the pain-discovery writer at line 1453.
  </action>
  <verify>
    <automated>node --test test/phases/character-generation.test.mjs 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "pain-report" lib/phases/index.js returns >= 1 (new reader present)
    - grep -c "requirement.json" lib/phases/index.js returns >= 2 (legacy path still present + the writer in pain-discovery at line 1478 still present)
    - The phrase "legacy" appears in a console.warn inside _loadCharactersForGeneration (grep -A 30 "_loadCharactersForGeneration" lib/phases/index.js | grep -c "legacy" returns >= 1)
    - A normalizeChar helper or equivalent de-duplicated normalization exists (no copy-pasted .map blocks across 3 tiers — verified by reading the function body)
    - The `requirement-bible` handler registration at line 541 is unchanged (grep -n "'requirement-bible'" lib/phases/index.js still returns line ~541)
    - The pain-discovery writer at line 1522 is unchanged (grep -n "pain-report.json" lib/phases/index.js still shows the writer line)
  </acceptance_criteria>
  <done>
    _loadCharactersForGeneration reads pain-report.json first; legacy requirement.json path retained with observable warning; pipeline.config fallback retained; no other handlers modified.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Update existing unit tests + add pain-report.json tier coverage</name>
  <files>test/phases/character-generation.test.mjs</files>
  <read_first>
    - test/phases/character-generation.test.mjs (lines 1-40 — imports + internals destructuring, reuse existing test infrastructure)
    - test/phases/character-generation.test.mjs (lines 275-335 — existing "describe 5: _loadCharactersForGeneration" block, the 4 tests to preserve + extend)
    - lib/phases/index.js (lines 499-528 AFTER Task 1 lands — confirms the new tier structure and warning text to assert against)
    - .planning/phases/26-data-spine-repair/26-CONTEXT.md (LOCKED test strategy: "单元测试：mock AssetBus，覆盖 pain-report.json 读取 / 旧 requirement.json fallback / pipeline.config 兜底 3 条路径")
  </read_first>
  <behavior>
    - Existing test "优先读 requirement.json" (line 288) RENAMED to "tier 2: 当 pain-report.json 缺失时降级到 requirement.json" and still passes (it does not create pain-report.json so the new tier 1 try/catch falls through)
    - Existing test "requirement.json 缺失时降级到 pipeline.config.characters" (line 301) still passes (no pain-report.json, no requirement.json → tier 3)
    - Existing tests "为缺 id 的角色生成 char-N" (line 312) and "无 costumes 字段时填默认 ['default']" (line 322) still pass (they exercise pipeline.config tier which still uses same normalization)
    - NEW test "tier 1: 优先读 pain-report.json 的 requirement.characters": creates pain-report.json with shape { requirement: { characters: [{id:'p1', name:'P', face:'pf', body:'pb', costumes:['c']}] } } in a fresh tmpdir, also creates requirement.json with different characters [{name:'LEGACY'}], calls _loadCharactersForGeneration({workdir, config:{characters:[]}}), asserts returned[0].name === 'P' (NOT 'LEGACY'), and asserts returned[0].id === 'p1'
    - NEW test "tier 1: pain-report.json 存在但 requirement.characters 为空时降级到 requirement.json": creates pain-report.json with { requirement: { characters: [] } } and requirement.json with [{name:'RL'}], asserts returned[0].name === 'RL'
    - NEW test "tier 1: pain-report.json 存在但缺 requirement 字段时降级到 requirement.json": creates pain-report.json with { pain_points: [] } (no requirement key) and requirement.json with [{name:'RL2'}], asserts returned[0].name === 'RL2'
    - NEW test "tier 1: pain-report.json 解析失败 (损坏 JSON) 降级到 requirement.json": writes invalid JSON to pain-report.json and valid requirement.json, asserts returned[0].name from requirement.json (no throw escapes)
    - NEW test "normalization 一致性: pain-report tier 保留 costumes / face-from-description / id 默认值": creates pain-report.json with character lacking id and costumes, asserts id defaults to 'char-1' and costumes defaults to ['default'] (mirrors existing pipeline.config tier test at line 322)
  </behavior>
  <action>
    In test/phases/character-generation.test.mjs, locate the describe block at line 278 ("describe('Phase 14: _loadCharactersForGeneration', ...)"). Do NOT delete the 4 existing tests — keep all 4 and rename the first one's title string only (rename "优先读 requirement.json" to "tier 2: 当 pain-report.json 缺失时降级到 requirement.json (legacy)"). Add 5 new `it(...)` blocks per the behavior list above, placed inside the same describe block AFTER the existing 4 tests. Each new test must use a fresh mkdtemp tmpdir (do not share state with the existing tmpDir fixture which has a stale requirement.json from test 1 — that would interfere). Pattern for each new test: `const t = await mkdtemp(join(tmpdir(), 'phase26-pain-report-')); ... await writeFile(join(t, 'pain-report.json'), JSON.stringify({...})); await writeFile(join(t, 'requirement.json'), JSON.stringify({...})); const chars = await _loadCharactersForGeneration({ workdir: t, config: { characters: [] } }); assert...; await rm(t, { recursive: true, force: true });`. Clean up each tmpdir at end of its test (no shared `after` hook needed since each test self-cleans). Use `assert.strictEqual` for scalars and `assert.deepEqual` for the costumes array. Do NOT modify any other describe block in the file.
  </action>
  <verify>
    <automated>node --test test/phases/character-generation.test.mjs 2>&1 | grep -E "tests [0-9]+|pass [0-9]+|fail [0-9]+" | head -3</automated>
  </verify>
  <acceptance_criteria>
    - node --test test/phases/character-generation.test.mjs reports `pass` count >= 9 (4 existing + 5 new) and `fail` = 0
    - grep -c "pain-report" test/phases/character-generation.test.mjs returns >= 5 (5 new tests each write the file)
    - The 4 original test names are still present in the file (grep -c "为缺 id 的角色生成 char-N\|无 costumes 字段时填默认\|降级到 pipeline.config.characters\|降级到 requirement.json" returns >= 4)
    - No existing describe block other than "Phase 14: _loadCharactersForGeneration" was modified (git diff shows changes only within that one describe block)
  </acceptance_criteria>
  <done>
    All 9+ tests in the _loadCharactersForGeneration describe block pass; 3-tier fallback fully covered; existing 4 tests preserved; full test/phases/character-generation.test.mjs suite green.
  </done>
</task>

</tasks>

<verification>
After both tasks:
1. `node --test test/phases/character-generation.test.mjs 2>&1 | tail -3` — pass count >= 9, fail = 0
2. `node --test 'test/**/*.test.mjs' 'test/**/*.test.js' 2>&1 | grep -E "^ℹ (tests|pass|fail)"` — tests 461+ (the +5 new tests bring baseline to ~466), pass = tests, fail = 0
3. `grep -c "pain-report" lib/phases/index.js` returns >= 1 (new reader wired)
4. `grep -n "'requirement-bible'" lib/phases/index.js` still returns the legacy handler registration (untouched)
</verification>

<success_criteria>
- _loadCharactersForGeneration reads pain-report.json first (tier 1), requirement.json second (tier 2 with deprecation warning), pipeline.config.characters third (tier 3)
- All 3 fallback tiers covered by unit tests
- 461-test baseline preserved (no regression) with +5 new tests added
- PIPE-DATA-01 root cause (V6 reader永久 falls back to empty pipeline.config.characters because requirement.json is never written) is closed
- No changes to pain-discovery writer, requirement-bible legacy handler, or any other phase handler
</success_criteria>

<output>
Create `.planning/phases/26-data-spine-repair/26-01-SUMMARY.md` when done
</output>
