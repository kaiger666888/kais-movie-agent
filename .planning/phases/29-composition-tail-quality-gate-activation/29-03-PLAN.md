---
phase: 29-composition-tail-quality-gate-activation
plan: 03
type: execute
wave: 3
depends_on: [29-02]
files_modified:
  - lib/phases/index.js
  - test/phases/consistency-guard-blocking.test.mjs
files_deleted:
  - lib/gate-constraints.js
  - lib/invariant-bus.js
autonomous: true
requirements: [PIPE-GUARD-01]

must_haves:
  truths:
    - "lib/gate-constraints.js and lib/invariant-bus.js no longer exist in the repository (dead code deleted)"
    - "No production or test file imports gate-constraints or invariant-bus (grep returns zero matches after deletion)"
    - "When consistency-guard audit fails (passed=false), quality-report.json contains _consistencyBlocked: true at top level"
    - "When consistency-guard audit fails, the episode run is marked as failed (not silently swallowed)"
    - "consistency-guard fail path uses console.error (not console.warn) for operator visibility"
    - "When consistency-guard audit passes, _consistencyBlocked is absent or false in quality-report.json"
  artifacts:
    - path: "lib/phases/index.js"
      provides: "consistency-guard blocking fail path with _consistencyBlocked marker + episode fail propagation"
      contains: "_consistencyBlocked"
    - path: "test/phases/consistency-guard-blocking.test.mjs"
      provides: "Unit test asserting _consistencyBlocked write on fail + episode fail propagation + absent on pass"
      min_lines: 70
  key_links:
    - from: "lib/phases/index.js consistency-guard handler"
      to: "quality-report.json"
      via: "_consistencyBlocked: true top-level field"
      pattern: "_consistencyBlocked"
    - from: "lib/phases/index.js consistency-guard handler"
      to: "Pipeline runPhase catch block"
      via: "throw error on audit fail → propagates to run() loop → marks episode failed"
      pattern: "throw new Error.*consistency"
---

<objective>
Activate consistency-guard as a blocking quality gate (no more silent warn-and-continue on audit fail) and delete the two dead-code modules (gate-constraints.js, invariant-bus.js) that have zero production imports.

Purpose: PIPE-GUARD-01 closes the audit finding that consistency-guard's fail path is a no-op warn (line 3050-3053) and that gate-constraints.js / invariant-bus.js are dead code. With Plan 01 + Plan 02 making composition the real output-producing phase, consistency-guard's "let composition judge quality" comment finally has a target — and the guard must actually block on fail.

Output: Modified consistency-guard handler in lib/phases/index.js + deleted dead-code files + regression test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-composition-tail-quality-gate-activation/29-CONTEXT.md
@.planning/phases/29-composition-tail-quality-gate-activation/29-01-SUMMARY.md
@.planning/phases/29-composition-tail-quality-gate-activation/29-02-SUMMARY.md

<interfaces>
<!-- consistency-guard handler (lib/phases/index.js:2940) — the handler we modify -->
<!-- Dead code files (lib/gate-constraints.js, lib/invariant-bus.js) — to be deleted -->

From lib/phases/index.js lines ~3036-3075 (consistency-guard fail path — TARGET OF CHANGE):
- Lines 3036-3047: builds `stubData` object with `_auditFailed`, `scores`, `overall`, `passed`, `findings`, etc.
- Line 3048: `await writeFile(join(pipeline.workdir, 'consistency-pass.json'), JSON.stringify(stubData, null, 2))` — KEEP this (success path writes here too)
- Line 3050: comment `// 不抛 fatal — 让质量门控在 composition/Phase 13 阶段统一判定` — OUTDATED comment, remove/replace
- Lines 3051-3053: `if (!stubData.passed) { console.warn(...) }` — TARGET: change to console.error + write _consistencyBlocked + throw to propagate fail
- Lines 3055-3058: `_hermesAudit(...)` — keep
- Lines 3060-3070: collector.record — keep
- Lines 3072+: `return { summary: stubData, ... }` — currently always returns success regardless of passed flag

From lib/pipeline.js runPhase catch block (lines 498-519): when handler throws, `state.phases[phaseId] = { status: 'failed', ... }` and the error propagates to `run()` which breaks the loop (line 567-568). So throwing from consistency-guard marks the episode failed — no new mechanism needed.

From CONTEXT.md `<decisions>` (PIPE-GUARD-01):
- Write `_consistencyBlocked: true` to quality-report.json top-level (composition handler writes quality-report.json — but consistency-guard runs BEFORE composition at stageOrder 15 vs 18). Discretion item: "_consistencyBlocked 标记的具体位置 (quality-report.json top-level vs 单独文件)" — choose separate file `consistency-blocked.json` to avoid ordering coupling with composition's quality-report.json write. (If placed in quality-report.json, consistency-guard would have to either pre-create or merge-into composition's later write — fragile.) Document this choice in code comment.
- Mark episode fail via throw (propagates to Pipeline.run loop)
- console.error (not console.warn)
- Keep consistency-pass.json write logic for success path

Grep evidence (pre-plan): `grep -rn "gate-constraints\|invariant-bus" --include="*.js" --include="*.mjs"` returns matches ONLY in lib/gate-constraints.js and lib/invariant-bus.js self-headers — zero external imports confirmed.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete dead-code files lib/gate-constraints.js + lib/invariant-bus.js</name>
  <files>lib/gate-constraints.js, lib/invariant-bus.js</files>
  <read_first>
    - lib/gate-constraints.js (full file — small, confirm it is self-contained with no exports consumed elsewhere)
    - lib/invariant-bus.js (full file — same check)
    - Run pre-delete grep to confirm zero external imports: `grep -rn "gate-constraints\|invariant-bus" --include="*.js" --include="*.mjs" --include="*.json" /data/workspace/kais-movie-agent/lib /data/workspace/kais-movie-agent/bin /data/workspace/kais-movie-agent/test` — expect matches only in the two files' own header comments
  </read_first>
  <action>
    Per PIPE-GUARD-01 and CONTEXT.md `<decisions>` (D-PIPE-GUARD-01: delete dead code):

    1. Run the grep audit documented in `<read_first>`. Confirm zero matches outside the two target files. If ANY external import is found (test file or production code), STOP and surface to the orchestrator — do not delete, the file is not dead code.

    2. Assuming grep confirms zero external imports, delete both files:
       - `git rm lib/gate-constraints.js`
       - `git rm lib/invariant-bus.js`

    3. Verify package.json does not reference them as entry points: `grep -n "gate-constraints\|invariant-bus" package.json` should return zero matches.

    This task has no test file of its own — its verification is the post-deletion grep (Task 2's verify block includes the negative grep).
  </action>
  <verify>
    <automated>cd /data/workspace/kais-movie-agent && test ! -f lib/gate-constraints.js && test ! -f lib/invariant-bus.js && echo "DELETED OK" && grep -rn "gate-constraints\|invariant-bus" --include="*.js" --include="*.mjs" lib/ bin/ test/ 2>/dev/null | grep -v "^Binary" | wc -l | tr -d ' ' | xargs -I{} sh -c '[ {} = 0 ] && echo "ZERO REFS OK" || (echo "REMAINING REFS: {}" && exit 1)'</automated>
  </verify>
  <done>
    - lib/gate-constraints.js removed from repo
    - lib/invariant-bus.js removed from repo
    - Zero references to either module name anywhere in lib/, bin/, test/ (excluding git history)
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: consistency-guard blocking fail path + _consistencyBlocked marker + episode fail propagation</name>
  <files>lib/phases/index.js, test/phases/consistency-guard-blocking.test.mjs</files>
  <read_first>
    - lib/phases/index.js lines 2940-3080 (consistency-guard handler full body — confirm exact line numbers before editing; the fail-path block is at ~3050-3053)
    - lib/pipeline.js lines 531-575 (Pipeline.run loop — confirm that a handler throw breaks the loop and marks the episode failed via runPhase catch at 498-519)
    - lib/phases/index.js lines 1368-1460 (composition handler — confirm it writes quality-report.json so we understand the ordering constraint; consistency-guard stageOrder 15 runs BEFORE composition stageOrder 18)
    - 29-02-SUMMARY.md (confirms quality-report.json schema after Plan 02, including the `_composition` field we will sibling with `_consistencyBlocked`)
  </read_first>
  <behavior>
    - Test 1 (fail path blocks): When consistency-guard's `auditResult.passed === false`, calling `phaseHandlers['consistency-guard'].after(...)` THROWS an error (assert.throws). The error message contains "consistency" or "一致性".
    - Test 2 (fail path writes marker): When the audit fails, a file `{workdir}/consistency-blocked.json` exists and contains `{ _consistencyBlocked: true, ... }` (parse and assert the field).
    - Test 3 (fail path uses console.error): Spy on console.error (override with a sink) — when audit fails, console.error is invoked at least once with a message containing "consistency-guard" or "审计未通过". Assert console.warn is NOT used for the fail message (it may be used elsewhere in the handler for incidental warns).
    - Test 4 (pass path no marker): When `auditResult.passed === true`, `phaseHandlers['consistency-guard'].after(...)` returns without throwing, NO `consistency-blocked.json` file is written (or if written, `_consistencyBlocked: false`), and `consistency-pass.json` is written as before.
  </behavior>
  <action>
    Per PIPE-GUARD-01 and CONTEXT.md `<decisions>` (D-PIPE-GUARD-01: consistency-guard fail writes _consistencyBlocked; D-PIPE-GUARD-01: marks episode fail; D-PIPE-GUARD-01: console.error not warn; discretion: place marker in separate file consistency-blocked.json to avoid ordering coupling with composition's quality-report.json write):

    1. In consistency-guard handler (lib/phases/index.js ~line 3050-3053), replace the silent warn block:
       BEFORE:
       ```
       // 不抛 fatal — 让质量门控在 composition/Phase 13 阶段统一判定
       if (!stubData.passed) {
         console.warn(`[consistency-guard] 审计未通过: ${stubData.recommendation || '部分维度低于阈值'}`);
       }
       ```
       AFTER:
       ```
       // PIPE-GUARD-01: blocking activation — composition is now a real output phase (Plan 29-01),
       // so consistency-guard no longer defers. Fail writes a marker file and propagates the error
       // to Pipeline.run, which marks the episode failed (lib/pipeline.js runPhase catch block).
       if (!stubData.passed) {
         console.error(`[consistency-guard] 审计未通过 (BLOCKING): ${stubData.recommendation || '部分维度低于阈值'}`);
         try {
           await writeFile(join(pipeline.workdir, 'consistency-blocked.json'), JSON.stringify({
             _consistencyBlocked: true,
             _phase: 'consistency-guard',
             _generatedAt: new Date().toISOString(),
             overall: stubData.overall,
             recommendation: stubData.recommendation || '',
             findings_count: (stubData.findings || []).length,
           }, null, 2));
         } catch (writeErr) {
           console.error(`[consistency-guard] consistency-blocked.json 写入失败: ${writeErr.message}`);
         }
         const blockErr = new Error(`一致性审计未通过 (consistency-guard blocking): ${stubData.recommendation || '部分维度低于阈值'}`);
         blockErr.code = 'CONSISTENCY_BLOCKED';
         blockErr.consistencyBlocked = true;
         throw blockErr;
       }
       ```

    2. Keep the `consistency-pass.json` write (line 3048) unchanged — it runs BEFORE the fail check and records the audit result regardless of pass/fail (useful for operator forensics).

    3. Keep the `_hermesAudit` call (line 3055) and `collector.record` (line 3060) BEFORE the throw — they must execute on both pass and fail paths so telemetry captures the failure. The current ordering already does this (fail check is at 3051, after these calls? VERIFY in read_first — if hermesAudit/collector run AFTER the fail check, move the throw to after them).

    4. Do NOT modify the success path return (lines 3072+) — when passed=true, behavior is unchanged.

    5. Do NOT touch composition handler (Plan 01) or delivery handler (Plan 02).

    Implementation note on ordering: read the actual handler body carefully during `<read_first>`. The desired final ordering is: (a) build stubData, (b) write consistency-pass.json, (c) _hermesAudit, (d) collector.record, (e) fail check → throw if !passed. If the current code has hermesAudit/collector AFTER the fail check, reorder so they run before the throw.

    Test file pattern: mkdtemp workdir, instantiate Pipeline. For the fail case, the handler sources `auditResult` from somewhere internal — likely `auditContinuity` or a visual-data scan that returns empty in a bare workdir (which should naturally produce passed=false since visual_count=0). Confirm by reading the handler body. If the bare-workdir case naturally passes (e.g., short-circuits on no visuals), inject a fail by mocking: pre-write a `{workdir}/consistency-pass.json` is NOT the right lever — instead, if the handler reads from AssetBus, write minimal visual data that triggers a low score. As a fallback, the test can call the internal audit function directly if exported, or set `pipeline.config.qualityGate = { thresholds: { overall: 999 } }` to force any real score to fall below threshold.
  </action>
  <verify>
    <automated>cd /data/workspace/kais-movie-agent && node --test test/phases/consistency-guard-blocking.test.mjs 2>&1 | tail -20 && test ! -f lib/gate-constraints.js && test ! -f lib/invariant-bus.js && echo "DEAD CODE GONE"</automated>
  </verify>
  <done>
    - lib/gate-constraints.js and lib/invariant-bus.js deleted (Task 1)
    - consistency-guard fail path uses console.error (not console.warn)
    - consistency-guard fail path writes consistency-blocked.json with `_consistencyBlocked: true`
    - consistency-guard fail path throws an error that propagates to Pipeline.run (episode marked failed)
    - consistency-guard pass path unchanged (no marker, no throw, consistency-pass.json written)
    - New test file test/phases/consistency-guard-blocking.test.mjs has ≥4 passing cases
    - grep confirms `_consistencyBlocked` appears in lib/phases/index.js and the test file
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| consistency-guard audit → Pipeline run loop | audit failure now propagates as thrown error across the handler→runPhase→run boundary |
| Filesystem → consistency-guard | handler reads visual assets from workdir to audit; tampered assets could force a false-pass |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-07 | Tampering | Attacker swaps visual assets to force consistency false-pass | accept | consistency-guard operates in trusted workdir during a single pipeline run; cross-episode tampering is out of v4.0 scope |
| T-29-08 | Denial of Service | consistency-blocked.json write fails, masking the block | mitigate | write wrapped in try/catch with console.error; the throw happens AFTER the write attempt regardless, so blocking still propagates even if the marker file is missing |
| T-29-09 | Repudiation | Operator cannot prove why an episode was blocked | mitigate | consistency-blocked.json captures overall score, recommendation, findings_count; consistency-pass.json still written with full detail on both paths |
| T-29-10 | Information Disclosure | Dead-code deletion removes audit trail | accept | git history preserves the deleted files; deletion only removes unused code, no data loss |
</threat_model>

<verification>
- `node --test test/phases/consistency-guard-blocking.test.mjs` passes (≥4 cases)
- `test ! -f lib/gate-constraints.js && test ! -f lib/invariant-bus.js` succeeds
- `grep -rn "gate-constraints\|invariant-bus" --include="*.js" --include="*.mjs" lib/ bin/ test/` returns zero matches
- Full baseline regression: `node --test test/phases/handlers.test.mjs` still passes (consistency-guard handler still registered and callable; the registration is unchanged, only the fail-path body changed)
</verification>

<success_criteria>
- Dead code (lib/gate-constraints.js, lib/invariant-bus.js) removed from repo with zero remaining references
- consistency-guard audit failure blocks the episode (throws → Pipeline.run marks failed)
- consistency-guard audit failure writes `_consistencyBlocked: true` marker visible to operators
- consistency-guard audit failure logs via console.error (operator-visible)
- consistency-guard audit success path unchanged
- Regression test committed and passing
</success_criteria>

<output>
Create `.planning/phases/29-composition-tail-quality-gate-activation/29-03-SUMMARY.md` when done
</output>
