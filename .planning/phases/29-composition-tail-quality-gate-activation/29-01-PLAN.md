---
phase: 29-composition-tail-quality-gate-activation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/phases/index.js
  - test/phases/composition-master-mp4.test.mjs
autonomous: true
requirements: [PIPE-COMPOSE-01]

must_haves:
  truths:
    - "composition handler writes output/EPXX/master.mp4 (not final.mp4)"
    - "composition handler writes output/EPXX/web-preview.mp4 alongside master.mp4"
    - "When FFmpeg compose fails or is unavailable, degraded mode touches 0-byte master.mp4 + web-preview.mp4 placeholders so delivery can still find them"
  artifacts:
    - path: "lib/phases/index.js"
      provides: "composition handler writes master.mp4 + web-preview.mp4 + degraded placeholder fallback"
      contains: "join(pipeline.workdir, 'master.mp4')"
    - path: "test/phases/composition-master-mp4.test.mjs"
      provides: "Unit test asserting master.mp4 + web-preview.mp4 filenames + degraded placeholder touch"
      min_lines: 60
  key_links:
    - from: "lib/phases/index.js composition handler"
      to: "output/{episode}/master.mp4"
      via: "composer.compose({ outputPath }) + post-compose web-preview transcode"
      pattern: "join\\(pipeline\\.workdir, 'master\\.mp4'\\)"
---

<objective>
Rename composition handler's output from `final.mp4` to `master.mp4`, add a sibling `web-preview.mp4` (low-res H.264 transcode of master via FFmpeg), and add a degraded-mode placeholder touch so delivery can always find both files even when FFmpeg fails.

Purpose: PIPE-COMPOSE-01 closes the audit finding that PHASES declares outputFiles `[master.mp4, web-preview.mp4]` but composition writes `final.mp4`. Without this, delivery (which the next plan aligns) cannot find the composed output.

Output: Modified composition handler in lib/phases/index.js + regression test asserting the new filenames.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-composition-tail-quality-gate-activation/29-CONTEXT.md

<interfaces>
<!-- Composition handler (lib/phases/index.js:1368) — the handler we modify -->
<!-- Key contracts the executor needs. No codebase exploration required. -->

From lib/phases/index.js lines ~1403-1430 (composition handler `after` body):
- `const composer = new CompositionEngine({ workdir, config, productionMode })` — already instantiated
- `composer.compose({ videoPath, dialoguePath, bgmAmbientPath, bgmSignaturePath, outputPath })` — returns `{ output }`
- Line 1415 currently: `outputPath: join(pipeline.workdir, 'final.mp4')` — TARGET OF RENAME
- Line 1430 catch block: `console.warn([composition] FFmpeg合成降级: ...)` — degraded path; CURRENTLY does NOT touch placeholder files
- After compose success block: `composer.runQualityCheck(composeResult.output)` consumes the output path

From lib/pipeline.js line 115: PHASES declares composition `outputFiles: ['master.mp4', 'web-preview.mp4']` — the contract we must satisfy.

From lib/composition-engine.js: CompositionEngine wraps FFmpeg execFile calls. No web-preview method exists yet — must be added inline or via direct ffmpeg invocation.

Established degraded-mode pattern (Phase 27): `pipeline.config.degradedMode === true` + warn-and-continue. Reuse here.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: composition handler master.mp4 rename + web-preview.mp4 + degraded placeholder</name>
  <files>lib/phases/index.js, test/phases/composition-master-mp4.test.mjs</files>
  <read_first>
    - lib/phases/index.js lines 1368-1460 (composition handler full body — confirm exact line numbers before editing)
    - lib/phases/index.js lines 26-27 (CompositionEngine import)
    - lib/composition-engine.js (search for `compose(` and `runQualityCheck(` method signatures — no need to read whole file, just confirm signature)
    - test/phases/motion-preview-camelcase.test.mjs (closest analog: standalone handler unit test using mkdtemp + Pipeline + phaseHandlers direct invocation pattern)
    - lib/pipeline.js line 115 (PHASES composition outputFiles declaration — the contract)
  </read_first>
  <behavior>
    - Test 1 (rename): When composition handler runs and composer.compose succeeds, the file at `{workdir}/master.mp4` exists and `{workdir}/final.mp4` does NOT exist. Assert via `existsSync`.
    - Test 2 (web-preview): When composition handler runs and compose succeeds, `{workdir}/web-preview.mp4` exists.
    - Test 3 (degraded placeholder): When composer.compose throws (mock by pointing videoPath at non-existent file or by passing a config flag forcing failure), the handler STILL touches 0-byte `{workdir}/master.mp4` and `{workdir}/web-preview.mp4` so downstream delivery can find them.
    - Test 4 (no final.mp4 regression): After handler runs, `final.mp4` is never written under any path.
  </behavior>
  <action>
    Per PIPE-COMPOSE-01 and CONTEXT.md `<decisions>` (D-PIPE-COMPOSE-01: rename final.mp4→master.mp4; D-PIPE-COMPOSE-01: add web-preview.mp4; D-PIPE-COMPOSE-01: degraded mode touch placeholders):

    1. In composition handler `after` body (lib/phases/index.js ~line 1415), change `outputPath: join(pipeline.workdir, 'final.mp4')` to `outputPath: join(pipeline.workdir, 'master.mp4')`. Store the master path in a local `const masterPath = join(pipeline.workdir, 'master.mp4')` for reuse.

    2. After the successful compose block (after `composer.runQualityCheck(composeResult.output)` around line 1423), add web-preview generation: invoke FFmpeg via `composer` or inline `execFile('ffmpeg', ['-i', masterPath, '-vf', 'scale=854:-2', '-c:v', 'libx264', '-preset', 'fast', '-crf', '28', '-an', '-y', join(pipeline.workdir, 'web-preview.mp4')])` wrapped in try/catch that console.warns on failure (web-preview is best-effort per CONTEXT.md "Claude's Discretion"). Use the same FFmpeg invocation pattern CompositionEngine uses (import execFile from node:child_process `promisify` if not already imported at file top — check existing imports first).

    3. Restructure the degraded path: currently the catch block at line 1430 only `console.warn`s. Add after the warn: `await writeFile(masterPath, '')` and `await writeFile(join(pipeline.workdir, 'web-preview.mp4'), '')` to touch 0-byte placeholders. `writeFile` is already imported at file top (used throughout). Wrap these in their own try/catch so a placeholder touch failure does not crash the handler — degrade warn and continue.

    4. Keep the existing degradedMode quality-gate bypass logic (lines 1441-1451) unchanged — that logic governs whether composition THROWS on low quality, separate from whether files exist.

    5. Do NOT touch the delivery handler (lines 3467+) or consistency-guard (lines 2940+) — those are Plan 02 and Plan 03.

    Implementation note: The web-preview transcode command must use `-an` (drop audio) since the input master.mp4 in degraded/CI environments may be 0-byte and FFmpeg will fail on audio stream copy. For the placeholder case the transcode is skipped (already touched 0-byte).

    Test file pattern: follow test/phases/motion-preview-camelcase.test.mjs structure — mkdtemp workdir, instantiate `new Pipeline({ workdir, episode: 'EP_TEST' })`, call `await phaseHandlers.composition.after(pipeline, { id: 'composition', stageOrder: 18, name: '剪辑合成' }, {})` directly, then assert file existence. For the degraded test case, pass a phaseConfig with a non-existent videoPath or set `pipeline.config.degradedMode = true` AND pre-break the compose by removing video_tasks.json.
  </action>
  <verify>
    <automated>cd /data/workspace/kais-movie-agent && node --test test/phases/composition-master-mp4.test.mjs 2>&1 | tail -20</automated>
  </verify>
  <done>
    - composition handler writes master.mp4 (not final.mp4) on success
    - composition handler writes web-preview.mp4 on success
    - composition handler touches 0-byte master.mp4 + web-preview.mp4 placeholders when compose throws
    - New test file test/phases/composition-master-mp4.test.mjs has ≥3 passing cases (rename, web-preview, degraded placeholder)
    - grep confirms `final.mp4` no longer appears in composition handler body (only delivery handler references remain — to be fixed in Plan 02)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| FFmpeg subprocess | composition handler invokes FFmpeg via CompositionEngine / execFile on pipeline workdir paths |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-01 | Tampering | FFmpeg outputPath path traversal | accept | outputPath derived from `pipeline.workdir` (trusted config) joined with literal filename; no user-controlled input reaches the path component |
| T-29-02 | Denial of Service | FFmpeg transcode hangs on corrupt master.mp4 | mitigate | web-preview transcode wrapped in try/catch with console.warn — failure does not block composition handler return |
| T-29-03 | Information Disclosure | 0-byte placeholder mp4 misinterpreted as real content | accept | Phase 30 SC#1 verifies degraded path explicitly; quality-report.json (Plan 02) will mark `_composition.delivered_mastermp4` state |
</threat_model>

<verification>
- `node --test test/phases/composition-master-mp4.test.mjs` passes (≥3 cases)
- Existing baseline still passes: `node --test test/phases/handlers.test.mjs` (composition handler still registered, still callable)
- `grep -n "final\.mp4" lib/phases/index.js` returns matches ONLY in delivery handler (lines ~3515-3531), NOT in composition handler
</verification>

<success_criteria>
- composition handler produces master.mp4 + web-preview.mp4 on the success path
- composition handler produces 0-byte master.mp4 + web-preview.mp4 placeholders on the degraded path
- No final.mp4 writes anywhere in the composition handler
- Regression test committed and passing
</success_criteria>

<output>
Create `.planning/phases/29-composition-tail-quality-gate-activation/29-01-SUMMARY.md` when done
</output>
