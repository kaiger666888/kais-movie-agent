---
phase: 29-composition-tail-quality-gate-activation
plan: 02
type: execute
wave: 2
depends_on: [29-01]
files_modified:
  - lib/phases/index.js
  - test/phases/delivery-master-mp4.test.mjs
autonomous: true
requirements: [PIPE-COMPOSE-02]

must_haves:
  truths:
    - "delivery handler checks for master.mp4 (not final.mp4) at the workdir root"
    - "When master.mp4 is present, quality-report.json records _composition.delivered_mastermp4: true"
    - "When master.mp4 is absent, quality-report.json records _composition.delivered_mastermp4: false with degrade note (delivery does not crash)"
    - "When web-preview.mp4 is absent, delivery handler emits a warn but does NOT fail (degrade-tolerant)"
  artifacts:
    - path: "lib/phases/index.js"
      provides: "delivery handler master.mp4-aligned check + web-preview degrade-tolerant check + _composition.delivered_mastermp4 marker in quality-report.json"
      contains: "delivered_mastermp4"
    - path: "test/phases/delivery-master-mp4.test.mjs"
      provides: "Unit test asserting master.mp4 detection + degrade-tolerant web-preview + _composition.delivered_mastermp4 marker"
      min_lines: 70
  key_links:
    - from: "lib/phases/index.js delivery handler"
      to: "output/{episode}/master.mp4"
      via: "stat() check on renamed composition output"
      pattern: "join\\(pipeline\\.workdir, 'master\\.mp4'\\)"
    - from: "lib/phases/index.js delivery handler"
      to: "quality-report.json"
      via: "_composition.delivered_mastermp4 field"
      pattern: "_composition"
---

<objective>
Align delivery handler's file check with composition's new output name. Change the check from `final.mp4` to `master.mp4`, add a degrade-tolerant `web-preview.mp4` check (warn-not-fail), and write a `_composition.delivered_mastermp4: true/false` marker into quality-report.json so operators can see at a glance whether composition actually delivered.

Purpose: PIPE-COMPOSE-02 closes the audit finding that delivery checks `final.mp4` while composition declares/produces `master.mp4` — even with Plan 01's composition fix, delivery would still fail to find the file without this alignment.

Output: Modified delivery handler in lib/phases/index.js + regression test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-composition-tail-quality-gate-activation/29-CONTEXT.md
@.planning/phases/29-composition-tail-quality-gate-activation/29-01-SUMMARY.md

<interfaces>
<!-- Delivery handler (lib/phases/index.js:3467) — the handler we modify -->
<!-- Plan 01 renamed composition output to master.mp4; this plan aligns the consumer. -->

From lib/phases/index.js lines ~3515-3542 (delivery handler `final.mp4` validation block):
- Line 3517: `const finalMp4Path = join(pipeline.workdir, 'final.mp4');` — TARGET OF RENAME to master.mp4
- Lines 3519-3533: `stat(finalMp4Path)` try/catch setting `finalMp4Status = 'present'` or building an absent note
- Line 3522-3526: writes `qualityReport.final_mp4 = { path, size_bytes, size_mb }` — rename to `qualityReport.master_mp4`
- Line 3528-3532: writes absent note — rename field and message
- Lines 3536-3541: `qualityData` object assembled before writeFile(quality-report.json) — INJECT `_composition` marker here
- Line 3559: `_hermesAudit(... final_mp4_status: finalMp4Status ...)` — rename to master_mp4_status for consistency

No web-preview.mp4 check exists currently — ADD a degrade-tolerant stat block.

Plan 01 produced `master.mp4` + `web-preview.mp4` (real or 0-byte placeholder) — this plan makes delivery verify their existence.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: delivery handler master.mp4 alignment + web-preview degrade-tolerant + _composition marker</name>
  <files>lib/phases/index.js, test/phases/delivery-master-mp4.test.mjs</files>
  <read_first>
    - lib/phases/index.js lines 3467-3620 (delivery handler full body — confirm exact line numbers before editing; final.mp4 block at ~3515-3533, qualityData assembly at ~3536-3542, hermes audit at ~3555-3560)
    - lib/phases/index.js composition handler output paths (post-Plan-01 — should now write master.mp4 + web-preview.mp4)
    - test/phases/handlers.test.mjs lines 1-40 (delivery handler invocation pattern via phaseHandlers.delivery.after)
    - 29-01-SUMMARY.md (confirms composition output filenames Plan 01 shipped)
  </read_first>
  <behavior>
    - Test 1 (master.mp4 present): When master.mp4 exists in workdir, delivery handler succeeds and quality-report.json contains `_composition: { delivered_mastermp4: true }`.
    - Test 2 (master.mp4 absent): When master.mp4 does NOT exist, delivery handler does NOT throw; quality-report.json contains `_composition: { delivered_mastermp4: false }` with a degrade note.
    - Test 3 (web-preview degrade-tolerant): When web-preview.mp4 is absent but master.mp4 present, delivery handler succeeds with a warn in the log; quality-report.json records `_composition.delivered_webpreview: false` but delivery still returns success.
    - Test 4 (no final.mp4 reference): grep delivery handler body — no occurrence of the literal string `final.mp4` remains.
  </behavior>
  <action>
    Per PIPE-COMPOSE-02 and CONTEXT.md `<decisions>` (D-PIPE-COMPOSE-02: change delivery check to master.mp4; D-PIPE-COMPOSE-02: web-preview degrade-tolerant; D-PIPE-COMPOSE-02: write _composition.delivered_mastermp4 marker):

    1. In delivery handler (lib/phases/index.js ~line 3515-3533), rename `finalMp4Path` → `masterMp4Path` and change the joined filename from `'final.mp4'` to `'master.mp4'`. Rename `finalMp4Status` → `masterMp4Status`. Rename `qualityReport.final_mp4` → `qualityReport.master_mp4`. Update the absent note message from `'final.mp4 未生成 ...'` to `'master.mp4 未生成 (degraded 模式或 composition 未执行)'`.

    2. After the master.mp4 stat block, ADD a web-preview.mp4 degrade-tolerant block:
       - `const webPreviewPath = join(pipeline.workdir, 'web-preview.mp4');`
       - try `stat(webPreviewPath)` → `qualityReport.web_preview_mp4 = { path, size_bytes }` on success
       - catch → `console.warn('[delivery] web-preview.mp4 缺失 (degrade-tolerant, 不阻断)')` and `qualityReport.web_preview_mp4 = { path: webPreviewPath, status: 'absent', note: 'web-preview 未生成 (degrade-tolerant)' }`. DO NOT set any failure flag — web-preview absence is explicitly non-blocking per CONTEXT.md.

    3. In the `qualityData` object assembly (~line 3536), INJECT a `_composition` field computed from the stat results:
       - `_composition: { delivered_mastermp4: masterMp4Status === 'present', delivered_webpreview: <webPreviewStatus === 'present'> }`
       - This goes at the top level of `qualityData` (sibling to `_phase`), per CONTEXT.md "Claude's Discretion" placement choice (top-level for operator visibility).

    4. Update `_hermesAudit` call (~line 3555-3560) to rename `final_mp4_status` → `master_mp4_status` and add `web_preview_status: webPreviewStatus`.

    5. Do NOT touch composition handler (Plan 01 territory) or consistency-guard (Plan 03 territory). Only the delivery handler block (~lines 3467-3620) is in scope.

    Test file pattern: mkdtemp workdir, instantiate Pipeline, manually write a `master.mp4` (can be 0-byte) into workdir for the present case, omit it for the absent case, call `await phaseHandlers.delivery.after(pipeline, { id: 'delivery', stageOrder: 19, name: '质检与交付' }, {})` directly, then read back `{workdir}/quality-report.json` and assert `_composition.delivered_mastermp4` / `_composition.delivered_webpreview` values.
  </action>
  <verify>
    <automated>cd /data/workspace/kais-movie-agent && node --test test/phases/delivery-master-mp4.test.mjs 2>&1 | tail -20</automated>
  </verify>
  <done>
    - delivery handler checks master.mp4 (not final.mp4)
    - delivery handler degrade-tolerates web-preview.mp4 absence (warn, no fail)
    - quality-report.json top-level `_composition.delivered_mastermp4` reflects actual master.mp4 presence
    - quality-report.json top-level `_composition.delivered_webpreview` reflects actual web-preview.mp4 presence
    - New test file test/phases/delivery-master-mp4.test.mjs has ≥4 passing cases
    - grep confirms no `final.mp4` literal remains anywhere in lib/phases/index.js
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Filesystem → delivery handler | delivery stat()s files written by composition handler in the same workdir |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-04 | Tampering | Attacker swaps master.mp4 between composition and delivery | accept | Both phases run in the same trusted workdir within a single pipeline run; no cross-trust-boundary file exchange |
| T-29-05 | Denial of Service | delivery crashes if master.mp4 stat throws unexpected error | mitigate | Existing try/catch preserved; absent path writes degrade note instead of throwing |
| T-29-06 | Information Disclosure | quality-report.json leaks absolute workdir paths | accept | Workdir is operator-controlled local path; report is consumed by operators, not external users |
</threat_model>

<verification>
- `node --test test/phases/delivery-master-mp4.test.mjs` passes (≥4 cases)
- `grep -n "final\.mp4" lib/phases/index.js` returns zero matches (Plan 01 removed composition references, Plan 02 removes delivery references)
- `grep -n "delivered_mastermp4" lib/phases/index.js` returns ≥2 matches (the write site + hermes audit)
</verification>

<success_criteria>
- delivery handler validates master.mp4 (aligned with Plan 01's composition output)
- web-preview.mp4 absence is degrade-tolerant (warn only)
- quality-report.json exposes `_composition.delivered_mastermp4` for operator visibility
- Regression test committed and passing
</success_criteria>

<output>
Create `.planning/phases/29-composition-tail-quality-gate-activation/29-02-SUMMARY.md` when done
</output>
