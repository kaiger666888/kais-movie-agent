---
phase: 42-feedback-ingestion
plan: 02
type: execute
wave: 2
depends_on: [42-01]
files_modified:
  - /data/workspace/hermes-agent/plugins/pipeline_state/recipe_library.py
  - /data/workspace/hermes-agent/plugins/kais_aigc/feedback_ingest.py
  - /data/workspace/hermes-agent/plugins/kais_aigc/tests/test_feedback_validation.py
  - /data/workspace/hermes-agent/plugins/pipeline_state/tests/test_recipe_library_continuous_ci.py
autonomous: true
requirements: [FEEDBACK-INGEST-02, FEEDBACK-INGEST-04, FEEDBACK-INGEST-06]
user_setup: []

must_haves:
  truths:
    - "HMAC-SHA256 verification helper exists with 5-min timestamp window + hmac.compare_digest"
    - "Payload schema validation rejects malformed JSON / missing fields with 422-shape response"
    - "Semantic validation rejects metrics outside [0,1] / unknown platform with 400-shape response"
    - "Continuous binomial rate Wilson CI works (passed += 0.48, total += 1.0 per feedback)"
    - "RecipeLibrary.update_validation supports the continuous-rate path"
    - "Validation pipeline order: signature(401) → schema(422) → semantic(400) → episode existence(404)"
    - "Each rejection logs to feedback-rejected JSONL slot"
  artifacts:
    - path: "/data/workspace/hermes-agent/plugins/kais_aigc/feedback_ingest.py"
      provides: "_verify_signature, _validate_schema, _validate_semantic, _lookup_episode helpers + submit_feedback full impl"
      contains: "_verify_signature"
    - path: "/data/workspace/hermes-agent/plugins/pipeline_state/recipe_library.py"
      provides: "_wilson_ci signature updated to Union[int, float]; new continuous-rate path in update_validation"
      contains: "Union"
    - path: "/data/workspace/hermes-agent/plugins/kais_aigc/tests/test_feedback_validation.py"
      provides: "All 4 validation stages tested + continuous-CI closure test"
      min_lines: 200
    - path: "/data/workspace/hermes-agent/plugins/pipeline_state/tests/test_recipe_library_continuous_ci.py"
      provides: "RecipeLibrary continuous CI tests"
      min_lines: 80
  key_links:
    - from: "plugins/kais_aigc/feedback_ingest.py"
      to: "plugins/pipeline_state/recipe_library.py"
      via: "recipe_library.update_validation(recipe_id, platform, completion_rate, sample_size_delta=1)"
      pattern: "_rl\\.update_validation"
    - from: "plugins/kais_aigc/feedback_ingest.py::_lookup_episode"
      to: "RecipeLibrary.get_recipe_by_episode OR list_recipes scan"
      via: "Episode existence check (404 if missing)"
      pattern: "episode"
---

<objective>
Build the validation pipeline that FeedbackIngestClient.submit_feedback uses to gate incoming feedback. Four stages with strict order (signature → schema → semantic → episode existence), each failure logged to feedback-rejected JSONL. Implement continuous-binomial-rate Wilson CI support in RecipeLibrary (passed += completion_rate, total += 1.0 per feedback — preserves information vs binary pass/fail).

Purpose: This is the security + correctness spine of Phase 42. HMAC BEFORE JSON parsing is a deliberate DoS mitigation (reject invalid signatures without burning CPU on potentially-malicious JSON). Continuous Wilson CI is mathematically correct — Wilson score interval works for continuous `p` in [0,1], not just binary.

Output: 4 new helper functions in feedback_ingest.py, full submit_feedback implementation, RecipeLibrary.update_validation float-completion_rate path, 2 new test files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/42-feedback-ingestion/42-CONTEXT.md

@/data/workspace/hermes-agent/plugins/kais_aigc/feedback_ingest.py
@/data/workspace/hermes-agent/plugins/kais_aigc/review_platform.py
@/data/workspace/hermes-agent/plugins/pipeline_state/recipe_library.py
@/data/workspace/hermes-agent/plugins/pipeline_state/asset_bus.py

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From plugins/kais_aigc/review_platform.py::verify_callback (HMAC template — Phase 42 INCOMING verification mirrors this):
```python
CALLBACK_TIMESTAMP_TOLERANCE_SEC = 300  # 5-minute window

def verify_callback(self, body, signature, timestamp) -> bool:
    # Step 1: dev-mode escape (no secret → accept all) — Phase 42 REVERSES this:
    #   Phase 42 ALWAYS requires a secret (production-facing endpoint). No dev escape.
    # Step 2: timestamp window check — abs(now - ts) > 300 → reject
    # Step 3: constant-time HMAC match:
    body_bytes = body.encode("utf-8") if isinstance(body, str) else body
    expected = "sha256=" + hmac.new(
        secret.encode("utf-8"), body_bytes, hashlib.sha256,
    ).hexdigest()
    return hmac.compare_digest(expected, signature)
```

From plugins/pipeline_state/recipe_library.py (Wilson CI + update_validation — Phase 42 needs float support):
```python
# CURRENT (Phase 41 — int-only passed):
def _wilson_ci(passed: int, total: int, z: float = 1.96) -> tuple[float, float]:
    if total <= 0: return (0.0, 1.0)
    p = passed / total
    ...

# CURRENT update_validation (Phase 41 — int-passed path, lines 764-785):
old_v = latest["validation"]
old_sample = old_v.get("sample_size", 0)
old_cr = old_v.get("completion_rate", 0.0)
new_sample_size = old_sample + sample_size_delta
cumulative_passed = old_cr * old_sample
new_completion_rate = (cumulative_passed + float(completion_rate) * sample_size_delta) / new_sample_size
passed_int = int(round(new_completion_rate * new_sample_size))   # ← QUANTIZATION LOSS
lower, upper = _wilson_ci(passed_int, new_sample_size)            # ← Phase 42 must call with float
```

Phase 42 changes (LOCKED in CONTEXT.md "Wilson CI"):
1. `_wilson_ci(passed: Union[int, float], total: Union[int, float], z: float = 1.96)` — accept float
2. New optional `use_continuous_rate: bool = False` param on update_validation:
   - When True: passed = completion_rate (float), total = 1.0 — preserves information
   - When False (default): preserves Phase 41 int-passed behavior (no regression)
3. CI format string: `f"±{int(round((upper - lower) / 2 * 100))}%"` — UNCHANGED

From plugins/pipeline_state/recipe_library.py::RecipeLibrary (Phase 42 needs episode lookup):
```python
# CURRENT methods (Phase 41):
def list_recipes(self, *, genre=None, converged=None) -> list[dict]: ...
def get_recipe(self, recipe_id: str, *, version=None) -> dict: ...

# Phase 42 ADDS:
def get_recipe_by_episode(self, source_episode: str) -> dict | None:
    """Return latest recipe whose provenance.source_episode == source_episode, or None."""
```

From CONTEXT.md validation pipeline order (LOCKED):
1. Signature (401) — hmac.compare_digest BEFORE json.loads (DoS mitigation)
2. Schema (422) — json.loads succeeds + required fields present
3. Semantic (400) — metrics in [0,1] + platform in {douyin, bilibili, youtube}
4. Episode existence (404) — recipe exists for episode_id

Required feedback payload schema (FEEDBACK-INGEST-02):
```json
{
  "episode_id": "ep-001",
  "platform": "douyin",
  "metrics": {
    "completion_rate": 0.48,
    "interaction_rate": 0.12,
    "follow_rate": 0.03
  },
  "measured_at": "2026-06-27T10:30:00Z"
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add continuous-rate Wilson CI support to RecipeLibrary</name>
  <files>/data/workspace/hermes-agent/plugins/pipeline_state/recipe_library.py, /data/workspace/hermes-agent/plugins/pipeline_state/tests/test_recipe_library_continuous_ci.py</files>
  <read_first>
    - /data/workspace/hermes-agent/plugins/pipeline_state/recipe_library.py (lines 292-336 — _wilson_ci + _is_converged current impl; lines 692-815 — update_validation current impl)
    - /data/workspace/hermes-agent/plugins/pipeline_state/tests/test_recipe_library_update_validation.py (full file — Phase 41 test patterns to mirror; verify Phase 42 changes don't regress them)
  </read_first>
  <behavior>
    - Test 1: _wilson_ci(0.48, 1.0) returns valid (lower, upper) tuple with upper - lower > 0.10 (wide for small float total)
    - Test 2: _wilson_ci(48.0, 100.0) returns nearly identical bounds to _wilson_ci(48, 100) — float path equivalent to int path for whole-number floats
    - Test 3: _wilson_ci(0, 0) still returns (0.0, 1.0) — divide-by-zero mitigation preserved
    - Test 4: update_validation(..., completion_rate=0.48, use_continuous_rate=True) appends a new version row with validation.completion_rate = 0.48 (not rounded)
    - Test 5: After 10 continuous-rate updates with cr=0.5, sample_size == 10 and converged may be true (depending on Wilson CI math)
    - Test 6: update_validation(..., use_continuous_rate=False) preserves Phase 41 int-passed behavior (regression guard)
    - Test 7: Phase 41's existing test_recipe_library_update_validation.py still passes UNCHANGED (regression)
    - Test 8: get_recipe_by_episode("ep-001") returns latest recipe with provenance.source_episode == "ep-001"
    - Test 9: get_recipe_by_episode("unknown") returns None (not KeyError — differs from get_recipe)
  </behavior>
  <action>
    Create the test file first (RED), then update recipe_library.py (GREEN).

    Changes to recipe_library.py:

    1. Update `_wilson_ci` signature:
       `def _wilson_ci(passed: Union[int, float], total: Union[int, float], z: float = 1.96) -> tuple[float, float]:`
       Need to add `from typing import Union` (or use `int | float` if Python 3.10+ syntax is already in use elsewhere — check existing imports first).
       Body math unchanged — `p = passed / total` already works for floats. The change is type-annotation + the divide-by-zero guard already handles total<=0.

    2. Add new method `get_recipe_by_episode(self, source_episode: str) -> dict | None`:
       - Scan list_recipes() output for matching provenance.source_episode
       - Return latest version of first match, or None
       - DO NOT raise KeyError (differs from get_recipe) — episode lookup is best-effort

    3. Update `update_validation` — add `use_continuous_rate: bool = False` parameter:
       - When False (default): existing Phase 41 int-passed path — NO regression
       - When True: skip the `passed_int = int(round(...))` quantization; call `_wilson_ci(cumulative_passed_float, new_sample_size)` directly where cumulative_passed_float = old_cr * old_sample + completion_rate * sample_size_delta
       - The validation.completion_rate field stored in the row is the running average (same formula as Phase 41) — this is the OUTPUT; the CONTINUOUS RATE only affects the Wilson CI computation input
       - Add a docstring note explaining the continuous-rate option and that Phase 42 feedback_ingest.py uses use_continuous_rate=True

    Update the module docstring near the top: "Phase 42 addition: update_validation(use_continuous_rate=True) preserves completion_rate information by passing the float rate directly to _wilson_ci (passed += cr, total += 1.0 per feedback). Phase 41 default (False) preserved unchanged."

    Test file structure: TestContinuousWilsonCi class with Tests 1-3, TestUpdateValidationContinuous class with Tests 4-6, TestGetRecipeByEpisode class with Tests 8-9, plus a subprocess regression test (Test 7) that runs test_recipe_library_update_validation.py in a clean subprocess (mirror the Phase 41 v50_regression pattern).
  </action>
  <verify>
    <automated>cd /data/workspace/hermes-agent && python -m pytest plugins/pipeline_state/tests/test_recipe_library_continuous_ci.py plugins/pipeline_state/tests/test_recipe_library_update_validation.py -v</automated>
  </verify>
  <done>
    - _wilson_ci accepts Union[int, float] for passed and total
    - update_validation(use_continuous_rate=True) path works without quantization
    - update_validation(use_continuous_rate=False) preserves Phase 41 behavior (Test 7 GREEN)
    - get_recipe_by_episode method added, returns None on unknown (not KeyError)
    - All 9 new tests GREEN + Phase 41 update_validation tests still GREEN
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement HMAC verification + 4-stage validation pipeline in feedback_ingest.py</name>
  <files>/data/workspace/hermes-agent/plugins/kais_aigc/feedback_ingest.py, /data/workspace/hermes-agent/plugins/kais_aigc/tests/test_feedback_validation.py</files>
  <read_first>
    - /data/workspace/hermes-agent/plugins/kais_aigc/review_platform.py (lines 311-368 — verify_callback template, exact HMAC primitive to mirror)
    - /data/workspace/hermes-agent/plugins/kais_aigc/feedback_ingest.py (42-01 skeleton — extend with helpers)
    - /data/workspace/hermes-agent/plugins/pipeline_state/recipe_library.py::update_validation (Phase 42 contract from Task 1)
  </read_first>
  <behavior>
    - Test 1: _verify_signature(body, valid_sig, secret) returns True
    - Test 2: _verify_signature(body, "sha256=invalid", secret) returns False
    - Test 3: _verify_signature(body, valid_sig, "") raises or returns False when secret is None (Phase 42 ALWAYS requires secret — NO dev-mode escape unlike review_platform)
    - Test 4: _verify_signature(body, sig_without_sha256_prefix, secret) returns False (header must start with "sha256=")
    - Test 5: _validate_schema(b'{"episode_id":"ep-001","platform":"douyin","metrics":{"completion_rate":0.48,"interaction_rate":0.12,"follow_rate":0.03},"measured_at":"2026-06-27T10:30:00Z"}') returns parsed dict
    - Test 6: _validate_schema(b'malformed json') raises SchemaError with 422 status hint
    - Test 7: _validate_schema(b'{"episode_id":"ep-001"}') raises SchemaError (missing platform, metrics, measured_at)
    - Test 8: _validate_semantic({"platform":"douyin","metrics":{...valid...}}) returns None (passes)
    - Test 9: _validate_semantic({"platform":"twitter",...}) raises SemanticError (unknown platform)
    - Test 10: _validate_semantic({"platform":"douyin","metrics":{"completion_rate":1.5,...}}) raises SemanticError (out of [0,1])
    - Test 11: client.submit_feedback(valid_body, valid_sig) writes 1 line to feedback-data slot with feedback_id + signature_valid=true
    - Test 12: client.submit_feedback(valid_body, valid_sig) calls recipe_library.update_validation(recipe_id, platform, completion_rate, sample_size_delta=1, use_continuous_rate=True)
    - Test 13: client.submit_feedback(invalid_sig_body, invalid_sig) returns dict with status="rejected", reason="signature", http_status=401 AND writes 1 line to feedback-rejected slot
    - Test 14: client.submit_feedback(malformed_json_body, valid_sig_for_that_body) returns status="rejected", reason="schema", http_status=422 AND writes to feedback-rejected
    - Test 15: client.submit_feedback(out_of_range_metrics_body, valid_sig) returns status="rejected", reason="semantic", http_status=400 AND writes to feedback-rejected
    - Test 16: client.submit_feedback(unknown_episode_body, valid_sig) returns status="rejected", reason="episode_not_found", http_status=404 AND writes to feedback-rejected
    - Test 17: RecipeLibrary is NEVER called when any validation stage fails (recipe_library.update_validation call count == 0 after rejected submissions)
    - Test 18: feedback_id is a stable hash (e.g., sha256 of body+timestamp first 16 hex chars)
  </behavior>
  <action>
    Create the test file first (RED), then extend feedback_ingest.py (GREEN).

    Add to feedback_ingest.py:

    1. Module-level constants:
       - TIMESTAMP_TOLERANCE_SEC = 300  # 5-min window (CONTEXT.md decision)
       - VALID_PLATFORMS = frozenset({"douyin", "bilibili", "youtube"})
       - REQUIRED_FIELDS = ("episode_id", "platform", "metrics", "measured_at")
       - REQUIRED_METRICS = ("completion_rate", "interaction_rate", "follow_rate")

    2. Exception classes (mirror ReviewClientError pattern):
       - class FeedbackValidationError(Exception): with .reason + .http_status attributes
       - Subclasses or reason strings: "signature" (401), "schema" (422), "semantic" (400), "episode_not_found" (404)

    3. Helper functions (module-level, pure — testable in isolation):

       def _verify_signature(body: bytes, signature: str, secret: str | None) -> bool:
           """HMAC-SHA256 verification with sha256= prefix requirement.
           Phase 42 ALWAYS requires a secret — NO dev-mode escape (unlike review_platform).
           Returns False if: secret is None/empty, signature lacks 'sha256=' prefix,
           or hmac.compare_digest fails."""
           if not secret:
               logger.warning("feedback_ingest: secret not configured — rejecting (NO dev escape)")
               return False
           if not signature.startswith("sha256="):
               return False
           expected = "sha256=" + hmac.new(
               secret.encode("utf-8"), body, hashlib.sha256
           ).hexdigest()
           return hmac.compare_digest(expected, signature)

       def _validate_schema(body: bytes) -> dict:
           """Parse JSON body + validate required fields.
           Raises FeedbackValidationError(reason='schema', http_status=422) on malformed
           JSON or missing required fields."""
           try:
               payload = json.loads(body)
           except (ValueError, TypeError) as e:
               raise FeedbackValidationError("schema", f"malformed JSON: {e}", 422)
           if not isinstance(payload, dict):
               raise FeedbackValidationError("schema", "payload must be JSON object", 422)
           for field in REQUIRED_FIELDS:
               if field not in payload:
                   raise FeedbackValidationError("schema", f"missing field: {field}", 422)
           metrics = payload.get("metrics", {})
           if not isinstance(metrics, dict):
               raise FeedbackValidationError("schema", "metrics must be object", 422)
           for m in REQUIRED_METRICS:
               if m not in metrics:
                   raise FeedbackValidationError("schema", f"missing metric: {m}", 422)
           return payload

       def _validate_semantic(payload: dict) -> None:
           """Validate platform enum + metrics ranges.
           Raises FeedbackValidationError(reason='semantic', http_status=400)."""
           if payload["platform"] not in VALID_PLATFORMS:
               raise FeedbackValidationError(
                   "semantic",
                   f"unknown platform: {payload['platform']}",
                   400,
               )
           metrics = payload["metrics"]
           for m in REQUIRED_METRICS:
               v = metrics[m]
               if not isinstance(v, (int, float)) or isinstance(v, bool):
                   raise FeedbackValidationError("semantic", f"{m} must be number", 400)
               if not (0.0 <= float(v) <= 1.0):
                   raise FeedbackValidationError(
                       "semantic", f"{m} out of [0,1]: {v}", 400,
                   )

    4. Replace the stub submit_feedback with the full implementation:

       def submit_feedback(self, body: bytes, signature: str) -> dict:
           """Full validation pipeline (4 stages, strict order) + RecipeLibrary update.

           Order: signature(401) → schema(422) → semantic(400) → episode existence(404).
           HMAC verification BEFORE JSON parsing (DoS mitigation — reject without
           burning CPU on potentially-malicious JSON). Each failure logs to
           feedback-rejected JSONL slot."""
           import uuid  # local import keeps top-of-file imports clean
           feedback_id = hashlib.sha256(body).hexdigest()[:16]
           ts = _now_iso()  # reuse Phase 41 helper if importable, else inline datetime.now(timezone.utc).isoformat()

           # Stage 1: signature (HMAC BEFORE JSON parse — DoS mitigation)
           if not _verify_signature(body, signature, self._secret):
               self._reject(feedback_id, "signature", body, ts)
               return {"status": "rejected", "reason": "signature", "http_status": 401, "feedback_id": feedback_id}

           # Stage 2: schema
           try:
               payload = _validate_schema(body)
           except FeedbackValidationError as e:
               self._reject(feedback_id, e.reason, body, ts)
               return {"status": "rejected", "reason": e.reason, "http_status": e.http_status, "feedback_id": feedback_id}

           # Stage 3: semantic
           try:
               _validate_semantic(payload)
           except FeedbackValidationError as e:
               self._reject(feedback_id, e.reason, body, ts)
               return {"status": "rejected", "reason": e.reason, "http_status": e.http_status, "feedback_id": feedback_id}

           # Stage 4: episode existence (404 if no recipe for episode_id)
           recipe = self._rl.get_recipe_by_episode(payload["episode_id"])
           if recipe is None:
               self._reject(feedback_id, "episode_not_found", body, ts)
               return {"status": "rejected", "reason": "episode_not_found", "http_status": 404, "feedback_id": feedback_id}

           # All 4 stages passed — persist + trigger RecipeLibrary update
           record = {
               "feedback_id": feedback_id,
               "episode_id": payload["episode_id"],
               "platform": payload["platform"],
               "metrics": payload["metrics"],
               "measured_at": payload["measured_at"],
               "received_at": ts,
               "signature_valid": True,
               "recipe_id": recipe["recipe_id"],
           }
           self._bus.append_line(SLOT_FEEDBACK_DATA, record)
           # Continuous-rate Wilson CI update (CONTEXT.md "Wilson CI continuous binomial rate")
           self._rl.update_validation(
               recipe["recipe_id"],
               payload["platform"],
               payload["metrics"]["completion_rate"],
               sample_size_delta=1,
               use_continuous_rate=True,
           )
           return {"status": "accepted", "feedback_id": feedback_id, "recipe_id": recipe["recipe_id"]}

       def _reject(self, feedback_id: str, reason: str, body: bytes, ts: str) -> None:
           """Write a rejection record to feedback-rejected JSONL slot. NEVER raises."""
           snippet = body.decode("utf-8", errors="replace")[:200]
           try:
               self._bus.append_line(SLOT_FEEDBACK_REJECTED, {
                   "feedback_id": feedback_id,
                   "reason": reason,
                   "payload_snippet": snippet,
                   "timestamp": ts,
               })
           except Exception as e:
               logger.error("feedback_ingest: failed to log rejection %s: %s", feedback_id, e)

       Add `_now_iso` helper if not already imported from recipe_library (it's defined in recipe_library.py as a module-private — either re-define locally or `from plugins.pipeline_state.recipe_library import _now_iso` — local redefinition is cleaner to avoid private-symbol import).

    Update module __all__ to include: ["FeedbackIngestClient", "FeedbackValidationError", "_verify_signature", "_validate_schema", "_validate_semantic"] (export helpers so tests + future 42-03 server can call them).
  </action>
  <verify>
    <automated>cd /data/workspace/hermes-agent && python -m pytest plugins/kais_aigc/tests/test_feedback_validation.py -v</automated>
  </verify>
  <done>
    - 4 helper functions (_verify_signature, _validate_schema, _validate_semantic, _reject) implemented
    - submit_feedback full pipeline works end-to-end (signature → schema → semantic → episode existence)
    - All 4 rejection paths write to feedback-rejected JSONL with correct reason
    - RecipeLibrary.update_validation called only on full pass, with use_continuous_rate=True
    - 18/18 validation tests GREEN
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Network → submit_feedback | Untrusted HTTP body crosses here — HMAC is the gate |
| submit_feedback → RecipeLibrary | Trusted internal call (already-validated data) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-42-03 | Spoofing | submit_feedback signature | mitigate | HMAC-SHA256 with `hmac.compare_digest` (constant-time). 5-min timestamp window. NO dev-mode escape (always requires secret). |
| T-42-04 | Tampering | submit_feedback body | mitigate | Signature gate happens BEFORE json.loads — malicious JSON rejected without burning CPU (DoS mitigation). Schema validation catches missing/extra fields. |
| T-42-05 | Repudiation | feedback-data records | mitigate | Every accepted feedback gets a feedback_id (sha256[:16] of body) + received_at timestamp. Every rejection gets logged to feedback-rejected. |
| T-42-06 | Information Disclosure | Rejection logs may leak body | mitigate | _reject stores only first 200 chars as payload_snippet (truncated), not full body. |
| T-42-07 | Denial of Service | Malformed JSON body | mitigate | HMAC verification first — invalid signatures rejected before JSON parsing burns CPU. |
| T-42-08 | Elevation of Privilege | RecipeLibrary update on bad data | mitigate | All 4 validation stages must pass before update_validation is called. Test 17 asserts recipe_library is NEVER called on rejection. |
| T-42-09 | Tampering | RecipeLibrary._wilson_ci float math | accept | Wilson score interval formula is mathematically correct for continuous `p` in [0,1] (CONTEXT.md specifics). Existing Phase 41 int-path preserved unchanged (regression test 7). |
</threat_model>

<verification>
- 4-stage validation pipeline enforces strict order: signature → schema → semantic → episode existence
- Each rejection logged to feedback-rejected with reason + truncated payload_snippet
- RecipeLibrary.update_validation(use_continuous_rate=True) preserves float completion_rate
- _wilson_ci type annotation accepts Union[int, float]
- Phase 41 regression: existing test_recipe_library_update_validation.py still GREEN
</verification>

<success_criteria>
- 9 continuous-CI tests GREEN (test_recipe_library_continuous_ci.py)
- 18 validation tests GREEN (test_feedback_validation.py)
- Phase 41 update_validation regression suite GREEN (unchanged)
- HMAC verification uses hmac.compare_digest (constant-time, NOT ==)
- HMAC verification happens BEFORE json.loads (DoS mitigation)
- RecipeLibrary.update_validation called only on full validation pass
</success_criteria>

<output>
Create `.planning/phases/42-feedback-ingestion/42-02-SUMMARY.md` when done
</output>
