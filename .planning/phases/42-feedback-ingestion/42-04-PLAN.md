---
phase: 42-feedback-ingestion
plan: 04
type: execute
wave: 4
depends_on: [42-03]
files_modified:
  - /data/workspace/hermes-agent/plugins/kais_aigc/tests/test_feedback_ingest_integration.py
  - /data/workspace/hermes-agent/plugins/kais_aigc/tests/test_v50_regression_phase42.py
  - /data/workspace/kais-movie-agent/.planning/phases/42-feedback-ingestion/42-SUMMARY.md
autonomous: true
requirements: [FEEDBACK-INGEST-01, FEEDBACK-INGEST-02, FEEDBACK-INGEST-03, FEEDBACK-INGEST-04, FEEDBACK-INGEST-05, FEEDBACK-INGEST-06]
user_setup: []

must_haves:
  truths:
    - "End-to-end feedback loop closes: feedback-data → RecipeLibrary.update_validation → recipe validation.completion_rate + CI + converged flag updated"
    - "RecipeLibrary Wilson CI converges after >=10 continuous-rate updates with stable completion_rate"
    - "Structural 'no auto-modify pipeline' check passes — feedback_ingest.py has NO import of p10b/runner/preview_engine"
    - "V5.0 502-test + Phase 40 174-test + Phase 41 91-test regression baselines preserved"
    - "All 6 FEEDBACK-INGEST-XX requirements satisfied (full coverage)"
  artifacts:
    - path: "/data/workspace/hermes-agent/plugins/kais_aigc/tests/test_feedback_ingest_integration.py"
      provides: "E2E tests for full validation pipeline + convergence closure + multi-feedback flow"
      min_lines: 250
    - path: "/data/workspace/hermes-agent/plugins/kais_aigc/tests/test_v50_regression_phase42.py"
      provides: "V5.0 + Phase 40 + Phase 41 brownfield regression guard for Phase 42"
      min_lines: 200
    - path: "/data/workspace/kais-movie-agent/.planning/phases/42-feedback-ingestion/42-SUMMARY.md"
      provides: "Phase 42 SUMMARY documenting v6.0 milestone completion"
  key_links:
    - from: "test_feedback_ingest_integration.py"
      to: "plugins/kais_aigc/feedback_ingest.py + plugins/pipeline_state/recipe_library.py"
      via: "E2E flow: HTTP POST → submit_feedback → feedback-data slot + update_validation → recipe validation{} updated"
      pattern: "update_validation"
    - from: "test_v50_regression_phase42.py"
      to: "asset_bus.py + recipe_library.py + feedback_ingest.py"
      via: "subprocess pytest runs of V5.0/Phase 40/Phase 41 canonical test files + ASSET_SCHEMA set equality"
      pattern: "EXPECTED_SLOTS"
---

<objective>
Final phase-42 plan: lock in the convergence closure end-to-end (HTTP POST → HMAC verify → feedback-data persist → RecipeLibrary.update_validation → Wilson CI recomputation → converged flag), enforce the STRUCTURAL "no auto-modify pipeline" invariant via a grep test (FEEDBACK-INGEST-05), and run the full V5.0 + Phase 40 + Phase 41 brownfield regression guard to prove no existing test breaks. This is the v6.0 milestone ship gate.

Purpose: Phase 42 is the LAST phase of v6.0. This plan verifies the "最速收敛闭环" (rapid convergence loop) actually closes — feedback data flows into recipe validation updates, and the system never auto-modifies the pipeline. The structural check is the load-bearing invariant: absence of forbidden imports IS the enforcement, not a config flag.

Output: 2 new test files (integration + regression), 1 phase SUMMARY.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/42-feedback-ingestion/42-CONTEXT.md

@/data/workspace/hermes-agent/plugins/kais_aigc/feedback_ingest.py
@/data/workspace/hermes-agent/plugins/pipeline_state/recipe_library.py
@/data/workspace/hermes-agent/plugins/pipeline_state/asset_bus.py

<interfaces>
<!-- Extracted from the codebase after 42-01/02/03 ship. -->

Expected FeedbackIngestClient surface (post 42-03):
```python
class FeedbackIngestClient:
    def __init__(self, *, asset_bus, recipe_library, port=None, secret=None): ...
    def get_feedback(self, *, episode_id=None) -> list[dict]: ...
    def submit_feedback(self, body: bytes, signature: str) -> dict:
        """Returns {'status': 'accepted'|'rejected', 'http_status': int, 'reason'?: str, 'feedback_id': str, 'recipe_id'?: str}"""
    def list_pending_updates(self, *, limit=10) -> list[dict]: ...
    def close(self) -> None: ...
    def __enter__(self): ...
    def __exit__(self, *exc): ...

def start_feedback_server(*, host="127.0.0.1", port=None, secret=None, recipe_library, asset_bus): ...
```

Expected RecipeLibrary.update_validation continuous-rate path (post 42-02):
```python
def update_validation(
    self, recipe_id, platform, completion_rate,
    sample_size_delta=1, use_continuous_rate=False,
) -> dict | None: ...

def get_recipe_by_episode(self, source_episode: str) -> dict | None: ...
```

Expected ASSET_SCHEMA after Phase 42 (V5.0 30 + Phase 40 2 + Phase 41 1 + Phase 42 2 = 35 slots):
```python
EXPECTED_SLOTS = {
    # V5.0 (30 slots — see test_v50_regression_phase41.py for full list)
    # Phase 40 (2): rapid-preview-clips, episode-meta
    # Phase 41 (1): emotion-recipe
    # Phase 42 (2): feedback-data, feedback-rejected  ← NEW
}
```

From REQUIREMENTS.md (FEEDBACK-INGEST-05 LOCKED):
> **不自动修改管线行为** — feedback 只更新配方库评分,绝不直接调用 p10b 改变 structure_delta。
> 配方消费方(operator / 下次创作决策)读取配方库做决策,系统不自动应用 — 人决策优先

From CONTEXT.md "Not auto-modify pipeline" enforcement (LOCKED):
> STRUCTURAL: FeedbackIngestClient has NO reference to pipeline.phases.p10b_rapid_preview, runner.py, preview_engine, or any pipeline-runner module
> Verified by import-graph check: `grep -E "from.*pipeline|import.*p10b|import.*runner|import.*preview_engine" plugins/kais_aigc/feedback_ingest.py` returns 0 matches
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: E2E integration tests for feedback → recipe convergence closure</name>
  <files>/data/workspace/hermes-agent/plugins/kais_aigc/tests/test_feedback_ingest_integration.py</files>
  <read_first>
    - /data/workspace/hermes-agent/plugins/kais_aigc/feedback_ingest.py (full current state)
    - /data/workspace/hermes-agent/plugins/pipeline_state/recipe_library.py (full current state — update_validation + get_recipe_by_episode)
    - /data/workspace/hermes-agent/plugins/pipeline_state/tests/test_recipe_library_integration.py (Pattern for E2E test setup — how to build a real recipe + simulate feedback flow)
    - /data/workspace/hermes-agent/plugins/kais_aigc/tests/test_feedback_validation.py (42-02 test patterns to extend)
  </read_first>
  <behavior>
    - Test 1 (E2E HAPPY PATH): Given a recipe exists (provenance.source_episode="ep-001"), when client.submit_feedback(valid_body_for_ep_001, valid_sig), then:
      (a) feedback-data slot has 1 record with signature_valid=true + recipe_id=<the recipe's id>
      (b) RecipeLibrary.get_recipe(recipe_id) returns version N+1 with validation.sample_size incremented by 1
      (c) validation.completion_rate is the running average (not the raw feedback value)
      (d) validation.last_validated is set to a recent ISO timestamp
    - Test 2 (CONVERGENCE): Create recipe with sample_size=0. Submit 10 feedbacks with completion_rate=0.5 each. After 10th:
      (a) recipe.validation.sample_size == 10
      (b) Wilson CI spread tight enough that converged may flip to True (assert sample_size>=10, do NOT assert converged=True since CI math depends on Python float precision)
      (c) confidence_interval string is "±N%" format
    - Test 3 (CONTINUOUS RATE PRESERVED): Submit feedback with completion_rate=0.48. Recipe validation.completion_rate (running avg with prev=0) becomes 0.48 exactly (not quantized to int-passed path).
    - Test 4 (REJECTION DOES NOT POLLUTE): Submit 4 rejected feedbacks (1 bad sig, 1 bad schema, 1 bad semantic, 1 unknown episode). Then submit 1 valid feedback.
      (a) feedback-data slot has exactly 1 record (only the valid one)
      (b) feedback-rejected slot has exactly 4 records with correct reasons
      (c) RecipeLibrary.get_recipe version is incremented exactly once (not 5 times)
    - Test 5 (MULTI-EPISODE): 2 recipes for 2 different episodes. Submit feedback for ep-002. Only ep-002's recipe validation updates; ep-001's recipe is unchanged.
    - Test 6 (LIST PENDING UPDATES INTEGRATION): Submit 3 feedbacks. client.list_pending_updates(limit=2) returns 2 records, newest first.
    - Test 7 (HTTP E2E): Using Starlette TestClient (in-process, no port binding), POST /api/v1/feedback with valid body + sig returns 200; recipe validation{} is updated as a side effect.
    - Test 8 (PLATFORM MATCHING): Submit feedback with platform="bilibili". Recipe validation.platform field is updated to "bilibili" (overrides the prior platform if different).
    - Test 9 (FULL FLOW METRICS PRESERVED): feedback-data record stores ALL of metrics{completion_rate, interaction_rate, follow_rate} even though only completion_rate feeds update_validation. Future-proofing per CONTEXT.md specifics.
    - Test 10 (NO RAISE ON BAD UPDATE_VALIDATION): If RecipeLibrary.update_validation returns None (e.g., AssetBus failure), submit_feedback still returns accepted (the feedback-data record was already persisted; update_validation failure is non-fatal — logged but not propagated).
  </behavior>
  <action>
    Create the test file (RED→GREEN — tests + implementation already exist from 42-01/02/03; this is verification).

    Test setup pattern (mirror test_recipe_library_integration.py):
    - Helper `_make_recipe(bus, episode_id, genre="Urban Fantasy")` — creates a real recipe via RecipeLibrary.create_recipe with a canonical structure{}
    - Helper `_make_feedback_body(episode_id, platform="douyin", cr=0.48, ir=0.12, fr=0.03)` — returns bytes of valid JSON
    - Helper `_sign(body, secret)` — returns "sha256=" + hmac.new(secret.encode(), body, hashlib.sha256).hexdigest()
    - Each test uses a fresh tmp_path AssetBus + new RecipeLibrary + new FeedbackIngestClient(asset_bus=bus, recipe_library=rl, secret="test-secret")

    Test 7 (HTTP E2E) uses `from starlette.testclient import TestClient` — synchronous, in-process, no port binding. Construct app via `_build_starlette_app(client)`, then `with TestClient(app) as c: response = c.post("/api/v1/feedback", content=body, headers={"X-Signature": sig})`.

    Test 10 needs a FakeRecipeLibrary whose update_validation returns None to verify the non-fatal path. Use unittest.mock.MagicMock with .update_validation.return_value = None OR a simple class stub.

    Each test docstring MUST cite the FEEDBACK-INGEST-XX requirement(s) it verifies (traceability for plan-checker).
  </action>
  <verify>
    <automated>cd /data/workspace/hermes-agent && python -m pytest plugins/kais_aigc/tests/test_feedback_ingest_integration.py -v</automated>
  </verify>
  <done>
    - 10/10 integration tests GREEN
    - E2E flow verified: HTTP → HMAC → schema → semantic → episode existence → feedback-data persist → RecipeLibrary.update_validation
    - Continuous-rate Wilson CI path exercised (Test 3)
    - Convergence-after-10-feedbacks exercised (Test 2)
    - Rejection-does-not-pollute-recipe-library verified (Test 4)
    - All 6 FEEDBACK-INGEST-XX requirements covered by at least one test
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: V5.0 + Phase 40 + Phase 41 brownfield regression guard + structural "no p10b import" check</name>
  <files>/data/workspace/hermes-agent/plugins/kais_aigc/tests/test_v50_regression_phase42.py</files>
  <read_first>
    - /data/workspace/hermes-agent/plugins/pipeline_state/tests/test_v50_regression_phase41.py (FULL — the exact pattern to mirror; Phase 42 is structurally identical but with the Phase 42 test files appended + EXPECTED_SLOTS expanded by 2)
    - /data/workspace/hermes-agent/plugins/kais_aigc/feedback_ingest.py (to identify the load-bearing file for the structural grep)
    - /data/workspace/hermes-agent/plugins/pipeline_state/asset_bus.py (to confirm ASSET_SCHEMA post-42-01 has 35 slots)
  </read_first>
  <behavior>
    - Test 1: V5.0 AssetBus canonical tests pass (subprocess isolation)
    - Test 2: V5.0 phase35 slot regression passes (subprocess)
    - Test 3: V5.0 creative_history tests pass (subprocess)
    - Test 4: V5.0 store tests pass (subprocess)
    - Test 5: Phase 40 p10b unit tests pass (subprocess)
    - Test 6: Phase 40 phase_registry_full passes (14 phases — p10b is inserted, not removed)
    - Test 7: Phase 40 runner_full_dag passes (p10 → p10b → p11 topology intact)
    - Test 8: Phase 40 v50_regression passes (excluding pre-existing flaky count test)
    - Test 9: Phase 41 v50_regression_phase41 passes (Phase 42 must not break Phase 41's own regression guard — subprocess)
    - Test 10: ASSET_SCHEMA contains exactly 35 slots (V5.0 30 + Phase 40 2 + Phase 41 1 + Phase 42 2). Assert set equality.
    - Test 11: JSONL_SLOTS frozenset unchanged at frozenset({"finetune-dataset"}) — Phase 42 did NOT add to JSONL_SLOTS
    - Test 12: All canonical V5.0/Phase 40/Phase 41 test files still exist on disk (no deletions)
    - Test 13 (LOAD-BEARING — STRUCTURAL "no auto-modify pipeline" check per FEEDBACK-INGEST-05): For feedback_ingest.py, run `grep -cE "from.*pipeline\.phases|import.*p10b|import.*runner|import.*preview_engine|from.*runner|from.*preview_engine" plugins/kais_aigc/feedback_ingest.py` and assert count == 0.
      - This is the STRUCTURAL enforcement of "feedback only updates recipe scores, NEVER directly modifies pipeline behavior"
      - The absence of imports IS the invariant — not a config flag, not a runtime check
    - Test 14 (openclaw regression on V5.0-baseline files Phase 42 modified): For asset_bus.py, grep -c openclaw == 0
    - Test 15 (openclaw regression on new Phase 42 module): For feedback_ingest.py, grep -c openclaw == 0
    - Test 16: Phase 42 test files (test_feedback_ingest_skeleton.py, test_feedback_validation.py, test_feedback_server.py, test_feedback_ingest_integration.py) each pass when run in clean subprocess (mirror Phase 41 Test 14 pattern)
    - Test 17 (AGGREGATE COUNT): Total pass count across pipeline_state + kais_aigc + skills/kais-movie-pipeline >= 700 (V5.0 502 + Phase 40 174 + Phase 41 91 + Phase 42 ~40 additions). Use >= to allow for additions.
  </behavior>
  <action>
    Create the test file (RED→GREEN — V5.0/Phase 40/Phase 41 should already pass; the new assertions are Tests 10, 13, 15, 16, 17).

    Structure (mirror test_v50_regression_phase41.py exactly):

    1. Constants block:
       - HERMES_ROOT = Path(__file__).resolve().parents[3]
       - EXPECTED_SLOTS = { ... 35 slots ... } — copy from test_v50_regression_phase41.py and ADD "feedback-data" + "feedback-rejected"
       - CANONICAL_TEST_FILES = [ ... V5.0 + Phase 40 files ... ] — copy unchanged
       - PHASE_41_TEST_FILES = [ ... 6 Phase 41 files ... ] — copy unchanged
       - PHASE_42_TEST_FILES = [
           "plugins/kais_aigc/tests/test_feedback_ingest_skeleton.py",
           "plugins/kais_aigc/tests/test_feedback_validation.py",
           "plugins/kais_aigc/tests/test_feedback_server.py",
           "plugins/kais_aigc/tests/test_feedback_ingest_integration.py",
         ]
       - V50_FILES_PHASE42_TOUCHED = [
           HERMES_ROOT / "plugins/pipeline_state/asset_bus.py",  # 42-01 added 2 slots
         ]
       - PHASE_42_NEW_MODULE = HERMES_ROOT / "plugins/kais_aigc/feedback_ingest.py"
       - BASELINE_TEST_COUNT = 700  # V5.0 502 + Phase 40 174 + Phase 41 91 + Phase 42 ~40

    2. Helper `_run_pytest(test_path, *extra)` — identical to Phase 41 helper.

    3. class TestV50RegressionPhase42:

       Tests 1-9: Per-file subprocess regression — copy from Phase 41 + add Test 9 for Phase 41's own regression suite (Phase 42 must not break Phase 41's regression).

       Test 10: ASSET_SCHEMA set equality with 35 slots. Include the comment about V5.0 30 + Phase 40 2 + Phase 41 1 + Phase 42 2.

       Test 11: JSONL_SLOTS unchanged.

       Test 12: No canonical test file deleted.

       Test 13 (LOAD-BEARING — STRUCTURAL): Use subprocess.run(["grep", "-cE", pattern, str(PHASE_42_NEW_MODULE)]). Pattern: "from.*pipeline\\.phases|import.*p10b|import.*runner|import.*preview_engine|from.*runner|from.*preview_engine"
       - IMPORTANT: grep -cE returns count of MATCHING LINES, not count of matches. So even one match fails the test.
       - The pattern is conservative — it would also flag legitimate imports of pipeline_state (different module) if phrased too broadly. The current pattern requires the literal tokens "p10b", "runner", or "preview_engine" so it's safe.
       - Document in the test docstring: "FEEDBACK-INGEST-05: structural enforcement. The absence of these imports IS the 'no auto-modify pipeline' invariant. If this test fails, someone added a forbidden import — the system would auto-modify the pipeline, violating the human-decision-first principle."

       Test 14: openclaw grep on V50_FILES_PHASE42_TOUCHED.

       Test 15: openclaw grep on PHASE_42_NEW_MODULE.

       Test 16: @pytest.mark.parametrize("test_file", PHASE_42_TEST_FILES) — each passes in clean subprocess.

       Test 17: Aggregate count >= 700. Scope: plugins/pipeline_state/tests/ + plugins/kais_aigc/tests/ + skills/kais-movie-pipeline/tests/. Exclude self + pre-existing flaky tests. Use SCOPED_THRESHOLD = 650 (allow some slack for test corpus changes).

    Be sure to add module docstring explaining: "Phase 42 brownfield regression guard. Phase 42 is the FINAL phase of v6.0 — this file is the v6.0 ship gate. Asserts V5.0 + Phase 40 + Phase 41 baselines preserved + Phase 42 structural invariants (no p10b import + openclaw-free) + Phase 42 test files pass."
  </action>
  <verify>
    <automated>cd /data/workspace/hermes-agent && python -m pytest plugins/kais_aigc/tests/test_v50_regression_phase42.py -v</automated>
  </verify>
  <done>
    - 17/17 regression tests GREEN
    - V5.0 + Phase 40 + Phase 41 canonical tests pass in clean subprocesses
    - ASSET_SCHEMA has exactly 35 slots (V5.0 30 + Phase 40 2 + Phase 41 1 + Phase 42 2)
    - JSONL_SLOTS unchanged at frozenset({"finetune-dataset"})
    - STRUCTURAL "no p10b/runner/preview_engine import" check PASSES (Test 13 — load-bearing for FEEDBACK-INGEST-05)
    - 0 openclaw references in V5.0-baseline files Phase 42 touched + new Phase 42 module
    - Aggregate test count >= 650 (scoped threshold)
  </done>
</task>

<task type="auto">
  <name>Task 3: Write Phase 42 SUMMARY + v6.0 milestone ship-ready declaration</name>
  <files>/data/workspace/kais-movie-agent/.planning/phases/42-feedback-ingestion/42-SUMMARY.md</files>
  <read_first>
    - /data/workspace/kais-movie-agent/.planning/phases/42-feedback-ingestion/42-01-SUMMARY.md (from 42-01)
    - /data/workspace/kais-movie-agent/.planning/phases/42-feedback-ingestion/42-02-SUMMARY.md (from 42-02)
    - /data/workspace/kais-movie-agent/.planning/phases/42-feedback-ingestion/42-03-SUMMARY.md (from 42-03)
    - /data/workspace/kais-movie-agent/.planning/STATE.md (current state — v6.0 progress)
  </read_first>
  <action>
    Write 42-SUMMARY.md following the standard GSD summary template. Contents:

    # Phase 42: Feedback Ingestion — SHIPPED

    ## What Shipped
    - Brief: FeedbackIngestClient + POST /api/v1/feedback (HMAC) + feedback-data/feedback-rejected JSONL slots + RecipeLibrary continuous-rate Wilson CI update + structural "no auto-modify pipeline" enforcement
    - v6.0 milestone COMPLETE (Phases 40-42 all shipped)

    ## Plans Delivered
    | Plan | Title | Tests Added |
    |------|-------|-------------|
    | 42-01 | AssetBus feedback slots + FeedbackIngestClient skeleton | N |
    | 42-02 | HMAC + 4-stage validation pipeline + continuous Wilson CI | N |
    | 42-03 | Starlette HTTP server + start_feedback_server + list_pending_updates | N |
    | 42-04 | E2E integration + V5.0/40/41 regression guard + structural check | N |
    (Fill in actual test counts from the prior SUMMARYs)

    ## Requirements Coverage
    - FEEDBACK-INGEST-01: FeedbackIngestClient with 3 methods (submit_feedback / get_feedback / list_pending_updates) ✓
    - FEEDBACK-INGEST-02: POST /api/v1/feedback + HMAC-SHA256 + 5-min window + compare_digest ✓
    - FEEDBACK-INGEST-03: feedback-data JSONL slot (append-only, signature_valid flag) ✓
    - FEEDBACK-INGEST-04: RecipeLibrary.update_validation trigger (continuous-rate Wilson CI + converged flag) ✓
    - FEEDBACK-INGEST-05: Structural "no auto-modify pipeline" enforcement (grep test T-42-13 — 0 matches for p10b/runner/preview_engine) ✓
    - FEEDBACK-INGEST-06: 4-stage data validation (signature/schema/semantic/episode existence) + feedback-rejected JSONL ✓

    ## v6.0 Milestone Complete
    - Phase 40: Rapid Preview Tier (4 plans, 174 tests)
    - Phase 41: Emotion Recipe Library (4 plans, ~91 tests)
    - Phase 42: Feedback Ingestion (4 plans, ~40+ tests)
    - TOTAL: 12 plans, ~700+ tests added on top of V5.0 502 baseline
    - "最速收敛闭环" (rapid convergence loop) closed: 调研萃取 → 配方建模 → 定向赛马 → 数据收敛 → 资产化沉淀

    ## Files Touched (Phase 42)
    - NEW: plugins/kais_aigc/feedback_ingest.py
    - MODIFIED (append-only): plugins/pipeline_state/asset_bus.py (+2 slots)
    - MODIFIED (continuous-rate path): plugins/pipeline_state/recipe_library.py (_wilson_ci Union[int,float> + update_validation use_continuous_rate param + get_recipe_by_episode method)
    - NEW tests: 6 files (skeleton, validation, server, integration, continuous_ci, v50_regression_phase42)

    ## Structural Invariants Verified
    - JSONL_SLOTS frozenset unchanged (frozenset({"finetune-dataset"}))
    - 0 openclaw references in V5.0-baseline files Phase 42 touched
    - 0 imports of p10b/runner/preview_engine in feedback_ingest.py (FEEDBACK-INGEST-05 structural enforcement)
    - ASSET_SCHEMA append-only (35 slots = 30 V5.0 + 2 Phase 40 + 1 Phase 41 + 2 Phase 42)

    ## Next: v6.0 Milestone Audit
    Run `/gsd:plan-phase 43` (or whichever phase number) for v6.0-MILESTONE-AUDIT.md creation, OR manually invoke the audit if no separate phase is desired.
  </action>
  <verify>
    <automated>test -f /data/workspace/kais-movie-agent/.planning/phases/42-feedback-ingestion/42-SUMMARY.md && grep -c "FEEDBACK-INGEST-0[1-6]" /data/workspace/kais-movie-agent/.planning/phases/42-feedback-ingestion/42-SUMMARY.md</automated>
  </verify>
  <done>
    - 42-SUMMARY.md exists at the expected path
    - All 6 FEEDBACK-INGEST-XX IDs mentioned in the SUMMARY (coverage traceable)
    - v6.0 milestone complete declaration present
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test environment ↔ hermes-agent codebase | Subprocess isolation prevents import-state leakage across regression-test invocations |
| feedback_ingest.py ↔ pipeline runner | STRUCTURAL absence of import — the boundary is enforced by lack of code path |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-42-13 | Elevation of Privilege | feedback_ingest auto-modifies pipeline | mitigate | LOAD-BEARING structural test: grep -cE for p10b/runner/preview_engine imports returns 0. Absence IS the enforcement. FEEDBACK-INGEST-05. |
| T-42-14 | Tampering | ASSET_SCHEMA append-only | mitigate | Test 10 asserts set equality on 35 slots — no existing slot modified/removed. |
| T-42-15 | Repudiation | Phase 42 changes undocumented | mitigate | 42-SUMMARY.md documents all files touched + all 6 requirements covered. |
| T-42-16 | Information Disclosure | Regression test gaps | mitigate | Test 17 aggregate count >= 650 catches any silent test deletion or major regression. Tests 1-9 catch per-file regressions with explicit failure messages. |
</threat_model>

<verification>
- 10/10 integration tests GREEN (test_feedback_ingest_integration.py)
- 17/17 regression tests GREEN (test_v50_regression_phase42.py)
- 42-SUMMARY.md exists with full requirements traceability
- STRUCTURAL "no p10b import" check passes (Test 13 — FEEDBACK-INGEST-05)
- V5.0 502-test + Phase 40 174-test + Phase 41 91-test baselines preserved
- v6.0 milestone COMPLETE
</verification>

<success_criteria>
- Full E2E feedback loop closes: HTTP → HMAC → schema → semantic → episode existence → feedback-data persist → RecipeLibrary.update_validation → Wilson CI recomputation
- Convergence-after-10-feedbacks verified (Test 2)
- Rejection-does-not-pollute-recipe-library verified (Test 4)
- STRUCTURAL "no auto-modify pipeline" check passes (grep -cE forbidden imports == 0)
- V5.0 + Phase 40 + Phase 41 regression clean
- All 6 FEEDBACK-INGEST-XX requirements satisfied
- v6.0 milestone SHIPPED
</success_criteria>

<output>
Phase 42 SUMMARY created at .planning/phases/42-feedback-ingestion/42-SUMMARY.md
v6.0 milestone complete — next: consider /gsd:plan-phase for v6.0-MILESTONE-AUDIT.md
</output>
