---
phase: 42-feedback-ingestion
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - /data/workspace/hermes-agent/plugins/pipeline_state/asset_bus.py
  - /data/workspace/hermes-agent/plugins/kais_aigc/feedback_ingest.py
  - /data/workspace/hermes-agent/plugins/kais_aigc/tests/test_feedback_ingest_skeleton.py
  - /data/workspace/hermes-agent/plugins/pipeline_state/tests/test_asset_bus_feedback_slots.py
autonomous: true
requirements: [FEEDBACK-INGEST-01, FEEDBACK-INGEST-03]
user_setup: []

must_haves:
  truths:
    - "feedback-data AssetBus JSONL slot is registered and append_line works"
    - "feedback-rejected AssetBus JSONL slot is registered and append_line works"
    - "FeedbackIngestClient class exists in plugins/kais_aigc/feedback_ingest.py with __init__ + get_feedback"
    - "submit_feedback method exists with correct signature (no impl yet — full impl in 42-02/03)"
  artifacts:
    - path: "/data/workspace/hermes-agent/plugins/pipeline_state/asset_bus.py"
      provides: "2 new ASSET_SCHEMA entries: feedback-data + feedback-rejected (both jsonl)"
      contains: "feedback-data"
    - path: "/data/workspace/hermes-agent/plugins/kais_aigc/feedback_ingest.py"
      provides: "FeedbackIngestClient skeleton with __init__ + get_feedback; submit_feedback stub"
      contains: "class FeedbackIngestClient"
    - path: "/data/workspace/hermes-agent/plugins/pipeline_state/tests/test_asset_bus_feedback_slots.py"
      provides: "Regression guard for the 2 new slots"
      min_lines: 40
    - path: "/data/workspace/hermes-agent/plugins/kais_aigc/tests/test_feedback_ingest_skeleton.py"
      provides: "Skeleton smoke + get_feedback tests"
      min_lines: 30
  key_links:
    - from: "plugins/kais_aigc/feedback_ingest.py"
      to: "plugins/pipeline_state/asset_bus.py"
      via: "AssetBus.append_line('feedback-data', ...) (used in 42-02)"
      pattern: "asset_bus.*feedback-data"
---

<objective>
Register the 2 new AssetBus JSONL slots (`feedback-data` + `feedback-rejected`) and scaffold the `FeedbackIngestClient` class skeleton with `__init__` + `get_feedback` implemented. `submit_feedback` returns a stub NotImplemented-shape dict — full HMAC + validation pipeline arrives in 42-02 and 42-03. This is low-risk scaffolding that unlocks the HTTP server work in 42-03.

Purpose: Anchor Phase 42 on the AssetBus append-only invariant (V5.0 + Phase 40 + Phase 41 regression safety) before any validation logic lands. The skeleton mirrors ReviewPlatformClient / GoldTeamClient constructor patterns (env-var config at construction time per D-06, sync API per D-07).

Output: 2 new ASSET_SCHEMA slots, 1 new module (feedback_ingest.py) with skeleton class, 2 new test files (slot regression + skeleton smoke).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/42-feedback-ingestion/42-CONTEXT.md

# Existing templates to follow
@/data/workspace/hermes-agent/plugins/kais_aigc/gold_team.py
@/data/workspace/hermes-agent/plugins/kais_aigc/review_platform.py
@/data/workspace/hermes-agent/plugins/pipeline_state/recipe_library.py
@/data/workspace/hermes-agent/plugins/pipeline_state/asset_bus.py

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From plugins/pipeline_state/asset_bus.py (ASSET_SCHEMA pattern — append-only contract):
```python
ASSET_SCHEMA: dict[str, dict] = {
    # Phase 40 example — pattern to follow for 42-01's 2 new slots:
    "rapid-preview-clips": {
        "file": "rapid-preview-clips.jsonl",
        "format": "jsonl",  # append-only — use append_line() / read_lines()
        "description": "...",
        "writer_phase": "p10b_rapid_preview",
        "reader_phases": [],
    },
    # Phase 41 example:
    "emotion-recipe": {
        "file": "emotion-recipe.jsonl",
        "format": "jsonl",
        "description": "...",
        "writer_phase": "recipe_library",
        "reader_phases": [],
    },
}
# JSONL_SLOTS frozenset MUST stay unchanged at frozenset({"finetune-dataset"})
# — dispatch consults ASSET_SCHEMA[slot]["format"], not JSONL_SLOTS (verified Phase 40-01).

class AssetBus:
    JSONL_SLOTS = frozenset({"finetune-dataset"})  # DO NOT TOUCH

    def append_line(self, slot: str, line_obj: dict) -> str: ...
    def read_lines(self, slot: str) -> list[dict]: ...
```

From plugins/kais_aigc/gold_team.py (constructor pattern — D-06 env-var at construction time):
```python
class GoldTeamClient:
    DEFAULT_BASE_URL = "http://192.168.71.140:8002"
    DEFAULT_TIMEOUT = 60.0

    def __init__(
        self,
        *,
        base_url: str | None = None,    # arg → KAIS_GOLD_TEAM_URL → DEFAULT_BASE_URL
        api_key: str | None = None,     # arg → KAIS_GOLD_TEAM_API_KEY → None
        timeout: float | None = None,
        transport: httpx.BaseTransport | None = None,  # tests inject MockTransport
    ) -> None: ...
```

From plugins/pipeline_state/recipe_library.py (constructor pattern — mirrors Phase 41):
```python
class RecipeLibrary:
    SLOT = "emotion-recipe"

    def __init__(self, *, asset_bus: Any) -> None:
        if asset_bus is None:
            raise ValueError("RecipeLibrary: asset_bus required")
        self._bus = asset_bus
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Register 2 new AssetBus JSONL slots (feedback-data + feedback-rejected)</name>
  <files>/data/workspace/hermes-agent/plugins/pipeline_state/asset_bus.py, /data/workspace/hermes-agent/plugins/pipeline_state/tests/test_asset_bus_feedback_slots.py</files>
  <read_first>
    - /data/workspace/hermes-agent/plugins/pipeline_state/asset_bus.py (lines 282-336 — Phase 40 + 41 slot registration pattern; specifically how "emotion-recipe" entry is shaped)
    - /data/workspace/hermes-agent/plugins/pipeline_state/tests/test_v50_regression_phase41.py (lines 42-103 — EXPECTED_SLOTS + JSONL_SLOTS invariants Phase 42 must preserve)
    - /data/workspace/hermes-agent/plugins/pipeline_state/tests/test_asset_bus_emotion_recipe_slot.py (Pattern for the new test file)
  </read_first>
  <behavior>
    - Test 1: ASSET_SCHEMA["feedback-data"]["file"] == "feedback-data.jsonl"
    - Test 2: ASSET_SCHEMA["feedback-data"]["format"] == "jsonl"
    - Test 3: ASSET_SCHEMA["feedback-data"]["writer_phase"] == "feedback_ingest"
    - Test 4: ASSET_SCHEMA["feedback-rejected"]["file"] == "feedback-rejected.jsonl"
    - Test 5: ASSET_SCHEMA["feedback-rejected"]["format"] == "jsonl"
    - Test 6: ASSET_SCHEMA["feedback-rejected"]["writer_phase"] == "feedback_ingest"
    - Test 7: AssetBus.JSONL_SLOTS == frozenset({"finetune-dataset"}) — UNCHANGED (D-36-05)
    - Test 8: bus.append_line("feedback-data", {...}) writes 1 JSONL line to .pipeline-assets/feedback-data.jsonl
    - Test 9: bus.append_line("feedback-rejected", {...}) writes 1 JSONL line to .pipeline-assets/feedback-rejected.jsonl
    - Test 10: bus.read_lines("feedback-data") returns the list of dicts (round-trip)
  </behavior>
  <action>
    Create the test file first (RED), then append 2 new entries to ASSET_SCHEMA in asset_bus.py (GREEN). Per D-36-05 append-only invariant: insert the 2 new entries AFTER the Phase 41 "emotion-recipe" entry (alphabetical/chronological by phase), DO NOT modify or remove any existing slot, DO NOT add the new slots to JSONL_SLOTS frozenset (dispatch consults ASSET_SCHEMA[slot]["format"] directly, verified Phase 40-01).

    New ASSET_SCHEMA entries (exact shape — use Phase 41 "emotion-recipe" as template):

    "feedback-data": {
        "file": "feedback-data.jsonl",
        "format": "jsonl",
        "description": "Raw platform feedback records (POST /api/v1/feedback payload + "
                       "received_at + signature_valid). One line per feedback "
                       "submission. Reader: operator-side + RecipeLibrary.update_validation "
                       "(Phase 42).",
        "writer_phase": "feedback_ingest",
        "reader_phases": [],
    },
    "feedback-rejected": {
        "file": "feedback-rejected.jsonl",
        "format": "jsonl",
        "description": "Rejected feedback submissions (signature/schema/semantic/episode "
                       "existence failures). One line per rejection. Each line contains "
                       "{feedback_id, reason, payload_snippet, timestamp}. Used for audit "
                       "trail — never feeds RecipeLibrary.update_validation.",
        "writer_phase": "feedback_ingest",
        "reader_phases": [],
    },

    Add a comment header above the block: "# ── Phase 42-01 additions — Feedback Ingestion slots ──────" and a brief D-36-05 preservation note mirroring Phase 41's comment style.

    Test file structure mirrors test_asset_bus_emotion_recipe_slot.py: import ASSET_SCHEMA + AssetBus, define TestFeedbackSlots class with the 10 tests above. Use `tmp_path` fixture for fresh AssetBus per test.
  </action>
  <verify>
    <automated>cd /data/workspace/hermes-agent && python -m pytest plugins/pipeline_state/tests/test_asset_bus_feedback_slots.py -v</automated>
  </verify>
  <done>
    - 2 new slots registered in ASSET_SCHEMA with correct file/format/writer_phase
    - JSONL_SLOTS frozenset unchanged (frozenset({"finetune-dataset"}))
    - append_line + read_lines round-trip works for both new slots
    - 10/10 new tests GREEN
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create FeedbackIngestClient skeleton + get_feedback method</name>
  <files>/data/workspace/hermes-agent/plugins/kais_aigc/feedback_ingest.py, /data/workspace/hermes-agent/plugins/kais_aigc/tests/test_feedback_ingest_skeleton.py</files>
  <read_first>
    - /data/workspace/hermes-agent/plugins/kais_aigc/gold_team.py (lines 75-160 — constructor env-var pattern, D-06)
    - /data/workspace/hermes-agent/plugins/kais_aigc/review_platform.py (lines 64-130 — class structure + close() lifecycle)
    - /data/workspace/hermes-agent/plugins/pipeline_state/asset_bus.py (lines 567-604 — append_line + read_lines signatures)
    - /data/workspace/hermes-agent/plugins/pipeline_state/recipe_library.py (lines 410-556 — RecipeLibrary constructor + SLOT pattern + list_recipes read pattern — get_feedback should mirror this)
  </read_first>
  <behavior>
    - Test 1: FeedbackIngestClient(asset_bus=bus, recipe_library=rl) constructs without error
    - Test 2: FeedbackIngestClient() without asset_bus raises ValueError("FeedbackIngestClient: asset_bus required")
    - Test 3: FeedbackIngestClient() without recipe_library raises ValueError("FeedbackIngestClient: recipe_library required")
    - Test 4: client.get_feedback() returns list (empty when feedback-data slot is empty)
    - Test 5: After bus.append_line("feedback-data", {...}), client.get_feedback() returns [that dict]
    - Test 6: client.get_feedback(episode_id="ep-001") filters to records with matching episode_id
    - Test 7: client.submit_feedback(body: bytes, signature: str) exists and returns a dict (stub — full impl in 42-02/03; stub returns {"status": "not_implemented", "reason": "Phase 42-02/03 pending"})
    - Test 8: FeedbackIngestClient supports context-manager protocol (__enter__ / __exit__) — __exit__ calls close()
    - Test 9: FeedbackIngestClient reads KAIS_FEEDBACK_PORT env var at construction (default 8091 if unset)
    - Test 10: FeedbackIngestClient reads KAIS_FEEDBACK_SECRET env var at construction (None if unset — dev mode)
  </behavior>
  <action>
    Create the test file first (RED), then create feedback_ingest.py module (GREEN).

    Module structure (mirror gold_team.py / review_platform.py — single class + module-level __all__):

    1. Module docstring explaining: Phase 42 final phase of v6.0, closes 最速收敛闭环, deliberately small (Phase 41 holds the "smart" Wilson CI logic — Phase 42 is receive→verify→store→forward).

    2. Imports — stdlib only at this stage (hashlib, hmac, json, logging, os, time, typing). NOTE: `httpx` and `starlette` imports come in 42-03 (server task). Do NOT import them in 42-01.

    3. Module-level constants:
       - DEFAULT_FEEDBACK_PORT = 8091  # CONTEXT.md decision — sibling to gold-team :8002 and review :8090
       - SLOT_FEEDBACK_DATA = "feedback-data"
       - SLOT_FEEDBACK_REJECTED = "feedback-rejected"
       - __all__ = ["FeedbackIngestClient"]

    4. class FeedbackIngestClient:
       def __init__(
           self,
           *,
           asset_bus: Any,
           recipe_library: Any,  # Phase 41 RecipeLibrary — type: RecipeLibrary (avoid hard import to prevent cycle; duck-typed)
           port: int | None = None,         # arg → KAIS_FEEDBACK_PORT → DEFAULT_FEEDBACK_PORT
           secret: str | None = None,       # arg → KAIS_FEEDBACK_SECRET → None (dev mode)
       ) -> None:
           if asset_bus is None: raise ValueError("FeedbackIngestClient: asset_bus required")
           if recipe_library is None: raise ValueError("FeedbackIngestClient: recipe_library required")
           self._bus = asset_bus
           self._rl = recipe_library
           self._port = port if port is not None else int(os.environ.get("KAIS_FEEDBACK_PORT", str(self.DEFAULT_FEEDBACK_PORT)))
           self._secret = secret if secret is not None else os.environ.get("KAIS_FEEDBACK_SECRET")
           # NOTE: no httpx client yet — 42-03 adds the server

       def get_feedback(self, *, episode_id: str | None = None) -> list[dict]:
           """Read raw feedback records from feedback-data slot.
           If episode_id provided, filter to matching records."""
           rows = self._bus.read_lines(SLOT_FEEDBACK_DATA)
           if episode_id is not None:
               rows = [r for r in rows if r.get("episode_id") == episode_id]
           return rows

       def submit_feedback(self, body: bytes, signature: str) -> dict:
           """STUB — full implementation in 42-02 (HMAC + validation) + 42-03 (HTTP handler wiring).
           Returns not_implemented envelope so tests can pin the skeleton contract."""
           return {
               "status": "not_implemented",
               "reason": "Phase 42-02 (HMAC + validation) + 42-03 (HTTP server) pending",
           }

       def close(self) -> None:
           """Lifecycle — 42-03 may add httpx client close; for now a no-op so __exit__ works."""
           pass

       def __enter__(self) -> "FeedbackIngestClient":
           return self

       def __exit__(self, *exc: object) -> None:
           self.close()

    Add a module-level comment near the top: "# STRUCTURAL 'no auto-modify pipeline' INVARIANT (FEEDBACK-INGEST-05): This module must NOT import pipeline.phases.p10b_rapid_preview, runner.py, or preview_engine. Verified by grep test in 42-04. The only references are: RecipeLibrary (Phase 41) + AssetBus (V5.0) + stdlib."

    Test file: define TestFeedbackIngestSkeleton class with 10 tests above. Use a minimal FakeRecipeLibrary stub (just `pass` for methods — not exercised in skeleton) + tmp_path AssetBus.
  </action>
  <verify>
    <automated>cd /data/workspace/hermes-agent && python -m pytest plugins/kais_aigc/tests/test_feedback_ingest_skeleton.py -v</automated>
  </verify>
  <done>
    - feedback_ingest.py module exists at plugins/kais_aigc/feedback_ingest.py
    - FeedbackIngestClient class with __init__ + get_feedback + submit_feedback (stub) + close + __enter__/__exit__
    - Constructor validates asset_bus + recipe_library required (ValueError)
    - Constructor reads KAIS_FEEDBACK_PORT + KAIS_FEEDBACK_SECRET from env (D-06)
    - get_feedback reads from feedback-data slot, filters by episode_id when provided
    - 10/10 skeleton tests GREEN
    - NO httpx/starlette imports yet (those land in 42-03)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| AssetBus append → filesystem | Atomic O_APPEND write — POSIX atomicity, no fsync (mirrors Node.js) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-42-01 | Tampering | ASSET_SCHEMA append | mitigate | D-36-05 invariant preserved: only ADD new slots, never modify/remove existing. Test asserts slot-set equality + JSONL_SLOTS unchanged. |
| T-42-02 | Information Disclosure | feedback-data records may leak across episodes | accept | Per-workdir AssetBus scoping already isolates episodes (Phase 41 invariant); cross-workdir queries are operator-side concern. |
</threat_model>

<verification>
- 2 new ASSET_SCHEMA slots registered, JSONL_SLOTS unchanged
- FeedbackIngestClient skeleton importable from plugins.kais_aigc.feedback_ingest
- V5.0 + Phase 40 + Phase 41 regression clean (verified in 42-04 final phase, but spot-check by running existing test_v50_regression_phase41.py)
</verification>

<success_criteria>
- All 10 slot tests GREEN (test_asset_bus_feedback_slots.py)
- All 10 skeleton tests GREEN (test_feedback_ingest_skeleton.py)
- No existing slot modified, no existing test broken
- feedback_ingest.py module has no httpx/starlette import yet (deferred to 42-03)
- STRUCTURAL "no p10b import" comment present in module header
</success_criteria>

<output>
Create `.planning/phases/42-feedback-ingestion/42-01-SUMMARY.md` when done
</output>
